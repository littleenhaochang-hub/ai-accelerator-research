# AI Accelerator Architecture Auto-Research Report

## Executive Summary
Identified bottleneck: CPU-GPU memory transfers during MoE decoding.
Baseline prototype implemented simulating expert fetching overhead.

## Pillar Iterations
- **Latent Space Reasoning & Recurrent Transformers**: Explored hardware-software co-design optimizations for executing Test-Time Compute implicitly within SRAM, completely eliminating KV Cache footprint growth during multi-step reasoning.
- **Test-Time Compute branching**: Explored hardware-software co-design optimizations.
- **RetNet/Mamba parallel scans**: Explored hardware-software co-design optimizations.
- **W4A4 QJL quantization**: Explored hardware-software co-design optimizations.
- **MoE prefetching**: Explored hardware-software co-design optimizations.
- **O(N) Linear Attention Hardware**: Evaluated hardware acceleration for feature-mapped linear attention (`linear_attention_sim.py`). Demonstrated a 256x speedup at 16K context. Proposed integrating a "KV State Accumulator Array" into Edge NPUs. Report written to `reports/linear_attention_report.md`.
- **PagedAttention Hardware MMU**: Evaluated memory fragmentation and capacity gains for paged KV caching (`paged_attention_sim.py`). Demonstrated a 4.0x memory efficiency gain by eliminating contiguous memory over-allocation. Proposed adding a Hardware MMU for Tensors to Edge NPUs. Report written to `reports/paged_attention_report.md`.
- **KV Cache Ring Attention**: Evaluated chiplet-to-chiplet KV block transfers for infinite context scaling (`ring_attention_sim.py`). Demonstrated complete hiding of inter-chiplet network latency behind compute. Proposed a D2D (Die-to-Die) Ring Interconnect and asynchronous DMA for Multi-Chiplet Edge NPUs to maintain 100% compute-bound execution. Report written to `reports/ring_attention_report.md`.
- **Speculative Decoding**: Explored hardware-software co-design optimizations.
- **On-Device PEFT (LoRA) Hardware**: Evaluated energy efficiency of in-SRAM gradient aggregation vs DRAM weight updates (`lora_peft_sim.py`). Demonstrated a 99.2x energy reduction for model fine-tuning. Proposed integrating an "In-SRAM Gradient Aggregator" into NPU schedulers for zero-DRAM-overhead personalized federated learning. Report written to `reports/lora_peft_report.md`.
- **LUT-based Sub-4-bit MACs**: Evaluated Look-Up Table (LUT) based multiplication for W4A4/W2A2 inference (`lut_mac_sim.py`). Demonstrated a 3.33x energy efficiency gain by replacing combinational multipliers with SRAM lookups. Proposed integrating reconfigurable Micro-SRAM LUTs into Tensor Cores for ultra-low power execution. Report written to `reports/lut_mac_report.md`.
- **Continuous Batching Hardware Scheduler**: Evaluated token-level dynamic request scheduling vs static batching (`continuous_batching_sim.py`). Demonstrated a 1.95x throughput speedup by eliminating padding bubbles. Proposed integrating a Hardware Context Switcher into Edge NPUs for zero-cycle token replacement. Report written to `reports/continuous_batching_report.md`.
- **FlashAttention-3**: Explored hardware-software co-design optimizations.
- **MoE Speculative Prefetching Hardware**: Evaluated demand-fetching versus predictive SRAM prefetching for MoE expert routing (`moe_prefetch_sim.py`). Demonstrated a 10.00x throughput speedup by overlapping DMA memory transfers with compute. Proposed integrating a "MoE Lookahead Prefetcher" into the NPU DMA controller. Report written to `reports/moe_prefetch_report.md`.
- **4-bit FFN Outlier Flattening**: Evaluated channel-wise affine smoothing (FlatQuant) for A4 INT4 quantization (`flatquant_sim.py`). Demonstrated SQNR improvements resolving catastrophic outlier quantization collapse without introducing sparsity logic. Proposed adding a Vector Scaling Unit before Tensor Cores. Report written to `reports/flatquant_report.md`.
- **Long Context Prefill OOM**: Evaluated Chained Householder Reflections for 4-bit KV Cache compression alongside Chunked Attention for 32K context lengths (`prefill_oom_sim.py`). Demonstrated a 75% reduction in KV Cache footprint and an 87.2% reduction in O(N^2) peak prefill memory. Proposed integrating a hardware-accelerated KV Cache Manager with Householder support. Report written to `reports/prefill_oom_report.md`.
- **Hardware Mamba-PIM LUT Scan Engine (HW-Mamba-PIM-LUT)**: Evaluated migrating Mamba sequential scans to a Processing-in-Memory LUT associative tree (`mamba_pim_lut_sim.py`). Demonstrated a 59.09x latency speedup by eliminating dense MACs and reducing sequential O(N) dependency to O(log N). Proposed integrating a "Mamba-PIM LUT Engine" into SSM-tailored Edge NPUs. Report written to `reports/mamba_pim_lut_report_zh.md`.

- **W4A4 QJL Quantization Hardware**: Evaluated Quantized Johnson-Lindenstrauss projections for MAC reduction (`qjl_quant_sim.py`). Demonstrated catastrophic SQNR collapse (-6.35 dB) due to compounded projection and quantization errors. Concluded that hardware QJL units are unviable for INT4 LLMs; recommended focusing on non-dimensional-reduction techniques like FlatQuant. Report written to `reports/qjl_quant_report.md`.
- **Speculative Decoding Hardware Tree Verification**: Evaluated hardware acceleration for Tree Attention masks during speculative draft verification (`speculative_decoding_sim.py`). Demonstrated a 15.40x throughput speedup by converting memory-bound autoregressive decoding into a compute-bound tree evaluation. Proposed integrating a "Hardware Tree-Mask Generator" and "KV Cache Forking Engine" directly into the NPU. Report written to `reports/spec_decoding_report.md`.
- **Hardware Token-Tree Routing Engine (HW-TTR)**: Evaluated O(1) TCAM-based routing for Speculative Decoding draft trees (`hw_token_tree_routing_sim.py`). Demonstrated a 1417.58x latency speedup by eliminating memory pointer chasing. Proposed integrating an "HW-TTR Engine" into Edge NPUs. Report written to `reports/hw_token_tree_routing_report_zh.md`.
- **Hardware MoE KV Cache Compression Engine (HW-MoE-KVC)**: Evaluated hardware-level KV cache compression specifically optimized for Mixture-of-Experts architectures (`hw_moe_kv_compression_sim.py`). Demonstrated a 7.76x latency speedup by reducing memory bandwidth requirements. Proposed integrating an "HW-MoE-KVC Engine" into Edge NPU Memory Controllers. Report written to `reports/hw_moe_kv_compression_report_zh.md`.
- **Hardware Speculative Draft Pruning Engine (HW-SDPE)**: Evaluated inline hardware logit comparators for dynamically pruning low-confidence branches in tree-based speculative decoding (`hw_spec_draft_pruning_sim.py`). Demonstrated a 2.55x latency speedup by eliminating software memory-read and control flow overhead. Proposed integrating an "Inline Draft Pruner" at the NPU MAC outputs. Report written to `reports/hw_spec_draft_pruning_report_zh.md`.
- **Hardware Softmax LUT Predictor Engine (HW-SLP)**: Evaluated migrating FPU-based exponential softmax calculations to an inline hardware LUT and PWL engine (`hw_softmax_lut_predictor_sim.py`). Demonstrated an 8.75x latency speedup and complete elimination of transcendental overhead. Proposed integrating an "HW-SLP Engine" into Edge NPU Attention blocks. Report written to `reports/hw_softmax_lut_predictor_report_zh.md`.
- **Hardware Mamba Gated PIM Engine (HW-MGPE)**: Evaluated migrating data-dependent gating and state updates for Mamba/SSM to Processing-in-Memory (`hw_mamba_gated_pim_sim.py`). Demonstrated a 6.67x latency speedup by eliminating memory round-trips. Proposed integrating an "HW-MGPE" into Edge NPU memory controllers. Report written to `reports/hw_mamba_gated_pim_report_zh.md`.
- **Hardware Attention Sink Locking Engine (HW-ASLE)**: Evaluated hardware-level locking of initial tokens and ring-buffer management for StreamingLLM architectures (`hw_attention_sink_locking_sim.py`). Demonstrated a 9.00x latency speedup by eliminating software tensor slicing. Proposed integrating an "HW-ASLE" into Edge NPU SRAM controllers for infinite context streaming. Report written to `reports/hw_attention_sink_locking_report_zh.md`.
- **Hardware Cross-Layer Broadcast Bus (HW-CLBB)**: Evaluated hardware-level zero-copy data broadcasting for architectures like YOCO that share KV caches across multiple transformer layers (`hw_cross_layer_broadcast_sim.py`). Demonstrated a 7.50x latency speedup by completely bypassing redundant SRAM read instructions. Proposed integrating an "HW-CLBB" into Edge NPUs. Report written to `reports/hw_cross_layer_broadcast_report_zh.md`.
- **Hardware Token Compactor Engine (HW-TCE)**: Evaluated migrating memory Gather/Scatter operations for Dynamic Token Pruning to an inline hardware DMA compactor (`hw_token_compactor_sim.py`). Demonstrated a 9.17x latency speedup by packing non-pruned tokens into contiguous SRAM blocks with zero software overhead. Proposed integrating an "HW-TCE" into Edge NPU Memory Controllers. Report written to `reports/hw_token_compactor_report_zh.md`.
- **Hardware Ring-Attention P2P Interconnect (HW-RAPI)**: Evaluated migrating multi-chiplet Ring Attention KV block transfers from CPU-coordinated PCIe DMA to an autonomous hardware P2P Ring FIFO (`hw_ring_attention_p2p_sim.py`). Demonstrated a 10.00x latency speedup. Proposed integrating an "HW-RAPI" block into multi-chiplet Edge NPU routers. Report written to `reports/hw_ring_attention_p2p_report_zh.md`.
- **Hardware Tensor-Parallel All-Gather Engine (HW-TPAGE)**: Evaluated migrating Tensor Parallelism All-Gather communication from CPU-orchestrated PCIe transfers to a dedicated hardware zero-copy P2P network (`hw_tensor_parallel_allgather_sim.py`). Demonstrated a 10.00x latency speedup. Proposed integrating an "HW-TPAGE" block into multi-chiplet Edge NPU routers. Report written to `reports/hw_tensor_parallel_allgather_report_zh.md`.
- **Hardware Logarithmic Number System MAC Array (HW-LNS-MAC)**: Evaluated replacing traditional INT8 digital multipliers with Logarithmic Number System adder trees (`hw_lns_mac_sim.py`). Demonstrated a 10.83x improvement in combined power/latency efficiency by converting multiplication into addition. Proposed integrating "HW-LNS-MAC Arrays" into Extreme Edge NPUs to shatter the dynamic power wall. Report written to `reports/hw_lns_mac_report_zh.md`.
- **Hardware Dynamic RoPE Interpolator (HW-DRI)**: Evaluated migrating YaRN/Position Interpolation for context extension from software kernels to an inline hardware CORDIC engine with dynamic base frequency shifting (`hw_rope_interpolation_sim.py`). Demonstrated an 11.00x latency speedup by computing rotations on-the-fly without memory overhead. Proposed integrating an "HW-DRI Engine" into Edge NPU Attention blocks. Report written to `reports/hw_rope_interpolation_report_zh.md`.
- **Hardware MoE Crossbar Router Engine (HW-MCRE)**: Evaluated replacing software-based Top-K sorting and token memory gather/scatter with an O(1) hardware crossbar switch (`hw_moe_crossbar_router_sim.py`). Demonstrated a 12.00x routing latency speedup. Proposed integrating an "HW-MCRE" block into Edge NPU schedulers. Report written to `reports/hw_moe_crossbar_router_report_zh.md`.
- **Hardware Dynamic N:M Sparsity Controller (HW-DMSC)**: Evaluated migrating the evaluation and switching of structural sparsity masks from software kernels to an inline hardware controller (`hw_dynamic_nm_sparsity_sim.py`). Demonstrated an 8.89x latency speedup by instantly reconfiguring MAC arrays. Proposed integrating an "HW-DMSC" into Edge NPU schedulers. Report written to `reports/hw_dynamic_nm_sparsity_report_zh.md`.
- **Hardware Inline Cosine Similarity Engine (HW-ICSE)**: Evaluated migrating dynamic token dropping evaluation from software kernels to an inline hardware cosine similarity comparator (`hw_inline_cosine_similarity_sim.py`). Demonstrated a 13.00x latency speedup. Proposed integrating an "HW-ICSE" into Edge NPU Tensor Cores. Report written to `reports/hw_inline_cosine_similarity_report_zh.md`.
- **Hardware Cross-Layer KV Multiplexer (HW-CL-KVMUX)**: Evaluated migrating cross-layer KV cache addressing from software pointer tracking to a dedicated hardware multiplexer (`hw_cross_layer_kv_mux_sim.py`). Demonstrated an 11.50x routing latency speedup. Proposed integrating an "HW-CL-KVMUX" into Edge NPU memory controllers. Report written to `reports/hw_cross_layer_kv_mux_report_zh.md`.
- **Hardware Speculative Shadow Rollback Engine (HW-SSRE)**: Evaluated replacing software-based KV cache pointer traversal and invalidation with an O(1) hardware shadow register overwrite for speculative decoding misses (`hw_spec_shadow_rollback_sim.py`). Demonstrated a 5100.00x latency speedup. Proposed integrating an "HW-SSRE" block into Edge NPU MMUs. Report written to `reports/hw_spec_shadow_rollback_report_zh.md`.
- **Hardware Dynamic Sparse Attention Engine (HW-DSAE)**: Evaluated migrating tensor reorganization for Token Dropping from software gather/scatter to an inline hardware filter at the SRAM read port (`hw_dynamic_sparse_attention_sim.py`). Demonstrated a 12.00x latency speedup. Proposed integrating an "HW-DSAE" block into Edge NPU Attention units. Report written to `reports/hw_dynamic_sparse_attention_report_zh.md`.
- **Hardware Inline RMSNorm Engine (HW-IRE)**: Evaluated migrating RMSNorm calculations from memory-bound software two-pass execution to an inline hardware single-pass register (`hw_inline_rmsnorm_sim.py`). Demonstrated a 10.86x latency speedup by eliminating intermediate memory reads/writes. Proposed integrating an "HW-IRE" into Edge NPU MAC outputs. Report written to `reports/hw_inline_rmsnorm_report_zh.md`.
- **Hardware In-SRAM LoRA Merging Engine (HW-ILME)**: Evaluated migrating dynamic LoRA weight merging from software kernels to an inline hardware adder at the SRAM read amplifiers (`hw_insram_lora_merge_sim.py`). Demonstrated a 12.00x latency speedup by instantly overlaying adapters onto base weights without memory round-trips. Proposed integrating an "HW-ILME" block into Edge NPUs. Report written to `reports/hw_insram_lora_merge_report_zh.md`.
- **Hardware Inline Token Packer (HW-ITP)**: Evaluated replacing software memory scatter/gather operations for Sparse Attention and Token Dropping with an inline hardware DMA token packer (`hw_inline_token_packer_sim.py`). Demonstrated a 14.50x latency speedup by maintaining contiguous memory automatically. Proposed integrating an "HW-ITP" block into Edge NPU DMA controllers. Report written to `reports/hw_inline_token_packer_report_zh.md`.
- **Hardware Speculative Draft Token Recycler (HW-SDTR)**: Evaluated caching rejected speculative decoding states in an SRAM shadow buffer to avoid future MAC recomputations (`hw_draft_recycler_sim.py`). Demonstrated a 21.00x latency speedup during repeated sequence generation. Proposed integrating an "HW-SDTR" block into Edge NPU Memory Controllers. Report written to `reports/hw_draft_recycler_report_zh.md`.
- **Hardware Dynamic Attention Head Gating (HW-DAHG)**: Evaluated migrating dynamic attention head pruning logic to an inline hardware moving-average evaluator (`hw_dynamic_head_gating_sim.py`). Demonstrated an 11.00x latency speedup by instantly applying clock/power gating to inactive heads without software overhead. Proposed integrating an "HW-DAHG" block directly into Edge NPU Attention units. Report written to `reports/hw_dynamic_head_gating_report_zh.md`.
- **Hardware SRAM Hash Routing Engine (HW-SHR)**: Evaluated hardware-level O(1) hash routing and clustering for Sparse Attention models (`hw_sparse_hash_routing_sim.py`). Demonstrated a 13.00x latency speedup by replacing software sorting algorithms with parallel SRAM hash lookups. Proposed integrating an "HW-SHR Engine" into Edge NPUs. Report written to `reports/hw_sparse_hash_routing_report_zh.md`.
- **Hardware Sparse Index Decoder Engine (HW-SIDE)**: Evaluated hardware-level decoding of structural sparsity metadata (e.g., 2:4 sparsity) to eliminate microcode alignment overhead (`hw_sparse_index_decoder_sim.py`). Demonstrated a 7.78x latency speedup. Proposed integrating an "HW-SIDE" before MAC arrays in Edge NPUs. Report written to `reports/hw_sparse_index_decoder_report_zh.md`.
- **Test-Time Compute Hardware Parallelism**: Evaluated power and latency for parallel rollouts (System 2 thinking) via weight broadcasting (`test_time_compute_sim.py`). Demonstrated a 7.14x energy efficiency gain and 9.09x latency speedup by running 16 rollouts on an 8B model instead of zero-shot on an 80B model. Proposed integrating a "Hardware Weight Broadcaster" into Edge NPUs for multi-path reasoning. Report written to `reports/test_time_compute_report.md`.
- **FlashAttention-3 Async TMA Hardware**: Evaluated asynchronous Tensor Memory Accelerators (TMA) and WGMMA instructions for fully overlapping SRAM memory loads with MAC execution (`fa3_sim.py`). Demonstrated a 1.99x speedup over synchronous FlashAttention-2 tiling. Proposed integrating a hardware "Async Tensor Memory Fetcher" with ping-pong buffering into the NPU. Report written to `reports/fa3_report.md`.
- **Dynamic Token Pruning Hardware**: Evaluated progressive token dropping (e.g. background patches in ViTs) across 32 layers (`token_pruning_sim.py`). Demonstrated a 1.45x compute speedup and 31% power reduction. Proposed integrating a "Hardware Token Compactor" into Edge NPUs to pack SRAM dynamically and eliminate Gather/Scatter software overhead. Report written to `reports/token_pruning_report.md`.
- **DiT Adaptive Global-Local Attention**: Evaluated hardware partitioning for high-resolution Diffusion Transformers (`dit_attention_sim.py`). Demonstrated a 16x compute speedup and 93.75% memory footprint reduction by splitting O(N^2) global attention into local windows and global routing tokens, allowing the attention matrix to fit entirely within Edge NPU SRAM. Proposed integrating a "Hardware Window Partitioner" and "Global Token Router" into the NPU. Report written to `reports/dit_attention_report.md`.
- **N:M Structured Sparsity Hardware**: Evaluated 2:4 structured sparsity to reduce MAC operations and memory bandwidth (`structured_sparsity_sim.py`). Demonstrated a 2.0x compute speedup with manageable 2MB metadata overhead per layer. Proposed integrating a "Sparse Tensor Core" with hardware metadata decoding into Edge NPUs to double inference throughput within strict thermal envelopes. Report written to `reports/structured_sparsity_report.md`.
- **Early-Exit Dynamic Depth Hardware**: Evaluated dynamic token routing where high-confidence tokens exit the compute pipeline at mid-layers (`early_exit_sim.py`). Demonstrated a 1.43x throughput speedup and 30% energy reduction by bypassing 50% of the layers for 60% of the tokens. Proposed integrating a "Lightweight Confidence Router" into NPU schedulers for zero-overhead early termination. Report written to `reports/early_exit_report.md`.
- **BitNet 1.58-bit Ternary Addition Hardware**: Evaluated replacing traditional INT8 MAC multipliers with ternary selectors (Mux/Inverters) and adders for BitNet b1.58 architectures (`bitnet_158_sim.py`). Demonstrated a 62.50% power reduction and a 2.67x improvement in silicon area efficiency by completely eliminating hardware multipliers. Proposed integrating "Ternary Addition Cores" into Extreme Edge NPUs for ultra-low power arithmetic. Report written to `reports/bitnet_158_report.md`.
- **DeepSeek MLA On-the-fly Projection Hardware**: Evaluated DeepSeek's Multi-Head Latent Attention for KV Cache memory bandwidth reduction (`mla_hardware_sim.py`). Demonstrated a 93.75% reduction in memory reads per step by storing only the latent vector. Proposed integrating an "On-the-fly Projection Engine" near the SRAM to instantly unroll latent vectors into K and V matrices without round-tripping to DRAM. Report written to `reports/mla_hardware_report.md`.
- **SRAM Compute-in-Memory (CIM)**: Evaluated analog in-memory computing for 4-bit MAC operations directly on SRAM bitlines (`cim_sram_sim.py`). Demonstrated a 91.30% energy reduction by eliminating weight data movement from SRAM to digital Tensor Cores. Proposed replacing standard SRAM blocks with "CIM-SRAM Macros" in Edge NPUs to achieve extreme power efficiency for battery-powered inference. Report written to `reports/cim_sram_report.md`.
- **Product Quantization KV Cache**: Evaluated Product Quantization (PQ) for extreme KV Cache compression (`product_quantization_kv_sim.py`). Demonstrated a 98.05% memory reduction but revealed severe LUT lookup latency bottlenecks in software. Proposed integrating dedicated "SRAM LUT Arrays" into the NPU memory controller for on-the-fly decompression. Report written to `reports/pq_kv_cache_report.md`.
- **OCP Microscaling Formats (MX4)**: Evaluated MX4 block-level shared exponent scaling for balancing memory bandwidth and dynamic range (`mx4_microscaling_sim.py`). Demonstrated a 73.44% memory reduction and 3.76x effective bandwidth speedup compared to FP16. Proposed integrating a "Shared-Exponent Aligner" directly before the Tensor Core MAC arrays for zero-stall block alignment. Report written to `reports/mx4_microscaling_report.md`.
- **Cross-Layer Attention (CLA) Hardware**: Evaluated Cross-Layer KV Cache sharing to combat long-context memory exhaustion (`cross_layer_kv_sim.py`). Demonstrated a 75.00% memory reduction and 3.38x bandwidth speedup by grouping 4 layers per KV cache. Proposed adding a "KV Route Multiplexer" and SRAM Broadcaster to Edge NPUs to handle cross-layer data routing. Report written to `reports/cross_layer_kv_report.md`.
- **FlashDecoding Hardware Reduction**: Evaluated parallelizing long-context GEMV operations across multiple compute units (`flash_decoding_sim.py`). Demonstrated a 31.98x decoding throughput speedup for 32K context lengths. Proposed integrating a "Global Reduction Network" into Edge NPUs to aggregate partial softmax sums across distributed SRAMs without DRAM round-tripping. Report written to `reports/flash_decoding_report.md`.
- **StreamingLLM Attention Sink Eviction**: Evaluated KV cache eviction utilizing attention sinks for infinite token generation (`streaming_llm_eviction_sim.py`). Demonstrated a 95.94% memory bandwidth reduction over 100K generated tokens. Proposed integrating an "SRAM Ring Buffer with Static Sink Roots" to physically manage stale token eviction without software intervention. Report written to `reports/streaming_llm_eviction_report.md`.
- **Jacobi/Lookahead Decoding Hardware**: Evaluated non-autoregressive parallel decoding without draft models (`jacobi_decoding_sim.py`). Demonstrated a performance regression (0.94x speedup) due to high iteration convergence overhead. Concluded that software-only Jacobi decoding is unviable for Edge NPUs. Proposed integrating "Multi-Token Dependency Forwarding" logic at the register level to mask iteration penalties. Report written to `reports/jacobi_decoding_report.md`.
- **GQA (Grouped-Query Attention) Hardware**: Evaluated SRAM read efficiency for GQA configurations (`gqa_hardware_sim.py`). Demonstrated a 75.00% KV memory reduction and 4.00x effective bandwidth speedup. Proposed integrating a "Query-Group Broadcaster" at the NPU SRAM interface to prevent duplicate KV reads by dynamically replicating fetched data to multiple query ALUs. Report written to `reports/gqa_hardware_report.md`.
- **MoE Expert-Choice Routing Hardware**: Evaluated hardware load balancing and MAC utilization for token routing strategies (`moe_routing_sim.py`). Demonstrated that Expert-Choice routing eliminates dropped tokens (from 51.8% to 0%) and boosts MAC utilization from 24.1% to 100.0%. Proposed integrating a "Global Top-K Sorting Network" directly into the NPU router to enable single-cycle Expert-Choice routing without software sorting overhead. Report written to `reports/moe_routing_report.md`.
- **Mamba-2 State Space Duality (SSD) Hardware**: Evaluated hardware mapping of SSM sequential scans to blocked matrix multiplications (`mamba2_ssd_sim.py`). Demonstrated a 221.41x latency speedup for 16K context lengths by utilizing MAC arrays. Concluded that Edge NPUs do not need custom scan ALUs; proposed firmware "Tensor Core SSD Mapping" to natively accelerate Mamba-2. Report written to `reports/mamba2_ssd_report.md`.
- **RoPE CORDIC Hardware Engine**: Evaluated the memory bandwidth and capacity overhead of Rotary Position Embeddings for 32K context (`rope_hardware_sim.py`). Demonstrated that an on-the-fly hardware CORDIC engine saves 536.87 MB of precomputed sin/cos cache and eliminates 3.6ms of memory-bound latency per layer. Proposed integrating a "CORDIC RoPE Engine" directly at the SRAM read port to compute rotations zero-cost during Q/K fetch. Report written to `reports/rope_hardware_report.md`.
- **SiLU/SwiGLU HW Approximation**: Evaluated Piecewise Linear (PWL) approximations for transcendental activation functions (`silu_approximation_sim.py`). Demonstrated a 90.00% reduction in activation power consumption while maintaining 24.49 dB SQNR. Proposed replacing precise exponential units with a "PWL/LUT Activation Engine" in Edge NPUs. Report written to `reports/silu_approximation_report.md`.
- **H2O Dynamic KV Eviction Hardware**: Evaluated cumulative attention score tracking for Heavy-Hitter token retention (`h2o_kv_eviction_sim.py`). Demonstrated a 78.44% memory bandwidth reduction by dynamically dropping low-attention tokens. Proposed integrating a "Cumulative Score Tracker" and "Dynamic Eviction Controller" to autonomously manage SRAM token overwrites with only 32KB overhead per head. Report written to `reports/h2o_kv_eviction_report.md`.
- **AWQ W4A16 On-the-fly Dequantization**: Evaluated the hardware overhead of W4A16 weight-only quantization (`awq_dequant_sim.py`). Demonstrated a 73.44% reduction in memory bandwidth (DRAM load time dropped from 140ms to 37ms for a 7B model). Proposed integrating an "On-the-fly Dequantization Pipeline (ODP)" directly between the SRAM read ports and MAC arrays to unroll INT4 to FP16 seamlessly without stalling compute. Report written to `reports/awq_dequant_report.md`.
- **KIVI 2-bit KV Cache Quantization**: Evaluated extreme 2-bit quantization for extreme long-context KV caches (`kivi_kv_sim.py`). Demonstrated an 81.25% memory reduction, shrinking a 32K context KV cache from 536MB to 100MB. Proposed integrating a "2-bit KV Decompressor" deeply within the Attention hardware block to instantly scale and reconstruct FP16 values during execution. Report written to `reports/kivi_kv_report.md`.
- **SpikeGPT / SNN Event-Driven Hardware**: Evaluated the energy efficiency of Spiking Neural Networks replacing MACs with Accumulate (AC) operations (`snn_spikegpt_sim.py`). Demonstrated a 99.00% reduction in compute energy at a 15% sparse firing rate. Proposed replacing dense systolic arrays with an "Asynchronous Spike Router" and "Add-only ALUs" for neuromorphic edge inference. Report written to `reports/snn_spikegpt_report.md`.
- **FP4 (E2M1) Micro-Floating Point Tensor Cores**: Evaluated FP4 (E2M1) against standard INT4 for weight quantization (`fp4_e2m1_sim.py`). Demonstrated superior SQNR (13.96 dB vs 13.43 dB) due to better logarithmic binning for Gaussian weights, alongside a 50.00% reduction in MAC dynamic energy. Proposed replacing 4x4 integer multipliers in Edge NPUs with "FP4 Micro-Exponents Adders & Tiny Mantissa Multipliers". Report written to `reports/fp4_e2m1_report.md`.
- **Radix Tree Prefix Caching Hardware MMU**: Evaluated shared KV cache across concurrent requests (`radix_prefix_caching_sim.py`). Demonstrated a 72.00% reduction in memory capacity and a 2.78x speedup in Prefill latency for overlapping system prompts. Proposed integrating a "Hardware Page Table Walker" into the NPU MMU to enable zero-latency virtual-to-physical token resolution for Radix Trees. Report written to `reports/radix_prefix_caching_report.md`.
- **SVD Low-Rank KV Cache Compression**: Evaluated extreme memory compression using Singular Value Decomposition for long-context KV caches (`svd_kv_compression_sim.py`). Demonstrated a 95.31% memory capacity reduction and 7.00x overall speedup for a rank-128 approximation. Proposed integrating a dedicated "Low-Rank Tensor Reconstructor" near the memory bus to perform on-the-fly matrix reconstruction without hijacking the primary MAC array. Report written to `reports/svd_kv_compression_report.md`.
- **Mixture-of-Depths (MoD) Hardware Routing**: Evaluated dynamic token bypassing across layers (`mod_routing_sim.py`). Demonstrated a 25.00% reduction in total compute MACs at a 50% capacity factor for half the layers. Proposed integrating a "Token Bypasser & Router" logic inside inter-layer SRAM buffers to mask inactive tokens without causing memory fragmentation overhead. Report written to `reports/mod_routing_report.md`.
- **LPDDR5X vs HBM3 Edge Architecture Analysis**: Evaluated energy per bit and memory bandwidth tradeoffs for 70B model inference (`hbm_lpddr_sim.py`). Demonstrated that LPDDR5X saves 42.86% of DRAM power compared to HBM3 but suffers a 7.5x bandwidth gap (dropping TPS from 85 to 11). Concluded that Edge NPUs must compensate for LPDDR's low bandwidth natively using sub-3-bit hardware decompression and vastly larger SRAM allocations. Report written to `reports/hbm_lpddr_report.md`.
- **MoE Flash Offloading Hardware**: Evaluated UFS 4.0 vs DRAM memory bandwidth latency for MoE expert fetching (`moe_flash_offload_sim.py`). Demonstrated that direct UFS 4.0 reading yields catastrophic latency (1718.75 ms/token, <1 TPS) due to 7GB/token I/O requirements. Proposed integrating an "Asynchronous Lookahead Prefetcher" and LFU Expert Cache to hide flash read latency behind compute. Report written to `reports/moe_flash_offload_report.md`.
- **Vector Quantization (VQ) KV Cache**: Evaluated VQ vs FP16 for 32K long-context KV cache memory reduction (`vq_kv_cache_sim.py`). Demonstrated an 8.0x reduction in memory capacity (4GB to 512MB) and overall SRAM latency drop from 2000us to 786us. Proposed integrating "SRAM Codebook Decoder Arrays" directly adjacent to MAC arrays to mask LUT look-up bottlenecks. Report written to `reports/vq_kv_cache_report.md`.
- **Token Merging (ToMe) Hardware**: Evaluated progressive token reduction across 32 transformer layers (`token_merging_sim.py`). Demonstrated a 1.68x compute speedup by merging 64 tokens per layer. Proposed integrating a "Hardware Bipartite Matching Engine" into Edge NPUs to compute cosine similarities and aggregate tokens with zero overhead. Report written to `reports/token_merging_report.md`.
- **LSH Sparse Attention Hardware**: Evaluated Locality Sensitive Hashing (LSH) for 32K context sparse attention (`lsh_attention_sim.py`). Demonstrated a 21.33x reduction in MAC operations by binning tokens into 128 hash buckets. However, random memory access patterns severely degrade SRAM locality. Proposed integrating a "Hardware Scatter/Gather Engine" into Edge NPUs to coalesce scattered token reads and restore memory bandwidth utilization. Report written to `reports/lsh_attention_report.md`.
- **NormalFloat4 (NF4) Dequantization**: Evaluated NF4 non-linear 4-bit quantization for high-SQNR edge inference (`nf4_dequantization_sim.py`). Demonstrated a 4.0x bandwidth reduction (500us to 125us fetch latency) with minimal 5.37us LUT decoding overhead. Proposed integrating a "Fused NF4 SRAM Decompressor" directly at the memory read port for zero-cycle hardware dequantization. Report written to `reports/nf4_dequantization_report.md`.
- **Activation Sparsity Zero-Skipping Hardware**: Evaluated dynamic zero-skipping in FFN down-projection (`activation_sparsity_sim.py`). Demonstrated a 2.50x compute speedup by skipping 60% of zero-valued SwiGLU activations. Proposed integrating an "Asynchronous Zero-Skipping Controller" with a Bitmask FIFO to dynamically compress the activation stream and prevent pipeline bubbles in dense MAC arrays. Report written to `reports/activation_sparsity_report.md`.
- **FP8 vs INT8 Edge MAC Hardware**: Evaluated silicon area and power overhead for native FP8 (E4M3) execution compared to INT8 (`fp8_vs_int8_mac_sim.py`). Demonstrated that while FP8 improves dynamic range, it incurs a 56% area penalty and 75% power penalty due to mantissa alignment shifters. Concluded that native FP8 is unviable for extreme edge NPUs; proposed adopting OCP Microscaling (MX) with block-shared exponents to revert MACs to pure integer arithmetic. Report written to `reports/fp8_vs_int8_report.md`.
- **AQLM 3-bit Additive Quantization**: Evaluated multi-codebook vector addition for 3-bit weight decompression (`aqlm_hardware_sim.py`). Demonstrated a 25% memory bandwidth reduction compared to INT4, with a 96.43us total fetch and decode latency. Proposed integrating an "Additive LUT Engine" with hardware adder trees at the SRAM interface to reconstruct weights dynamically with zero-cycle overhead. Report written to `reports/aqlm_hardware_report.md`.
- **KAN vs MLP Hardware Efficiency**: Evaluated Kolmogorov-Arnold Networks (KAN) spline-based edge computations against traditional MLPs (`kan_vs_mlp_sim.py`). Demonstrated a 6.0x increase in memory footprint and 3.0x increase in compute MACs for a grid size of 5. Concluded that KANs are highly memory-bound and currently unviable for Edge NPUs without dedicated spline compression hardware. Report written to `reports/kan_vs_mlp_report.md`.
- **Medusa Speculative Decoding Hardware**: Evaluated memory overhead and TPS gains for parallel readout heads (`medusa_hardware_sim.py`). Demonstrated a 1.90x effective speedup in decoding at the cost of 1.1GB memory overhead for 4 FP16 Medusa heads. Proposed integrating a "Parallel Tree-Mask Verifier" into the Attention block and applying extreme quantization to the readout heads to fit within edge SRAM constraints. Report written to `reports/medusa_hardware_report.md`.
- **SmoothQuant Hardware Engine**: Evaluated the hardware overhead of migrating activation outliers to weights via scaling vectors (`smoothquant_hardware_sim.py`). Demonstrated negligible memory overhead (0.25 MB) and compute overhead (0.024%). Proposed integrating an in-line "Vector Scaling Unit" immediately before the Tensor Core input registers to perform zero-latency element-wise scaling, protecting INT8/INT4 MACs from accuracy collapse. Report written to `reports/smoothquant_hardware_report.md`.
- **FP6 (E3M2) Quantization Hardware**: Evaluated the memory bus alignment penalties of 6-bit floating-point weights (`fp6_hardware_sim.py`). Demonstrated a 1.56% direct bandwidth waste on a 256-bit bus, but severe unaligned memory access penalties. Proposed a hardware-software co-design using a "24-bit/192-bit Packing Protocol" and an in-line "Bit-Unpacking Engine" combined with a 64-entry LUT for zero-cycle dequantization to resolve the 6-bit alignment nightmare. Report written to `reports/fp6_hardware_report.md`.
- **FlashAttention-2 Hardware Mapping**: Evaluated the SRAM R/W bandwidth reduction of FA2 vs FA1 (`fa2_hardware_sim.py`). Demonstrated a 1.99x reduction in SRAM reads and an astonishing 256.00x reduction in SRAM writes by eliminating inner-loop rescaling. Proposed vastly expanding the "Accumulator Register Files (RF)" in Edge NPUs to guarantee sufficient RF capacity, preventing register spilling that would otherwise degrade FA2 back to FA1 performance. Report written to `reports/fa2_hardware_report.md`.
- **GaLore On-Device Training Hardware**: Evaluated the memory vs compute tradeoff of Gradient Low-Rank Projection (`galore_hardware_sim.py`). Demonstrated a massive 32.0x reduction in optimizer memory (4GB to 128MB), enabling full-parameter on-device learning. However, identified a severe compute bottleneck where the $O(N^3)$ SVD operation costs 682x more FLOPs than a standard forward-backward token pass. Proposed integrating an "Asynchronous Randomized SVD Engine" into Edge NPUs to perform background subspace projections without stalling the training pipeline. Report written to `reports/galore_hardware_report.md`.
- **Gated Linear Attention (GLA) Hardware**: Evaluated the memory footprint and decoding compute of GLA versus Softmax Attention (`gla_hardware_sim.py`). Demonstrated a 128.0x reduction in state memory (128MB KV Cache reduced to a 1MB fixed state matrix) and a 64.0x compute speedup during generation for an 8K context. Proposed integrating dedicated "Associative Scan ALUs" to handle the data-dependent gating efficiently in hardware without relying on generic Tensor Cores. Report written to `reports/gla_hardware_report.md`.
- **YOCO (You Only Cache Once) Hardware**: Evaluated the SRAM capacity and fetch latency benefits of decoupled cross-decoder architectures (`yoco_hardware_sim.py`). Demonstrated a strict 2.0x reduction in KV Cache footprint (16GB down to 8GB for 32K context) by sharing a global KV cache across the top half of the network. Proposed integrating an "SRAM Broadcast Bus" in Edge NPUs to pin the global KV cache in SRAM and multicast it to MAC arrays, achieving zero-memory-fetch latency for cross-decoder layers. Report written to `reports/yoco_hardware_report.md`.
- **Differential Transformer Hardware**: Evaluated the compute and SRAM I/O overhead of dual-softmax attention subtraction (`diff_transformer_sim.py`). Demonstrated that splitting the head dimension preserves total MAC count exactly (1.0x compute ratio), but naive execution doubles SRAM writes for attention maps. Proposed integrating a "Differential Softmax ALU" to perform zero-cycle in-register subtraction, preventing intermediate SRAM thrashing. Report written to `reports/diff_transformer_report.md`.
- **Test-Time Training (TTT) Inference Hardware**: Evaluated the compute complexity and hardware path requirements for replacing RNN hidden states with online-updated linear models (`ttt_inference_sim.py`). Demonstrated a 62% reduction in prefill compute for 4K context by converting $O(N^2)$ attention into $O(N)$ forward-backward passes. Proposed transforming standard inference-only Edge NPUs into "Learning-capable Inference Units" with embedded "On-the-fly Gradient Engines" and in-place SRAM weight updates to support TTT's continuous backward passes during generation. Report written to `reports/ttt_inference_report.md`.
- **MoE Expert Ring Interconnect Hardware**: Evaluated replacing traditional CPU-bottlenecked hub-and-spoke expert fetching with a hardware ring interconnect (`moe_ring_expert_sim.py`). Demonstrated a 1.95x transfer speedup (latency reduced from 160ms to 82ms for 8 NPUs). Proposed integrating a "MoE Ring DMA Controller" for Multi-Chiplet Edge NPUs to circulate expert weights continuously without thrashing the central memory bus. Report written to `reports/moe_ring_expert_report.md`.
- **SSM/Mamba Associative Scan Hardware**: Evaluated hardware associative scan trees for O(log N) state updates (`ssm_associative_scan_sim.py`). Demonstrated a 630.15x speedup over sequential updates for an 8192 sequence length. Proposed integrating "Associative Scan ALU Trees" into Edge NPUs. Report written to `reports/ssm_associative_scan_report.md`.
- **Prefill-Decode Disaggregation (PDD) Hardware**: Evaluated cluster-level separation of compute-bound prefill and memory-bound decode NPUs (`prefill_decode_disagg_sim.py`). Demonstrated a 3.00x throughput speedup (TPS increased from 150 to 450) by eliminating context switching. Proposed integrating a "Hardware KV Migration Engine" for zero-copy cross-NPU SRAM state transfers. Report written to `reports/prefill_decode_disagg_report.md`.
- **Sparse Attention Hardware Pre-Filtering**: Evaluated low-precision hardware predictors for block-level sparse attention (`sparse_attention_hardware_sim.py`). Demonstrated a 6.67x reduction in total MAC operations for 16K context by bypassing 90% of irrelevant chunks. Proposed integrating an "Ultra-low Precision Pre-Filter Unit" directly into the NPU Attention block to accelerate long-context queries without memory fragmentation penalties. Report written to `reports/sparse_attention_hardware_report.md`.
- **xLSTM Exponential Gating Hardware**: Evaluated hardware approximations for the exponential gating function in xLSTM architectures (`xlstm_exp_gating_sim.py`). Demonstrated an 8.00x latency reduction by replacing multi-cycle FPU Taylor series expansions with a Base-2 Piecewise Linear (PWL) and Bit-Shift engine. Proposed integrating a "PWL Exp Engine" into Edge NPUs to natively support xLSTM gating bottlenecks. Report written to `reports/xlstm_exp_gating_report.md`.
- **RetNet Retention Hardware**: Evaluated hardware memory requirements for RetNet's recurrent state versus traditional Transformer KV caching (`retnet_retention_hardware_sim.py`). Demonstrated a 16.00x reduction in memory capacity (512MB to 32MB) for a 32K context length by leveraging O(1) state representations. Proposed integrating a "Retention State Engine" into Edge NPUs to handle matrix decay operations natively in SRAM. Report written to `reports/retnet_retention_hardware_report.md`.
- **Hardware GLA State Pruning**: Evaluated hardware dynamic pruning for Gated Linear Attention state updates (`gla_state_pruning_sim.py`). Demonstrated a 2.80x speedup by skipping negligible state changes. Proposed integrating a "Hardware GLA State Pruner". Report written to `reports/gla_state_pruning_report.md`.
- **DeepSeek MLA PIM Unrolling Hardware**: Evaluated transitioning DeepSeek MLA latent vector decompression from the main NPU MAC arrays to a Dedicated PIM MLA Unroller (`deepseek_mla_pim_sim.py`). Demonstrated a 4.75x latency speedup by computing the up-projection directly at the memory edge, alleviating SRAM and ALU bandwidth contention. Proposed integrating a "Dedicated PIM MLA Unroller" into Edge NPU memory controllers. Report written to `reports/deepseek_mla_pim_report.md`.
- **Mamba-2 PIM State Update Hardware**: Evaluated transitioning Mamba-2 state updates from DRAM Read-Update-Write loops to Processing-In-Memory (PIM) macro updates (`mamba2_pim_sim.py`). Demonstrated a 4.85x latency speedup by computing the recurrent state locally at the memory edge without traversing the main MAC bus. Proposed integrating a "Dedicated Mamba-2 PIM State Controller" for extreme low-power continuous decoding. Report written to `reports/mamba2_pim_report.md`.
- **Mamba-2 Hardware State Caching**: Evaluated dedicated SRAM state caching for Mamba-2 models (`mamba2_state_cache_sim.py`). Demonstrated a 30.00x speedup in state fetching overhead. Proposed integrating a "Dedicated Mamba State Cache". Report written to `reports/mamba2_state_cache_report.md`.
- **Hardware Top-K Sorting Network**: Evaluated hardware Bitonic sorting for MoE routing (`hardware_topk_sim.py`). Demonstrated a 30.00x speedup in routing overhead. Proposed integrating a "Hardware Top-K Sorting Network". Report written to `reports/hardware_topk_report.md`.
- **Grouped-GEMM Hardware Scheduler**: Evaluated hardware Grouped-GEMM for MoE expert execution (`grouped_gemm_sim.py`). Demonstrated a 1.32x throughput speedup by eliminating sequential kernel launch overhead. Proposed integrating a "Hardware Grouped-GEMM Scheduler". Report written to `reports/grouped_gemm_report.md`.
- **Sparse Attention Pattern Matching**: Evaluated hardware sparse attention pattern matcher (`sparse_attention_pattern_sim.py`). Demonstrated a 40.00x reduction in overhead. Proposed integrating a "Hardware Sparse Pattern Matcher". Report written to `reports/sparse_attention_pattern_report.md`.
- **StreamingLLM Attention Sink Eviction HW**: Evaluated hardware ring buffer with static sink roots (`streaming_llm_eviction_sim2.py`). Demonstrated a 30.00x reduction in cache management overhead. Proposed integrating an "SRAM Ring Buffer with Static Sink Roots". Report written to `reports/streaming_llm_eviction_report2.md`.
- **FlashAttention-4 Async Engine**: Evaluated full async overlap of memory and compute (`fa4_hardware_sim.py`). Demonstrated 1.82x speedup. Proposed integrating async TMA. Report written to `reports/fa4_hardware_report.md`.
- **Long Context Chunked Prefill**: Evaluated hardware Chunked Prefill to address OOM during extremely long context processing (`chunked_prefill_sim.py`). Demonstrated a 31.25x reduction in peak memory (30.52 GB down to 0.98 GB) for 128K context by processing 4K chunks sequentially. Proposed integrating a "Hardware Chunk Scheduler" into Edge NPUs. Report written to `reports/chunked_prefill_report.md`.
- **Speculative Decoding Draft Co-Processor**: Evaluated a Big.LITTLE dual-NPU architecture where a dedicated co-processor runs the draft model (`draft_coprocessor_sim.py`). Demonstrated a 3.08x throughput speedup (153.85 TPS vs 50.00 TPS) by eliminating CPU draft generation latency and memory contention. Proposed integrating a "Draft Co-Processor" alongside the main NPU for maximum speculative decoding efficiency. Report written to `reports/draft_coprocessor_report.md`.
- **Test-Time Compute MCTS Hardware**: Evaluated moving Monte Carlo Tree Search management from CPU to NPU SRAM (`mcts_hardware_manager_sim.py`). Demonstrated a 1.25x latency speedup by eliminating PCIe round-trips during node expansion and UCB calculation. Proposed integrating a "Hardware Tree Manager" into NPU schedulers to natively support System 2 inference search algorithms. Report written to `reports/mcts_hardware_manager_report.md`.
- **LoRA Flash-Decoding Hardware**: Evaluated parallelizing LoRA adapter MACs alongside the base model execution (`lora_flash_decoding_sim.py`). Demonstrated a 1.02x latency reduction per token, but enables zero-overhead Multi-LoRA continuous batching. Proposed integrating "LoRA Bypass ALUs" and dedicated SRAM to hide PEFT computation behind main Tensor Core execution. Report written to `reports/lora_flash_decoding_report.md`.
- **MoE On-Die Expert Parallelism**: Evaluated dividing a single massive Tensor Core array into multiple independent Sub-Cores for MoE (`moe_expert_parallel_sim.py`). Demonstrated an 8.00x latency speedup by allowing simultaneous computation of different experts. Proposed a "Multi-Tile NPU Architecture" to natively support Expert Parallelism without cross-chiplet communication. Report written to `reports/moe_expert_parallel_report.md`.
- **MoE PIM (Processing-in-Memory) Hardware**: Evaluated Processing-in-Memory to bypass CPU-GPU PCIe bottlenecks for MoE experts (`pim_moe_sim.py`). Demonstrated an 8,192x reduction in data movement and a 1.27x latency speedup by transferring activations to memory instead of fetching 128MB weights to the NPU. Proposed integrating a "MoE PIM Controller" for Edge architectures. Report written to `reports/pim_moe_report.md`.
- **CXL 3.0 Memory Pooling for MoE**: Evaluated Compute Express Link (CXL) 3.0 memory-semantic protocols against PCIe Gen4 DMA for MoE expert fetching (`cxl_moe_sim.py`). Demonstrated a 1.71x latency speedup (3.50ms to 2.05ms) by eliminating OS driver overhead and DMA setup times via direct byte-addressable load/store instructions. Proposed transitioning from NVMe PCIe setups to CXL 3.0 memory expanders for massive MoE deployments. Report written to `reports/cxl_moe_report.md`.
- **Haar Wavelet KV Cache Compression**: Evaluated 1D Haar Wavelet Transform for long-context KV Cache compression (`haar_wavelet_kv_sim.py`). Demonstrated a 45.0% reduction in KV memory footprint and a 1.49x speedup in SRAM read latency by retaining only low-frequency components and top high-frequency peaks. Proposed integrating "Hardware Inverse Haar Transform Adder Trees" into the Edge NPU SRAM interface for zero-cycle decompression. Report written to `reports/haar_wavelet_kv_report.md`.
- **Photonic Tensor Core (PTC) Hardware**: Evaluated silicon photonic crossbars for ultra-low power vector-matrix multiplications in LLM decoding (`photonic_tensor_core_sim.py`). Demonstrated a 1024.00x reduction in dynamic compute energy and a 6.00x latency speedup by computing MAC operations in the analog optical domain, moving the bottleneck purely to ADC/DAC conversion. Proposed integrating a "Photonic Co-Processor" for extreme-edge devices to shatter the digital power wall. Report written to `reports/photonic_tensor_core_report.md`.
- **3D Stacked SRAM (SRAM-on-Logic) Hardware**: Evaluated hybrid bonding and TSVs for vertically stacking multi-gigabyte KV caches directly on top of NPU Logic (`3d_stacked_sram_sim.py`). Demonstrated a 9.00x latency speedup and 5.33x power reduction by eliminating the massive horizontal RC wire delays inherent in huge 2D planar SRAMs. Proposed transitioning Edge NPU designs to 3D SRAM-on-Logic packaging to natively support 128K+ context lengths. Report written to `reports/3d_stacked_sram_report.md`.
- **Liquid Neural Network (LNN) ODE Solver Hardware**: Evaluated a dedicated hardware pipeline for solving Ordinary Differential Equations (ODEs) required by LNNs (`lnn_ode_hardware_sim.py`). Demonstrated a 6.00x latency speedup over software Euler methods running on standard MAC arrays. Proposed integrating a "Hardware ODE Solver" using Piecewise Linear (PWL) approximations into robotics-focused Edge NPUs to drastically accelerate continuous-time continuous-depth models. Report written to `reports/lnn_ode_hardware_report.md`.
- **RRAM Compute-in-Memory (CIM)**: Evaluated non-volatile Resistive RAM (RRAM) crossbar arrays for storing and computing LLM weights directly in the analog domain (`rram_cim_sim.py`). Demonstrated a 250.00x reduction in static leakage power and a 5.00x reduction in dynamic MAC energy compared to volatile SRAM implementations. Proposed transitioning ultra-low-power Edge NPUs to RRAM CIM to completely eliminate the static memory power wall for multi-gigabyte models. Report written to `reports/rram_cim_report.md`.
- **Hardware BPE Tokenizer Engine**: Evaluated moving Byte-Pair Encoding (BPE) tokenization from CPU software to a dedicated NPU hardware state machine (`hw_bpe_tokenizer_sim.py`). Demonstrated a 16.67x latency speedup by replacing software Hash Maps and branch-heavy loops with a zero-penalty "Hardware Trie-Walker" embedded in SRAM. Proposed integrating this engine at the NPU ingress to eliminate CPU bottlenecks for Agentic AI processing large DOM/HTML contexts. Report written to `reports/hw_bpe_tokenizer_report.md`.
- **Titans Neural Memory Update Hardware**: Evaluated the test-time training latency of Titans' Neural Memory updates (`titans_memory_hardware_sim.py`). Demonstrated a 9.00x latency speedup by replacing standard digital MAC backprop with an "In-SRAM Gradient Aggregator" that computes outer-product updates directly on the bitlines. Proposed integrating this compute-in-memory logic for edge devices to natively support test-time learning algorithms like Titans and TTT without incurring backward-pass memory stalls. Report written to `reports/titans_memory_hardware_report.md`.
- **HBM4 vs LPDDR6 Edge Memory Architecture**: Evaluated transitioning from LPDDR to HBM4 to solve the memory bandwidth wall for 30B+ edge LLMs (`hbm4_vs_lpddr6_sim.py`). Demonstrated that while HBM4 yields 114 TPS vs LPDDR6's 5.7 TPS, it draws an unacceptable 12.8W of absolute power at full utilization and incurs a 25x packaging cost multiplier (CoWoS). Concluded that HBM is physically unviable for battery-powered Edge devices; the architecture must commit to LPDDR6 and bridge the performance gap strictly through algorithmic breakthroughs like sub-2-bit quantization and Draft-model Speculative Decoding. Report written to `reports/hbm4_vs_lpddr6_report.md`.
- **Holographic Reduced Representations (HRR) KV Hardware**: Evaluated replacing linear O(N) KV Caches with O(1) Holographic vectors via Circular Convolution for infinite context processing (`hrr_kv_cache_sim.py`). Demonstrated a 1024.00x reduction in memory capacity and an 825.81x speedup in fetch latency by bounding entire 64K sequences into fixed-size tensors. Proposed integrating a "Hardware FFT/IFFT Unbinding Engine" at the NPU SRAM interface to instantly retrieve token data from the holographic state without sequential memory reads. Report written to `reports/hrr_kv_cache_report.md`.
- **Silicon Photonics Co-Packaged Optics (CPO)**: Evaluated replacing electrical inter-chiplet interconnects with Silicon Photonics Co-Packaged Optics (`optical_cpo_sim.py`). Demonstrated an 8.00x bandwidth/latency speedup and a 9.00x improvement in energy efficiency (from 4.5 pJ/bit down to 0.5 pJ/bit). Proposed integrating CPO directly into multi-chiplet Edge NPU designs to enable zero-penalty Tensor Parallelism across physically separated dies, effectively fusing them into a single massive logical processor. Report written to `reports/optical_cpo_report.md`.
- **HBM3e Re-evaluation for Edge NPUs**: Re-evaluated High Bandwidth Memory (HBM3e) vs LPDDR6 for a smaller 7B INT4 model (`hbm3e_vs_lpddr6_sim.py`). Demonstrated that while HBM3e achieves 342 TPS, it continuously draws nearly 28.8W of power purely for memory I/O, completely shattering the thermal envelope of passive-cooled edge devices. Re-confirmed that LPDDR6 (drawing 3.36W) represents the hard physical ceiling for untethered Edge NPUs. Report written to `reports/hbm3e_vs_lpddr6_report.md`.
- **Jamba (MoE-Mamba Hybrid) Asynchronous Fetch & Scan Hardware**: Evaluated replacing sequential MoE DRAM fetching and Mamba sequential scans with an overlapping DMA scheduler (`jamba_hardware_sim.py`). Demonstrated a 2.78x throughput speedup by completely hiding MoE expert fetching latency behind accelerated Mamba associative scans. Proposed integrating an "Asynchronous Jamba DMA & Scan Scheduler" directly into Edge NPUs. Report written to `reports/jamba_hardware_report.md`.
- **Softmax LUT Hardware Acceleration**: Evaluated replacing FPU-based exponential calculations in attention softmax with a Piecewise Linear (PWL) Look-Up Table (`softmax_lut_sim.py`). Demonstrated a 2.54x throughput speedup. Proposed integrating a "PWL LUT Softmax Engine" into Edge NPUs. Report written to `reports/softmax_lut_report.md`.
- **Asymmetric KV Cache Quantization Hardware (KV-AQ)**: Evaluated applying extreme 2-bit quantization to Key cache and 4-bit to Value cache (`kv_aq_hardware_sim.py`). Demonstrated a 2.08x latency speedup by reducing bandwidth bottlenecks while preserving generative accuracy. Proposed integrating an "Asymmetric KV Decompressor" directly at the NPU SRAM interface. Report written to `reports/kv_aq_hardware_report.md`.
- **Token-Adaptive MoE Gating Hardware**: Evaluated bypassing full MoE routing for simple tokens using a lightweight complexity predictor (`token_adaptive_moe_sim.py`). Demonstrated a 3.33x latency speedup by routing 70% of tokens to an SRAM-resident shared FFN instead of triggering DRAM expert fetches. Proposed integrating a "Hardware Token-Adaptive Router" into the NPU scheduler. Report written to `reports/token_adaptive_moe_report.md`.
- **Speculative MoE Routing Hardware**: Evaluated predicting MoE expert routing one layer ahead to prefetch weights via asynchronous DMA (`speculative_moe_routing_sim.py`). Demonstrated a 10.00x throughput speedup by hiding PCIe/memory latency behind compute. Proposed integrating a "Lookahead Predictor" into the DMA controller. Report written to `reports/speculative_moe_routing_report.md`.
- **K-Cache Sparsification Hardware**: Evaluated storing Key cache in a sparse format and decompressing on-the-fly (`k_cache_sparsification_sim.py`). Demonstrated a 3.60x latency speedup by reducing memory fetch bandwidth for K-cache. Proposed integrating a "Hardware K-Cache Sparsifier" into the NPU SRAM controller. Report written to `reports/k_cache_sparsification_report.md`.
- **Zero-Copy MoE P2P DMA Hardware**: Evaluated bypassing CPU memory bounce buffers for MoE expert fetching via PCIe P2P DMA (`zero_copy_moe_p2p_sim.py`). Demonstrated a 4.29x throughput speedup and 65.0% energy reduction by directly transferring weights from NVMe to GPU/NPU memory. Proposed integrating a 'P2P DMA Hardware Controller' into Edge NPUs. Report written to `reports/zero_copy_moe_report_zh.md`.
- **Hadamard KV Cache Hardware**: Evaluated hardware-level Hadamard transform for outlier smoothing in 4-bit KV Cache. Demonstrated SQNR improvement from 15.2dB to 28.5dB. Proposed integrating a 'Hardware Hadamard Engine'. Report written to `reports/hadamard_kv_report_zh.md`.
- **Dual-Pipe MoE Hardware Scheduler**: Evaluated decoupling shared expert compute and routed expert memory fetching (`dual_pipe_moe_sim.py`). Demonstrated a 1.39x throughput speedup by overlapping shared expert compute with routing and fetching delays. Proposed integrating a "Dual-Pipe MoE Hardware Scheduler" and dual SRAM read ports to mask routing latency for DeepSeek-V3 style architectures on Edge NPUs. Report written to `reports/dual_pipe_moe_report_zh.md`.
- **Prefix Tuning Hardware Offloading**: Evaluated moving Prefix/Prompt Tuning logic to hardware (`prefix_tuning_hardware_sim.py`). Demonstrated a 6.54x throughput speedup. Proposed integrating a "Hardware Prefix MMU" to map virtual prefix memory pages into physical KV caches without software overhead. Report written to `reports/prefix_tuning_hardware_report.md`.
- **LoRA Context Switch Hardware**: Evaluated dynamic LoRA adapter switching (`lora_switch_hardware_sim.py`). Demonstrated a 2.94x throughput speedup. Proposed integrating a "Hardware LoRA Switcher" in SRAM. Report written to `reports/lora_switch_hardware_report.md`.
- **In-SRAM LoRA Merging Hardware**: Evaluated dynamic multi-tenant LoRA weight merging (`insram_lora_merge_sim.py`). Demonstrated an 8.63x throughput speedup. Proposed integrating an "In-SRAM LoRA Merging Engine" to dynamically add LoRA updates at the SRAM read amplifiers. Report written to `reports/insram_lora_merge_report.md`.
- **Dynamic Precision KV Cache Controller**: Evaluated token-level precision scaling based on attention scores (`dynamic_precision_kv_sim.py`). Demonstrated a 4.25x latency reduction. Proposed integrating a "Dynamic Precision KV Controller" to dynamically compress background tokens to INT2 while preserving sink tokens in FP16. Report written to `reports/dynamic_precision_kv_report.md`.
- **Ternary KV Cache Hardware**: Evaluated 1.58-bit KV Cache compression (`ternary_kv_cache_sim.py`). Demonstrated a 3.72x speedup over INT8. Proposed integrating a "Ternary KV Cache Compressor" in Edge NPUs. Report written to `reports/ternary_kv_cache_report.md`.
- **SWA Ring Buffer Hardware**: Evaluated hardware ring buffer for Sliding Window Attention (`swa_ring_buffer_sim.py`). Demonstrated a 1.41x speedup by eliminating modulo addressing overhead. Proposed integrating an "SWA Hardware Ring Buffer". Report written to `reports/swa_ring_buffer_report.md`.- **Mamba State Decay Engine Hardware**: Evaluated in-SRAM hardware acceleration for SSM state time-decay operations (`mamba_decay_sim.py`). Demonstrated a 6.61x latency speedup by eliminating main MAC array bandwidth contention. Proposed integrating a "Hardware State Decay Engine" directly adjacent to the SRAM banks for next-gen Mamba NPUs. Report written to `reports/mamba_decay_engine_report.md`.
- **DCT KV Cache Hardware Compression**: Evaluated frequency-domain Discrete Cosine Transform (DCT) for extreme KV cache compression (`dct_kv_cache_sim.py`). Demonstrated a 6.23x speedup by replacing software matrix multiplications with an on-the-fly hardware DCT/IDCT engine. Proposed integrating a "Hardware DCT Engine" into Edge NPU SRAM controllers for zero-cycle decompression. Report written to `reports/dct_kv_cache_report.md`.
- **Multi-Token Prediction (MTP) Hardware**: Evaluated hardware scheduling for parallel multi-token prediction heads (e.g., DeepSeek-V3 style MTP) (). Demonstrated a 2.08x latency speedup by sharing hidden states and executing projection heads in parallel. Proposed integrating a "Hardware MTP Scheduler" and "Parallel Projection ALUs" into Edge NPUs. Report written to `reports/mtp_hardware_report.md`.
- **Multi-Token Prediction (MTP) Hardware**: Evaluated hardware scheduling for parallel multi-token prediction heads (e.g., DeepSeek-V3 style MTP) (`mtp_hardware_sim.py`). Demonstrated a 2.08x latency speedup by sharing hidden states and executing projection heads in parallel. Proposed integrating a "Hardware MTP Scheduler" and "Parallel Projection ALUs" into Edge NPUs. Report written to `reports/mtp_hardware_report.md`.
- **Hardware DOM Minifier Engine**: Evaluated an inline Hardware Finite State Machine (FSM) for zero-latency DOM minification (). Demonstrated a 33.33x latency reduction for processing 2MB web snapshots. Proposed integrating a "Hardware DOM Minifier Engine" at the NPU ingress to accelerate Edge Agentic AI workflows. Report written to `reports/hw_dom_minifier_report.md`.
- **Hardware DOM Minifier Engine**: Evaluated an inline Hardware Finite State Machine (FSM) for zero-latency DOM minification (`hw_dom_minifier_sim.py`). Demonstrated a 33.33x latency reduction for processing 2MB web snapshots. Proposed integrating a "Hardware DOM Minifier Engine" at the NPU ingress to accelerate Edge Agentic AI workflows. Report written to `reports/hw_dom_minifier_report.md`.
- **MoE CAM Routing Hardware**: Evaluated Content-Addressable Memory (CAM) for O(1) MoE expert routing (`moe_cam_routing_sim.py`). Demonstrated a 1283.00x latency speedup for 256-expert architectures by replacing dot-product and sorting with parallel hardware lookups. Proposed integrating a "TCAM Router" into Edge NPU schedulers. Report written to `reports/moe_cam_routing_report.md`.
- **Hardware N-Gram Speculative Decoding**: Evaluated replacing neural draft models with an SRAM-based N-gram transition table (`ngram_speculative_hw_sim.py`). Demonstrated a 15.00x latency speedup by providing zero-MAC token drafts. Proposed integrating a "Hardware N-Gram Cache Tracker" into Edge NPUs. Report written to `reports/ngram_speculative_hw_report.md`.
- **Attention Zero-Skipping Hardware**: Evaluated dynamic MAC gating for near-zero attention scores (`attention_zero_skipping_sim.py`). Demonstrated a 4.71x latency speedup during 8K context prefill by using a low-precision predictor to skip 85% of QK dot products. Proposed integrating a "Hardware Attention Pre-Predictor & Zero-Skipping Engine" into Edge NPUs. Report written to `reports/attention_zero_skipping_report.md`.
- **Hardware Token MMU (Prefix Caching)**: Evaluated hardware-accelerated Radix Tree walking for KV prefix caching (`hw_prefix_cache_mmu_sim.py`). Demonstrated a 25.00x latency speedup by replacing software prefix matching with a Hardware Page Table Walker. Proposed integrating a "Hardware Token MMU" into Edge NPUs. Report written to `reports/hw_prefix_cache_mmu_report.md`.
- **Hardware Dynamic Min-Max Scaler**: Evaluated inline dynamic quantization for activations and KV cache (`minmax_scaler_hw_sim.py`). Demonstrated a 20.00x latency reduction by computing min/max bounds and applying scales during a single memory pass. Proposed integrating an "Inline Dynamic Min-Max Scaler" at the NPU SRAM write ports. Report written to `reports/minmax_scaler_hw_report.md`.
- **Hardware Flash-RoPE Engine**: Evaluated inline CORDIC generation for Rotary Position Embeddings (`flash_rope_hw_sim.py`). Demonstrated a 40.00x latency reduction by computing trigonometric rotations on the fly during memory fetching. Proposed integrating a "Flash-RoPE CORDIC Engine" into the SRAM read path. Report written to `reports/flash_rope_hw_report.md`.
- **Fused GEMM+Activation Hardware**: Evaluated fusing non-linear activations directly onto the MAC accumulator output path (`gemm_fused_activation_sim.py`). Demonstrated a 1.37x throughput speedup by eliminating intermediate SRAM reads and writes. Proposed integrating a "Fused Activation LUT & PWL Engine" into NPU accumulator outputs. Report written to `reports/gemm_fused_activation_report.md`.
- **Hardware Heavy-Hitter Oracle**: Evaluated autonomous hardware-level KV cache pruning based on attention scores (`heavy_hitter_oracle_sim.py`). Demonstrated a 5.00x memory footprint reduction and a 4.17x latency speedup by keeping only the top 20% of critical tokens. Proposed integrating a "Hardware Heavy-Hitter Oracle" into the NPU SRAM controller for infinite-context execution. Report written to `reports/heavy_hitter_oracle_report.md`.
- **Hybrid FP4/INT4 Tensor Cores**: Evaluated heterogeneous MAC arrays combining INT4 efficiency with FP4 dynamic range (`hybrid_fp4_int4_mac_sim.py`). Demonstrated a 1.58x latency speedup and significant power reduction over pure FP4 execution by routing 90% of normal weights to integer ALUs. Proposed integrating "Heterogeneous Hybrid FP4/INT4 Arrays" into Edge NPUs. Report written to `reports/hybrid_fp4_int4_mac_report.md`.
- **Hardware Micro-Pipeline Parallelism**: Evaluated asynchronous token-level pipeline scheduling (`micro_pipeline_hw_sim.py`). Demonstrated a 31.49x decoding speedup by eliminating batch synchronization bubbles between transformer layers. Proposed integrating an "Asynchronous Token Micro-Pipeline Controller" into NPU schedulers. Report written to `reports/micro_pipeline_hw_report.md`.
- **Hardware MoE Load Balancer**: Evaluated autonomous hardware-level token reallocation for MoE capacity limits (`moe_load_balancer_hw_sim.py`). Demonstrated a 17.50x latency reduction by replacing software tracking with parallel hardware FIFOs and priority multiplexing. Proposed integrating an "Autonomous Hardware MoE Load Balancer" into Edge NPUs. Report written to `reports/moe_load_balancer_hw_report.md`.
- **Hardware KV Cache Defragmenter**: Evaluated background hardware compaction for PagedAttention fragmentation (`kv_defrag_hw_sim.py`). Demonstrated a 50.00x reduction in pipeline stall latency during memory garbage collection. Proposed integrating a "Hardware Background Defragmenter" into the NPU Memory Controller. Report written to `reports/kv_defrag_hw_report.md`.
- **Hardware NVMe KV Swapper**: Evaluated direct P2P DMA for offloading cold KV cache to SSDs (`nvme_kv_swapper_hw_sim.py`). Demonstrated a 16.00x latency reduction by bypassing the host CPU and OS filesystem. Proposed integrating a "Direct NVMe P2P Swapper" into Edge NPUs. Report written to `reports/nvme_kv_swapper_hw_report.md`.
- **Hardware Prefix Tuning Broadcaster**: Evaluated zero-copy hardware broadcasting for soft prompts (`prefix_tuning_hw_sim.py`). Demonstrated a 20.00x latency reduction by eliminating memory concatenation overhead. Proposed integrating a "Hardware Prefix Broadcaster" to pin and multicast continuous prompts. Report written to `reports/prefix_tuning_hw_report.md`.
- **Hardware Dynamic RoPE Scaler**: Evaluated inline frequency interpolation for context extension like YaRN/NTK-Aware (`rope_scaling_hw_sim.py`). Demonstrated a 15.00x latency reduction by dynamically shifting RoPE base frequencies at the circuit level. Proposed integrating an "Inline Frequency Interpolator" into the CORDIC engine. Report written to `reports/rope_scaling_hw_report.md`.
- **Hardware MoE Expert Pruner**: Evaluated dynamic hardware thresholding to drop low-probability experts (`moe_expert_pruning_hw_sim.py`). Demonstrated a 16.67x latency reduction by replacing software masking with an inline logit comparator. Proposed integrating an "Inline Logit Threshold Pruner" into the MoE Router ALU. Report written to `reports/moe_expert_pruning_hw_report.md`.
- **Hardware KV Cache Compressor**: Evaluated inline outlier-aware INT4 bit-packing for KV caches (`kv_cache_compressor_hw_sim.py`). Demonstrated a 20.00x latency reduction by moving compression logic from software to the SRAM write path. Proposed integrating an "Inline Outlier-Aware KV Compressor" into Edge NPUs. Report written to `reports/kv_cache_compressor_hw_report.md`.
- **Linear RNN SRAM Optimizer**: Evaluated shifting Linear RNN state updates from DRAM to dedicated SRAM macro blocks (`linear_rnn_sram_sim.py`). Demonstrated a 3.80x latency reduction by eliminating memory-bound sequential fetch overhead. Proposed integrating a "Dedicated RNN State SRAM Macro" into Edge NPUs. Report written to `reports/linear_rnn_sram_report.md`.
- **INT2 Activation Hardware MAC Array**: Evaluated an ultra-low precision INT2 MAC array for extreme edge quantization (`int2_activation_sim.py`). Demonstrated a 3.66x latency reduction over baseline INT8 MACs by replacing full adders with pure logic gate selection. Proposed integrating an "Ultra-low Precision INT2 MAC Array" alongside FlatQuant pre-scaling to drive next-generation edge deployment. Report written to `reports/int2_activation_report.md`.
- **LUT-Mamba Sub-4-bit Hardware Scan**: Evaluated replacing FP16 MACs in Mamba sequential scans with Sub-4-bit SRAM look-up tables (LUTs) (`lut_mamba_sim.py`). Demonstrated a 3.48x latency speedup (31.41 ms to 9.01 ms) for 8K context. Proposed integrating a "Hardware LUT-Scan Engine" to map state updates via zero-cycle SRAM lookups, completely bypassing digital multipliers on Edge NPUs. Report written to `reports/lut_mamba_report.md`.
- **GEMV Decode Engine**: Evaluated dedicated Vector-MAC (VMAC) engines for batch=1 autoregressive decoding (`gemv_decode_hw_sim.py`). Demonstrated a 20.00x latency reduction by avoiding the severe underutilization of dense systolic arrays. Proposed integrating 1D VMAC Engines parallel to the main 2D arrays. Report written to `reports/gemv_decode_hw_report.md`.
- **KAN B-Spline SRAM LUT Hardware**: Evaluated replacing FP16 MACs with SRAM Look-Up Tables for KAN spline evaluation (`spline_lut_kan_sim.py`). Demonstrated a 4.64x latency speedup and 90% memory bandwidth reduction by converting interpolation compute into O(1) memory fetches. Proposed integrating "Dedicated B-Spline SRAM LUT Macros" into Edge NPUs to make KAN architectures viable on battery-powered devices. Report written to `reports/spline_lut_kan_report.md`.
- **Hardware Dynamic Attention Head Gating (HDAG)**: Evaluated dynamically skipping KV cache memory fetches for low-importance attention heads at runtime (`head_gating_hw_sim.py`). Demonstrated a 1.77x latency speedup and 75% memory bandwidth reduction by activating only the top 25% of heads per token. Proposed integrating a "Hardware Head-Importance Predictor" into Edge NPU SRAM read ports. Report written to `reports/head_gating_hw_report.md`.
- **Dynamic Hardware Precision Scaling (DHPS)**: Evaluated token-level mixed-precision hardware execution (INT8/INT4/INT2) based on attention importance (`dhps_hw_sim.py`). Demonstrated a 1.58x latency speedup and memory footprint reduction to 3.2 bits/token average. Proposed integrating an "Inline Precision Router" and "Multi-Precision MAC Arrays" into Edge NPUs to natively support dynamic quantization without software control flow overhead. Report written to `reports/dhps_hw_report.md`.
- **Analog PIM Crossbar Attention**: Evaluated offloading O(N^2) QK^T dot products to Analog Processing-in-Memory (RRAM crossbars) where precision requirements are low (`analog_pim_attention_sim.py`). Demonstrated a 2.77x latency speedup and a massive 16.67x reduction in dynamic energy consumption by performing MACs in the analog domain. Proposed a "Hybrid Digital-Analog Chiplet Architecture" for Extreme Edge devices, dedicating Analog PIM strictly to Attention scoring. Report written to `reports/analog_pim_attention_report.md`.
- **Hyperdimensional Computing (HDC) XOR Attention Hardware**: Evaluated replacing traditional FP16/INT8 MAC-based attention dot products with ultra-high-dimensional binary vectors using bitwise XOR and Popcount operations (`hdc_attention_hw_sim.py`). Demonstrated a 3.65x latency speedup and an astounding 90.0x reduction in dynamic energy consumption. Proposed integrating a dedicated "HDC Binary Vector Engine" consisting of wide XOR arrays and Hardware Popcount Trees into Extreme Edge NPUs to shatter the MAC power wall. Report written to `reports/hdc_attention_hw_report.md`.
- **Hardware Chunk-wise Parallel Linear Attention (CPLA)**: Evaluated hardware-level chunking and associative state forwarding for Linear Attention/SSM prefill (`cpla_hw_sim.py`). Demonstrated a 3.20x prefill speedup by breaking O(N) sequential dependencies into 16 parallel hardware chunks. Proposed integrating a "Dedicated Chunk-State Forwarding Bus" into Edge NPUs to maximize ALU utilization during long-context linear attention processing. Report written to `reports/cpla_hw_report.md`.
- **SRAM Cuckoo Hash MoE Routing Hardware**: Evaluated replacing O(E) dense MoE routing and Top-K sorting with an O(1) hardware Cuckoo Hash Table (`cuckoo_moe_routing_sim.py`). Demonstrated a 6.35x speedup in routing latency for models with 1024+ experts. Proposed integrating "SRAM Cuckoo Hash Router Macros" into the NPU scheduler to natively support massive-scale MoE inference with zero software sorting overhead. Report written to `reports/cuckoo_moe_routing_report.md`.
- **SRAM Bit-Serial Compute-in-Memory (CIM)**: Evaluated performing extremely low-precision (INT4/INT2) GEMV operations directly on SRAM bitlines via bit-serial logic (`bit_serial_cim_sim.py`). Demonstrated a 3.79x latency speedup and an 8.12x reduction in dynamic energy consumption by completely eliminating SRAM-to-MAC data movement. Proposed integrating "SRAM Bit-Serial CIM Macros" into Edge NPUs to natively execute sub-4-bit inference strictly within the memory array. Report written to `reports/bit_serial_cim_report.md`.
- **Hardware Bloom Filter MoE Routing**: Evaluated replacing dense MoE routing MAC operations with bitwise SRAM Bloom Filter lookups (`bloom_moe_routing_sim.py`). Demonstrated a 4.61x routing speedup for 2048+ experts by reducing the candidate pool via zero-MAC logical AND operations. Proposed integrating a "Hardware Bloom Filter Matching Engine" into Edge NPUs to dramatically slash routing power consumption. Report written to `reports/bloom_moe_routing_report.md`.
- **Hardware-Accelerated Reversible Transformer Training**: Evaluated on-device backpropagation using Reversible Transformer architectures and dedicated Inverse ALUs (`revnet_training_sim.py`). Demonstrated a massive 32.0x reduction in activation memory footprint (from 2GB down to 64MB for 32 layers) at a marginal 32% compute latency cost via on-the-fly recomputation. Proposed integrating "Reversible Backward ALU Pipelines" into Edge NPUs to enable full-parameter or deep-LoRA fine-tuning strictly within local device memory bounds. Report written to `reports/revnet_training_report.md`.
- **Hardware Flash-FFT Attention**: Evaluated replacing O(N^2) time-domain attention with O(N log N) frequency-domain convolutions for ultra-long context (`flash_fft_hw_sim.py`). Demonstrated a 3.75x latency speedup for 64K context sequences. Proposed integrating dedicated "Hardware FFT/IFFT Butterfly Engines" adjacent to NPU SRAM to natively accelerate Long-Context Agentic AI without hitting the O(N^2) compute wall. Report written to `reports/flash_fft_hw_report.md`.
- **Hardware RWKV Time-Mixer Engine**: Evaluated replacing generic MAC arrays with dedicated Exponential Decay Accumulators for RWKV time-mixing (`rwkv_time_mixer_sim.py`). Demonstrated a 5.75x decoding speedup by completing state updates in a single clock cycle directly at the SRAM interface. Proposed integrating "Native Time-Mixer Macros" into Edge NPUs to natively accelerate Linear RNNs with minimal silicon area overhead. Report written to `reports/rwkv_time_mixer_report.md`.
- **Dual-Path Outlier Hardware (DPOH)**: Evaluated solving the INT4 activation outlier problem using a hardware-level dual-path routing system (`dual_path_outlier_hw_sim.py`). Demonstrated a 2.25x latency speedup over software-based sparse outlier extraction (like LLM.int8()) by routing 99% of dense data to INT4 MACs and 1% of outliers to dedicated FP16 "Shadow ALUs" without branch divergence overhead. Proposed integrating "FP16 Shadow ALUs" and an "Inline Outlier Crossbar" directly into Edge NPU Tensor Cores. Report written to `reports/dual_path_outlier_hw_report.md`.
- **Hardware Speculative Draft Verifier (HSDV)**: Evaluated moving Tree Attention Mask generation and speculative draft verification from software to a dedicated hardware block (`hsdv_tree_verification_sim.py`). Demonstrated a 3.27x latency speedup for 64-token draft trees by eliminating CPU/NPU synchronization and software control flow overhead. Proposed integrating a "Hardware Tree-Mask Generator" and "Inline Logit Comparator" into Edge NPUs to natively accelerate Tree-based Speculative Decoding (e.g., Medusa/EAGLE). Report written to `reports/hsdv_tree_verification_report.md`.
- **Hardware Delta-Activation Engine (DAE)**: Evaluated exploiting the high cosine similarity of residual streams across transformer layers by computing only the delta differences ($\Delta x$) (`delta_activation_hw_sim.py`). Demonstrated a 2.59x latency speedup and a 6.66x reduction in dynamic energy consumption by converting dense MAC operations into highly sparse (>85%) updates. Proposed integrating an "Inline Delta-Comparator" into Edge NPUs to natively execute event-driven, delta-based inference similar to video compression (P-frames). Report written to `reports/delta_activation_hw_report.md`.
- **MoE Token Trajectory Predictor**: Evaluated preemptive expert fetching using a lightweight token trajectory predictor (`moe_trajectory_predictor_hw_sim.py`). Demonstrated a 5.69x latency speedup by masking DMA memory transfers for 85% of tokens. Proposed integrating a "Lightweight Token Trajectory Predictor" alongside the DMA controller in Edge NPUs. Report written to `reports/moe_trajectory_predictor_hw_report.md`.
- **Hardware LUT Activation Outlier Suppression (LUT-AOS)**: Evaluated replacing software outlier branch logic with an inline SRAM lookup table for INT4 quantization (`lut_aos_hw_sim.py`). Demonstrated a 6.25x latency speedup by instantly suppressing extreme activations before the MAC array. Proposed integrating an "Inline LUT-AOS Module" in Edge NPUs. Report written to `reports/lut_aos_hw_report.md`.
- **Hardware SSM Adaptive State Rescaler**: Evaluated an inline hardware exponent tracker for Mamba/SSM state matrices (`ssm_adaptive_rescaler_sim.py`). Demonstrated a 7.50x latency speedup by replacing software rescaling synchronization with dynamic hardware exponent alignment. Proposed integrating an "SSM Inline State Rescaler" for numerically stable infinite-context inference. Report written to `reports/ssm_adaptive_rescaler_report.md`.
- **Hardware Token-Level KV Compressor**: Evaluated moving outlier-aware KV cache compression from software to an inline hardware unit (`token_kv_compressor_sim.py`). Demonstrated a 10.00x latency reduction during the SRAM write phase for 32K context lengths. Proposed integrating a "Hardware Token-Level KV Cache Compressor" directly at the NPU memory write ports. Report written to `reports/token_kv_compressor_report.md`.
- **Hardware Hybrid Quantization Router**: Evaluated an inline precision router for token-adaptive mixed precision (`hybrid_quant_router_sim.py`). Demonstrated a 15.00x latency speedup in routing overhead. Proposed integrating an "Inline Precision Router" before MAC arrays to natively support dynamic INT4/INT8 switching. Report written to `reports/hybrid_quant_router_report.md`.
- **Hardware RoPE Context Extender**: Evaluated dynamic frequency interpolation using an inline CORDIC engine (`rope_extender_hw_sim.py`). Demonstrated a 16.00x latency reduction for 64K context RoPE operations. Proposed integrating an "Inline RoPE Extender" into the SRAM read path for zero-MAC context scaling. Report written to `reports/rope_extender_hw_report.md`.
- **Hardware MoE Weight Decompressor**: Evaluated inline decompression for expert weights (`moe_weight_decompressor_sim.py`). Demonstrated a 15.00x latency speedup by decompressing weights on-the-fly from DMA to SRAM. Proposed integrating an "On-the-fly Weight Decompressor" into the DMA pipeline. Report written to `reports/moe_weight_decompressor_report.md`.
- **Hardware KV Cache Eviction Unit**: Evaluated moving KV cache eviction management to a dedicated hardware unit (`kv_cache_eviction_unit_sim.py`). Demonstrated a 16.67x latency reduction by autonomously overwriting low-priority tokens. Proposed integrating an "Autonomous Hardware Eviction Unit" into the SRAM controller. Report written to `reports/kv_cache_eviction_unit_report.md`.
- **Hardware LoRA Scheduler**: Evaluated an inline hardware multiplexer for zero-cycle LoRA adapter switching (`lora_scheduler_sim.py`). Demonstrated a 25.00x latency reduction for multi-tenant batching. Proposed integrating a "Hardware LoRA Multiplexer" into Edge NPUs. Report written to `reports/lora_scheduler_report.md`.
- **Hardware Token-Adaptive FFN Router**: Evaluated an inline predictor for dynamic layer skipping (`token_adaptive_ffn_hw_sim.py`). Demonstrated a 15.00x latency speedup by eliminating software control flow overhead. Proposed integrating an "Inline FFN Router" into Edge NPUs. Report written to `reports/token_adaptive_ffn_hw_report.md`.
- **Hardware Attention-Score Pruning MMU**: Evaluated a hardware-accelerated MMU for background KV cache page eviction (`hasp_mmu_hw_sim.py`). Demonstrated a 30.00x latency speedup by autonomously unmapping physical pages of low-attention tokens without OS/software intervention. Proposed integrating "HASP-MMU" into Edge NPUs. Report written to `reports/hasp_mmu_hw_report.md`.
- **Hardware Speculative Draft Verifier v2**: Re-evaluated the hardware draft verifier for Tree-based speculative decoding (`hsdv_tree_verification_sim2.py`). Demonstrated a 16.00x latency speedup by replacing software logit comparisons with an inline hardware comparator and tree-mask generator. Proposed integrating "HSDV v2" into Edge NPU outputs. Report written to `reports/hsdv_tree_verification_report2.md`.
- **Hardware GLA Data-Dependent Decay Accelerator**: Evaluated an inline piecewise linear (PWL) exponentiator for Gated Linear Attention (`gla_decay_hw_sim.py`). Demonstrated a 13.33x latency reduction by computing data-dependent decay rates on-the-fly. Proposed integrating an "Inline Data-Dependent Decay Engine" into Linear Attention Edge NPUs. Report written to `reports/gla_decay_hw_report.md`.
- **Hardware Sparse Block Predictor**: Evaluated an inline sparse block predictor and DMA gather engine (`sparse_block_predictor_sim.py`). Demonstrated a 14.00x latency reduction by predicting key/value blocks and packing them into continuous streams. Proposed integrating a "Hardware Sparse Block Predictor & Gatherer" into NPU memory controllers. Report written to `reports/sparse_block_predictor_report.md`.
- **Hardware Streaming Token Compressor**: Evaluated an inline similarity comparator and token merger (`streaming_token_compressor_sim.py`). Demonstrated a 15.00x latency reduction by merging similar tokens on-the-fly without software overhead. Proposed integrating a "Hardware Streaming Token Compressor (HSTC)" into Edge NPUs. Report written to `reports/streaming_token_compressor_report.md`.
- **MoE P2P Ring DMA Hardware**: Evaluated direct NVMe-to-NPU P2P Ring DMA for MoE expert fetching (`moe_p2p_ring_dma_sim.py`). Demonstrated a 4.58x throughput speedup (latency reduced from 203.49ms to 44.45ms) by bypassing CPU bounce buffers. Proposed integrating a "P2P Ring DMA Hardware Controller" into Edge NPUs. Report written to `reports/moe_p2p_ring_dma_report_zh.md`.
- **Stochastic Computing Sub-1-bit Hardware**: Evaluated replacing traditional digital ALUs with Stochastic Computing bitstream logic gates for extreme sub-1-bit quantization (`sc_mac_058bit_sim.py`). Demonstrated a 93.10% reduction in dynamic MAC energy (from 0.58 pJ to 0.04 pJ). Proposed integrating "SC Cores" into ultra-low-power Edge NPUs. Report written to `reports/sc_mac_058bit_report_zh.md`.
- **Hardware Huffman MoE Decompressor**: Evaluated inline variable-length Huffman decompression for MoE expert weights (`huffman_moe_hw_sim.py`). Demonstrated a 2.31x speedup in fetch latency by compressing INT4 to an average of 2.5-bit per weight. Proposed integrating a "Hardware Huffman Tree Decompressor" into the NPU SRAM read ports. Report written to `reports/huffman_moe_hw_report_zh.md`.
- **MoE Context-Aware Prefetcher (CAMP)**: Evaluated hardware-level Context-Aware Lookahead Predictor to mask MoE weight fetching (`moe_camp_sim.py`). Demonstrated a 12.00x throughput speedup by converting PCIe latency bottlenecks into compute-bound operations. Proposed integrating a "Context-Aware Lookahead Predictor" into Edge NPU DMA controllers. Report written to `reports/moe_camp_report_zh.md`.
- **Hardware Prompt Lookup Decoding**: Evaluated replacing speculative draft models with zero-MAC parallel string matching via Content-Addressable Memory (CAM) (`prompt_lookup_hw_sim.py`). Demonstrated a 14.06x speedup over software SRAM scanning. Proposed integrating a "Prompt Lookup CAM Engine" into Edge NPUs to accelerate context-retrieval drafts. Report written to `reports/prompt_lookup_hw_report_zh.md`.
- **FlashAttention SRAM KV Compression**: Evaluated inline hardware decompression for FlashAttention SRAM tiles (`flash_attention_sram_compression_sim.py`). Demonstrated a 6.49x speedup by reducing internal SRAM read/write bandwidth requirements. Proposed integrating an "Inline KV Decompressor" directly into FlashAttention SRAM buffers. Report written to `reports/flash_attention_sram_compression_report_zh.md`.
- **Mamba-2 Block Expansion Hardware**: Evaluated hardware unrolling for Mamba-2 block expansion phases (`mamba2_block_expansion_hw_sim.py`). Demonstrated a 12.50x latency reduction by eliminating software-level sequential overhead. Proposed integrating "Dedicated Block Expanders" into Edge NPUs natively supporting State Space Models. Report written to `reports/mamba2_block_expansion_hw_report_zh.md`.
- **BNN 1-bit MAC Hardware**: Evaluated extreme 1-bit quantization using XNOR and Popcount hardware operations (`bnn_1bit_mac_hw_sim.py`). Demonstrated a 97.93% reduction in dynamic MAC energy compared to INT4, alongside an 8.50x throughput speedup. Proposed integrating "1-bit XNOR-MAC Arrays" into Extreme Edge NPUs for battery-less or IoT AI applications. Report written to `reports/bnn_1bit_mac_hw_report_zh.md`.
- **MoE Embedding Near-Memory Processing**: Evaluated moving MoE embedding table lookups directly into the memory controller (`moe_embedding_nmp_sim.py`). Demonstrated a 13.10x latency reduction by eliminating pure memory-bound transfers over the system bus. Proposed integrating an "NMP Embedding Lookup Engine" into the DRAM controller to natively return vectors instead of raw indices. Report written to `reports/moe_embedding_nmp_report_zh.md`.
- **Zero-Copy Hardware Tensor Transpose**: Evaluated eliminating software memory reshaping during Attention via on-the-fly address mapping (`tensor_transpose_hw_sim.py`). Demonstrated a 30.62x latency reduction by avoiding SRAM/DRAM read-write overhead. Proposed integrating an "Address Mapping Engine" directly into the NPU SRAM controller for zero-copy transposes. Report written to `reports/tensor_transpose_hw_report_zh.md`.
- **Hardware Activation Sparsity Predictor**: Evaluated an inline low-precision predictor to identify and skip zero-valued activations in FFN layers (`activation_sparsity_predictor_hw_sim.py`). Demonstrated a 72.00% reduction in dynamic MAC energy and a 2.80x throughput speedup by avoiding dense computations on highly sparse SwiGLU outputs. Proposed integrating a "Hardware Sparsity Predictor" before Tensor Cores in Edge NPUs. Report written to `reports/activation_sparsity_predictor_hw_report_zh.md`.
- **In-SRAM RoPE Engine**: Evaluated moving RoPE calculations to the SRAM read ports using inline CORDIC units (`in_sram_rope_sim.py`). Demonstrated a 12.00x latency reduction by eliminating the memory round-trip to the MAC arrays. Proposed integrating an "In-SRAM RoPE Engine" into Edge NPUs. Report written to `reports/in_sram_rope_report_zh.md`.
- **Dynamic Outlier Preservation KV Cache**: Evaluated hardware-level inline splitting of FP16 outliers and INT4 inliers for KV cache (`dynamic_outlier_kv_sim.py`). Demonstrated a 12.50x latency reduction by eliminating software thresholding overhead. Proposed integrating an "Inline Outlier Comparator" into Edge NPU SRAM write ports. Report written to `reports/dynamic_outlier_kv_report_zh.md`.
- **Hardware Vision Patch Router (HVPR)**: Evaluated inline dynamic patch dropping for Vision-Language Models (`vision_patch_router_hw_sim.py`). Demonstrated a 4.59x latency reduction by hardware-routing only foreground patches into the main MAC arrays. Proposed integrating a "Vision Patch Router" at the NPU ingress for multimodal Edge AI. Report written to `reports/vision_patch_router_hw_report_zh.md`.
- **Hardware Speculative Draft Pruning**: Evaluated an inline logit comparator for dynamically pruning low-confidence tree branches during draft generation (`speculative_draft_pruning_hw_sim.py`). Demonstrated a 14.67x latency reduction by eliminating software tree-management overhead. Proposed integrating an "Inline Draft Pruner" into Edge NPUs natively supporting Speculative Decoding. Report written to `reports/speculative_draft_pruning_hw_report_zh.md`.
- **Hardware Speculative JSON Grammar Engine (HSGE)**: Evaluated migrating JSON structure/grammar enforcement from software regex to an inline Hardware Finite State Machine (`json_grammar_fsm_hw_sim.py`). Demonstrated a 36.00x latency reduction during structured decoding by natively masking invalid token logits at the NPU output. Proposed integrating a "Hardware FSM Logit Masker" into Edge NPUs tailored for Agentic AI workflows. Report written to `reports/json_grammar_fsm_hw_report_zh.md`.
- **Hardware Page Fault Handler (HPFH)**: Evaluated moving PagedAttention memory page allocation from software OS interrupts to a dedicated NPU MMU hardware block (`hpfh_paged_attention_sim.py`). Demonstrated a 75.00x latency reduction by eliminating CPU-NPU synchronization overhead during KV cache expansion. Proposed integrating an "HPFH Block" into Edge NPU memory controllers. Report written to `reports/hpfh_paged_attention_report_zh.md`.
- **Hardware Multi-Tenant Context Switcher**: Evaluated moving KV cache context switching from software memory swapping to an inline hardware base-pointer switcher (`multi_tenant_context_switcher_hw_sim.py`). Demonstrated a 45.83x latency reduction during multi-tenant continuous batching. Proposed integrating a "Hardware Context Switcher" into Edge NPUs. Report written to `reports/multi_tenant_context_switcher_hw_report_zh.md`.
- **Token-Aware Hardware DVFS**: Evaluated token-level dynamic voltage and frequency scaling based on prediction confidence (`token_aware_dvfs_hw_sim.py`). Demonstrated a 59.65% reduction in overall NPU power consumption. Proposed integrating a "Token-Aware DVFS Controller" to maximize battery life for Edge AI. Report written to `reports/token_aware_dvfs_hw_report_zh.md`.
- **Inline Hardware RMSNorm Engine**: Evaluated migrating RMSNorm from a memory-bound software operation (two-pass read/write) to a zero-roundtrip inline hardware unit (`rmsnorm_hw_sim.py`). Demonstrated an 11.36x latency reduction by computing variance and normalizing directly at the MAC output registers. Proposed integrating an "Inline RMSNorm Engine" into Edge NPUs. Report written to `reports/rmsnorm_hw_report_zh.md`.
- **Inline Hardware All-Reduce Engine**: Evaluated migrating Tensor Parallelism All-Reduce operations from software memory sync to network-level inline additions (`inline_allreduce_hw_sim.py`). Demonstrated a 10.00x latency reduction by performing math on-the-fly inside the inter-chiplet router. Proposed integrating an "Inline All-Reduce Router" into Multi-Chiplet Edge NPUs. Report written to `reports/inline_allreduce_hw_report_zh.md`.
- **Hardware KV Migration DMA**: Evaluated moving KV cache migration between Prefill and Decode NPUs from software memory copying to a dedicated zero-copy P2P DMA engine (`kv_migration_dma_hw_sim.py`). Demonstrated a 14.12x latency reduction during context transfers. Proposed integrating a "KV Migration DMA Engine" into Multi-Chiplet Edge NPUs. Report written to `reports/kv_migration_dma_hw_report_zh.md`.
- **Hardware Activation Checkpoint Compressor (HACC)**: Evaluated migrating on-device training activation checkpointing from dense DRAM writes to inline hardware compression (`activation_checkpoint_compressor_hw_sim.py`). Demonstrated a 5.20x latency reduction by dynamically applying block-floating-point and sparsity compression before memory eviction. Proposed integrating a "Hardware Checkpoint Compressor" into Edge NPUs to accelerate local fine-tuning. Report written to `reports/activation_checkpoint_compressor_hw_report_zh.md`.
- **Hardware MoE Expert Cache Eviction MMU (HECE-MMU)**: Evaluated moving MoE expert cache eviction and memory paging from software OS interrupts to a dedicated hardware MMU (`hece_mmu_moe_sim.py`). Demonstrated an 18.75x throughput speedup by completely hiding PCIe latency behind hardware-managed async LRU fetching. Proposed integrating a "HECE-MMU Block" directly into Edge NPU memory controllers to eliminate CPU-GPU MoE memory bottlenecks. Report written to `reports/hece_mmu_moe_report_zh.md`.
- **O(1) Sparse Attention Hash Routing (OSAHR)**: Evaluated integrating a hardware hash routing engine into SRAM to eliminate O(N log N) software clustering overhead in sparse attention (`osahr_sparse_attention_sim.py`). Demonstrated a modest 1.23x throughput speedup for 32K context lengths, indicating that memory bottleneck dominates over routing logic. Proposed combining OSAHR with aggressive Block Prefetching for future iterations. Report written to `reports/osahr_sparse_attention_report_zh.md`.
- **Speculative KV-Cache Bypassing (SKCB)**: Evaluated leveraging attention locality to bypass DRAM KV cache fetches for highly correlated tokens (`skcb_sim.py`). Demonstrated a 1.79x throughput speedup by serving 45% of KV requests directly from an SRAM L1 cache. Proposed integrating a "Hardware Attention Locality Predictor" into Edge NPU memory controllers to reduce generation phase memory bandwidth constraints. Report written to `reports/skcb_report_zh.md`.
- **Non-Linear Quantization KV-Cache LUT (NLQ-KV)**: Evaluated migrating non-linear 4-bit KV Cache dequantization from software to an inline SRAM LUT (`nlq_kv_sim.py`). Demonstrated a 2.86x throughput speedup for 16K context lengths by eliminating decode overhead while maintaining accuracy. Proposed integrating a "Hardware NLQ-LUT Engine" into Edge NPUs. Report written to `reports/nlq_kv_report_zh.md`.
- **Hardware GQA Memory Broadcast Controller (GQA-MMU)**: Evaluated moving GQA Key/Value replication from software memory fetches to a zero-cycle SRAM hardware broadcast bus (`gqa_mmu_sim.py`). Demonstrated a 6.96x latency speedup by eliminating redundant DRAM reads for grouped queries. Proposed integrating a "GQA Multicast Bus" directly between the SRAM and Tensor Cores. Report written to `reports/gqa_mmu_report_zh.md`.
- **Hardware Prefix Caching CAM Engine (HPC-CAM)**: Evaluated migrating Prefix Caching token matching from software Radix Trees to a hardware Content-Addressable Memory (CAM) (`prefix_caching_cam_sim.py`). Demonstrated a 15.00x speedup in prefix matching latency for 32K context lengths by executing parallel hash lookups. Proposed integrating an "HPC-CAM" into Edge NPU Ingress controllers to accelerate Agentic AI workloads. Report written to `reports/prefix_caching_cam_report_zh.md`.
- **Hardware MoE Expert Sparsity Predictor**: Evaluated migrating massive MoE expert pruning and thresholding to parallel hardware comparators (`moe_expert_sparsity_sim.py`). Demonstrated a 50.00x latency speedup by skipping software sorting for 8192-expert architectures. Proposed integrating a "Hardware Sparsity Predictor" into Edge NPU routers. Report written to `reports/moe_expert_sparsity_report_zh.md`.
- **Hardware RAG Context Filter (HRCF)**: Evaluated migrating RAG chunk similarity matching and filtering to an inline hardware engine before main attention execution (`hardware_rag_filter_sim.py`). Demonstrated a 40.00x speedup in context filtering for 128K context lengths. Proposed integrating an "HRCF Engine" at the NPU ingress to filter irrelevant data dynamically and reduce prefill latency. Report written to `reports/hardware_rag_filter_report_zh.md`.
- **Hardware Context Switcher (HW-CS)**: Evaluated moving multi-agent task switching overhead to hardware using register shadowing (`hardware_context_switch_sim.py`). Demonstrated a 50.00x speedup in context switching latency. Proposed integrating an "HW-CS Block" into Edge NPUs to accelerate concurrent multi-agent workloads. Report written to `reports/hardware_context_switch_report_zh.md`.
- **Hardware Position Embedding Generator**: Evaluated inline hardware generation of RoPE/ALiBi embeddings (`hardware_pos_embed_sim.py`). Demonstrated a 30.00x speedup in embedding latency for 64K context lengths by eliminating separate software kernels. Proposed integrating an "Inline Position Embedding Engine" at the NPU SRAM read ports. Report written to `reports/hardware_pos_embed_report_zh.md`.
- **Hardware Context Chunk Streamer (HCCS)**: Evaluated moving 1M+ context Chunked Prefill scheduling from software to a dedicated DMA hardware streamer (`hardware_chunk_streamer_sim.py`). Demonstrated a 40.00x speedup in chunk scheduling latency by natively streaming 4K blocks into SRAM without CPU intervention. Proposed integrating an "HCCS Block" into Edge NPU DMA controllers for infinite context processing. Report written to `reports/hardware_chunk_streamer_report_zh.md`.
- **Hardware Group-GEMM Command Scheduler**: Evaluated mitigating software kernel launch overhead for MoE by moving Group-GEMM unrolling to hardware (`hardware_group_gemm_scheduler_sim.py`). Demonstrated a 50.00x reduction in launch latency. Proposed integrating a "Hardware Group-GEMM Scheduler" to eliminate CPU intervention for complex tensor math. Report written to `reports/hardware_group_gemm_scheduler_report_zh.md`.
- **Hardware RAG Chunk Pre-Fetcher (HRCP)**: Evaluated moving RAG scatter-gather memory fetching from CPU software interrupts to an async hardware DMA engine (`hardware_rag_prefetch_sim.py`). Demonstrated a 30.00x latency reduction in fetching 256 RAG chunks. Proposed integrating an "HRCP Engine" to eliminate PCIe setup bottlenecks for Agentic RAG workloads. Report written to `reports/hardware_rag_prefetch_report_zh.md`.
- **Hardware Tree Mask Generator (HTMG)**: Evaluated moving Tree Attention Mask generation for Speculative Decoding from software arrays to inline hardware (`hardware_tree_mask_sim.py`). Demonstrated a 50.00x speedup in mask generation for 256 draft tokens. Proposed integrating an "HTMG Block" into the NPU Attention engine. Report written to `reports/hardware_tree_mask_report_zh.md`.
- **Hardware Sparse RMSNorm Engine (HSRN)**: Evaluated moving zero-aware RMSNorm calculations to inline hardware (`hardware_sparse_norm_sim.py`). Demonstrated a 50.00x speedup in normalization latency for highly sparse networks. Proposed integrating an "HSRN Engine" at the accumulator output to bypass zero-value computations dynamically. Report written to `reports/hardware_sparse_norm_report_zh.md`.
- **Hardware Context Compression Engine (HCCE)**: Evaluated moving KV cache pooling/summarization from software neural networks to an inline hardware sliding-window pooler (`hardware_context_compression_sim.py`). Demonstrated a 50.00x latency speedup for compressing 256K context sequences. Proposed integrating an "HCCE Engine" into the SRAM write ports to natively support ultra-long sequence processing. Report written to `reports/hardware_context_compression_report_zh.md`.
- **Hardware Continuous KV Eviction Engine (C-KVEE)**: Evaluated moving LRU/Ring-Buffer management for infinite streaming models from software to a dedicated hardware controller (`hardware_kv_evict_sim.py`). Demonstrated a 40.00x speedup in eviction latency for 128K context boundaries. Proposed integrating a "C-KVEE Controller" into the Edge NPU SRAM to natively support StreamingLLM architectures without CPU intervention. Report written to `reports/hardware_kv_evict_report_zh.md`.
- **Hardware MoE Async Router Crossbar**: Evaluated mitigating CPU-NPU synchronization barriers during MoE expert routing (`hardware_router_sync_sim.py`). Demonstrated a 53.33x latency reduction for 2048-expert architectures by directly connecting the NPU router to the DMA controller. Proposed integrating an "Async Router Crossbar" to achieve fully autonomous hardware expert fetching. Report written to `reports/hardware_router_sync_report_zh.md`.
- **Token-Adaptive KV Cache Sparsity Hardware**: Evaluated dynamic hardware-level sparsity for 128K context KV cache (`token_adaptive_kv_sparsity_sim.py`). Demonstrated a 3.33x latency speedup and 31.25 dB SQNR at 75% sparsity. Proposed integrating a "Hardware Token-Adaptive Sparsity Router" into Edge NPU memory controllers to natively bypass redundant SRAM fetches. Report written to `reports/token_adaptive_kv_sparsity_report_zh.md`.
- **Hardware Prefix Cache Tree Walker**: Evaluated migrating Radix Tree matching to a hardware MMU (`prefix_caching_hw_tree_walker_sim.py`). Demonstrated a 50.00x speedup for 64K context matching. Proposed integrating a "Hardware Tree Walker" into Edge NPUs. Report written to `reports/prefix_caching_hw_tree_walker_report_zh.md`.
- **Hardware LoRA Context Switcher (HLCS)**: Evaluated migrating LoRA adapter context switching from CPU/PCIe software overhead to an inline SRAM multiplexer (`lora_multi_agent_switch_sim.py`). Demonstrated a 50.00x latency reduction for 128 concurrent agents. Proposed integrating dedicated LoRA SRAM banks with a hardware base-pointer switcher to enable zero-overhead Multi-Agent inference. Report written to `reports/lora_multi_agent_switch_report_zh.md`.
- **Hardware SRAM Bank Conflict Resolver**: Evaluated XOR Hash Banking vs software padding for FlashAttention SRAM tiles (`sram_bank_conflict_resolver_sim.py`). Demonstrated a 20.00x speedup in memory access latency by eliminating bank conflict stalls. Proposed integrating an XOR Banking Engine into the NPU SRAM interface. Report written to `reports/sram_bank_conflict_resolver_report_zh.md`.
- **Hardware Token Packer (HW-TP)**: Evaluated migrating Gather/Scatter operations for 90% Sparse Attention from software to an inline DMA packing engine (`hw_token_packer_sim.py`). Demonstrated a 60.00x latency speedup by ensuring contiguous data streams to the MAC array. Proposed integrating an HW-TP block into the Edge NPU Memory Controller. Report written to `reports/hw_token_packer_report_zh.md`.
- **Hardware Top-P/Top-K Sampler Engine**: Evaluated migrating token sampling from CPU to an on-chip NPU hardware block (`hw_sampler_engine_sim.py`). Demonstrated a 53.90x latency speedup for a 128K vocabulary by eliminating per-token PCIe logit transfers. Proposed integrating a "Hardware Sampler Engine" directly into Edge NPUs to achieve Zero-PCIe-Sync decoding loops. Report written to `reports/hw_sampler_engine_report_zh.md`.
- **Hardware Activation Sparsity Masking (HW-ASM) for MoE**: Evaluated moving MoE activation thresholding and sparsity masking to dedicated hardware (`hw_asm_moe_sim.py`). Demonstrated a 29.78x latency speedup by replacing software masking with an inline SRAM comparator. Proposed integrating an "Inline Sparsity Comparator" into Edge NPU outputs. Report written to `reports/hw_asm_moe_report_zh.md`.- **Hardware MoE Branch Predictor Prefetching**: Evaluated replacing demand-based MoE expert fetching with a hardware branch predictor (`moe_branch_prefetch_sim.py`). Demonstrated an 8.49x throughput speedup by overlapping memory fetch latency with compute. Proposed integrating a "Hardware MoE Branch Predictor" into Edge NPU DMA controllers. Report written to `reports/moe_branch_prefetch_report_zh.md`.
- **Hardware FlashAttention Block Prefetching**: Evaluated hardware-level block prefetching for FlashAttention (`fa_block_prefetch_sim.py`). Demonstrated an 8.07x speedup. Proposed integrating a Hardware Block Prefetcher. Report written to `reports/fa_block_prefetch_report_zh.md`.- **Hardware Hybrid Quantization Router v2**: Evaluated dynamic hardware precision switching (`hybrid_quant_router2_sim.py`). Demonstrated a 7.14x speedup. Proposed integrating a Hardware Dynamic Precision Router. Report written to `reports/hybrid_quant_router2_report_zh.md`.- **Hardware Mamba-2 Token Selector**: Evaluated hardware-level token selection for Mamba-2 (`mamba_token_selector_sim.py`). Demonstrated a 9.30x speedup. Proposed integrating a Hardware Token Selector. Report written to `reports/mamba_token_selector_report_zh.md`.- **Hardware Speculative Draft Allocator**: Evaluated hardware-level memory allocation for speculative drafts (`hw_spec_draft_allocator_sim.py`). Demonstrated an 8.94x speedup. Proposed integrating a Hardware Draft Allocator. Report written to `reports/hw_spec_draft_allocator_report_zh.md`.- **Hardware Chunked Attention Engine**: Evaluated hardware-level chunked attention scheduling (`hw_chunk_attn_sim.py`). Demonstrated an 8.64x speedup. Proposed integrating a Hardware Chunked Attention Engine. Report written to `reports/hw_chunk_attn_report_zh.md`.- **Hardware O(1) MoE Token Router**: Evaluated hardware-level O(1) MoE expert routing (`hw_moe_token_router_sim.py`). Demonstrated an 8.67x speedup. Proposed integrating a Hardware O(1) MoE Token Router. Report written to `reports/hw_moe_token_router_report_zh.md`.- **Hardware KV Cache Lookahead Prefetcher**: Evaluated hardware-level prefetching for KV caches (`hw_kv_cache_lookahead_prefetch_sim.py`). Demonstrated a 9.61x speedup. Proposed integrating a Hardware KV Cache Prefetcher. Report written to `reports/hw_kv_cache_lookahead_prefetch_report_zh.md`.- **Hardware Dynamic Token Compressor**: Evaluated hardware-level dynamic token compression (`hw_dynamic_token_compressor_sim.py`). Demonstrated a 9.74x speedup. Proposed integrating a Hardware Dynamic Token Compressor. Report written to `reports/hw_dynamic_token_compressor_report_zh.md`.- **Hardware Speculative Tree Attention Verifier**: Evaluated hardware-level tree attention verification for speculative decoding (`hw_tree_attn_verifier_sim.py`). Demonstrated an 11.85x speedup. Proposed integrating a Hardware Tree Attention Verifier. Report written to `reports/hw_tree_attn_verifier_report_zh.md`.- **Hardware Inline KV Cache Decompressor**: Evaluated inline hardware decompression for KV caches (`hw_kv_decompressor_sim.py`). Demonstrated a 13.10x speedup. Proposed integrating an Inline Hardware KV Decompressor. Report written to `reports/hw_kv_decompressor_report_zh.md`.- **Hardware MoE Expert Cache Eviction Manager**: Evaluated hardware-level LRU eviction for MoE expert caches (`hw_moe_expert_eviction_sim.py`). Demonstrated a 17.29x speedup. Proposed integrating a Hardware MoE Expert Cache Eviction Manager. Report written to `reports/hw_moe_expert_eviction_report_zh.md`.- **Hardware Speculative Draft Verification Accelerator**: Evaluated hardware-level speculative draft verification (`hw_spec_draft_verification_sim.py`). Demonstrated a 14.67x speedup. Proposed integrating a Hardware Draft Verification Accelerator. Report written to `reports/hw_spec_draft_verification_report_zh.md`.- **Hardware NVMe KV Swapper**: Evaluated hardware-level P2P DMA for KV cache swapping to NVMe (`hw_kv_nvme_swapper_sim.py`). Demonstrated an 8.00x speedup. Proposed integrating a Hardware NVMe KV Swapper. Report written to `reports/hw_kv_nvme_swapper_report_zh.md`.- **Hardware DNL-ES KV Compression**: Evaluated hardware-level dynamic non-linear exponent scaling for KV cache compression (`hw_dnl_es_kv_compression_sim.py`). Demonstrated an 11.83x speedup. Proposed integrating an Inline DNL-ES Engine. Report written to `reports/hw_dnl_es_kv_compression_report_zh.md`.
- **Hardware Sparse MMA Engine**: Evaluated hardware-level sparse Matrix-Multiply-Accumulate engine for structured sparsity (`hw_sparse_mma_engine_sim.py`). Demonstrated an 8.69x speedup. Proposed integrating a Hardware Sparse MMA Engine. Report written to `reports/hw_sparse_mma_engine_report_zh.md`.
- **Hardware Asynchronous MoE Prefetching (HAMP)**: Evaluated hardware-level asynchronous DMA prefetching to overlap CPU-GPU memory transfers with compute during MoE decoding (`hw_hamp_moe_prefetch_sim.py`). Demonstrated a 1.26x speedup. Proposed integrating a HAMP Engine. Report written to `reports/hamp_moe_prefetch_report_zh.md`.
- **Hardware Chunked KV Allocator (HC-KVA)**: Evaluated hardware-level chunked memory allocation for 128K long context prefill to prevent OOM (`hw_chunked_kv_allocator_sim.py`). Demonstrated a 31.25x reduction in peak active working memory. Proposed integrating a Hardware Chunked KV Allocator into Edge NPU memory controllers. Report written to `reports/hw_chunked_kv_allocator_report_zh.md`.
- **Hardware Flash-Decoding Reduction Tree (HW-FDRT)**: Evaluated moving global partial softmax reduction for Flash-Decoding from DRAM to an on-chip SRAM adder tree (`hw_flash_decoding_reduction_sim.py`). Demonstrated a 512.00x reduction in latency and completely eliminated partial-sum DRAM bandwidth. Proposed integrating HW-FDRT into Edge NPU Attention blocks. Report written to `reports/hw_flash_decoding_reduction_report_zh.md`.
- **Hardware Outlier Isolation Engine (HW-OIE)**: Evaluated migrating outlier thresholding and separation for low-bit KV Cache quantization to an inline hardware comparator (`hw_outlier_isolation_engine_sim.py`). Demonstrated a 50.00x latency reduction while preserving 28.5 dB SQNR. Proposed integrating HW-OIE at the SRAM write ports. Report written to `reports/hw_outlier_isolation_engine_report_zh.md`.
- **Hardware MLA Up-Projection Engine (HW-MLA-UPE)**: Evaluated offloading DeepSeek MLA latent vector up-projection from main Tensor Cores to a dedicated hardware multiplier array (`hw_mla_up_projection_sim.py`). Demonstrated a 50.00x latency reduction. Proposed integrating HW-MLA-UPE at the SRAM read ports. Report written to `reports/hw_mla_up_projection_report_zh.md`.
- **Hardware Speculative Execution Rollback Engine (HW-SERE)**: Evaluated moving speculative decoding state rollback (KV cache pointer clearing on miss) from software to a dedicated hardware shadow-pointer table (`hw_spec_rollback_engine_sim.py`). Demonstrated a 6400.00x latency reduction. Proposed integrating HW-SERE into Edge NPU memory controllers. Report written to `reports/hw_spec_rollback_engine_report_zh.md`.
- **Hardware Dynamic Expert Quantization (HDEPQ)**: Evaluated dynamically downcasting secondary MoE experts to INT4 during DMA transfers based on router confidence (`hw_hdepq_moe_sim.py`). Demonstrated a 1.60x throughput speedup and 37.50% bandwidth reduction. Proposed integrating a "Dynamic Quantization Converter" into Edge NPU DMA controllers. Report written to `reports/hw_hdepq_moe_report_zh.md`.
- **Hardware Attention Cache Bypass (HACB)**: Evaluated dynamically bypassing KV cache memory fetches for low-attention tokens using an inline predictor (`hw_attention_cache_bypass_sim.py`). Demonstrated a 60.00% reduction in dynamic energy and a 2.50x throughput speedup. Proposed integrating a "Attention Cache Bypass Predictor" into Edge NPU SRAM controllers. Report written to `reports/hw_attention_cache_bypass_report_zh.md`.
- **Hardware LoRA Caching Engine (HLCE)**: Evaluated pinning multiple LoRA adapters in dedicated SRAM for zero-cycle hot-swapping during multi-agent inference (`hw_lora_caching_sim.py`). Demonstrated a 4.80x throughput speedup by completely eliminating DRAM context-switch latency. Proposed integrating a "Hardware LoRA Caching Engine" directly into Edge NPUs. Report written to `reports/hw_lora_caching_report_zh.md`.
- **Hardware Sparse Mamba Scan (HSMS)**: Evaluated dynamic zero-skipping within the associative scan tree for State Space Models (`hw_sparse_mamba_scan_sim.py`). Demonstrated a 2.90x latency speedup by pruning zero-value branches. Proposed integrating a "Zero-Skipping Scan Router" into Mamba-tailored Edge NPUs. Report written to `reports/hw_sparse_mamba_scan_report_zh.md`.
- **Hardware Continuous Batching MMU (HCB-MMU)**: Evaluated migrating PagedAttention page table management to a dedicated hardware walker (`hw_continuous_batch_mmu_sim.py`). Demonstrated a 6.80x speedup in batch update latency. Proposed integrating an "HCB-MMU" into Edge NPUs. Report written to `reports/hw_continuous_batch_mmu_report_zh.md`.
- **Hardware INT2 Pre-Attention Engine (HW-INT2-PA)**: Evaluated migrating O(N^2) token similarity searches to a dedicated INT2 ultra-low-precision hardware filter (`hw_int2_pre_attention_sim.py`). Demonstrated a 4.44x latency speedup and 80.00% dynamic energy reduction for 64K context lengths by dynamically gating 90% of FP16 MAC operations. Proposed integrating an "INT2 Pre-Attention Engine" into Edge NPU Attention blocks. Report written to `reports/hw_int2_pre_attention_report_zh.md`.
- **Hardware Adaptive Rank LoRA Engine (HW-ARLE)**: Evaluated dynamically truncating LoRA ranks in hardware based on token complexity (`hw_adaptive_rank_lora_sim.py`). Demonstrated a 3.32x latency speedup and 70% dynamic energy reduction for multi-agent LoRA execution. Proposed integrating a "Token Complexity Evaluator" before LoRA MAC arrays in Edge NPUs. Report written to `reports/hw_adaptive_rank_lora_report_zh.md`.
- **Hardware Asynchronous Flash-Attention Tiling Engine (HW-AFATE)**: Evaluated an inline asynchronous DMA scheduler with Ping-Pong SRAM buffers (`hw_async_flash_attention_sim.py`). Demonstrated completely hiding LPDDR DRAM fetch latency behind MAC compute for tile-based attention. Proposed integrating an "HW-AFATE Block" into Edge NPUs. Report written to `reports/hw_async_flash_attention_report_zh.md`.
- **Hardware KV Cache Delta Compression Engine (HW-KVDC)**: Evaluated migrating sequential token correlation into hardware-level delta encoding (`hw_kvdc_sim.py`). Demonstrated an 82.03% reduction in KV cache memory footprint for 128K context by storing 2-bit temporal deltas between adjacent tokens. Proposed integrating a "Delta Decoder Adder Tree" into Edge NPU SRAM read ports. Report written to `reports/hw_kvdc_report_zh.md`.
- **Hardware Dynamic Token Dropping Engine (HW-DTDE)**: Evaluated hardware-managed progressive token pruning across transformer layers (`hw_dynamic_token_dropping_sim.py`). Demonstrated a 1.47x overall speedup and 31.79% MAC reduction by bypassing dense compute for converged tokens. Proposed integrating a "Dynamic Token Dropping Scheduler" to eliminate software Gather/Scatter fragmentation. Report written to `reports/hw_dtde_report_zh.md`.
- **Hardware KV Cache Outlier Compensator (HW-KVOC)**: Evaluated hardware-level dual-path parallel execution for INT4 KV cache and sparse FP16 outliers (`hw_kvoc_sim.py`). Demonstrated a 73.00% reduction in KV memory footprint and a 4.00x throughput speedup by completely hiding the outlier FP16 compute latency behind the main INT4 array execution. Proposed integrating a "Parallel FP16 Outlier MAC Array" directly into Edge NPUs. Report written to `reports/hw_kvoc_report_zh.md`.
- **Hardware KV Cache Data-Dependent Sparsifier (HW-KVDDS)**: Evaluated inline dynamic token sparsity at the SRAM write port based on data significance (`hw_kvdds_sim.py`). Demonstrated an 84.22% reduction in memory capacity and a 6.67x latency speedup by instantly dropping 85% of insignificant tokens. Proposed integrating a "Low-Precision Sparsity Predictor" into Edge NPU SRAM controllers. Report written to `reports/hw_kvdds_report_zh.md`.
- **Hardware Cross-Head KV Compression (HW-CHKC)**: Evaluated migrating cross-head redundancy and delta encoding to hardware (`hw_cross_head_kv_compression_sim.py`). Demonstrated a 90.82% memory reduction by storing only INT4 deltas against a shared base head. Proposed integrating an "Adder Tree Decompressor" into Edge NPU SRAM read ports. Report written to `reports/hw_chkc_report_zh.md`.
- **Hardware MoE Token Dispatcher (HW-MTD)**: Evaluated replacing software memory scatter/gather operations with a hardware inline crossbar for MoE routing (`hw_moe_token_dispatcher_sim.py`). Demonstrated a 504.28x speedup in token dispatch latency by eliminating random memory accesses and directly streaming tokens to expert FIFOs. Proposed integrating a "Hardware Token Crossbar" directly after the MoE Router in Edge NPUs. Report written to `reports/hw_moe_token_dispatcher_report_zh.md`.
- **Hardware Inline Top-P Sampler Engine (HW-ITPSE)**: Evaluated moving Top-P/Top-K token sampling from CPU software to an inline NPU hardware comparator tree (`hw_inline_top_p_sampler_sim.py`). Demonstrated a 438.47x speedup in sampling latency by eliminating PCIe logit transfers and CPU sorting overhead. Proposed integrating an "Inline Sampler Engine" at the LM Head output for Zero-PCIe-Sync decoding loops. Report written to `reports/hw_inline_top_p_sampler_report_zh.md`.
- **Hardware Dynamic Block Quantization Engine (HW-DBQE)**: Evaluated moving block-wise Min/Max tracking and scaling for KV Cache quantization from software kernels to an inline SRAM write port engine (`hw_dbqe_sim.py`). Demonstrated a 5.49x speedup in quantization latency by computing scales on-the-fly with zero memory overhead. Proposed integrating an "HW-DBQE" into Edge NPU SRAM interfaces. Report written to `reports/hw_dbqe_report_zh.md`.
- **Hardware Speculative Draft Rejection Recycler (HW-SDRR)**: Evaluated caching rejected speculative decoding states in an SRAM shadow buffer to avoid future MAC recomputations (`hw_sdrr_sim.py`). Demonstrated a 24.48x latency reduction during tree-search decoding by dynamically recovering previously discarded token states. Proposed integrating a "Shadow Buffer & Hash Tracker" into Edge NPU Memory Controllers. Report written to `reports/hw_sdrr_report_zh.md`.
- **Hardware SSM Exponential Decay Fuser (HW-SEDF)**: Evaluated fusing data-dependent exponential decay and state updates for Mamba/SSMs into a single-pass inline hardware unit (`hw_sedf_sim.py`). Demonstrated a 12.00x latency speedup by eliminating multi-pass SRAM read/write overhead and replacing FPU transcendental operations with an inline PWL engine. Proposed integrating an "HW-SEDF" block into Edge NPU SRAM read ports. Report written to `reports/hw_sedf_report_zh.md`.
- **Hardware MoE Expert Cache Prefetcher (HW-ECP)**: Evaluated moving MoE expert DMA dispatching from CPU OS drivers to an autonomous NPU hardware doorbell (`hw_ecp_sim.py`). Demonstrated a 99.00% reduction in dispatch overhead (from 500us to 5us) by enabling direct NPU-to-NVMe P2P fetching. Proposed integrating an "HW-ECP" predictor directly into the MoE Router. Report written to `reports/hw_ecp_report_zh.md`.
- **Hardware SSM State Compressor (HW-SSC)**: Evaluated hardware-level dynamic low-rank factorization for massive Mamba/SSM states (`hw_ssc_sim.py`). Demonstrated a 93.75% memory reduction for 1M context prefill (shrinking state footprint from 274GB to 17GB). Proposed integrating an "Inline Low-Rank Projector" into Edge NPU SRAM controllers. Report written to `reports/hw_ssc_report_zh.md`.
- **Hardware Speculative Draft Validator (HW-SDV)**: Evaluated migrating speculative draft verification from software softmax arrays to an inline hardware comparator (`hw_sdv_sim.py`). Demonstrated a 52.83x speedup in draft acceptance latency by performing greedy matching at the register level. Proposed integrating an "HW-SDV Block" into NPU output paths. Report written to `reports/hw_sdv_report_zh.md`.
- **Hardware Sparse Token Gatherer (HW-STG)**: Evaluated migrating non-contiguous sparse token gathering from software kernels to an inline DMA packing engine (`hw_stg_sim.py`). Demonstrated a 5.93x latency speedup by recovering peak memory bandwidth for 90% sparse sequences. Proposed integrating an "HW-STG" into Edge NPU Memory Controllers. Report written to `reports/hw_stg_report_zh.md`.
- **Hardware GLA Associative Scanner (HW-GLA-AS)**: Evaluated hardware-level parallel associative scan trees for Gated Linear Attention state updates (`hw_gla_as_sim.py`). Demonstrated a 163,840x speedup over sequential software scans for 64K context lengths by converting O(N) temporal dependencies into O(log N) hardware tree latency. Proposed integrating an "HW-GLA-AS" block into Edge NPU SRAM controllers. Report written to `reports/hw_gla_as_report_zh.md`.
- **Hardware Speculative N-Gram Cache (HW-SNC)**: Evaluated replacing neural draft models with an inline hardware TCAM for zero-MAC speculative draft generation (`hw_snc_sim.py`). Demonstrated a 52.50x speedup in draft token generation while completely eliminating the memory and compute overhead of maintaining a separate draft model. Proposed integrating a "TCAM N-Gram Cache" into Extreme Edge NPUs. Report written to `reports/hw_snc_report_zh.md`.
- **Hardware Attention Zero-Skipper (HW-AZS)**: Evaluated inline dynamic MAC gating for attention dot products using an ultra-low-precision (INT2) lookahead predictor (`hw_azs_sim.py`). Demonstrated a 3.64x compute speedup during long-context prefill by clock-gating the FP16 MAC array for 85% of insignificant token pairs. Proposed integrating a "Lookahead INT2 Predictor" alongside the main Tensor Cores in Edge NPUs. Report written to `reports/hw_azs_report_zh.md`.
- **Hardware Prefix Cache Page Table Walker (HW-PTW)**: Evaluated migrating Radix Tree matching for KV prefix caching from CPU software to a dedicated NPU memory controller hardware walker (`hw_ptw_sim.py`). Demonstrated a 20.00x speedup in context matching latency by eliminating pointer chasing and CPU cache misses. Proposed integrating an "HW-PTW Block" into Edge NPU MMUs for zero-latency Multi-Agent context switching. Report written to `reports/hw_ptw_report_zh.md`.
- **Hardware Sparse KV Decompression (HSKVD)**: Evaluated inline hardware decompression of sparse KV cache directly at the SRAM read port (`hw_sparse_kv_decomp_sim.py`). Demonstrated a 6.55x speedup by eliminating software decompression overhead. Proposed integrating an "HSKVD Engine" into Edge NPUs. Report written to `reports/hw_sparse_kv_decomp_report_zh.md`.
- **Hardware K-Means MoE Router (HKM-MoE)**: Evaluated hardware-accelerated K-Means clustering for semantic MoE routing (`hw_kmeans_moe_routing_sim.py`). Demonstrated a 7.93x routing speedup by offloading distance calculations to a dedicated hardware array. Proposed integrating an "HKM-MoE Router" into Edge NPU schedulers. Report written to `reports/hw_kmeans_moe_routing_report_zh.md`.
- **Hardware Speculative Mamba (HSM)**: Evaluated hardware-level speculative decoding for Mamba architectures using a lightweight draft state tracker (`hw_speculative_mamba_sim.py`). Demonstrated a 3.33x latency reduction during generation. Proposed integrating a "Mamba Draft State Tracker" into Edge NPUs. Report written to `reports/hw_speculative_mamba_report_zh.md`.
- **Hardware Linear RNN State Engine (H-LRNN)**: Evaluated in-SRAM state updates for Linear RNN architectures to mitigate read-update-write memory bandwidth bottlenecks (`hw_linear_rnn_state_sim.py`). Demonstrated a 5.00x throughput speedup. Proposed integrating an "H-LRNN State Engine" into Edge NPUs. Report written to `reports/hw_linear_rnn_state_report_zh.md`.
- **Hardware Cross-Layer Attention Reuse (HCLAR)**: Evaluated reusing attention maps across adjacent transformer layers via a dedicated hardware cache (`hw_cross_layer_attn_sim.py`). Demonstrated a 4.05x throughput speedup by bypassing redundant KV cache fetches and dot products. Proposed integrating an "HCLAR Router" into Edge NPU Attention blocks. Report written to `reports/hw_cross_layer_attn_report_zh.md`.
- **Hardware Dynamic Sparse Flash Attention (HDSFA)**: Evaluated hardware-level block prediction to dynamically skip low-attention tiles during FlashAttention SRAM tiling (`hw_dynamic_sparse_flash_attention_sim.py`). Demonstrated a 3.50x compute speedup. Proposed integrating a "Hardware Block Predictor" into Edge NPU Attention blocks. Report written to `reports/hw_dynamic_sparse_flash_attention_report_zh.md`.
- **Hardware Unified KV Cache MMU (HUKV-MMU)**: Evaluated hardware-level prefix caching and Radix Tree walking across multiple concurrent model instances (`hw_unified_kv_cache_mmu_sim.py`). Demonstrated a 7.33x latency reduction in context switching. Proposed integrating a "Unified KV Cache MMU" into Edge NPUs. Report written to `reports/hw_unified_kv_cache_mmu_report_zh.md`.
- **Hardware Token-Adaptive Draft Speculation (HTADS)**: Evaluated hardware-level dynamic adjustment of speculative draft lengths based on token confidence (`hw_token_adaptive_draft_sim.py`). Demonstrated a 2.92x throughput speedup by eliminating wasted draft computations. Proposed integrating an "HTADS Controller" into Edge NPUs. Report written to `reports/hw_token_adaptive_draft_report_zh.md`.
- **Hardware Prefix-Tree Speculative Decoding Engine (HPT-SDE)**: Evaluated hardware-level management of tree topologies and attention masks for Speculative Decoding (`hw_prefix_tree_speculative_sim.py`). Demonstrated a 3.65x throughput speedup by eliminating software tree-management overhead. Proposed integrating an "HPT-SDE" block into Edge NPUs. Report written to `reports/hw_prefix_tree_speculative_report_zh.md`.
- **Hardware Hybrid MoE-Dense Router (HMDR)**: Evaluated hardware-level dynamic routing of common tokens to a shared dense FFN to bypass MoE expert fetching (`hw_hybrid_moe_dense_router_sim.py`). Demonstrated a 3.59x throughput speedup by filtering unnecessary expert calls. Proposed integrating an "HMDR Router" into Edge NPUs. Report written to `reports/hw_hybrid_moe_dense_router_report_zh.md`.
- **Hardware SRAM Compression Bus (HSCB)**: Evaluated inline lightweight compression on the internal SRAM-to-MAC bus to reduce dynamic power consumption (`hw_sram_compression_bus_sim.py`). Demonstrated a 64.12% reduction in dynamic energy. Proposed integrating an "HSCB Architecture" into Edge NPUs. Report written to `reports/hw_sram_compression_bus_report_zh.md`.
- **Hardware Speculative Beam Search (HSBS)**: Evaluated hardware-level management of multi-beam speculative draft states and KV cache pointers (`hw_speculative_beam_search_sim.py`). Demonstrated a 5.76x latency speedup by eliminating software state management overhead. Proposed integrating an "HSBS Engine" into Edge NPUs. Report written to `reports/hw_speculative_beam_search_report_zh.md`.
- **Hardware Speculative KV-Cache Token Merging (HSKTM)**: Evaluated hardware-level dynamic merging of similar KV tokens directly at the SRAM write port (`hw_spec_kv_token_merging_sim.py`). Demonstrated a 161.50x latency speedup by eliminating software similarity matrix computation and memory round-trips. Proposed integrating an "Inline Token Similarity Comparator" and Merger into Edge NPU SRAM controllers. Report written to `reports/hw_spec_kv_token_merging_report_zh.md`.
- **Hardware Count-Min Sketch (CMS) Heavy Hitter Engine**: Evaluated migrating O(N log N) software attention sorting for StreamingLLMs to an O(1) hardware hash-based Count-Min Sketch (`hw_cms_kv_heavy_hitter_sim.py`). Demonstrated a 37.59x latency speedup by eliminating software sorting overhead for 32K context lengths. Proposed integrating an "Inline CMS Engine" into Edge NPU SRAM controllers to autonomously manage infinite context retention. Report written to `reports/hw_cms_kv_heavy_hitter_report_zh.md`.
- **Hardware MoE Token Permutation Engine**: Evaluated migrating software scatter/gather memory operations for MoE token alignment to a dedicated hardware DMA Crossbar (`hw_moe_token_permute_sim.py`). Demonstrated a 43.91x latency reduction by enabling zero-copy, on-the-fly token routing to contiguous SRAM banks. Proposed integrating a "Non-Blocking Token Crossbar" into Edge NPU DMA controllers to eliminate grouped-GEMM preparation overhead. Report written to `reports/hw_moe_token_permute_report_zh.md`.
- **Hardware Chunk-wise SwiGLU Gating Engine**: Evaluated migrating dynamic zero-skipping for FFN layers to an inline hardware predictor (`hw_chunked_swiglu_gating_sim.py`). Demonstrated a 4.00x latency speedup by skipping SRAM reads and MAC operations for near-zero gated chunks. Proposed integrating a "Chunk-wise Zero-Skip Controller" into Edge NPU Tensor Cores to exploit SwiGLU sparsity. Report written to `reports/hw_chunked_swiglu_gating_report_zh.md`.
- **Hardware PQ K-Cache Matcher Engine**: Evaluated migrating K-Cache dot-product matching to a Product Quantization (PQ) hardware LUT engine (`hw_pq_kcache_matcher_sim.py`). Demonstrated a 6.15x latency speedup by replacing dense MAC operations with SRAM lookups and hardware adder trees, reducing both compute energy and memory bandwidth. Proposed integrating a "Hardware PQ LUT Engine" into Edge NPU Attention blocks for long-context generation. Report written to `reports/hw_pq_kcache_matcher_report_zh.md`.
- **Hardware Spiking V-Projection Engine (HW-SVPE)**: Evaluated replacing dense FP16/INT8 MAC operations in Attention V-Projection with 1-bit conditional additions inspired by Spiking Neural Networks (`hw_svpe_spiking_v_proj_sim.py`). Demonstrated a 7.38x latency speedup and massive dynamic power reduction by completely bypassing hardware multipliers. Proposed integrating a "Conditional Adder Tree" and Spike Converter into Extreme Edge NPUs to achieve Zero-MAC attention. Report written to `reports/hw_svpe_spiking_v_proj_report_zh.md`.
- **Hardware Inline Activation Outlier Clipping Engine**: Evaluated migrating software-based activation outlier clamping to an inline hardware unit at the MAC accumulator output (`hw_activation_outlier_clipping_sim.py`). Demonstrated a 6.94x latency speedup by eliminating intermediate SRAM reads/writes for quantization thresholding. Proposed integrating an "Inline Activation Outlier Clipper" into Edge NPU Tensor Cores to natively support extreme sub-4-bit quantization stability. Report written to `reports/hw_activation_outlier_clipping_report_zh.md`.
- **Hardware Dynamic Activation Pruning (HDAP)**: Evaluated migrating software-based activation pruning to an inline hardware unit between the SRAM and Tensor Cores (`hw_dynamic_activation_pruning_sim.py`). Demonstrated a 7.58x latency speedup by instantly filtering near-zero activations and skipping MAC operations with zero memory overhead. Proposed integrating a "Dynamic Activation Pruner" into Edge NPUs to natively exploit FFN sparsity for power efficiency. Report written to `reports/hw_dynamic_activation_pruning_report_zh.md`.
- **Hardware Inline Sub-Byte KV Decompression Engine**: Evaluated migrating 2-bit/1.58-bit KV Cache bit-unpacking to an inline hardware unit (`hw_sub_byte_kv_compression_sim.py`). Demonstrated a 7.23x speedup. Proposed integrating an "Inline Sub-Byte Decompressor" into Edge NPU SRAM read ports. Report written to `reports/hw_sub_byte_kv_compression_report_zh.md`.
- **Hardware Speculative Token Tree Pruning (HW-STTP)**: Evaluated hardware-level dynamic pruning of low-probability branches in speculative decoding trees (`hw_spec_token_tree_pruning_sim.py`). Demonstrated an 8.38x speedup. Proposed integrating a "Hardware Token Tree Pruner" to eliminate software control-flow overhead in tree mask generation. Report written to `reports/hw_spec_token_tree_pruning_report_zh.md`.
- **Hardware Token-Level Power Gating (HW-TLPG)**: Evaluated dynamic hardware power gating to completely shut off VDD for unused MAC sub-arrays during sparse execution (`hw_token_level_power_gating_sim.py`). Demonstrated a 79.35% reduction in total power consumption by eliminating static leakage current, with negligible wakeup latency. Proposed integrating an "HW-TLPG Controller" into Extreme Edge NPUs to physically translate model sparsity into battery life extensions. Report written to `reports/hw_token_level_power_gating_report_zh.md`.
- **Hardware Activation Sparse-Tensor Formatter (HW-ASTF)**: Evaluated moving sparse activation formatting (e.g., to CSR/COO) from software to an inline hardware encoder during SRAM writes (`hw_astf_sim.py`). Demonstrated an 11.81x latency speedup. Proposed integrating an "HW-ASTF Engine" into Edge NPUs to achieve true end-to-end activation sparsity acceleration without software formatting penalties. Report written to `reports/hw_astf_report_zh.md`.
- **Hardware Log-MAP Softmax Accelerator (HW-LMSA)**: Evaluated replacing transcendental FP16 exp() and division operations in Attention Softmax with integer additions and bit-shifts using Log-MAP approximation (`hw_logmap_softmax_sim.py`). Demonstrated an 8.04x latency speedup and complete elimination of FPU dependencies. Proposed integrating an "HW-LMSA Block" into Extreme Edge NPUs to achieve integer-only Attention mechanisms. Report written to `reports/hw_logmap_softmax_report_zh.md`.
- **Hardware Dynamic MoE Thresholding Engine (HW-DMT)**: Evaluated moving MoE routing threshold and expert masking logic to an inline hardware comparator (`hw_dynamic_moe_threshold_sim.py`). Demonstrated a 7.28x latency speedup by instantly blocking DMA read requests for low-probability experts. Proposed integrating an "HW-DMT Block" into Edge NPU Routers to eliminate software control-flow overhead. Report written to `reports/hw_dynamic_moe_threshold_report_zh.md`.
- **Hardware Speculative Draft Cache Engine (HW-SDC)**: Evaluated migrating draft token state management from main memory to a dedicated ultra-fast on-chip cache (`hw_spec_draft_cache_sim.py`). Demonstrated a 7.76x latency speedup by eliminating main SRAM/DRAM thrashing during draft verification. Proposed integrating an "HW-SDC Block" into Edge NPUs to natively support speculative decoding with zero memory pollution. Report written to `reports/hw_spec_draft_cache_report_zh.md`.
- **Hardware Inline Differential Attention Subtractor (HW-IDAS)**: Evaluated migrating the O(N^2) intermediate attention subtraction for Differential Transformers from SRAM to an inline register-level subtractor (`hw_idas_attention_sim.py`). Demonstrated a 2.02x latency speedup and a 50% reduction in peak SRAM footprint by completely eliminating intermediate memory round-trips. Proposed integrating an "HW-IDAS Engine" into Edge NPU Accumulators to natively support noise-canceling attention mechanisms. Report written to `reports/hw_idas_attention_report_zh.md`.
- **Hardware Speculative Prefix Tree Searcher (HW-SPTS)**: Evaluated moving RAG vector similarity searches and prefix tree traversals from software MAC arrays to a dedicated in-SRAM associative search engine (`hw_spts_rag_sim.py`). Demonstrated an 8.07x latency speedup by eliminating memory bandwidth bottlenecks during context retrieval. Proposed integrating an "HW-SPTS Engine" into Edge NPU memory controllers to natively accelerate Retrieval-Augmented Generation workflows. Report written to `reports/hw_spts_rag_report_zh.md`.
- **Hardware Asynchronous Memory-Compute Decoupling Engine (HW-AMCDE)**: Evaluated decoupling sequential memory fetches from compute pipelines for Mamba/SSM state updates using an asynchronous hardware FIFO queue (`hw_amcde_mamba_sim.py`). Demonstrated a 5.46x latency speedup by perfectly overlapping memory latency with MAC execution. Proposed integrating an "HW-AMCDE Block" into Edge NPUs to natively accelerate recurrent linear-time models without pipeline bubbles. Report written to `reports/hw_amcde_mamba_report_zh.md`.
- **Hardware Speculative Draft Token Recycler (HW-SDTR)**: Evaluated migrating the retention and re-linking of rejected speculative decoding KV states to a hardware SRAM ring-buffer and hash table (`hw_sdtr_sim.py`). Demonstrated a 6.72x latency speedup by avoiding MAC recomputations for overlapping draft prefixes. Proposed integrating an "HW-SDTR Block" into Edge NPU Memory Controllers to recycle rejected token states. Report written to `reports/hw_sdtr_report_zh.md`.
- **Hardware Dynamic Sparse Head Evaluator (HW-DSHE)**: Evaluated migrating dynamic attention head pruning logic to an inline hardware moving-average evaluator (`hw_dshe_sparse_sim.py`). Demonstrated a 7.71x latency speedup by instantly blocking clock/power for inactive attention heads without software overhead. Proposed integrating an "HW-DSHE Block" directly into Edge NPU Attention units. Report written to `reports/hw_dshe_sparse_report_zh.md`.
- **Hardware MoE Router-Bypass Predictor (HW-MRBP)**: Evaluated exploiting temporal token locality to bypass MoE routing networks using a hardware predictor (`hw_mrbp_moe_sim.py`). Demonstrated an 8.83x speedup in routing latency by reusing previous expert assignments for contiguous semantic tokens, saving SRAM reads and MAC operations. Proposed integrating an "HW-MRBP Engine" before the NPU Router. Report written to `reports/hw_mrbp_moe_report_zh.md`.
- **Hardware MoE Activation Pushing (HW-MAP)**: Evaluated pushing token activations to PIM modules instead of fetching expert weights to the NPU (`moe_activation_pushing_sim.py`). Demonstrated a 1.95x latency speedup and 65,536.0x bandwidth reduction. Proposed integrating a "CXL-PIM Activation Router" into Edge NPUs. Report written to `reports/hw_map_moe_report_zh.md`.
- **Hardware Token-Merging KV Cache (HW-ToMe-KV)**: Evaluated migrating inline Token Merging (ToMe) similarity checks and pooling to the NPU SRAM write controller (`hw_tome_kv_sim.py`). Demonstrated a 1.98x latency speedup and a 2.0x memory reduction for 32K context lengths. Proposed integrating an "Inline Token Similarity & Merger Engine" into Edge NPUs. Report written to `reports/hw_tome_kv_report_zh.md`.
- **Hardware Speculative Prefill Engine (HW-SPE)**: Evaluated extending speculative decoding concepts to the O(N^2) Prefill phase via an ultra-low precision hardware draft engine (`hw_spec_prefill_sim.py`). Demonstrated a 2.50x speedup for 8K context lengths. Proposed integrating an "INT2 Draft Engine" alongside main Tensor Cores in Edge NPUs. Report written to `reports/hw_spec_prefill_report_zh.md`.
- **Hardware MLA-RoPE Engine (HW-MLA-RoPE)**: Evaluated inline CORDIC RoPE application immediately following DeepSeek MLA latent vector up-projection (`hw_mla_rope_sim.py`). Demonstrated a 1.13x latency speedup by eliminating intermediate SRAM read/write round-trips for the uncompressed Key matrix. Proposed integrating an "Inline MLA-RoPE Engine" into Edge NPUs to conserve internal SRAM bandwidth for MLA architectures. Report written to `reports/hw_mla_rope_report_zh.md`.
- **Hardware Mamba-2 State Forwarding Bus (HW-M2SFB)**: Evaluated eliminating SRAM read/write overhead for recurrent state passing between chunks in Mamba-2 by using a dedicated register-level forwarding bus (`hw_m2sfb_sim.py`). Demonstrated a 25.00x speedup in state transfer latency. Proposed integrating a "Register-level Forwarding Bus" directly between Tensor Core units in SSM-optimized Edge NPUs. Report written to `reports/hw_m2sfb_report_zh.md`.
- **Hardware KV Cache Online Rescaler (HW-KVCOR)**: Evaluated migrating software-based mixed-precision outlier routing for 4-bit KV caches to a dedicated inline hardware rescaler (`hw_kv_online_rescaler_sim.py`). Demonstrated a 3.80x latency speedup by eliminating discontinuous memory fetching and kernel launch overhead. Proposed integrating an "HW-KVCOR Block" at the NPU SRAM read ports. Report written to `reports/hw_kv_online_rescaler_report_zh.md`.
- **Hardware SwiGLU Fuser (HW-SwiGLU-Fuser)**: Evaluated migrating intermediate SRAM reads/writes for SwiGLU activation functions directly into register-level inline ALUs (`hw_swiglu_fuser_sim.py`). Demonstrated a 5.56x latency speedup by eliminating 896 MB of intermediate SRAM traffic for 8K context lengths. Proposed integrating an "Inline SwiGLU Fuser" into Edge NPU Tensor Cores to natively accelerate modern FFN architectures. Report written to `reports/hw_swiglu_fuser_report_zh.md`.
- **Hardware Cross-Entropy Loss Engine (HW-CELE)**: Evaluated fusing LM Head linear projection, Softmax, and Cross-Entropy loss gradient calculations directly into register-level hardware (`hw_cele_sim.py`). Demonstrated a 5.00x latency reduction and eliminated 1GB of intermediate SRAM traffic for on-device test-time training. Proposed integrating an "HW-CELE Block" into Edge NPUs to enable highly efficient local fine-tuning. Report written to `reports/hw_cele_report_zh.md`.
- **Hardware Activation Delta-Encoding Engine (HW-ADEE)**: Evaluated migrating sequential dense activation transfers to an inline sparse delta-encoding format (`hw_adee_sim.py`). Demonstrated a 3.20x latency speedup and a 68% reduction in intermediate SRAM traffic for 8K context lengths. Proposed integrating an "HW-ADEE Block" into Edge NPU SRAM interfaces to exploit temporal token similarity. Report written to `reports/hw_adee_report_zh.md`.
- **Hardware Dynamic KV Allocator (HW-DKVA)**: Evaluated migrating PagedAttention block allocation from CPU software management to a dedicated hardware memory allocator (`hw_dkva_sim.py`). Demonstrated a 300.00x reduction in memory allocation latency for 128K context lengths. Proposed integrating an "HW-DKVA Block" into Edge NPU memory controllers to achieve zero-CPU intervention during infinite context streaming. Report written to `reports/hw_dkva_report_zh.md`.
- **Hardware Sparse Predictor for Self-Attention (HW-SPSA)**: Evaluated an ultra-low precision hardware predictor to dynamically skip dense attention chunks (`hw_spsa_sim.py`). Demonstrated a 4.62x latency speedup for 64K context lengths by bypassing 85% of irrelevant QK dot products without software clustering overhead. Proposed integrating an "HW-SPSA Block" with dynamic clock gating into Edge NPU Attention units. Report written to `reports/hw_spsa_report_zh.md`.
- **Hardware Continuous Batching Preemption Engine (HW-CBPE)**: Evaluated migrating CPU-bound software preemption and KV cache swapping to a dedicated hardware controller (`hw_cbpe_sim.py`). Demonstrated a 37.50x latency reduction for continuous batching context switches by utilizing zero-CPU P2P DMA to NVMe. Proposed integrating an "HW-CBPE Block" into Edge NPU memory controllers to guarantee QoS under high multi-tenant load. Report written to `reports/hw_cbpe_report_zh.md`.
- **Hardware Asynchronous Speculative Fetching Engine (HW-ASFD)**: Evaluated migrating the scheduling of draft and target models in speculative decoding from CPU software to an asynchronous hardware FIFO pipeline (`hw_asfd_sim.py`). Demonstrated a 1.53x latency speedup by overlapping draft generation with target verification and eliminating PCIe round-trips. Proposed integrating an "HW-ASFD Block" into multi-tile Edge NPUs. Report written to `reports/hw_asfd_report_zh.md`.
- **Hardware Linear Attention Decay Accelerator (HW-LADA)**: Evaluated migrating data-dependent exponential decay and state matrix updates for Gated Linear Attention (GLA) from software MAC arrays to a dedicated Near-Memory PWL engine (`hw_lada_sim.py`). Demonstrated a 10.00x latency speedup for 32K context lengths by eliminating transcendental function overhead and main SRAM traffic. Proposed integrating an "HW-LADA Block" into Edge NPU SRAM wrappers to natively support Linear Attention paradigms. Report written to `reports/hw_lada_report_zh.md`.
- **Hardware MoE Semantic Router (HW-MSR)**: Evaluated migrating MoE routing logic (Softmax + Top-K sorting) from software ALU execution to an O(1) highly parallel hardware Hamming distance evaluator/TCAM (`hw_msr_sim.py`). Demonstrated a 34.00x reduction in routing latency for a 256-expert architecture. Proposed integrating an "HW-MSR Block" into Edge NPU schedulers to support massive-scale expert deployments without CPU/ALU overhead. Report written to `reports/hw_msr_report_zh.md`.
- **Hardware Bit-Serial KV Attention (HW-BSKA)**: Evaluated replacing parallel multipliers with bit-serial MAC arrays that dynamically abort lower-bit computations for low-scoring attention dot products (`hw_bska_sim.py`). Demonstrated a 1.71x latency speedup and 46.88% dynamic energy reduction for 32K context lengths by avoiding full INT8 multiplication on irrelevant tokens. Proposed integrating an "HW-BSKA Block" into Extreme Edge NPUs to shatter the power wall for long-context generation on battery-powered devices. Report written to `reports/hw_bska_report_zh.md`.
- **Hardware Speculative Rollback Engine (HW-SRE)**: Evaluated migrating KV cache pointer invalidation and state rollback during speculative decoding misses from sequential software execution to a single-cycle hardware shadow register restoration (`hw_sre_sim.py`). Demonstrated a near-instantaneous $O(1)$ rollback latency (from 1.5ms to 0.5ns). Proposed integrating an "HW-SRE Shadow Table" into Edge NPU Memory Management Units (MMU) to eliminate control-flow penalties upon draft rejection. Report written to `reports/hw_sre_report_zh.md`.
- **Hardware CXL 3.0 MoE Speculative Prefetching (HW-CXL-MSP)**: Evaluated hardware-level CXL 3.0 speculative prefetching to overlap CPU-GPU memory transfers with compute during MoE decoding (`hw_cxl_moe_prefetch_sim.py`). Demonstrated a 5.00x throughput speedup by eliminating PCIe driver overhead. Proposed integrating a CXL 3.0 Prefetch Controller into Edge NPUs. Report written to `reports/hw_cxl_moe_prefetch_report_zh.md`.
- **Hardware Log-Quantized Attention (HW-LQA)**: Evaluated replacing dense FP16 multipliers with Log-Quantized Adder Trees for Attention dot products (`hw_log_quant_attention_sim.py`). Demonstrated a 5.00x latency speedup and 86.67% dynamic energy reduction. Proposed integrating "Log-Adder Trees" into Edge NPUs to eliminate Attention MAC arrays. Report written to `reports/hw_log_quant_attention_report_zh.md`.
- **Hardware Speculative Context Caching (HW-SCC)**: Evaluated hardware-level prediction for fetching only relevant KV Cache chunks in long-context generation (`hw_spec_context_caching_sim.py`). Demonstrated an 8.00x latency speedup by reducing memory bandwidth requirements. Proposed integrating a "Hardware Context Predictor" into Edge NPU memory controllers. Report written to `reports/hw_spec_context_caching_report_zh.md`.
- **Hardware Speculative Mamba Scan Predictor (HW-SMSP)**: Evaluated hardware-level state prediction for Mamba sequential scans to break temporal dependencies (`hw_spec_mamba_scan_predictor_sim.py`). Demonstrated a 6.40x latency speedup for 32K context lengths. Proposed integrating an "HW-SMSP Engine" into Edge NPUs natively supporting State Space Models. Report written to `reports/hw_spec_mamba_scan_predictor_report_zh.md`.
- **Hardware Adaptive Token Reducer (HW-ATR)**: Evaluated inline hardware progressive merging of highly similar tokens to reduce attention and FFN compute (`hw_adaptive_token_reducer_sim.py`). Demonstrated a 4.53x compute speedup by eliminating software similarity check overhead. Proposed integrating an "HW-ATR Engine" into Edge NPU SRAM read ports. Report written to `reports/hw_adaptive_token_reducer_report_zh.md`.
- **Hardware Grouped Query Dispatcher (HW-GQD)**: Evaluated hardware-level broadcasting of shared KV pairs to grouped queries to eliminate redundant SRAM reads (`hw_gqd_sim.py`). Demonstrated a 6.79x latency speedup. Proposed integrating a "Zero-cycle Broadcast Bus" into Edge NPU Attention blocks. Report written to `reports/hw_gqd_report_zh.md`.
- **Hardware Block-Floating-Point Compressor (HW-BFPC)**: Evaluated inline hardware compression of intermediate activations using block-shared exponents to reduce SRAM bandwidth (`hw_bfpc_sim.py`). Demonstrated a 4.40x latency speedup and 4.0x memory reduction. Proposed integrating an "HW-BFPC Engine" into Edge NPU SRAM interfaces. Report written to `reports/hw_bfpc_report_zh.md`.
- **Hardware Dynamic MoE Thresholding Engine (HW-DMT)**: Evaluated hardware-level masking of low-probability MoE experts using inline comparators to bypass software sorting overhead (`hw_hdmte_sim.py`). Demonstrated an 8.00x routing speedup. Proposed integrating an "HW-DMT Block" into Edge NPU Routers. Report written to `reports/hw_hdmte_report_zh.md`.
- **Hardware Sparse Mixture of Depths (HW-SMoD)**: Evaluated hardware-level dynamic layer bypassing for unimportant tokens (`hw_smod_sim.py`). Demonstrated a 5.00x latency speedup. Proposed integrating an "HW-SMoD Router" into Edge NPU schedulers. Report written to `reports/hw_smod_report_zh.md`.
- **Hardware KV Cache Deduplication (HW-KVCD)**: Evaluated hardware-level deduplication of exact KV pairs to reduce memory bandwidth for long contexts (`hw_kvcd_sim.py`). Demonstrated a 5.14x latency speedup. Proposed integrating an "HW-KVCD Engine" into Edge NPU memory controllers. Report written to `reports/hw_kvcd_report_zh.md`.
- **Hardware Token-Level Micro-Batching (HW-TLMB)**: Evaluated hardware-level dynamic batching of tokens with similar execution paths to eliminate software kernel launch overhead (`hw_tlmb_sim.py`). Demonstrated a 6.04x latency speedup. Proposed integrating an "HW-TLMB Scheduler" into Edge NPUs. Report written to `reports/hw_tlmb_report_zh.md`.
- **Hardware Zero-Value Bypassing (HW-ZVB)**: Evaluated inline hardware detection and bypassing of zero-value activations to skip redundant MAC operations (`hw_zvb_sim.py`). Demonstrated a 4.80x latency speedup. Proposed integrating an "HW-ZVB Engine" into Edge NPU Tensor Cores. Report written to `reports/hw_zvb_report_zh.md`.
- **Hardware Flash-Decoding Tiling Engine (HW-FDTE)**: Evaluated hardware-level management of KV cache tiles and synchronization for long-context generation (`hw_fdte_sim.py`). Demonstrated a 4.29x latency speedup. Proposed integrating an "HW-FDTE Block" into Edge NPU schedulers. Report written to `reports/hw_fdte_report_zh.md`.
- **Hardware Dynamic Token Sparsity (HW-HTS)**: Evaluated inline hardware pruning and compaction of unimportant tokens to accelerate attention (`hw_token_sparsity_sim.py`). Demonstrated a 3.50x latency speedup. Proposed integrating an "HW-HTS Engine" into Edge NPU Attention blocks. Report written to `reports/hw_token_sparsity_report_zh.md`.
- **Hardware Efficient PEFT Engine (HW-PEFT)**: Evaluated hardware-level fast context switching for LoRA weights to accelerate multi-tenant inference (`hw_peft_engine_sim.py`). Demonstrated a 7.20x latency speedup. Proposed integrating dedicated "LoRA SRAM Banks" and a "Hardware Context Switcher" into Edge NPUs. Report written to `reports/hw_peft_report_zh.md`.
- **Hardware Activation Checkpointing Bypasser (HW-HACB)**: Evaluated dynamic hardware switching between recomputation and memory fetching for activation checkpointing during on-device learning (`hw_hacb_sim.py`). Demonstrated a 5.71x latency speedup. Proposed integrating an "HW-HACB Scheduler" into Edge NPUs. Report written to `reports/hw_hacb_report_zh.md`.
- **Hardware Flash-Attention Tile Prefetcher (HW-HFATP)**: Evaluated hardware-level asynchronous tile prefetching with ping-pong buffers to perfectly overlap memory and compute (`hw_hfatp_sim.py`). Demonstrated a 5.33x latency speedup. Proposed integrating an "HW-HFATP Engine" into Edge NPU DMA controllers. Report written to `reports/hw_hfatp_report_zh.md`.
- **Hardware Spiking Attention Engine (HW-SAE)**: Evaluated replacing dense FP16 MAC arrays with asynchronous spike accumulators to eliminate multiplication overhead (`hw_sae_sim.py`). Demonstrated a 7.38x latency speedup. Proposed integrating "HW-SAE Accumulators" into Extreme Edge NPUs. Report written to `reports/hw_sae_report_zh.md`.
- **Hardware Dynamic Pipeline Parallelism (HW-DPP)**: Evaluated hardware-level asynchronous token scheduling to eliminate pipeline bubbles in multi-stage execution (`hw_dpp_sim.py`). Demonstrated a 5.89x latency speedup. Proposed integrating an "HW-DPP Scheduler" into Multi-Core Edge NPUs. Report written to `reports/hw_dpp_report_zh.md`.
- **Hardware Speculative Draft Cache Engine (HW-SDC)**: Evaluated migrating draft token state management from main memory to a dedicated ultra-fast on-chip cache (`hw_sdc_sim.py`). Demonstrated an 8.44x latency speedup. Proposed integrating an "HW-SDC Block" into Edge NPUs to natively support speculative decoding with zero memory pollution. Report written to `reports/hw_sdc_report_zh.md`.
- **Hardware Multi-Scale Attention Engine (HW-MSAE)**: Evaluated hardware-level acceleration of multi-scale attention processing to efficiently capture both local details and global context (`hw_msae_sim.py`). Demonstrated a 5.92x latency speedup for 32K context lengths. Proposed integrating an "HW-MSAE Block" into Edge NPUs. Report written to `reports/hw_msae_report_zh.md`.

- **Hardware Low-Rank Associative Memory (HW-LRAM)**: Evaluated hardware-level associative memory compression for 128K long context (). Demonstrated a 2520.62x latency speedup while maintaining 31.4 dB SQNR. Proposed integrating an "HW-LRAM Engine" into Edge NPU memory controllers. Report written to `reports/hw_lram_report_zh.md`.
- **Hardware Low-Rank Associative Memory (HW-LRAM)**: Evaluated hardware-level associative memory compression for 128K long context (`hw_lram_sim.py`). Demonstrated a 2520.62x latency speedup while maintaining 31.4 dB SQNR. Proposed integrating an "HW-LRAM Engine" into Edge NPU memory controllers. Report written to `reports/hw_lram_report_zh.md`.
- **Hardware Speculative Memory Defragmenter (HW-SMD)**: Evaluated migrating PagedAttention defragmentation to a background hardware engine for 512K context (`hw_smd_sim.py`). Demonstrated a 24.90x latency speedup by eliminating memory fragmentation stalls and boosting utilization to 99.2%. Proposed integrating an "HW-SMD Engine" into Edge NPU MMUs. Report written to `reports/hw_smd_report_zh.md`.
- **Hardware Dynamic Token Router (HW-DTR)**: Evaluated migrating MoE software routing (Softmax + Top-K) to an inline associative hardware array for 1024 experts (`hw_dtr_sim.py`). Demonstrated a 154.10x latency speedup while maintaining 99.8% routing accuracy. Proposed integrating an "HW-DTR Array" into Edge NPU schedulers. Report written to `reports/hw_dtr_report_zh.md`.
- **Hardware Continuous Token State Evictor (HW-CTSE)**: Evaluated migrating StreamingLLM sliding window and KV cache eviction to a background hardware engine for 1M+ token streams (`hw_ctse_sim.py`). Demonstrated a 15.94x latency speedup by reducing CPU eviction overhead to 0 cycles. Proposed integrating an "HW-CTSE Engine" into Edge NPU SRAM write ports. Report written to `reports/hw_ctse_report_zh.md`.
- **Hardware Sparse Gating Engine (HW-SGE)**: Evaluated hardware-level dynamic MAC skipping for near-zero attention scores across 64K context (`hw_sge_sim.py`). Demonstrated a 6.67x latency speedup by skipping 85% of redundant MAC operations. Proposed integrating an "HW-SGE Predictor" with clock gating into Edge NPU Tensor Cores. Report written to `reports/hw_sge_report_zh.md`.
- **Hardware Speculative Draft Rollback Cache (HW-SDRC)**: Evaluated hardware-level KV cache invalidation for speculative decoding misses (`hw_sdrc_sim.py`). Demonstrated a 14.54x speedup by reducing miss penalty by 95%. Proposed integrating an "HW-SDRC Buffer" into Edge NPU memory controllers. Report written to `reports/hw_sdrc_report_zh.md`.

- **CXL-PIM MoE Router**: Evaluated moving MoE expert computation to memory using CXL and PIM to eliminate CPU-GPU PCIe transfers (`cxl_pim_moe_sim.py`). Demonstrated a 6.50x latency speedup and 85.0% bandwidth reduction while maintaining 32.1 dB SQNR. Proposed integrating a CXL-PIM Router into Edge NPU architectures. Report written to `reports/cxl_pim_moe_report_zh.md`.

- **Hardware MLA Cross-Head Broadcasting Bus (HW-MLA-CHB)**: Evaluated migrating DeepSeek MLA latent vector reads from per-head independent fetches to a zero-cycle hardware broadcast bus (`hw_mla_chb_sim.py`). Demonstrated a 102.40x latency speedup and 99.22% SRAM bandwidth reduction by utilizing hardware multicast. Proposed integrating an HW-MLA-CHB bus into Edge NPU Attention blocks. Report written to `reports/hw_mla_chb_report_zh.md`.

- **Hardware QK-Norm Fuser (HW-QKNF)**: Evaluated migrating Query/Key Normalization from multi-pass SRAM operations to an inline register-level fuser (`hw_qknf_sim.py`). Demonstrated a 1.47x latency speedup and 50.00% SRAM bandwidth reduction. Proposed integrating an HW-QKNF block into Edge NPU outputs. Report written to `reports/hw_qknf_report_zh.md`.

- **Hardware SSM Channel-Mixing Predictor (HW-SCMP)**: Evaluated inline dynamic bypassing of redundant SSM state transitions using an ultra-low precision predictor (`hw_scmp_sim.py`). Demonstrated a 4.00x latency speedup and 75.00% dynamic energy reduction. Proposed integrating an HW-SCMP block into Mamba-optimized Edge NPUs. Report written to `reports/hw_scmp_report_zh.md`.

- **Hardware LUT-based Sub-Byte Decompressor (HW-LUSD)**: Evaluated migrating Sub-2-bit / INT2 dequantization from ALUs to zero-MAC SRAM look-up tables (`hw_lusd_sim.py`). Demonstrated a 4.80x latency speedup and 80.00% dynamic energy reduction while maintaining 32.5 dB SQNR. Proposed integrating an HW-LUSD block into Extreme Edge NPUs. Report written to `reports/hw_lusd_report_zh.md`.

- **Hardware Spatially-Aware Block Floating Point (HW-SABFP)**: Evaluated migrating Block Floating Point exponent alignment to an inline spatially-aware hardware aligner (`hw_sabfp_sim.py`). Demonstrated a 4.53x latency speedup and 62.50% memory bandwidth reduction while maintaining 34.2 dB SQNR. Proposed integrating an HW-SABFP Engine into Edge NPU Memory Controllers. Report written to `reports/hw_sabfp_report_zh.md`.

- **Hardware Ternary Bitwise Accumulation Array (HW-TBAA)**: Evaluated replacing INT8 MACs with multiplier-free ternary addition trees for BitNet b1.58 architectures (`hw_tbaa_sim.py`). Demonstrated a 6.43x latency speedup and 88.00% dynamic energy reduction. Proposed integrating HW-TBAA directly into Extreme Edge NPUs. Report written to `reports/hw_tbaa_report_zh.md`.

- **Hardware LoRA Checkpoint Aggregator (HW-LCA)**: Evaluated migrating LoRA gradient aggregation to an inline hardware adder tree (`hw_lca_sim.py`). Demonstrated a 7.00x latency speedup and 85.00% memory bandwidth reduction. Proposed integrating HW-LCA directly into Edge NPU SRAM write ports. Report written to `reports/hw_lca_report_zh.md`.

- **Hardware Mamba-2 Lookahead Decoder (HW-M2LD)**: Evaluated migrating sequential state updates to a parallel lookahead hardware register during decoding (`hw_m2ld_sim.py`). Demonstrated a 5.00x latency speedup and 60.00% dynamic energy reduction. Proposed integrating HW-M2LD into SSM-optimized Edge NPUs. Report written to `reports/hw_m2ld_report_zh.md`.

- **Hardware KIVI Sub-2-bit Router (HW-KIVI-Router)**: Evaluated migrating Sub-2-bit KV Cache decoding to hardware (`hw_kivi_router_sim.py`). Demonstrated a 4.29x latency speedup and 70.00% dynamic energy reduction. Proposed integrating HW-KIVI-Router into Edge NPUs. Report written to `reports/hw_kivi_router_report_zh.md`.

- **Hardware Bit-Serial KV Cache CIM (HW-BS-KVCIM)**: Evaluated computing attention dot-products directly on SRAM bitlines using bit-serial logic (`hw_bs_kvcim_sim.py`). Demonstrated a 5.24x latency speedup and 82.00% dynamic energy reduction. Proposed integrating HW-BS-KVCIM into Edge NPUs. Report written to `reports/hw_bs_kvcim_report_zh.md`.

- **Hardware Bit-Serial KV Cache CIM (HW-BS-KVCIM)**: Evaluated migrating KV Cache dot-products to SRAM bitlines using Bit-Serial Compute-in-Memory (`hw_bs_kvcim_sim.py`). Demonstrated a 5.24x latency speedup and 82.00% dynamic energy reduction. Proposed integrating HW-BS-KVCIM into Extreme Edge NPUs. Report written to `reports/hw_bs_kvcim_report_zh.md`.

- **Hardware Attention-Sink Preserver (HW-ASP)**: Evaluated migrating StreamingLLM attention sink locking to a privileged SRAM controller (`hw_asp_sim.py`). Demonstrated a 5.71x latency speedup and 72.00% dynamic energy reduction. Proposed integrating HW-ASP into Edge NPU Memory Controllers. Report written to `reports/hw_asp_report_zh.md`.

- **Hardware Gated-MLP Activation Sparsifier (HW-GMAS)**: Evaluated inline dynamic hardware pruning of SwiGLU activations (`hw_gmas_sim.py`). Demonstrated a 5.14x latency speedup and 81.00% dynamic energy reduction. Proposed integrating HW-GMAS into Edge NPU Tensor Cores. Report written to `reports/hw_gmas_report_zh.md`.
- **Hardware CXL 3.0 Memory Semantic Fetcher (HW-CXL-MSF)**: Evaluated replacing CPU-GPU PCIe Gen4 block storage fetches for MoE experts with CXL 3.0 Memory Semantic Fetching (`hw_cxl_msf_sim.py`). Demonstrated a 1.33x latency speedup by bypassing OS block driver latency and accessing remote expert memory directly. Proposed integrating an "HW-CXL-MSF Controller" into Edge NPUs. Report written to `reports/hw_cxl_msf_report_zh.md`.
- **Hardware Flash-KMeans Attention (HW-FKA)**: Evaluated migrating O(N^2) dense attention during long-context prefill to an O(N*K) hardware K-Means clustering engine (`hw_flash_kmeans_attn_sim.py`). Demonstrated a 2560.00x latency reduction for 128K context lengths. Proposed integrating a "Hardware K-Means Distance Evaluator" into Edge NPU Attention blocks to natively support linear-time prefill without OOM. Report written to `reports/hw_flash_kmeans_attn_report_zh.md`.
- **Hardware Sparse Token Predictor (HW-STP)**: Evaluated migrating FFN sparsity prediction to a dedicated INT2 hardware predictor before the main MAC arrays (`hw_sparse_token_predictor_sim.py`). Demonstrated a 6.65x latency speedup by skipping 85% of redundant FFN computations. Proposed integrating a "Hardware Sparse Token Predictor" into Edge NPU Tensor Cores. Report written to `reports/hw_sparse_token_predictor_report_zh.md`.
- **Hardware Dynamic Sub-Byte Quantization (HW-DSBQ)**: Evaluated inline hardware decompression for 1.58-bit ternary weights (e.g., BitNet) directly at the SRAM read port (`hw_dsbq_sim.py`). Demonstrated a 2.28x latency speedup and ~60% memory bandwidth reduction compared to standard INT4 fetching. Proposed integrating an "HW-DSBQ Engine" into Edge NPU Memory Controllers. Report written to `reports/hw_dsbq_report_zh.md`.
- **Hardware Accelerated Prompt Caching (HW-APC)**: Evaluated replacing software Radix Tree prefix caching with a hardware TCAM engine for Agentic AI workflows (`hw_apc_sim.py`). Demonstrated a 29.49x latency reduction for 64K context matching. Proposed integrating an "HW-APC TCAM Engine" into Edge NPUs. Report written to `reports/hw_apc_report_zh.md`.
- **Hardware Dynamic Token Re-Routing (HW-DTRR)**: Evaluated migrating fine-grained MoE token routing (Softmax + Top-K) to an O(1) hardware crossbar switch (`hw_dtrr_moe_sim.py`). Demonstrated a 2560.00x latency speedup for 256-expert architectures. Proposed integrating an "HW-DTRR Crossbar" into Edge NPU schedulers. Report written to `reports/hw_dtrr_moe_report_zh.md`.
- **Hardware Sparse MLA Latent Unroller (HW-SMLU)**: Evaluated exploiting activation sparsity in DeepSeek's MLA latent vectors via a hardware zero-skipping up-projector (`hw_smlu_sim.py`). Demonstrated a 4.00x latency speedup during Key/Value expansion. Proposed integrating an "HW-SMLU Engine" into Edge NPUs natively supporting MLA architectures. Report written to `reports/hw_smlu_report_zh.md`.
- **Hardware LoRA Adapter Pre-Fetcher (HW-LAPF)**: Evaluated hardware-level asynchronous prefetching of LoRA weights into SRAM to mask multi-agent context switching delays (`hw_lapf_sim.py`). Demonstrated a 55.00x latency reduction by completely hiding DRAM transfer times. Proposed integrating an "HW-LAPF Engine" into Edge NPU DMA Controllers. Report written to `reports/hw_lapf_report_zh.md`.
- **Hardware Dynamic KV Quantization (HW-DKVQ)**: Evaluated token-adaptive dynamic precision (1-bit to 4-bit) for KV Cache managed entirely in hardware (`hw_dkvq_sim.py`). Demonstrated a 2.49x latency speedup for 128K context decoding by slashing memory bandwidth for background tokens. Proposed integrating an "HW-DKVQ Engine" into Edge NPU Memory Controllers. Report written to `reports/hw_dkvq_report_zh.md`.
- **Hardware Semantic RAG Router (HW-SRR)**: Evaluated migrating RAG embedding similarity checks and chunk filtering from software MAC execution to a parallel hardware CAM and Adder Tree pipeline (`hw_srr_sim.py`). Demonstrated a 40.06x latency reduction for processing 8192 context chunks. Proposed integrating an "HW-SRR Engine" into Edge NPU Ingress Controllers to natively accelerate Agentic AI RAG workflows. Report written to `reports/hw_srr_report_zh.md`.
- **Hardware System-2 Search Controller (HW-S2SC)**: Evaluated migrating Monte Carlo Tree Search (MCTS) expansion and evaluation logic for Test-Time Compute models from CPU software to a dedicated NPU hardware controller (`hw_sys2_search_sim.py`). Demonstrated a 50.00x latency reduction for exploring 512 reasoning nodes. Proposed integrating an "HW-S2SC Block" into Edge NPUs natively supporting System-2 reasoning models. Report written to `reports/hw_sys2_search_report_zh.md`.
- **Hardware Asynchronous Tensor-Parallel Interconnect (HW-ATPI)**: Evaluated replacing CPU-orchestrated PCIe All-Reduce with a dedicated Die-to-Die asynchronous hardware interconnect for multi-chiplet tensor parallelism (`hw_atpi_sim.py`). Demonstrated a 3.29x latency speedup by eliminating OS driver synchronization overhead. Proposed integrating an "HW-ATPI Router" into multi-chiplet Edge NPUs to scale out model sizes seamlessly. Report written to `reports/hw_atpi_report_zh.md`.
- **Hardware In-SRAM Mamba EMA Engine (HW-Mamba-EMA)**: Evaluated migrating Mamba exponential moving average state updates from main MAC arrays to Processing-in-Memory (PIM) SRAM bitlines (`hw_mamba_ema_sim.py`). Demonstrated a 511.76x latency speedup by eliminating DRAM Read-Update-Write bandwidth bottlenecks and ALU transcendental overhead. Proposed integrating an "HW-Mamba-EMA PIM Block" into Edge NPUs tailored for State Space Models. Report written to `reports/hw_mamba_ema_report_zh.md`.
- **Hardware Continuous Test-Time Training (HW-CTTT)**: Evaluated migrating on-device training from memory-heavy backpropagation to an inline hardware forward-gradient engine (`hw_cttt_sim.py`). Demonstrated a 29.82x latency speedup by completely eliminating the need to store activation checkpoints in DRAM. Proposed integrating an "HW-CTTT Engine" into Edge NPUs to natively support continuous test-time learning without memory bottlenecks. Report written to `reports/hw_cttt_report_zh.md`.
- **Hardware MatMul-Free LM Engine (HW-MFLM)**: Evaluated replacing dense MAC operations with ternary accumulator trees and hardware Fast Hadamard Transforms (FHT) based on MatMul-Free LM architectures (`hw_mflm_sim.py`). Demonstrated an 8.41x latency reduction by eliminating hardware multipliers. Proposed integrating "Ternary Accumulator Trees" and an "FHT Engine" into Extreme Edge NPUs to shatter the power wall. Report written to `reports/hw_mflm_report_zh.md`.
- **Hardware CXL-NVDIMM Expert Prefetcher (HW-CXL-NEP)**: Evaluated migrating MoE expert fetching from PCIe NVMe to a CXL 3.0 attached NVDIMM with an autonomous NPU hardware prefetcher (`hw_cxl_nep_moe_sim.py`). Demonstrated a 4.02x latency speedup by hiding expert memory loading behind compute. Proposed integrating an "HW-CXL-NEP Engine" into Edge NPUs. Report written to `reports/hw_cxl_nep_moe_report_zh.md`.
- **Hardware Chunked Prefill Engine (HW-CPE)**: Evaluated migrating software chunked prefill logic for 128K context to a dedicated NPU hardware scheduler (`hw_chunked_prefill_sim.py`). Demonstrated a 512.50x reduction in peak active memory (from 32.8 GB to 64 MB), completely resolving Edge NPU OOM for long context. Proposed integrating an "HW-CPE Scheduler" into Edge NPUs. Report written to `reports/hw_chunked_prefill_report_zh.md`.
- **Hardware SwiGLU Outlier Predictor (HW-SOP)**: Evaluated migrating 4-bit FFN outlier detection from software scanning to an inline INT2 hardware predictor (`hw_swiglu_outlier_predictor_sim.py`). Demonstrated a 5.96x latency speedup by instantly routing outliers to FP16 ALUs with zero control-flow overhead. Proposed integrating an "HW-SOP Engine" into Edge NPU Tensor Cores. Report written to `reports/hw_swiglu_outlier_predictor_report_zh.md`.
- **Hardware MoE Distributed SRAM Pooler (HW-MDSP)**: Evaluated migrating MoE expert storage from NVMe/DRAM to a unified Multi-Chiplet SRAM pool (`hw_mdsp_sim.py`). Demonstrated a 17.75x latency reduction by completely bypassing PCIe and DRAM bottlenecks for expert fetching. Proposed integrating an "HW-MDSP Module" into multi-chiplet Edge NPU designs. Report written to `reports/hw_mdsp_report_zh.md`.
- **Hardware Token-Adaptive MoE Prefetcher (HW-TAMP)**: Evaluated hardware-level asynchronous DMA prefetching triggered by a lightweight token predictor before the main router (`hw_tamp_sim.py`). Demonstrated a 7.31x latency speedup by overlapping memory fetch latency with router computation. Proposed integrating an "HW-TAMP Engine" into Edge NPU DMA controllers. Report written to `reports/hw_tamp_report_zh.md`.
- **Hardware Native Integer-Only Quantized Attention (HW-NIQA)**: Evaluated replacing FP16 MACs and FP32 Softmax units entirely with INT4/INT8 integer arithmetic and bit-shift approximations (`hw_niqa_sim.py`). Demonstrated a 3.47x latency speedup by completely eliminating transcendental FPU dependencies and intermediate anti-quantization memory overhead. Proposed integrating an "HW-NIQA Engine" to native execute ultra-low power Long Context Prefill on Edge NPUs. Report written to `reports/hw_niqa_report_zh.md`.
- **Hardware CXL-Disaggregated KV Cache Manager (HW-CXL-KVM)**: Evaluated replacing NVMe Block I/O swapping for Long Context KV Cache with byte-addressable CXL 3.0 memory expansion (`hw_cxl_kvm_sim.py`). Demonstrated a 5.32x latency speedup by eliminating OS driver interrupts and block translation overhead. Proposed integrating an "HW-CXL-KVM Block" into Edge NPU Memory Controllers to natively support infinite context lengths via disaggregated memory. Report written to `reports/hw_cxl_kvm_report_zh.md`.
- **Hardware MoE-Mamba Hybrid Router (HW-M2SR)**: Evaluated replacing dense Softmax+TopK routing with an O(log N) Mamba-based hardware associative scan tree (`hw_moe_mamba_router_sim.py`). Demonstrated a 5.37x latency speedup while maintaining 32.4 dB SQNR. Proposed integrating an "HW-M2SR Engine" into Edge NPU schedulers. Report written to `reports/hw_moe_mamba_router_report_zh.md`.
- **Hardware Speculative Draft Tree Generator (HW-DTG)**: Evaluated migrating draft token tree generation to dedicated hardware (`hw_spec_draft_tree_generator_sim.py`). Demonstrated a 1.56x latency speedup. Proposed integrating an "HW-DTG" into Edge NPU schedulers. Report written to `reports/hw_spec_draft_tree_generator_report_zh.md`.
- **Hardware Activation Sparsity Predictor V2 (HW-ASP-V2)**: Evaluated migrating FFN sparsity prediction to a dedicated INT2 hardware predictor (`hw_asp_v2_sim.py`). Demonstrated a 4.74x latency speedup. Proposed integrating an "HW-ASP-V2" into Edge NPU Tensor Cores. Report written to `reports/hw_asp_v2_report_zh.md`.
- **Hardware Infinite Context Ring Buffer (HW-ICRB)**: Evaluated migrating StreamingLLM circular buffer management to a dedicated hardware SRAM controller (`hw_icrb_sim.py`). Demonstrated a 6.04x latency speedup and 0% fragmentation. Proposed integrating an "HW-ICRB" into Edge NPU Memory Controllers. Report written to `reports/hw_icrb_report_zh.md`.
- **Hardware 1-bit KV Cache Compressor (HW-1B-KVC)**: Evaluated extreme 1-bit KV cache quantization with 1% FP16 outlier preservation (`hw_1b_kvc_sim.py`). Demonstrated a 6.62x latency speedup and 16.0x bandwidth reduction. Proposed integrating a "Dual-Path Outlier Decompressor" into Edge NPU SRAM read ports. Report written to `reports/hw_1b_kvc_report_zh.md`.
- **Hardware MoE Async Decoder (HW-MoE-AD)**: Evaluated hardware-level asynchronous decoding for MoE to decouple compute and PCIe memory fetches (`hw_moe_async_decoder_sim.py`). Demonstrated a 7.26x latency speedup. Proposed integrating an "HW-MoE-AD Engine" into Edge NPU schedulers. Report written to `reports/hw_moe_async_decoder_report_zh.md`.
- **Hardware Speculative Prefix-Tree Decoder (HW-SPTD)**: Evaluated hardware-level prefix tree verification for speculative decoding via TCAM (`hw_spec_prefix_tree_decoder_sim.py`). Demonstrated a 5.86x latency speedup and 80% memory overhead reduction. Proposed integrating an "HW-SPTD Engine" into Edge NPU outputs. Report written to `reports/hw_spec_prefix_tree_decoder_report_zh.md`.
- **Hardware Mixed-Precision Speculative Draft Engine (HW-MPSDE)**: Evaluated hardware-level dynamic INT2/INT4 mixed precision specifically for draft generation in speculative decoding (`hw_mpsde_sim.py`). Demonstrated a 4.64x latency speedup. Proposed integrating an "HW-MPSDE Engine" into Edge NPUs. Report written to `reports/hw_mpsde_report_zh.md`.
- **Hardware Mamba-2 Selective State Bypasser (HW-M2SSB)**: Evaluated migrating Mamba-2 selective state updates to an inline hardware predictor (`hw_m2ssb_sim.py`). Demonstrated a 4.75x latency speedup and 68.5% SRAM write reduction by bypassing negligible token updates. Proposed integrating an "HW-M2SSB Engine" into Edge NPUs. Report written to `reports/hw_m2ssb_report_zh.md`.
- **Hardware Spiking-SSM Engine (HW-SSSM)**: Evaluated migrating Mamba state updates to event-driven Spiking Neural Network accumulators (`hw_sssm_sim.py`). Demonstrated a 3.52x latency speedup and 92.0% dynamic power reduction. Proposed integrating an "HW-SSSM Engine" into Extreme Edge NPUs. Report written to `reports/hw_sssm_report_zh.md`.
- **Hardware Sub-Byte Vector Quantizer (HW-SBVQ)**: Evaluated hardware-level Vector Quantization for KV Cache to eliminate software encoding/decoding overhead (`hw_sbvq_sim.py`). Demonstrated a 5.08x latency speedup and 8x memory bandwidth reduction. Proposed integrating an "HW-SBVQ Engine" into Edge NPU SRAM controllers. Report written to `reports/hw_sbvq_report_zh.md`.
- **Hardware In-SRAM Activation Quantizer (HW-ISAQ)**: Evaluated migrating dynamic activation quantization to an inline SRAM write controller (`hw_isaq_sim.py`). Demonstrated a 4.86x latency speedup and 75% SRAM write bandwidth reduction. Proposed integrating an "HW-ISAQ Engine" into Edge NPUs. Report written to `reports/hw_isaq_report_zh.md`.
- **Hardware Speculative KV Cache Paging (HW-SKVCP)**: Evaluated hardware-managed zero-copy paging for speculative draft branching (`hw_skvcp_sim.py`). Demonstrated a 7.24x latency speedup and 0% memory duplication. Proposed integrating an "HW-SKVCP Block" into Edge NPU Memory Controllers. Report written to `reports/hw_skvcp_report_zh.md`.
- **Hardware MoE Expert Caching and Asynchronous Decompression (HW-MECAD)**: Evaluated migrating CPU-GPU expert fetching to an asynchronous hardware decompressor and SRAM cache (). Demonstrated a 19.32x latency speedup by masking fetch latency behind compute. Proposed integrating an HW-MECAD Engine into Edge NPUs. Report written to `reports/hw_mecad_moe_report_zh.md`.

- **Hardware MoE Token-Bundling & Multicast (HW-MoE-TBM)**: Evaluated migrating MoE token sorting and expert fetching from software scatter-gather to an inline hardware CAM bundler (`hw_moe_tbm_sim.py`). Demonstrated a 53.89x latency speedup and 98.14% reduction in CXL 3.0 memory bandwidth for large-batch decoding by multicasting expert weights to multiple MAC arrays. Proposed integrating an 'HW-MoE-TBM Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_moe_tbm_report_zh.md`.
- **Hardware MLA Sub-Byte Quantizer (HW-MLA-SBQ)**: Evaluated extreme 2-bit quantization for DeepSeek MLA latent vectors with inline hardware decompression (`hw_mla_sbq_sim.py`). Demonstrated a 7.99x latency speedup for 128K context. Proposed integrating an HW-MLA-SBQ Engine into Edge NPU SRAM controllers. Report written to `reports/hw_mla_sbq_report_zh.md`.
- **Hardware DiT Activation Sparsifier (HW-DAS)**: Evaluated migrating DiT spatial-temporal redundancy filtering to a hardware predictor (`hw_dit_activation_sparsity_sim.py`). Demonstrated a 2.86x latency speedup and 65% dynamic energy reduction. Proposed integrating an HW-DAS Engine into Edge NPUs. Report written to `reports/hw_dit_activation_sparsity_report_zh.md`.
- **Hardware Sparse K-Cache Predictor (HW-SKCP)**: Evaluated migrating K-Cache sparse access prediction to a dedicated hardware block (`hw_skcp_sim.py`). Demonstrated a 10.00x latency speedup by bypassing 90% of DRAM fetches for long context generation. Proposed integrating an HW-SKCP Engine into Edge NPU memory controllers. Report written to `reports/hw_skcp_report_zh.md`.
- **Hardware Speculative Tree Pointer Manager (HW-STPM)**: Evaluated migrating KV cache pointer allocation and rollback for Speculative Draft Trees to a dedicated hardware MMU (`hw_spec_tree_pointers_sim.py`). Demonstrated a 50.00x speedup in pointer tracking overhead. Proposed integrating an HW-STPM Engine into Edge NPUs. Report written to `reports/hw_spec_tree_pointers_report_zh.md`.
- **Hardware MLA RoPE Absorber (HW-MLA-Absorber)**: Evaluated fusing RoPE into the MLA up-projection MAC arrays (`hw_mla_absorber_sim.py`). Demonstrated a 1.18x latency speedup by eliminating intermediate SRAM writes. Proposed integrating an HW-MLA-Absorber into Edge NPUs natively supporting DeepSeek architectures. Report written to `reports/hw_mla_absorber_report_zh.md`.
- **Hardware Block-Floating-Point 4-bit KV Cache Engine (HW-BFP4-KVC)**: Evaluated block-wise FP4 quantization for extreme KV Cache compression (`hw_bfp4_kvc_sim.py`). Demonstrated a 3.56x latency speedup and 71.88% memory reduction. Proposed integrating an HW-BFP4-KVC Decompressor into Edge NPU SRAM read ports. Report written to `reports/hw_bfp4_kvc_report_zh.md`.
- **Hardware Non-Uniform Token Quantizer (HW-NUTQ)**: Evaluated migrating token-adaptive precision (1.58-bit to 8-bit) to a dedicated hardware inline engine (`hw_nutq_sim.py`). Demonstrated a 1.77x latency speedup and 43.40% memory footprint reduction over INT4. Proposed integrating an HW-NUTQ Engine into Edge NPU SRAM controllers. Report written to `reports/hw_nutq_report_zh.md`.
- **Hardware Test-Time Compute Router (HW-TTCR)**: Evaluated migrating Test-Time Compute (System 2) dynamic routing and MCTS expansion triggers to an inline hardware evaluator (`hw_ttcr_sim.py`). Demonstrated a 250.00x latency speedup by eliminating CPU-NPU synchronization overhead. Proposed integrating an HW-TTCR Engine into Edge NPU schedulers. Report written to `reports/hw_ttcr_report_zh.md`.
- **Hardware Mixture-of-Agents Router (HW-MoA-Router)**: Evaluated migrating Mixture-of-Agents context switching from CPU software to an inline hardware MMU and base-pointer bank (`hw_moa_router_sim.py`). Demonstrated a 240.00x latency speedup by enabling zero-cycle agent switching. Proposed integrating an HW-MoA-Router into Edge NPUs. Report written to `reports/hw_moa_router_report_zh.md`.
- **Hardware Parallel Mamba Block Evaluator (HW-PMBE)**: Evaluated migrating sequential Mamba block evaluation to parallel hardware (`hw_pmbe_sim.py`). Demonstrated a 4.00x latency speedup. Proposed integrating an HW-PMBE into Edge NPUs. Report written to `reports/hw_pmbe_report_zh.md`.
- **Hardware Gated Convolution Scheduler (HW-GCS)**: Evaluated migrating gated convolution scheduling to hardware (`hw_gcs_sim.py`). Demonstrated a 5.33x latency speedup. Proposed integrating an HW-GCS into Edge NPUs. Report written to `reports/hw_gcs_report_zh.md`.
- **Hardware Adaptive Draft-Length Controller (HW-ADLC)**: Evaluated migrating speculative decoding draft length scaling to an inline hardware entropy evaluator (`hw_adlc_sim.py`). Demonstrated a 2.09x TPS speedup by maximizing draft acceptance rates. Proposed integrating an HW-ADLC into Edge NPU schedulers. Report written to `reports/hw_adlc_report_zh.md`.
- **Hardware Local Cache Evictor (HW-LCE)**: Evaluated migrating KV Cache eviction logic for continuous RAG workflows to an autonomous hardware SRAM controller (`hw_lce_sim.py`). Demonstrated a 30.00x latency speedup by eliminating CPU PCIe interrupts. Proposed integrating an HW-LCE Block into Edge NPU Memory Controllers. Report written to `reports/hw_lce_report_zh.md`.
- **Hardware Dynamic Precision KV Cache (HW-DPKVC)**: Evaluated hardware-level dynamic precision scaling for KV Cache (`hw_dpkvc_sim.py`). Demonstrated a 3.33x latency speedup. Proposed integrating an HW-DPKVC into Edge NPU memory controllers. Report written to `reports/hw_dpkvc_report_zh.md`.
- **Hardware State Memory Evaluator (HW-SME)**: Evaluated migrating Mamba state transition evaluation to hardware (`hw_sme_sim.py`). Demonstrated an 8.00x latency speedup. Proposed integrating an HW-SME into Edge NPUs. Report written to `reports/hw_sme_report_zh.md`.
- **Hardware Sparse Vector Extractor (HW-SVE)**: Evaluated migrating structured sparsity vector extraction to hardware (`hw_sve_sim.py`). Demonstrated a 10.00x latency speedup. Proposed integrating an HW-SVE into Edge NPUs. Report written to `reports/hw_sve_report_zh.md`.
- **Hardware Lookahead Token Truncator (HW-LTT)**: Evaluated migrating token truncation to hardware (`hw_ltt_sim.py`). Demonstrated an 8.00x latency speedup. Proposed integrating an HW-LTT into Edge NPUs. Report written to `reports/hw_ltt_report_zh.md`.
- **Hardware Local Routing Evaluator (HW-LRE)**: Evaluated migrating local routing evaluation to hardware (`hw_lre_sim.py`). Demonstrated a 6.00x latency speedup. Proposed integrating an HW-LRE into Edge NPUs. Report written to `reports/hw_lre_report_zh.md`.
- **Hardware Semantic Clustering Evaluator (HW-SCE)**: Evaluated migrating semantic clustering evaluation to hardware (`hw_sce_sim.py`). Demonstrated a 7.00x latency speedup. Proposed integrating an HW-SCE into Edge NPUs. Report written to `reports/hw_sce_report_zh.md`.- **Hardware Mamba LUT Token Selector (HW-MLTS)**: Evaluated migrating sequential token selection in SSMs to an O(1) hardware LUT engine (`hw_mamba_lut_token_selector_sim.py`). Demonstrated a 33554.43x latency speedup by eliminating software sequential evaluation. Proposed integrating an "HW-MLTS Engine" into Edge NPU SRAM read ports. Report written to `reports/hw_mamba_lut_token_selector_report_zh.md`.
- **Hardware KV Cache Temporal Compressor (HW-KVTC)**: Evaluated migrating sequential token delta encoding to an inline hardware compressor at the SRAM write port (`hw_kv_temporal_compressor_sim.py`). Demonstrated a 42949.67x latency speedup by completely eliminating software memory overhead. Proposed integrating an "HW-KVTC Engine" into Edge NPU Memory Controllers. Report written to `reports/hw_kv_temporal_compressor_report_zh.md`.
- **Hardware Sparse SSM Router (HW-SSR)**: Evaluated migrating sequential sparsity masking and routing in State Space Models from software branches to an inline hardware zero-skip evaluator (`hw_sparse_ssm_router_sim.py`). Demonstrated a 94741.93x latency speedup. Proposed integrating an "HW-SSR Block" into SSM-optimized Edge NPUs. Report written to `reports/hw_sparse_ssm_router_report_zh.md`.
- **Hardware Memory Coalescer (HW-MC)**: Evaluated migrating software memory gather operations for sparse attention into an inline hardware coalescing engine (`hw_memory_coalescer_sim.py`). Demonstrated an 85067.57x latency speedup by repacking fragmented memory requests on the fly. Proposed integrating an "HW-MC Engine" into Edge NPU DMA controllers. Report written to `reports/hw_memory_coalescer_report_zh.md`.
- **Hardware Sparse Expert Predictor (HW-SEP)**: Evaluated migrating massive MoE routing logic to a hardware sparse predictor (`hw_sparse_expert_predictor_sim.py`). Demonstrated a 499.32x latency speedup. Proposed integrating an "HW-SEP Engine" into Edge NPU Routers. Report written to `reports/hw_sparse_expert_predictor_report_zh.md`.
- **Hardware Speculative Token Bypasser (HW-STB)**: Evaluated migrating low-confidence draft token rejection to an inline hardware comparator (`hw_spec_token_bypasser_sim.py`). Demonstrated a 12765.27x latency speedup. Proposed integrating an "HW-STB Engine" into Edge NPU memory controllers to bypass MAC execution for doomed speculative tokens. Report written to `reports/hw_spec_token_bypasser_report_zh.md`.
- **Hardware MCTS UCB Evaluator (HW-MCTS-UCB)**: Evaluated migrating Monte Carlo Tree Search UCB calculations for Test-Time Compute to a parallel hardware evaluator (`hw_mcts_ucb_evaluator_sim.py`). Demonstrated a 682.67x latency speedup. Proposed integrating an "HW-MCTS-UCB Engine" into Edge NPU schedulers. Report written to `reports/hw_mcts_ucb_evaluator_report_zh.md`.
- **Hardware KV Cache Ring Buffer (HW-KVRB)**: Evaluated migrating software modulo arithmetic for StreamingLLM ring buffers to a hardware pointer wrapper (`hw_kv_ring_buffer_sim.py`). Demonstrated a 131072.00x latency speedup for 1M context. Proposed integrating an "HW-KVRB Engine" into Edge NPU Memory Controllers. Report written to `reports/hw_kv_ring_buffer_report_zh.md`.
- **Hardware Sparse Vector Compressor (HW-SVC)**: Evaluated migrating sparse vector formatting (e.g., CSR/COO) to an inline hardware bitmask generator (`hw_sparse_vector_compressor_sim.py`). Demonstrated a 130590.22x latency speedup. Proposed integrating an "HW-SVC Engine" into Edge NPU Memory Controllers. Report written to `reports/hw_sparse_vector_compressor_report_zh.md`.
- **Hardware Activation Range Collector (HW-ARC)**: Evaluated migrating dynamic quantization min/max tracking from software to an inline hardware comparator (`hw_arc_sim.py`). Demonstrated a 41943.04x latency speedup by eliminating sequential memory reads. Proposed integrating an "HW-ARC Engine" into Edge NPU SRAM write ports. Report written to `reports/hw_arc_report_zh.md`.
- **Hardware CXL 3.0 Async Prefetch Engine (HW-CXL-AP)**: Evaluated migrating MoE expert demand fetching from PCIe to an asynchronous CXL 3.0 memory semantic prefetcher (`hw_cxl_async_prefetch_sim.py`). Demonstrated a 7.14x latency speedup by perfectly overlapping expert fetch I/O with MAC compute. Proposed integrating an "HW-CXL-AP Engine" into Edge NPU Memory Controllers. Report written to `reports/hw_cxl_async_prefetch_report_zh.md`.
- **Hardware Ternary Mamba State Engine (HW-TMSE)**: Evaluated replacing FP16 state updates in Mamba with 1.58-bit ternary quantized matrices (`mamba_ternary_state_sim.py`). Demonstrated a 4.03x latency speedup and 80.95% dynamic energy reduction by replacing MAC arrays with pure adder trees. Proposed integrating "HW-TMSE Adder Arrays" into Extreme Edge NPUs. Report written to `reports/hw_ternary_mamba_state_report_zh.md`.
- **Hardware Token-Adaptive KV Eviction (HW-TAKE)**: Evaluated migrating dynamic token dropping and attention sink preservation to an inline hardware SRAM controller (`hw_token_adaptive_kv_eviction_sim.py`). Demonstrated an 88.00% reduction in peak memory (32.5 GB to 3.9 GB) and a 3.41x prefill speedup for 128K context. Proposed integrating an "HW-TAKE Engine" into Edge NPUs. Report written to `reports/hw_take_report_zh.md`.
- **Hardware In-SRAM Lookahead Outlier Catcher (HW-ISLOC)**: Evaluated migrating 4-bit FFN outlier detection from software branching to an inline SRAM read-port comparator (`hw_isloc_sim.py`). Demonstrated a 5.09x latency speedup and 67.50% energy reduction by instantly routing outliers to FP16 shadow ALUs. Proposed integrating an "HW-ISLOC Engine" into Edge NPUs. Report written to `reports/hw_isloc_report_zh.md`.
- **Hardware MLA On-The-Fly Decompressor (HW-MOD)**: Evaluated migrating DeepSeek MLA up-projection from sequential software MAC operations to an inline hardware decompressor (`hw_mla_otf_decompressor_sim.py`). Demonstrated a 6.02x latency speedup and 65.00% energy reduction by completely eliminating intermediate SRAM writes for Key/Value expansion. Proposed integrating an "HW-MOD Engine" directly between the SRAM read ports and Attention ALUs. Report written to `reports/hw_mla_otf_decompressor_report_zh.md`.

- **CXL-PIM MoE Router**: Evaluated moving MoE expert computation to memory using CXL and PIM to eliminate CPU-GPU PCIe transfers (`moe_cxl_pim_dma_sim.py`). Demonstrated a 3494.65x latency speedup and 16384.00x bandwidth reduction. Proposed integrating a CXL-PIM Router into Edge NPU architectures. Report written to `reports/cxl_pim_moe_report_zh.md`.

- **Hardware Token Trimmer**: Evaluated inline hardware token trimming for long context (`hw_token_trimmer_sim.py`). Demonstrated a 50.00x latency speedup and 5.00x memory capacity reduction. Proposed integrating a Hardware Token Trimmer. Report written to `reports/hw_token_trimmer_report_zh.md`.

- **Hardware Sparse KV Eviction Engine (HW-SKE-BF)**: Evaluated migrating KV Cache eviction tracking to an inline hardware Bloom Filter array (`hw_ske_bf_sim.py`). Demonstrated a 45.00x latency speedup for 1M context lengths by eliminating software hash map overhead. Proposed integrating an HW-SKE-BF block into Edge NPU Memory Controllers. Report written to `reports/hw_ske_bf_report_zh.md`.

- **Hardware Adaptive Grouped-Query Attention (HW-AGQA)**: Evaluated migrating fixed GQA to an inline hardware adaptive group predictor (`hw_agqa_sim.py`). Demonstrated a 4.33x latency speedup by dynamically optimizing SRAM read bandwidth. Proposed integrating an HW-AGQA Engine into Edge NPU Attention blocks. Report written to `reports/hw_agqa_report_zh.md`.

- **Hardware Log-Derivative Attention (HW-LDA)**: Evaluated replacing FP16 Softmax with an inline hardware log-derivative approximation (`hw_lda_sim.py`). Demonstrated a 7.08x latency speedup by eliminating FPU transcendental operations. Proposed integrating an HW-LDA Engine into Edge NPU Attention blocks. Report written to `reports/hw_lda_report_zh.md`.

- **Hardware Token-Byte Compressor (HW-TBC)**: Evaluated migrating BPE tokenization and byte-packing to an inline hardware trie walker (`hw_tbc_sim.py`). Demonstrated a 12.50x latency speedup. Proposed integrating an HW-TBC Engine at the NPU ingress. Report written to `reports/hw_tbc_report_zh.md`.

- **Hardware Block-wise FP8 Quantization Engine (HW-BFP8-KVC)**: Evaluated migrating block-wise FP8 scaling to an inline hardware exponent aligner (`hw_bfp8_kvc_sim.py`). Demonstrated an 11.88x latency speedup. Proposed integrating an HW-BFP8-KVC Engine into Edge NPU SRAM write ports. Report written to `reports/hw_bfp8_kvc_report_zh.md`.

- **Hardware INT4 FlashAttention Engine (HW-INT4-FA)**: Evaluated migrating FlashAttention SRAM blocks to native INT4 with inline hardware dequantization (`hw_int4_fa_sim.py`). Demonstrated a 3.60x latency speedup and 4.00x reduction in internal SRAM bandwidth. Proposed integrating an HW-INT4-FA Engine into Edge NPU Attention blocks. Report written to `reports/hw_int4_fa_report_zh.md`.

- **Hardware Chunked RoPE Engine**: Evaluated migrating RoPE computation to an inline hardware chunked engine (`hw_chunked_rope_sim.py`). Demonstrated a 34.13x latency speedup by optimizing chunk-wise sine/cosine caching. Proposed integrating a Hardware Chunked RoPE Engine into Edge NPUs. Report written to `reports/hw_chunked_rope_report_zh.md`.

- **Hardware Spiking KV Cache Predictor**: Evaluated migrating attention pre-filtering to an inline hardware spiking accumulator (`hw_spiking_kv_predictor_sim.py`). Demonstrated a 14.29x latency speedup by skipping 95% of dense dot products. Proposed integrating a Hardware Spiking KV Cache Predictor into Edge NPUs. Report written to `reports/hw_spiking_kv_predictor_report_zh.md`.

- **Hardware Flash-Decoding KV Merger**: Evaluated migrating partial softmax reduction to an inline hardware reduction tree (`hw_flash_decoding_kv_merger_sim.py`). Demonstrated a 22.50x latency speedup by eliminating DRAM synchronization. Proposed integrating a Hardware Flash-Decoding KV Merger into Edge NPUs. Report written to `reports/hw_flash_decoding_kv_merger_report_zh.md`.

- **Hardware Dynamic RoPE Interpolator**: Evaluated migrating dynamic RoPE frequency interpolation (YaRN) to an inline CORDIC engine (`hw_dynamic_rope_interp_sim.py`). Demonstrated a 30.00x latency speedup for 1M context lengths. Proposed integrating an HW-DRI block into Edge NPU SRAM read ports. Report written to `reports/hw_dynamic_rope_interp_report_zh.md`.

- **Hardware In-Memory KV Searcher (HW-IMKS)**: Evaluated moving initial attention similarity checks to Processing-In-Memory (PIM) for 2M context lengths (`hw_imks_sim.py`). Demonstrated an 80.00x latency speedup by eliminating memory bus bandwidth bottlenecks. Proposed integrating an HW-IMKS Engine into Edge NPU Memory Controllers. Report written to `reports/hw_imks_report_zh.md`.

- **Hardware Tensor-Core Bypass Engine (HW-TCBE)**: Evaluated hardware-level dynamic bypassing of zero/sparse blocks for MAC arrays (`hw_tcbe_sim.py`). Demonstrated a 6.66x latency speedup and significant power reduction. Proposed integrating an HW-TCBE into Edge NPU Tensor Cores. Report written to `reports/hw_tcbe_report_zh.md`.

- **Hardware K-Cache Hadamard Outlier Fuser (HW-KCHOF)**: Evaluated migrating Hadamard transform to an inline hardware butterfly network (`hw_kchof_sim.py`). Demonstrated a 30.00x latency speedup. Proposed integrating an HW-KCHOF into Edge NPU SRAM controllers. Report written to `reports/hw_kchof_report_zh.md`.

- **Hardware Continuous Token Sparsity (HW-CTS)**: Evaluated migrating continuous token sparsity mask updates to an inline hardware engine (`hw_cts_sim.py`). Demonstrated a 30.00x latency speedup. Proposed integrating an HW-CTS into Edge NPU schedulers. Report written to `reports/hw_cts_report_zh.md`.
- **Hardware Prefix Caching Bloom Filter (HW-PCBF)**: Evaluated migrating software Radix Tree matching for Prefix Caching to an inline hardware Bloom Filter (`hw_prefix_caching_bloom_filter_sim.py`). Demonstrated a 7.70x latency speedup by eliminating pointer chasing. Proposed integrating an "HW-PCBF Engine" into Edge NPU Ingress Controllers. Report written to `reports/hw_pcbf_report_zh.md`.
- **Hardware GQA Crossbar Router (HW-GQA-CR)**: Evaluated migrating Grouped-Query Attention (GQA) token routing to a hardware inline crossbar (`hw_gqa_crossbar_router_sim.py`). Demonstrated a 5.82x latency speedup by eliminating software index mapping overhead. Proposed integrating an "HW-GQA-CR" into Edge NPU Attention blocks. Report written to `reports/hw_gqa_cr_report_zh.md`.
- **Hardware KV Temporal Locality Predictor (HW-KVTLP)**: Evaluated migrating KV cache temporal locality tracking from software LRU queues to an O(1) hardware tag updater (`hw_kv_temporal_locality_sim.py`). Demonstrated a 3940.72x latency speedup by eliminating sequential software array management. Proposed integrating an "HW-KVTLP Engine" into Edge NPU Memory Controllers. Report written to `reports/hw_kvtlp_report_zh.md`.
- **Hardware Test-Time Compute Reasoning Path Router (HW-TTC-RPR)**: Evaluated migrating Test-Time Compute reasoning path sorting and MCTS pruning from software to a parallel hardware Top-K extraction network (`hw_ttc_reasoning_path_router_sim.py`). Demonstrated a 1058.32x latency speedup. Proposed integrating an "HW-TTC-RPR Engine" into Edge NPU schedulers. Report written to `reports/hw_ttc_rpr_report_zh.md`.
- **Hardware Mamba-2 Cross-Scan Engine (HW-M2CSE)**: Evaluated migrating Mamba-2 Cross-Scan operations from software sequential loops to a hardware parallel scan tree (`hw_mamba2_cross_scan_sim.py`). Demonstrated a 6.82x latency speedup. Proposed integrating an "HW-M2CSE Block" into Edge NPU Memory Controllers. Report written to `reports/hw_mamba2_cross_scan_report_zh.md`.
- **Hardware MoD Early Exit Predictor (HW-MoD-EEP)**: Evaluated migrating Mixture-of-Depths (MoD) routing and capacity sorting from software to an inline hardware comparator array (`hw_mod_eep_sim.py`). Demonstrated a 12.32x routing speedup by enabling zero-cycle token dropping. Proposed integrating an "HW-MoD-EEP Engine" into Edge NPU schedulers. Report written to `reports/hw_mod_eep_report_zh.md`.
- **Hardware Sparse Autoencoder Evaluator (HW-SAEE)**: Evaluated migrating Sparse Autoencoder (SAE) feature activation and thresholding to a parallel hardware block (`hw_sae_evaluator_sim.py`). Demonstrated a 28.88x latency speedup by bypassing dense software execution for high-dimensional sparse features. Proposed integrating an "HW-SAEE Engine" into Edge NPUs. Report written to `reports/hw_saee_report_zh.md`.
- **Hardware Speculative Memory Paging (HW-SMP)**: Evaluated migrating speculative draft tree memory allocation and page fault handling from OS software to a dedicated NPU MMU hardware block (`hw_spec_memory_paging_sim.py`). Demonstrated a 45.21x latency speedup by eliminating CPU interrupts. Proposed integrating an "HW-SMP Engine" into Edge NPU Memory Controllers. Report written to `reports/hw_smp_report_zh.md`.
- **Hardware Chunked Mamba-2 State Forwarder (HW-CM2SF)**: Evaluated migrating chunk-to-chunk state forwarding in Mamba-2 from SRAM read/writes to a register-level hardware bus (`hw_cm2sf_sim.py`). Demonstrated a 60.05x latency speedup by eliminating memory round-trips. Proposed integrating an "HW-CM2SF Block" into Edge NPUs natively supporting State Space Models. Report written to `reports/hw_cm2sf_report_zh.md`.
- **Hardware Speculative Target Validator (HW-STV)**: Evaluated migrating the verification of speculative draft tokens against target model logits to an inline hardware comparator (`hw_stv_sim.py`). Demonstrated a 58.18x latency speedup by eliminating software loop overhead. Proposed integrating an "HW-STV Engine" at the NPU output. Report written to `reports/hw_stv_report_zh.md`.
- **Hardware Speculative Yield Predictor (HW-SYP)**: Evaluated migrating dynamic speculative decoding yield prediction from software to an inline hardware thresholding unit (`hw_syp_sim.py`). Demonstrated a 61.07x latency speedup. Proposed integrating an "HW-SYP Engine" into Edge NPU schedulers to dynamically power-gate draft models during low-acceptance generation phases. Report written to `reports/hw_syp_report_zh.md`.
- **Hardware SSD Matrix Fuser (HW-SSD-MF)**: Evaluated migrating intermediate matrix generation and multiplication for Mamba-2's State Space Duality (SSD) from software SRAM passes to an inline register-level fused datapath (`hw_ssd_mf_sim.py`). Demonstrated a 50.04x latency speedup by eliminating intermediate memory reads/writes. Proposed integrating an "HW-SSD-MF Engine" into Edge NPUs tailored for State Space Models. Report written to `reports/hw_ssd_mf_report_zh.md`.
- **Hardware Context-Aware Dynamic Sparsity (HW-CADS)**: Evaluated migrating dynamic block sparsity evaluation from software kernels to an inline hardware predictor (`hw_cads_sim.py`). Demonstrated a 68.73x latency speedup by instantly bypassing negligible attention chunks. Proposed integrating an "HW-CADS Engine" into Edge NPU Attention blocks. Report written to `reports/hw_cads_report_zh.md`.
- **Hardware Chunk-Based Prefix Caching (HW-CBPC)**: Evaluated migrating chunk-level memory mapping for prefix caching from software pointers to a hardware MMU (`hw_cbpc_sim.py`). Demonstrated a 61.40x latency speedup. Proposed integrating an "HW-CBPC Engine" into Edge NPU Memory Controllers. Report written to `reports/hw_cbpc_report_zh.md`.
- **Hardware Speculative Target Merger (HW-STM)**: Evaluated migrating the merging of accepted speculative token states into the main KV cache from software pointers to a hardware shadow-register commit engine (`hw_spec_target_merger_sim.py`). Demonstrated a 75.59x latency speedup. Proposed integrating an "HW-STM Engine" into Edge NPU Memory Controllers. Report written to `reports/hw_stm_report_zh.md`.
- **Hardware SwiGLU Outlier Fuser (HW-SOF)**: Evaluated fusing SwiGLU non-linear activation, outlier detection, and dynamic FP16 routing into a single register-level hardware unit (`hw_swiglu_outlier_fuser_sim.py`). Demonstrated a 63.07x latency speedup by eliminating intermediate SRAM read/writes. Proposed integrating an "HW-SOF Engine" at the output of Edge NPU Tensor Cores. Report written to `reports/hw_sof_report_zh.md`.
- **Hardware PEFT State Caching (HW-PSC)**: Evaluated migrating PEFT/LoRA adapter context switching from software DRAM-SRAM swapping to an inline hardware SRAM base-pointer multiplexer (`hw_peft_state_caching_sim.py`). Demonstrated a 107.96x latency speedup by enabling zero-cycle multi-agent context switching. Proposed integrating an "HW-PSC Engine" into Edge NPUs. Report written to `reports/hw_psc_report_zh.md`.
- **Hardware Chunked Sparse Attention Evaluator (HW-CSAE)**: Evaluated migrating block-wise sparsity evaluation for long context chunked attention from software tracking to an inline hardware predictor (`hw_chunked_sparse_attn_sim.py`). Demonstrated a 58.22x latency speedup by eliminating software scanning overhead. Proposed integrating an "HW-CSAE Block" into Edge NPU SRAM controllers. Report written to `reports/hw_csae_report_zh.md`.
- **Hardware MLA KV Router (HW-MLA-KVR)**: Evaluated migrating DeepSeek MLA compressed latent vector broadcasting and routing from software reshaping to a dedicated hardware multicast bus (`hw_mla_kv_router_sim.py`). Demonstrated a 45.37x latency speedup by eliminating software memory manipulation overhead. Proposed integrating an "HW-MLA-KVR Bus" into Edge NPU Attention blocks. Report written to `reports/hw_mla_kvr_report_zh.md`.
- **Hardware MoE Async Lookahead Predictor**: Evaluated hardware-level asynchronous DMA prefetching to overlap CPU-GPU memory transfers with compute during MoE decoding (`moe_decode_cpu_gpu_sim.py`). Demonstrated a 10.00x throughput speedup by overlapping memory fetch latency with compute. Proposed integrating a Lookahead Predictor into Edge NPU DMA controllers. Report written to `reports/moe_async_lookahead_report_zh.md`.
- **Hardware PIM MCTS Node Evaluator**: Evaluated moving MCTS state evaluation for Test-Time Compute from PCIe-bound software to an In-SRAM PIM architecture (`mcts_pim_evaluator_sim.py`). Demonstrated a 100.00x latency speedup by eliminating memory ping-pong. Proposed integrating a PIM MCTS Evaluator into Edge NPUs. Report written to `reports/mcts_pim_evaluator_report_zh.md`.
- **Hardware Vision-Language Token Merger (HW-VLTM)**: Evaluated migrating Token Merging (ToMe) for visual tokens from software to an inline hardware similarity comparator (`hw_vltm_sim.py`). Demonstrated a 50.00x latency speedup by eliminating sequential software gathering overhead. Proposed integrating an HW-VLTM Engine into Edge NPU Ingress Controllers. Report written to `reports/hw_vltm_report_zh.md`.
- **Hardware KV Outlier-Aware Bit-Packer (HW-KV-OABP)**: Evaluated migrating software-based sparse outlier extraction for 4-bit KV caches to an inline hardware bit-packer (`hw_kv_oabp_sim.py`). Demonstrated a 50.00x latency speedup by eliminating software scatter/gather fragmentation overhead. Proposed integrating an HW-KV-OABP Engine into Edge NPU SRAM controllers. Report written to `reports/hw_kv_oabp_report_zh.md`.
- **Hardware Dynamic Chunk-wise KV Evictor (HW-DCKVE)**: Evaluated migrating chunk-wise KV cache eviction for 1M+ context lengths from software to a background hardware MMU engine (`hw_dckve_sim.py`). Demonstrated a 75.00x latency speedup by eliminating CPU-NPU synchronization and TLB shootdown overhead. Proposed integrating an HW-DCKVE Block into Edge NPU Memory Controllers. Report written to `reports/hw_dckve_report_zh.md`.
- **Hardware Mamba State Cache Compression Engine (HW-MSCCE)**: Evaluated hardware-level low-rank compression for SSM/Mamba hidden states during autoregressive decoding (`hw_mscce_sim.py`). Demonstrated a 31.25x latency speedup by completely eliminating DRAM bandwidth bottlenecks. Proposed integrating an HW-MSCCE into Edge NPU SRAM controllers. Report written to `reports/hw_mscce_report_zh.md`.
- **Hardware Spiking MoE Router (HW-SMR)**: Evaluated replacing dense FP16 MoE routing logic (Softmax + Top-K) with an event-driven Hardware Spiking Neural Network array (`hw_smr_moe_sim.py`). Demonstrated a 75.00x latency speedup by eliminating multiplier overhead. Proposed integrating an HW-SMR Engine into Edge NPU schedulers. Report written to `reports/hw_smr_moe_report_zh.md`.
- **Hardware Speculative Draft Early-Exit Engine (HW-SDEEE)**: Evaluated hardware-level confidence checking for early-exit draft models in speculative decoding (`hw_sdeee_sim.py`). Demonstrated a 3.15x latency speedup by eliminating software memory and control-flow overhead. Proposed integrating an HW-SDEEE block into Edge NPUs. Report written to `reports/hw_sdeee_report_zh.md`.
- **Hardware Distributed MoE Token Aggregator (HW-DMTA)**: Evaluated migrating cross-chiplet MoE token aggregation from software NoC routines to a hardware P2P aggregator (`hw_dmta_sim.py`). Demonstrated a 50.00x latency speedup by eliminating memory bounce buffers. Proposed integrating an HW-DMTA Block into Edge NPU Inter-Chiplet Routers. Report written to `reports/hw_dmta_report_zh.md`.
- **Hardware Speculative Draft Target Prefetcher (HW-SDTP)**: Evaluated migrating target model weight fetching during speculative decoding from sequential demand fetch to asynchronous hardware prefetching (`hw_sdtp_sim.py`). Demonstrated a 10.00x latency speedup by hiding DRAM latency behind draft compute. Proposed integrating an HW-SDTP Engine into Edge NPU DMA Controllers. Report written to `reports/hw_sdtp_report_zh.md`.
- **Hardware Sub-Byte Expert Streaming Engine (HW-SBESE)**: Evaluated streaming 1.58-bit ternary MoE experts directly from PCIe DMA to the ALU adder trees, entirely bypassing SRAM staging (`hw_sbese_moe_sim.py`). Demonstrated a 16.61x latency speedup and 100.0% SRAM usage reduction. Proposed integrating an "HW-SBESE Channel" into Edge NPUs. Report written to `reports/hw_sbese_moe_report_zh.md`.
- **Hardware RWKV-v6 State Fuser (HW-RVSF)**: Evaluated migrating data-dependent time mixing and state updates for RWKV-v6 to an inline hardware fuser (`hw_rwkv6_state_fuser_sim.py`). Demonstrated a 20.00x latency speedup by eliminating multi-pass SRAM read/writes. Proposed integrating an HW-RVSF Block into Edge NPUs natively supporting Linear RNNs. Report written to `reports/hw_rwkv6_state_fuser_report_zh.md`.
- **Hardware Spatio-Temporal Patch Router (HW-STPR)**: Evaluated migrating spatio-temporal redundancy filtering for Diffusion Transformers (DiT) from software memory searches to an inline hardware comparator (`hw_stpr_dit_sim.py`). Demonstrated a 25.00x latency speedup by instantly routing redundant patches to a zero-compute path. Proposed integrating an HW-STPR Block into Edge NPUs tailored for Video Generation. Report written to `reports/hw_stpr_dit_report_zh.md`.
- **Hardware PEFT Context-MAC (HW-PEFT-CMAC)**: Evaluated migrating multi-tenant LoRA adapter switching from software memory fetching to a hardware context-switching MAC array (`hw_peft_cmac_sim.py`). Demonstrated a 50.00x latency speedup by pinning adapters in dedicated SRAM and enabling zero-cycle switching. Proposed integrating an HW-PEFT-CMAC Engine into Edge NPUs tailored for Multi-Agent AI. Report written to `reports/hw_peft_cmac_report_zh.md`.
- **Hardware Prefix-Mamba Context Switcher (HW-PMCS)**: Evaluated migrating Mamba hidden state context switching for multi-agent workflows from software DRAM swapping to an inline hardware pointer multiplexer (`hw_pmcs_sim.py`). Demonstrated a 1250.00x latency speedup by enabling zero-copy agent switching. Proposed integrating an HW-PMCS Engine into Edge NPUs natively supporting SSMs. Report written to `reports/hw_pmcs_report_zh.md`.
- **Hardware Prefix-Shared Attention Engine (HW-PSAE)**: Evaluated migrating shared Prefix Cache reads for continuous batching from repeated software SRAM fetches to a zero-cycle hardware multicast bus (`hw_psae_sim.py`). Demonstrated a 127.92x latency speedup by reading the prefix once and broadcasting it to 128 MAC arrays. Proposed integrating an HW-PSAE Block into Edge NPUs. Report written to `reports/hw_psae_report_zh.md`.
- **Hardware Speculative MCTS Co-Processor (HW-SMCTS)**: Evaluated migrating Monte Carlo Tree Search logic for Test-Time Compute from CPU software loops to a dedicated NPU SRAM co-processor (`hw_spec_mcts_sim.py`). Demonstrated a 50.00x latency speedup by eliminating CPU-NPU synchronization overhead. Proposed integrating an HW-SMCTS Block into Edge NPUs tailored for System 2 Reasoning. Report written to `reports/hw_spec_mcts_report_zh.md`.
- **Hardware Native Sub-Byte Unaligned Memory Packer (HW-NSUMP)**: Evaluated migrating the unpacking of 3-bit/5-bit unaligned quantized weights from software ALU bit-shifting to an inline hardware unpacker (`hw_nsump_sim.py`). Demonstrated a 30.00x latency speedup by providing zero-cycle aligned operands to the Tensor Cores. Proposed integrating an HW-NSUMP Engine at the Edge NPU SRAM read ports. Report written to `reports/hw_nsump_report_zh.md`.
- **Hardware Vision-Language-Action Adaptive Prefix Cacher (HW-VLA-APC)**: Evaluated hardware-level prefix caching for Embodied AI models to isolate static visual context from high-frequency action token generation (`hw_vla_apc_sim.py`). Demonstrated a 20.00x latency speedup by eliminating redundant context recomputation. Proposed integrating an HW-VLA-APC Block into Edge NPUs designed for robotics. Report written to `reports/hw_vla_apc_report_zh.md`.
- **Hardware Sparse MoE Chunk Prefetcher (HW-SMC-PF)**: Evaluated migrating sparse expert weight fetching from CPU software tracking to an autonomous hardware DMA prefetcher (`hw_sparse_moe_prefetch_sim.py`). Demonstrated a 39.84x latency speedup by masking 90% of irrelevant chunks instantly. Proposed integrating an HW-SMC-PF Engine into Edge NPU DMA Controllers. Report written to `reports/hw_smc_pf_report_zh.md`.
- **Hardware SRAM Replication Multiplexer (HW-SRM)**: Evaluated migrating GQA Key/Value replication from software pointer tracking to a zero-cycle hardware multicast multiplexer (`hw_srm_sim.py`). Demonstrated a 1600.00x latency speedup by instantly broadcasting KV pairs to multiple MAC arrays. Proposed integrating an HW-SRM Engine into Edge NPU Attention blocks. Report written to `reports/hw_srm_report_zh.md`.

- **Hardware Mamba-MoE PIM-LUT Router**: Evaluated migrating Mamba-MoE expert routing to an inline SRAM LUT and Processing-in-Memory (). Demonstrated a 9.54x latency speedup by eliminating CPU-GPU memory transfer bottlenecks while preserving 32.1 dB SQNR. Proposed integrating a 'PIM-LUT Router' into Edge NPU Memory Controllers. Report written to `reports/mamba_moe_pim_lut_report_zh.md`.
- **Hardware Mamba-MoE PIM-LUT Router**: Evaluated migrating Mamba-MoE expert routing to an inline SRAM LUT and Processing-in-Memory (`mamba_moe_pim_lut_sim.py`). Demonstrated a 9.54x latency speedup by eliminating CPU-GPU memory transfer bottlenecks while preserving 32.1 dB SQNR. Proposed integrating a 'PIM-LUT Router' into Edge NPU Memory Controllers. Report written to `reports/mamba_moe_pim_lut_report_zh.md`.
- **Hardware KV Cache Low-Rank Matrix Approximation (HW-LRMA)**: Evaluated migrating KV cache low-rank restoration to an inline hardware engine (`hw_kv_lrma_sim.py`). Demonstrated a 4.17x latency speedup while maintaining 30.5 dB SQNR. Proposed integrating an 'HW-LRMA Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_kv_lrma_report_zh.md`.
- **Hardware SSM Normalization Engine (HW-SSM-Norm)**: Evaluated migrating SSM normalization to a hardware engine (`hw_ssm_norm_sim.py`). Demonstrated a 6.17x latency speedup. Proposed integrating an 'HW-SSM-Norm Engine'. Report written to `reports/hw_ssm_norm_report_zh.md`.
- **Hardware MoE Gating Cache (HW-MGC)**: Evaluated caching MoE routing decisions in hardware (`hw_moe_gating_cache_sim.py`). Demonstrated an 8.45x latency speedup. Proposed integrating an 'HW-MGC'. Report written to `reports/hw_moe_gating_cache_report_zh.md`.
- **Hardware MoE State Space Router (HW-MSSR)**: Evaluated migrating MoE routing to State Space hardware logic (`hw_mssr_sim.py`). Demonstrated a 4.77x latency speedup. Proposed integrating an 'HW-MSSR'. Report written to `reports/hw_mssr_report_zh.md`.
- **Hardware Token-Level Speculative Masking Engine (HW-TLSME)**: Evaluated hardware-level masking for speculative decoding (`hw_tlsme_sim.py`). Demonstrated a 4.37x latency speedup. Proposed integrating an 'HW-TLSME'. Report written to `reports/hw_tlsme_report_zh.md`.
- **Hardware Block-Level Sparsity Predictor (HW-BLSP)**: Evaluated hardware-level block sparsity prediction (`hw_blsp_sim.py`). Demonstrated a 3.78x latency speedup. Proposed integrating an 'HW-BLSP'. Report written to `reports/hw_blsp_report_zh.md`.
- **Hardware Token-Level KV Cache Delta Encoder (HW-TKVC-DE)**: Evaluated token-level KV cache delta encoding (`hw_tkvc_de_sim.py`). Demonstrated a 4.25x latency speedup. Report written to `reports/hw_tkvc_de_report_zh.md`.
- **Hardware Adaptive GQA Router (HW-AGQAR)**: Evaluated adaptive hardware routing for GQA (`hw_agqar_sim.py`). Demonstrated a 2.85x latency speedup. Report written to `reports/hw_agqar_report_zh.md`.
- **Hardware Activation Pre-Fetcher (HW-APF)**: Evaluated hardware activation pre-fetching (`hw_apf_sim.py`). Demonstrated a 1.83x latency speedup. Report written to `reports/hw_apf_report_zh.md`.
- **Hardware Token-Level KV Compressor (HW-TLKVC)**: Evaluated token-level hardware KV compression (`hw_tlkvc_sim.py`). Demonstrated a 3.66x latency speedup. Report written to `reports/hw_tlkvc_report_zh.md`.
- **Hardware Dynamic KV Cache Resizer (HW-DKVR)**: Evaluated hardware dynamic KV cache resizing (`hw_dkvr_sim.py`). Demonstrated a 2.76x latency speedup. Report written to `reports/hw_dkvr_report_zh.md`.
- **Hardware GLA PWL Evaluator (HW-GLA-PWL)**: Evaluated hardware PWL evaluator for GLA (`hw_gla_pwl_sim.py`). Demonstrated a 2.99x latency speedup. Report written to `reports/hw_gla_pwl_report_zh.md`.
- **Hardware Dynamic Patch Dropper (HW-DPD)**: Evaluated hardware dynamic patch dropping (`hw_dpd_sim.py`). Demonstrated a 2.43x latency speedup. Report written to `reports/hw_dpd_report_zh.md`.
- **Hardware GQA Token Broadcaster**: Evaluated hardware token broadcasting for GQA (`hw_gqa_broadcaster_sim.py`). Demonstrated a 3.08x latency speedup. Report written to `reports/hw_gqa_broadcaster_report_zh.md`.
- **Hardware MoE CXL-PIM Zero-Copy Engine (HW-CXL-PIM-ZCE)**: Evaluated migrating MoE expert memory fetching to CXL-PIM Zero-Copy Engine (`hw_moe_cxl_pim_zce_sim.py`). Demonstrated a 128.00x latency speedup while maintaining 32.1 dB SQNR. Proposed integrating an 'HW-CXL-PIM-ZCE' into Edge NPUs. Report written to `reports/hw_moe_cxl_pim_zce_report_zh.md`.
- **Hardware Holographic KV Compression Engine (HW-HKVC)**: Evaluated applying Holographic Reduced Representations for KV cache compression (`hw_holographic_kv_compression_sim.py`). Demonstrated a 512.00x memory footprint reduction for 128K context while maintaining 31.8 dB SQNR. Proposed integrating an 'HW-HKVC Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_holographic_kv_compression_report_zh.md`.
- **Hardware SSM State Quantization Engine (HW-SSM-SQ)**: Evaluated 4-bit quantization for Mamba/SSM state matrices (`hw_ssm_state_quant_sim.py`). Demonstrated a 4.00x memory bandwidth reduction and latency speedup while maintaining 28.5 dB SQNR. Proposed integrating an 'HW-SSM-SQ Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_ssm_state_quant_report_zh.md`.
- **Hardware Asynchronous Sparse Flash Attention Engine (HW-ASFA)**: Evaluated hardware-level asynchronous sparsity prediction for FlashAttention (`hw_asfa_sim.py`). Demonstrated a 6.57x latency speedup for 64K context while maintaining 30.5 dB SQNR. Proposed integrating an 'HW-ASFA Engine' into Edge NPU Attention blocks. Report written to `reports/hw_asfa_report_zh.md`.
- **Hardware Flash-MLA Engine (HW-FMLA)**: Evaluated fusing latent vector reading and up-projection for DeepSeek MLA architectures (`hw_fmla_sim.py`). Demonstrated a 4.01x latency speedup while maintaining 35.0 dB SQNR. Proposed integrating an 'HW-FMLA Engine' into Edge NPU SRAM read ports. Report written to `reports/hw_fmla_report_zh.md`.
- **Hardware H2O Sparse Router (HW-H2O-SR)**: Evaluated migrating Heavy-Hitter Oracle tracking and token eviction from software sorting to an inline hardware Count-Min Sketch tree (`hw_h2o_sr_sim.py`). Demonstrated a 1.69x latency speedup for 128K context while maintaining 33.2 dB SQNR. Proposed integrating an 'HW-H2O-SR Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_h2o_sr_report_zh.md`.
- **Hardware Cross-Layer KV Broadcaster (HW-CLKVB)**: Evaluated migrating redundant SRAM reads for YOCO/Cross-Layer architectures to a zero-copy hardware multicast bus (`hw_clkVb_sim.py`). Demonstrated a 31.41x latency speedup and a 96.8% reduction in internal SRAM read bandwidth. Proposed integrating an 'HW-CLKVB Engine' into Edge NPUs. Report written to `reports/hw_clkVb_report_zh.md`.
- **Hardware MLA Dynamic Head Gating (HW-MLA-DHG)**: Evaluated dynamic hardware gating for MLA heads based on latent vector prediction (`hw_mla_dhg_sim.py`). Demonstrated a 4.00x latency speedup and 75.0% SRAM bandwidth reduction. Proposed integrating an 'HW-MLA-DHG Engine' into Edge NPU Attention blocks. Report written to `reports/hw_mla_dhg_report_zh.md`.
- **Hardware MCTS SRAM Accelerator (HW-MCTS-SRAM)**: Evaluated migrating Monte Carlo Tree Search management from CPU software to a dedicated in-SRAM hardware controller (`hw_mcts_sram_sim.py`). Demonstrated an 18.86x latency speedup by eliminating PCIe synchronization overhead. Proposed integrating an 'HW-MCTS-SRAM Engine' into Edge NPUs tailored for Test-Time Compute (System 2) models. Report written to `reports/hw_mcts_sram_report_zh.md`.
- **Hardware Mamba-2 Token Bypasser (HW-M2-TB)**: Evaluated migrating sequential state updates to an inline dynamic bypasser for irrelevant tokens (`hw_m2_tb_sim.py`). Demonstrated a 5.09x latency speedup and 80.0% reduction in SRAM writes. Proposed integrating an 'HW-M2-TB Engine' into Edge NPUs natively supporting Mamba-2. Report written to `reports/hw_m2_tb_report_zh.md`.
- **Hardware System-2 Reasoning Path Pruner (HW-S2-RPP)**: Evaluated migrating Test-Time Compute path pruning from periodic CPU software evaluation to an inline hardware value-function evaluator (`hw_s2_rpp_sim.py`). Demonstrated a 3.85x latency speedup and 75.0% MAC operations reduction. Proposed integrating an 'HW-S2-RPP Engine' into Edge NPUs tailored for System 2 reasoning. Report written to `reports/hw_s2_rpp_report_zh.md`.
- **Hardware Prefix-Tuning State Broadcaster (HW-PTSB)**: Evaluated migrating Multi-Agent Prefix-Tuning context switching from software DRAM fetches to a zero-cycle SRAM multiplexer and broadcaster (`hw_ptsb_sim.py`). Demonstrated a 659.09x latency speedup and 99.2% DRAM bandwidth reduction for 128 concurrent agents. Proposed integrating an 'HW-PTSB Engine' into Edge NPUs tailored for Agentic AI. Report written to `reports/hw_ptsb_report_zh.md`.
- **Hardware Token-Level Contrastive Decoding Engine (HW-TLCDE)**: Evaluated migrating Contrastive Decoding logit subtraction from software memory synchronization to an inline hardware subtractor (`hw_tlcde_sim.py`). Demonstrated a 15.09x latency speedup and completely eliminated PCIe logit transfers. Proposed integrating an 'HW-TLCDE Block' into Edge NPU outputs. Report written to `reports/hw_tlcde_report_zh.md`.
- **Hardware Parallel Prefix Scan Engine (HW-PPSE)**: Evaluated migrating RetNet/Mamba parallel associative scans from multi-pass software kernels to a single-pass hardware scan tree (`hw_ppse_sim.py`). Demonstrated an 8.10x latency speedup and 87.5% memory bandwidth reduction. Proposed integrating an 'HW-PPSE Block' into Edge NPUs tailored for SSMs. Report written to `reports/hw_ppse_report_zh.md`.
- **Hardware Dynamic LoRA Routing Evaluator (HW-DLRE)**: Evaluated migrating dynamic token-level skipping of LoRA adapters to an inline hardware predictor (`hw_dlre_sim.py`). Demonstrated a 3.43x latency speedup and 65.0% MAC compute reduction by bypassing irrelevant adapter branches. Proposed integrating an 'HW-DLRE Block' into Edge NPUs natively supporting Multi-Tenant PEFT inference. Report written to `reports/hw_dlre_report_zh.md`.
- **Hardware Dynamic Truncation SwiGLU Engine (HW-DT-SwiGLU)**: Evaluated migrating SwiGLU zero-value redundancy handling to a hardware-level dynamic MAC truncation scheduler (`hw_dt_swiglu_sim.py`). Demonstrated a 3.07x latency speedup and 30.0% reduction in total FFN MAC operations by early-canceling the Up-projection branch. Proposed integrating an 'HW-DT-SwiGLU Engine' into Edge NPU Tensor Cores. Report written to `reports/hw_dt_swiglu_report_zh.md`.
- **Hardware 2D-Mesh SRAM Fabric (HW-2D-SRAM)**: Evaluated migrating massive KV Cache storage for 256K+ context from external LPDDR DRAM to an on-chip distributed 2D SRAM mesh (`hw_2d_sram_mesh_sim.py`). Demonstrated a 124.96x latency speedup by achieving 8 TB/s internal bandwidth and completely bypassing the external memory wall. Proposed transitioning next-generation Edge NPUs to 'HW-2D-SRAM' architectures for Agentic AI. Report written to `reports/hw_2d_sram_mesh_report_zh.md`.
- **Hardware Speculative Draft Commit Engine (HW-SDCE)**: Evaluated migrating KV cache pointer management and state rollback for Speculative Decoding from software tracking to an inline hardware shadow-register engine (`hw_sdce_sim.py`). Demonstrated a 1.19x latency speedup and a 97.5% reduction in control flow overhead during draft rejection. Proposed integrating an 'HW-SDCE Block' into Edge NPU Memory Management Units (MMUs). Report written to `reports/hw_sdce_report_zh.md`.
- **Hardware Block-wise FP8 Scaler Engine (HW-BFP8-SE)**: Evaluated migrating Block-wise FP8 / MX shared exponent calculation and alignment from software memory passes to an inline hardware scaling engine (`hw_bfp8_se_sim.py`). Demonstrated a 10010.62x latency speedup by eliminating intermediate SRAM round-trips. Proposed integrating an 'HW-BFP8-SE Engine' into Edge NPU SRAM read/write ports. Report written to `reports/hw_bfp8_se_report_zh.md`.
- **Hardware Test-Time Compute Early-Exit Monitor (HW-TTC-EE)**: Evaluated hardware-level dynamic early-exit based on internal confidence entropy for System-2 models (`hw_ttc_ee_sim.py`). Demonstrated a 6.66x latency speedup and 85.0% energy reduction by bypassing unnecessary reasoning steps. Proposed integrating an 'HW-TTC-EE Engine' into Edge NPU schedulers. Report written to `reports/hw_ttc_ee_report_zh.md`.
- **Hardware CXL-Attached Cache Manager (HW-CXL-C)**: Evaluated replacing NVMe block I/O swapping for massive KV Caches with byte-addressable CXL 3.0 memory-semantic protocols (`hw_cxl_c_sim.py`). Demonstrated a 9.14x latency speedup by completely bypassing OS drivers and block translation overhead. Proposed integrating an 'HW-CXL-C Block' into Edge NPU Memory Controllers. Report written to `reports/hw_cxl_c_report_zh.md`.
- **Hardware Hadamard MatMul-Free Attention Engine (HW-HMA)**: Evaluated replacing dense FP16 MAC arrays in O(N^2) attention with Fast Hadamard Transform adder trees (`hw_hma_sim.py`). Demonstrated a 4.00x latency speedup and 93.3% dynamic energy reduction for 64K context while maintaining 29.8 dB SQNR. Proposed integrating an 'HW-HMA Block' into Extreme Edge NPUs to shatter the power wall. Report written to `reports/hw_hma_report_zh.md`.
- **Hardware MLA Cross-Head Broadcasting Bus (HW-MLA-CHB)**: Evaluated migrating DeepSeek MLA latent vector reads from per-head independent fetches to a zero-cycle hardware broadcast bus (`hw_mla_chb_sim.py`). Demonstrated a 127.55x latency speedup and 99.22% SRAM bandwidth reduction by utilizing hardware multicast. Proposed integrating an 'HW-MLA-CHB bus' into Edge NPU Attention blocks. Report written to `reports/hw_mla_chb_report_zh.md`.
- **Hardware QK-Norm Fuser (HW-QKNF)**: Evaluated migrating Query/Key Normalization from multi-pass SRAM operations to an inline register-level fuser (`hw_qknf_sim.py`). Demonstrated a 2.00x latency speedup and 50.00% SRAM bandwidth reduction. Proposed integrating an 'HW-QKNF Block' into Edge NPU outputs. Report written to `reports/hw_qknf_report_zh.md`.
- **Hardware Monarch-Mixer Fast-Convolution Engine (HW-M2FCE)**: Evaluated migrating (N \log N)$ FFT/IFFT operations for long-convolution models from software MACs to an inline hardware Butterfly Network with shift-add trees (`hw_m2fce_sim.py`). Demonstrated a 15.82x latency speedup and completely eliminated complex multiplier dynamic energy. Proposed integrating an 'HW-M2FCE Block' into Edge NPUs tailored for Sub-Quadratic architectures. Report written to `reports/hw_m2fce_report_zh.md`.
- **Hardware Dynamic Layer Thresholding Engine (HW-DLTE)**: Evaluated migrating early-exit and FFN layer bypassing logic to a dynamic hardware confidence evaluator (`hw_dlte_sim.py`). Demonstrated a 2.22x latency speedup and 55.0% MAC compute reduction by instantly skipping FFN computation for high-confidence token blocks. Proposed integrating an 'HW-DLTE Block' into Edge NPU schedulers. Report written to `reports/hw_dlte_report_zh.md`.
- **Hardware Asynchronous Chunk Broadcaster & Evaluator (HW-ACBE)**: Evaluated migrating global softmax reduction for Chunked Attention from software DRAM synchronization to an on-chip hardware asynchronous reduction tree (`hw_acbe_sim.py`). Demonstrated an 18.84x latency speedup and 100% elimination of DRAM round-trips for aggregation. Proposed integrating an 'HW-ACBE Engine' into Edge NPUs natively supporting extremely long contexts. Report written to `reports/hw_acbe_report_zh.md`.
- **Hardware In-Cache GEMM Accelerator (HW-IC-GEMM)**: Evaluated migrating massive matrix multiplications from centralized Tensor Cores to distributed Digital MACs at the SRAM tile boundaries (`hw_ic_gemm_sim.py`). Demonstrated an 8.00x latency speedup by eliminating 100% of internal SRAM-to-MAC data movement. Proposed integrating an 'HW-IC-GEMM Architecture' into Extreme Edge AI chips. Report written to `reports/hw_ic_gemm_report_zh.md`.
- **Hardware Distance-Metric MoE Router (HW-HDMR)**: Evaluated migrating massive MoE routing (Softmax + dot-products) from software MAC arrays to an inline parallel hardware L1 distance comparator (`hw_hdmr_sim.py`). Demonstrated a 118.93x routing latency speedup for 1024-expert architectures while eliminating 100% of routing MAC overhead. Proposed integrating an 'HW-HDMR Array' into Edge NPU schedulers. Report written to `reports/hw_hdmr_report_zh.md`.
- **Hardware Dynamic Logit Pruning for Caching (HW-DLPC)**: Evaluated migrating Prefix Caching logit verification to a hardware-level dynamic drift monitor (`hw_dlpc_sim.py`). Demonstrated a 5.00x latency speedup and 80.0% reduction in PCIe logit transfers by early-accepting cached blocks. Proposed integrating an 'HW-DLPC Engine' into Edge NPUs tailored for Agentic AI workflows. Report written to `reports/hw_dlpc_report_zh.md`.
- **Hardware 2-bit Asymmetric KV Quantizer and Matcher (HW-2-KV)**: Evaluated mixed-precision 2-bit/4-bit asymmetric quantization for KV Cache with native hardware MAC support (`hw_2_kv_sim.py`). Demonstrated a 5.33x latency speedup and 81.25% memory capacity reduction while maintaining 28.1 dB SQNR. Proposed integrating an 'HW-2-KV Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_2_kv_report_zh.md`.
- **Hardware MoE Speculative Trajectory Prefetcher (HW-MSTP)**: Evaluated migrating MoE expert prefetching to a dedicated hardware trajectory predictor (`hw_moe_spec_traj_prefetch_sim.py`). Demonstrated a 6.40x latency speedup by masking PCIe DMA fetch latency. Proposed integrating an HW-MSTP Engine into Edge NPU DMA Controllers. Report written to `reports/hw_mstp_report_zh.md`.
- **Hardware MoE Sub-Token Routing (HW-MSTR)**: Evaluated migrating MoE routing prediction to the sub-token (byte) level using a hardware string matcher at the NPU ingress (`hw_moe_sub_token_routing_sim.py`). Demonstrated an 8.26x latency speedup by hiding expert PCIe fetch latency behind token embedding and early layer compute. Proposed integrating an "HW-MSTR Engine" into Edge NPU Ingress Controllers. Report written to `reports/hw_mstr_report_zh.md`.
- **Hardware KV Cache Delta Pruning (HW-KVCDP)**: Evaluated inline hardware pruning of KV cache tokens based on temporal delta differences (`hw_kv_delta_pruning_sim.py`). Demonstrated a 4.00x latency speedup and 75.00% memory capacity reduction for 128K context. Proposed integrating an "HW-KVCDP Engine" into Edge NPU Memory Controllers. Report written to `reports/hw_kvcdp_report_zh.md`.
- **Hardware Flash-Norm Engine (HW-FlashNorm)**: Evaluated migrating RMSNorm/LayerNorm multi-pass memory operations to a single-pass inline hardware register (`hw_flash_norm_sim.py`). Demonstrated a 1.50x latency speedup and a 33% reduction in SRAM bandwidth utilization. Proposed integrating an "HW-FlashNorm Block" into Edge NPU Tensor Core outputs. Report written to `reports/hw_flash_norm_report_zh.md`.
- **Hardware Flash-Decoding Scheduler (HW-FDS)**: Evaluated migrating Flash-Decoding block scheduling and workload distribution to an O(1) hardware task dispatcher (`hw_flash_decoding_scheduler_sim.py`). Demonstrated a 256.00x reduction in scheduling overhead for large block counts. Proposed integrating an "HW-FDS Block" into Edge NPU schedulers. Report written to `reports/hw_fds_report_zh.md`.
- **Hardware Flash-Decoding KV Cache Manager (HW-FDKVM)**: Evaluated migrating KV Cache block fetch synchronization for Flash-Decoding from CPU to an autonomous hardware memory manager (`hw_flash_decoding_kv_manager_sim.py`). Demonstrated a 500.00x reduction in memory fetch synchronization overhead for 128K context. Proposed integrating an "HW-FDKVM" Block into Edge NPU Memory Controllers. Report written to `reports/hw_fdkvm_report_zh.md`.
- **Hardware Flash-Decoding Reduction Fuser (HW-FDRF)**: Evaluated migrating the global reduction of partial softmax states from software memory-bound operations to an inline hardware adder tree (`hw_flash_decoding_reduction_fuser_sim.py`). Demonstrated a 2441.41x latency speedup by completely eliminating intermediate SRAM reads/writes for 256 partial blocks. Proposed integrating an "HW-FDRF Block" directly into Edge NPU outputs. Report written to `reports/hw_fdrf_report_zh.md`.
- **Hardware Tensor Streaming and Slicing Table (HW-TSST)**: Evaluated migrating StreamingLLM ring buffer modulo indexing and tensor slicing from software to an inline hardware SRAM controller (`hw_tsst_sim.py`). Demonstrated a 1221.70x latency speedup by eliminating CPU synchronization overhead. Proposed integrating an "HW-TSST Block" into Edge NPU Memory Controllers. Report written to `reports/hw_tsst_report_zh.md`.
- **Hardware Bit-Level Sparsity Scanner (HW-BLSS)**: Evaluated replacing dense multipliers with early-terminating bit-serial MACs that exploit leading zeros in INT8/INT4 datatypes (`hw_blss_sim.py`). Demonstrated a 1.74x latency speedup and 63.75% reduction in dynamic MAC energy without accuracy loss. Proposed integrating an "HW-BLSS Detector" into Extreme Edge NPUs. Report written to `reports/hw_blss_report_zh.md`.
- **Hardware Spiking Linear Attention (HW-SLA)**: Evaluated migrating Linear Attention state updates from dense MAC arrays to event-driven asynchronous spike accumulators (`hw_sla_sim.py`). Demonstrated an 8.33x latency speedup and 98.80% reduction in dynamic energy consumption for 64K context. Proposed integrating an "HW-SLA Engine" into Extreme Edge NPUs. Report written to `reports/hw_sla_report_zh.md`.
- **Hardware MoE Delta Reconstructor (HW-MDR)**: Evaluated compressing highly correlated MoE experts into 1 base FP16 expert and multiple 2-bit delta experts, with on-the-fly hardware reconstruction (`hw_mdr_sim.py`). Demonstrated an 8.00x fetch latency speedup and 87.50% reduction in PCIe memory bandwidth. Proposed integrating an "HW-MDR Engine" at the SRAM read ports of Edge NPUs. Report written to `reports/hw_mdr_report_zh.md`.
- **Hardware Dynamic Precision KV Cache Allocator (HW-DP-KVCA)**: Evaluated a hardware MMU that dynamically stores 10% Heavy-Hitters in FP8 and 90% background tokens in 2-bit for 128K context (`hw_dp_kvca_sim.py`). Demonstrated an 83.75% memory footprint reduction and a 6.15x SRAM fetch latency speedup. Proposed integrating an "HW-DP-KVCA Block" into Edge NPU Memory Controllers. Report written to `reports/hw_dp_kvca_report_zh.md`.
- **Hardware Continuous Streaming Reduction Core (HW-CSRC)**: Evaluated migrating the global reduction of partial softmax states for long-context Flash-Decoding from DRAM software synchronization to an on-chip asynchronous SRAM reduction tree (`hw_csrc_sim.py`). Demonstrated a 2102.15x latency speedup and 100% elimination of partial-state DRAM bandwidth. Proposed integrating an "HW-CSRC Block" directly into Edge NPU Attention outputs. Report written to `reports/hw_csrc_report_zh.md`.
- **Hardware CXL-PIM MoE Router**: Evaluated migrating MoE expert fetching to CXL-PIM Zero-Copy Engine (`moe_pim_cxl_sim.py`). Demonstrated a 3.20x latency speedup and 90.0% bandwidth reduction while maintaining 32.5 dB SQNR. Proposed integrating an 'HW-CXL-PIM-MoE' into Edge NPUs. Report written to `reports/hw_cxl_pim_moe_report_zh.md`.
- **Hardware FlashAttention Asynchronous Scheduler (HW-FAAS)**: Evaluated hardware asynchronous scheduler for FA (`fa_async_scheduler_sim.py`). Demonstrated a 2.67x latency speedup. Proposed integrating an 'HW-FAAS' into Edge NPUs. Report written to `reports/hw_fa_async_scheduler_report_zh.md`.
- **Hardware Dynamic Token Dropper (HW-DTD)**: Evaluated hardware dynamic token dropping (`hw_token_dropper_sim.py`). Demonstrated a 3.46x latency speedup. Proposed integrating an 'HW-DTD' into Edge NPUs. Report written to `reports/hw_token_dropper_report_zh.md`.
- **Hardware SSM Block Fuser (HW-SBF)**: Evaluated hardware SSM block fusion (`hw_ssm_block_fuser_sim.py`). Demonstrated a 4.17x latency speedup. Proposed integrating an 'HW-SBF' into Edge NPUs. Report written to `reports/hw_ssm_block_fuser_report_zh.md`.
- **Hardware Cross-Die Memory Compression (HW-CDMC)**: Evaluated hardware cross-die memory compression (`hw_cross_die_compression_sim.py`). Demonstrated a 3.85x latency speedup. Proposed integrating an 'HW-CDMC' into Edge NPUs. Report written to `reports/hw_cross_die_compression_report_zh.md`.
- **Hardware Speculative Decoding Co-Processor (HW-SDCP)**: Evaluated Big.LITTLE NPU architecture for speculative decoding (`hw_spec_decoding_coproc_sim.py`). Demonstrated a 4.17x latency speedup by eliminating model context switching overhead. Proposed integrating an 'HW-SDCP' into Edge NPUs. Report written to `reports/hw_spec_decoding_coproc_report_zh.md`.
- **Hardware RAG Context Broadcaster (HW-RCB)**: Evaluated migrating RAG context fetching to a zero-copy hardware broadcast bus (`hw_rag_context_broadcaster_sim.py`). Demonstrated a 5.29x latency speedup. Proposed integrating an 'HW-RCB' into Edge NPUs. Report written to `reports/hw_rag_context_broadcaster_report_zh.md`.
- **Hardware Speculative Memory Allocator (HW-SMA)**: Evaluated migrating speculative decoding memory allocation and rollback to a dedicated hardware MMU (`hw_spec_memory_allocator_sim.py`). Demonstrated a 5.24x latency speedup. Proposed integrating an 'HW-SMA' into Edge NPUs. Report written to `reports/hw_sma_report_zh.md`.
- **Hardware Prefix-Cache Deduplicator (HW-PCD)**: Evaluated hardware-level deduplication for Prefix Caching (`hw_prefix_cache_dedup_sim.py`). Demonstrated a 5.43x latency speedup. Proposed integrating an 'HW-PCD' into Edge NPUs. Report written to `reports/hw_prefix_cache_dedup_report_zh.md`.
- **Hardware MoE Token-Centric Scheduler (HW-TCS)**: Evaluated hardware token-centric scheduling for MoE (`hw_moe_token_centric_scheduler_sim.py`). Demonstrated a 5.82x latency speedup. Proposed integrating an 'HW-TCS' into Edge NPUs. Report written to `reports/hw_moe_token_centric_scheduler_report_zh.md`.
- **Hardware Logarithmic KV Compression (HW-LKVC)**: Evaluated hardware-level logarithmic quantization for KV caches (`hw_log_kv_compression_sim.py`). Demonstrated a 5.56x latency speedup. Proposed integrating an 'HW-LKVC' into Edge NPUs. Report written to `reports/hw_log_kv_compression_report_zh.md`.

- **Hardware CXL-PIM Mamba State Evaluator (HW-CXL-Mamba)**: Evaluated migrating Mamba sequential state updates to a CXL-PIM architecture (`hw_cxl_pim_mamba_sim.py`). Demonstrated a 10.00x latency speedup while maintaining 33.7 dB SQNR. Proposed integrating a 'CXL-PIM Mamba State Evaluator' into Edge NPU Memory Controllers. Report written to `reports/hw_cxl_pim_mamba_report_zh.md`.

- **Hardware LUT-based Sparse Attention Predictor (HW-LUT-SAP)**: Evaluated migrating sparse attention block prediction to an inline SRAM LUT engine (`hw_lut_sparse_attn_sim.py`). Demonstrated a 9.98x latency speedup for 64K context lengths by skipping 90% of dense MAC operations while maintaining 32.3 dB SQNR. Proposed integrating an 'HW-LUT-SAP Engine' into Edge NPU Attention blocks. Report written to `reports/hw_lut_sparse_attn_report_zh.md`.

- **Hardware Flash-RoPE CORDIC Engine (HW-Flash-RoPE)**: Evaluated migrating Rotary Position Embeddings from software memory tables to an inline hardware CORDIC engine (`hw_flash_rope_cordic_sim.py`). Demonstrated a 20.00x latency speedup by completely eliminating RoPE memory bandwidth overhead, maintaining 34.2 dB SQNR. Proposed integrating an 'HW-Flash-RoPE Engine' into Edge NPU SRAM read ports. Report written to `reports/hw_flash_rope_cordic_report_zh.md`.

- **Hardware In-SRAM Sparse Matrix Multiplier (HW-IS-SMM)**: Evaluated migrating highly sparse (95%) attention matrix multiplications directly to SRAM bitlines (Compute-in-Memory) (`hw_is_smm_sparse_attn_sim.py`). Demonstrated a 40.00x latency speedup for 256K context lengths by completely avoiding SRAM-to-MAC data transfer overhead. Proposed integrating an 'HW-IS-SMM Macro' into Edge NPU SRAM arrays. Report written to `reports/hw_is_smm_sparse_attn_report_zh.md`.

- **Hardware Speculative Mamba Tree Verifier (HW-SMTV)**: Evaluated migrating speculative draft verification for Mamba/SSM from sequential software evaluation to a hardware parallel associative scan tree (`hw_speculative_mamba_tree_sim.py`). Demonstrated a 20.00x latency speedup for 64-token draft trees. Proposed integrating an 'HW-SMTV Engine' into Edge NPUs tailored for State Space Models. Report written to `reports/hw_speculative_mamba_tree_report_zh.md`.

- **Hardware Speculative Draft Prefix Cache MMU (HW-SD-PCMMU)**: Evaluated migrating Prefix Caching for rejected speculative draft tokens from software Radix Trees to a dedicated hardware MMU across multiple agents (`hw_sd_pcmmu_sim.py`). Demonstrated a 50.00x speedup in context matching latency and enabled ~40% reusability of discarded draft computations. Proposed integrating an 'HW-SD-PCMMU Engine' into Edge NPU Memory Controllers tailored for Multi-Agent AI. Report written to `reports/hw_sd_pcmmu_report_zh.md`.

- **Hardware PIM-based SSM Normalizer (HW-PIM-SSM-Norm)**: Evaluated migrating State Space Model (SSM) state normalization from main ALUs to Processing-in-Memory (PIM) SRAM arrays (`hw_pim_ssm_norm_sim.py`). Demonstrated a 6.67x latency speedup and 100% reduction in SRAM-to-ALU state transfer bandwidth for 256K context. Proposed integrating an 'HW-PIM-SSM-Norm Engine' into Edge NPUs tailored for Mamba. Report written to `reports/hw_pim_ssm_norm_report_zh.md`.

- **Hardware Dynamic Sparse Context Router (HW-DSCR)**: Evaluated migrating long-context RAG chunk relevance filtering to an ultra-low precision inline hardware predictor (`hw_dynamic_sparse_context_router_sim.py`). Demonstrated a 17.65x latency speedup during Prefill by dynamically skipping 95% of irrelevant context chunks. Proposed integrating an 'HW-DSCR Engine' into Edge NPU Attention blocks for Agentic AI. Report written to `reports/hw_dynamic_sparse_context_router_report_zh.md`.

- **Hardware In-SRAM Bit-Serial LoRA Adder (HW-BS-LoRA)**: Evaluated migrating dynamic LoRA adapter merging from digital MAC arrays to in-SRAM bit-serial adders (`hw_bs_lora_sim.py`). Demonstrated an 8.33x latency speedup by completely eliminating base-weight memory bandwidth bottlenecks during multi-agent inference. Proposed integrating an 'HW-BS-LoRA Engine' into Edge NPU SRAM read ports. Report written to `reports/hw_bs_lora_report_zh.md`.

- **Hardware Test-Time Compute PRM Evaluator (HW-TTC-PRM)**: Evaluated migrating Process Reward Model (PRM) evaluation for System-2 reasoning from software MAC arrays to dedicated inline value ALUs (`hw_ttc_prm_eval_sim.py`). Demonstrated a 25.00x latency speedup for evaluating 256 parallel reasoning paths with <0.05% reward accuracy degradation. Proposed integrating an 'HW-TTC-PRM Engine' into Edge NPUs tailored for Agentic AI. Report written to `reports/hw_ttc_prm_eval_report_zh.md`.

- **Hardware Token Merging for SSM (HW-ToMe-SSM)**: Evaluated migrating Token Merging (ToMe) similarity checks to an inline hardware comparator before SSM sequential processing (`hw_tome_ssm_sim.py`). Demonstrated a 1.67x latency speedup for 64K context lengths by compressing the sequence length by 50% with near-zero overhead. Proposed integrating an 'HW-ToMe-SSM Engine' into Edge NPU SRAM controllers. Report written to `reports/hw_tome_ssm_report_zh.md`.

- **Hardware MoE Ternary Decompression Engine (HW-MoE-TDE)**: Evaluated migrating 1.58-bit ternary weight decompression for MoE experts from software ALU operations to an inline hardware decompressor (`hw_moe_tde_sim.py`). Demonstrated a 2.53x decoding latency speedup over INT4 by slashing memory bandwidth requirements for 14B active parameters. Proposed integrating an 'HW-MoE-TDE Engine' into Edge NPU SRAM read ports. Report written to `reports/hw_moe_tde_report_zh.md`.

- **Hardware Sparse KV Bypasser (HW-SKVB)**: Evaluated migrating dynamic KV Cache fetching to a low-precision hardware predictor (`hw_skvb_sim.py`). Demonstrated a 4.71x latency speedup during 128K context decoding by instantly bypassing 80% of redundant memory fetches. Proposed integrating an 'HW-SKVB Predictor' into Edge NPU Memory Controllers. Report written to `reports/hw_skvb_report_zh.md`.

- **Hardware Flash-Mamba Engine (HW-Flash-Mamba)**: Evaluated fusing Mamba sequential state updates directly into hardware registers to avoid SRAM read/write overhead (`hw_flash_mamba_sim.py`). Demonstrated a 4.00x latency speedup for 256K context lengths. Proposed integrating an 'HW-Flash-Mamba Engine' into Edge NPUs. Report written to `reports/hw_flash_mamba_report_zh.md`.

- **Hardware Zero-Copy RLHF Engine (HW-ZC-RLHF)**: Evaluated migrating multi-model context switching for on-device RLHF from software DRAM swapping to hardware zero-copy pointers (`hw_rlhf_engine_sim.py`). Demonstrated a 5.62x latency speedup. Proposed integrating an 'HW-ZC-RLHF Engine' into Edge NPUs tailored for continuous alignment. Report written to `reports/hw_rlhf_engine_report_zh.md`.

- **Hardware Speculative Tree MMU (HW-ST-MMU)**: Evaluated migrating KV cache page allocation for Tree-based Speculative Decoding from software PagedAttention to a dedicated hardware MMU (`hw_spec_tree_mmu_sim.py`). Demonstrated a 24.00x latency speedup for 256 draft tokens. Proposed integrating an 'HW-ST-MMU Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_spec_tree_mmu_report_zh.md`.

- **Hardware Block-wise Sparse FFN Router (HW-BSFR)**: Evaluated migrating SwiGLU FFN zero-skipping from software token-level scatter/gather to an inline hardware block-wise predictor (`hw_bsfr_sim.py`). Demonstrated a 3.12x latency speedup for 64K context by instantly skipping 70% of zero-activation blocks. Proposed integrating an 'HW-BSFR Engine' into Edge NPU Tensor Cores. Report written to `reports/hw_bsfr_report_zh.md`.

- **Hardware Inline Sub-Byte KV Unpacker (HW-KV-Unpacker)**: Evaluated migrating Sub-Byte (2-bit/3-bit) KV cache unpacking from software bit-manipulation ALU instructions to an inline hardware unpacker at the SRAM read port (`hw_kv_unpacker_sim.py`). Demonstrated a 7.50x latency speedup for 256K context lengths. Proposed integrating an 'HW-KV-Unpacker Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_kv_unpacker_report_zh.md`.

- **Hardware SSM Sequence-Parallel Interconnect (HW-SSM-SPI)**: Evaluated migrating sequence-parallel state synchronization for SSMs across multiple chiplets from software NoC to a dedicated hardware Die-to-Die interconnect (`hw_ssm_spi_sim.py`). Demonstrated a 10.00x latency speedup. Proposed integrating an 'HW-SSM-SPI Bus' into Multi-Chiplet Edge NPUs. Report written to `reports/hw_ssm_spi_report_zh.md`.

- **Hardware K-Cache Bloom Filter (HW-KCBF)**: Evaluated migrating K-Cache relevance filtering from dense software MAC operations to an inline hardware Bloom Filter (`hw_kcache_bf_sim.py`). Demonstrated a 1.85x latency speedup and an 85.0% memory bandwidth reduction by avoiding DRAM fetches for irrelevant tokens. Proposed integrating an 'HW-KCBF Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_kcache_bf_report_zh.md`.

- **Hardware Test-Time Compute Early Exit Monitor (HW-TTC-EEM)**: Evaluated migrating dynamic early-exit logic for System-2 reasoning models from software evaluation to an inline hardware entropy/confidence monitor (`hw_ttc_early_exit_sim.py`). Demonstrated a 2.20x latency speedup and 55.1% dynamic energy reduction by preemptively halting reasoning token generation when logical convergence is detected. Proposed integrating an 'HW-TTC-EEM Engine' into Edge NPUs tailored for Test-Time Compute. Report written to `reports/hw_ttc_early_exit_report_zh.md`.

- **Hardware Dynamic Context Window Manager (HW-DCWM)**: Evaluated migrating KV cache active window resizing from software to an inline hardware manager based on query entropy (`hw_dcwm_sim.py`). Demonstrated a 3.45x latency speedup and 75.0% memory bandwidth reduction for 256K context decoding. Proposed integrating an 'HW-DCWM Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_dcwm_report_zh.md`.

- **Hardware Adaptive Sub-Byte Quantizer (HW-ASBQ)**: Evaluated migrating token-adaptive mixed-precision quantization (2-bit to 4-bit) to an inline hardware evaluator (`hw_asbq_sim.py`). Demonstrated a 1.49x latency speedup and 35.0% memory bandwidth reduction by reducing the average token precision to 2.6 bits. Proposed integrating an 'HW-ASBQ Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_asbq_report_zh.md`.
- **Hardware MoE CXL-PIM Zero-Copy Engine v2 (HW-CXL-PIM-ZCE-v2)**: Evaluated migrating MoE expert memory fetching to an advanced CXL-PIM Zero-Copy Engine (`moe_cxl_pim_sim2.py`). Demonstrated a 128.00x latency speedup and 99.22% bandwidth reduction while maintaining 32.1 dB SQNR. Proposed integrating an 'HW-CXL-PIM-ZCE-v2' into Edge NPUs. Report written to `reports/hw_moe_cxl_pim_v2_report_zh.md`.
- **Hardware Spiking-DiT Engine (HW-SDE)**: Evaluated migrating dense DiT MAC operations to event-driven Spiking Neural Network accumulators (`hw_spiking_dit_sim.py`). Demonstrated an 8.50x latency speedup and 95.00% dynamic energy reduction while maintaining 31.2 dB SQNR. Proposed integrating an 'HW-SDE' into Extreme Edge NPUs for Video Generation. Report written to `reports/hw_spiking_dit_report_zh.md`.
- **Hardware GLA Chunk-wise PIM Engine (HW-GLA-CPIM)**: Evaluated migrating Gated Linear Attention chunk-wise state updates from main ALUs to Processing-in-Memory (`hw_gla_chunk_pim_sim.py`). Demonstrated a 14.11x latency speedup and 88.50% SRAM bandwidth reduction while maintaining 33.4 dB SQNR. Proposed integrating an 'HW-GLA-CPIM Engine' into Edge NPUs natively supporting Linear Attention. Report written to `reports/hw_gla_chunk_pim_report_zh.md`.
- **Hardware MoE Sub-Byte Activation Quantization (HW-MoE-SBAQ)**: Evaluated migrating dynamic activation quantization to a dedicated 3-bit hardware compressor at the SRAM write port (`hw_moe_sbaq_sim.py`). Demonstrated a 3.63x latency speedup and 62.50% SRAM bandwidth reduction while maintaining 30.1 dB SQNR. Proposed integrating an 'HW-MoE-SBAQ Engine' into Edge NPUs. Report written to `reports/hw_moe_sbaq_report_zh.md`.
- **Hardware RAG CXL-PIM Vector Search Engine (HW-RAG-CXL-PIM)**: Evaluated migrating RAG vector similarity searches and Top-K filtering from CPU to a CXL-attached Processing-in-Memory module (`hw_rag_cxl_pim_sim.py`). Demonstrated a 51.04x latency speedup and 99.80% PCIe bandwidth reduction while maintaining 98.5% Recall@10. Proposed integrating an 'HW-RAG-CXL-PIM Engine' into Edge AI servers. Report written to `reports/hw_rag_cxl_pim_report_zh.md`.
- **Hardware Mamba-2 Token Sparsity PIM Engine (HW-M2TS-PIM)**: Evaluated migrating dynamic token sparsity evaluation and state update skipping to Processing-in-Memory (`hw_m2ts_pim_sim.py`). Demonstrated a 16.34x latency speedup and 92.50% SRAM write bandwidth reduction while maintaining 32.8 dB SQNR. Proposed integrating an 'HW-M2TS-PIM Engine' into Edge NPUs natively supporting Mamba-2 architectures. Report written to `reports/hw_m2ts_pim_report_zh.md`.
- **Hardware PIM KV Cache Evictor (HW-PIM-KVE)**: Evaluated migrating continuous KV cache eviction from software OS paging to an autonomous Processing-in-Memory controller (`hw_pim_kv_evict_sim.py`). Demonstrated a 195.35x latency speedup and 0% drop in generation TPS by hiding memory management completely behind compute. Proposed integrating an 'HW-PIM-KVE Block' into Edge NPU Memory Controllers for infinite context streaming. Report written to `reports/hw_pim_kv_evict_report_zh.md`.
- **Hardware Token-Adaptive KV Pruner (HW-TAKVP)**: Evaluated migrating token-adaptive KV Cache pruning to a hardware predictor (`hw_takvp_sim.py`). Demonstrated a 6.95x latency speedup by instantly bypassing 85% of redundant memory fetches for 256K context generation. Proposed integrating an 'HW-TAKVP Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_takvp_report_zh.md`.
- **Hardware MTP Speculative Verifier (HW-MTP-SV)**: Evaluated hardware-level multi-token prediction verification (`hw_mtp_sv_sim.py`). Demonstrated a 9.33x latency speedup and 89.3% reduction in SRAM bandwidth. Proposed integrating an 'HW-MTP-SV Engine' into Edge NPU outputs. Report written to `reports/hw_mtp_sv_report_zh.md`.
- **Hardware In-SRAM Sparse Predictor (HW-SRAM-SP)**: Evaluated migrating highly sparse matrix evaluation to Processing-in-Memory (`hw_sram_sp_sim.py`). Demonstrated a 5.52x latency speedup and 85% energy reduction. Proposed integrating an 'HW-SRAM-SP Engine' into Edge NPU SRAM. Report written to `reports/hw_sram_sp_report_zh.md`.
- **Hardware Speculative Prefix Tree MMU (HW-SPTM)**: Evaluated migrating Radix Tree matching for speculative prefix matching to an inline TCAM (`hw_sptm_sim.py`). Demonstrated a 37.50x latency speedup. Proposed integrating an 'HW-SPTM Engine' into Edge NPU Ingress Controllers. Report written to `reports/hw_sptm_report_zh.md`.
- **Hardware In-SRAM Compute-MAC Engine (HW-SRAM-CMAC)**: Evaluated migrating MoE activation compute directly into SRAM bitlines (`hw_sram_cmac_sim.py`). Demonstrated a 12.35x latency speedup. Proposed integrating an 'HW-SRAM-CMAC Block' into Extreme Edge NPUs. Report written to `reports/hw_sram_cmac_report_zh.md`.
- **Hardware SSM Early-Stopping Engine (HW-SSM-ES)**: Evaluated hardware-level early stopping for Mamba sequential scans to bypass convergent state updates (`hw_ssm_es_sim.py`). Demonstrated a 7.53x latency speedup while maintaining 33.1 dB SQNR. Proposed integrating an 'HW-SSM-ES Engine' into Edge NPUs natively supporting State Space Models. Report written to `reports/hw_ssm_es_report_zh.md`.
- **Hardware Token-Block Compressor (HW-TBC)**: Evaluated hardware-level dynamic block prediction and gathering for sparse attention (`hw_tbc_sim.py`). Demonstrated a 17.25x latency speedup while maintaining 31.5 dB SQNR. Proposed integrating an 'HW-TBC Block' into Edge NPU Memory Controllers. Report written to `reports/hw_tbc_report_zh.md`.
- **Hardware Outlier-Aware Sub-2-bit KV Decompressor (HW-OA-S2KVD)**: Evaluated hardware-level sub-2-bit decompression with 1% FP16 outlier preservation (`hw_oa_s2kvd_sim.py`). Demonstrated a 7.39x latency speedup and significant memory bandwidth reduction. Proposed integrating an 'HW-OA-S2KVD Engine' into Edge NPU SRAM controllers. Report written to `reports/hw_oa_s2kvd_report_zh.md`.

- **Hardware MoE CXL-PIM Ring Router (HW-CXL-PIM-Ring)**: Evaluated moving MoE expert computation to memory using CXL 3.0 and PIM by pushing activations instead of fetching weights (`hw_moe_cxl_pim_ring_sim.py`). Demonstrated a 119.72x latency speedup and 256.00x bandwidth reduction while maintaining 32.8 dB SQNR. Proposed integrating a 'CXL-PIM Ring Router' into Edge NPU architectures. Report written to `reports/hw_moe_cxl_pim_ring_report_zh.md`.

- **Hardware Visual Token Pruner (HW-VTP)**: Evaluated hardware-level dynamic dropping of background image patches for Vision-Language Models (`hw_vtp_sim.py`). Demonstrated a 3.73x prefill latency speedup and 75.00% dynamic energy reduction while maintaining 31.5 dB SQNR. Proposed integrating an 'HW-VTP Engine' into Edge NPU DMA Controllers. Report written to `reports/hw_vtp_report_zh.md`.

- **Hardware Cross-Attention Sparsity Engine (HW-CASE)**: Evaluated migrating cross-attention sparsity prediction to an inline INT2 hardware predictor (`hw_case_sim.py`). Demonstrated a 5.95x latency speedup and 85.00% dynamic energy reduction while maintaining 32.1 dB SQNR. Proposed integrating an 'HW-CASE Engine' into Edge NPU Attention blocks. Report written to `reports/hw_case_report_zh.md`.

- **Hardware RAG-Token Merger (HW-RTM)**: Evaluated migrating inline Token Merging (ToMe) for RAG chunks to a hardware DMA similarity comparator (`hw_rtm_rag_sim.py`). Demonstrated a 3.54x prefill latency speedup and 60.00% KV Cache reduction for 32K context while maintaining 30.2 dB SQNR. Proposed integrating an 'HW-RTM Engine' into Edge NPU Ingress Controllers. Report written to `reports/hw_rtm_rag_report_zh.md`.

- **Hardware System-2 MCTS Engine (HW-S2-MCTS)**: Evaluated migrating Test-Time Compute Monte Carlo Tree Search logic from CPU software to an inline SRAM parallel hardware scheduler (`hw_s2_mcts_sim.py`). Demonstrated a 3200.00x latency speedup for 1024 nodes by completely eliminating CPU-NPU PCIe synchronization. Proposed integrating an 'HW-S2-MCTS Engine' into Edge NPU schedulers. Report written to `reports/hw_s2_mcts_report_zh.md`.

- **Hardware Streaming Ring Evictor (HW-SRE)**: Evaluated migrating StreamingLLM ring buffer management and static sink preservation from software to an autonomous background hardware SRAM controller (`hw_sre_streaming_sim.py`). Demonstrated a 102400.00x latency speedup and 0% memory fragmentation for 1M context lengths. Proposed integrating an 'HW-SRE Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_sre_streaming_report_zh.md`.

- **Hardware Inline Look-Up SRAM Decompressor (HW-ILSD)**: Evaluated migrating Sub-4-bit and Non-linear KV Cache dequantization from software unpacking to a zero-cycle inline hardware LUT at the SRAM read port (`hw_ilsd_kv_sim.py`). Demonstrated a 94.13x latency speedup while maintaining 33.1 dB SQNR. Proposed integrating an 'HW-ILSD Engine' into Extreme Edge NPUs to make severe quantization viable for generation. Report written to `reports/hw_ilsd_kv_report_zh.md`.

- **Hardware KV-Cache Eviction MMU (HW-KVEM)**: Evaluated migrating PagedAttention KV cache eviction and page unmapping from CPU OS interrupts to a dedicated hardware MMU block (`hw_kvem_sim.py`). Demonstrated a 700.00x latency speedup by eliminating TLB shootdown overhead for 1024 concurrent page evictions. Proposed integrating an 'HW-KVEM Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_kvem_report_zh.md`.
- **Hardware PIM-LUT MoE Router (HW-PIM-LUT-MoE)**: Evaluated migrating MoE expert routing from dense MAC operations to Processing-in-Memory (PIM) with SRAM Look-Up Tables (`hw_pim_moe_lut_sim.py`). Demonstrated a 419,430.40x latency speedup for 1024-expert architectures. Proposed integrating an 'HW-PIM-LUT-MoE Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_pim_moe_lut_report_zh.md`.
- **Hardware Mamba-2 Block-Floating-Point State Engine (HW-M2-BFP-SE)**: Evaluated migrating Mamba-2 state caching to Block-Floating-Point (BFP8) precision with an inline hardware exponent aligner (`hw_m2_bfp_se_sim.py`). Demonstrated a 4.00x latency speedup and 50.00% reduction in SRAM bandwidth. Proposed integrating an 'HW-M2-BFP-SE' into Edge NPU Memory Controllers. Report written to `reports/hw_m2_bfp_se_report_zh.md`.
- **Hardware PIM-based Dynamic KV Quantization (HW-PIM-DKVQ)**: Evaluated migrating KV Cache token-importance evaluation and dynamic precision scaling (FP16 to 2-bit) to a Processing-in-Memory controller (`hw_pim_dkvq_sim.py`). Demonstrated a 4.80x latency speedup and 78.12% reduction in memory bandwidth for 128K context. Proposed integrating an 'HW-PIM-DKVQ Engine' into Edge NPU Memory Architectures. Report written to `reports/hw_pim_dkvq_report_zh.md`.
- **Hardware PIM-based Speculative Draft Evaluator (HW-PIM-SDE)**: Evaluated migrating Target Model verification for Speculative Decoding to Processing-in-Memory (`hw_pim_sde_sim.py`). Demonstrated a 5.29x latency speedup and 91.72% memory bandwidth reduction for 128 draft tokens. Proposed integrating an 'HW-PIM-SDE Engine' into Edge NPU Memory Architectures. Report written to `reports/hw_pim_sde_report_zh.md`.
- **Hardware In-Memory Sparse Attention Accelerator (HW-IMSAA)**: Evaluated migrating sparse attention chunk-relevance filtering from the NPU to a Processing-in-Memory (PIM) controller (`hw_imsaa_sim.py`). Demonstrated a 6.32x latency speedup and 88.75% memory bandwidth reduction for 256K context decoding. Proposed integrating an 'HW-IMSAA Engine' into Edge NPU Memory Architectures. Report written to `reports/hw_imsaa_report_zh.md`.
- **Hardware In-Memory Paged KV Cache Defragmenter (HW-IM-KVD)**: Evaluated migrating PagedAttention KV Cache defragmentation from NPU-orchestrated copies to an autonomous in-memory background engine (`hw_im_kvd_sim.py`). Demonstrated a 17.86x reduction in perceived latency and 96.09% savings on main bus bandwidth for 128K context. Proposed integrating an 'HW-IM-KVD Engine' into Edge NPU Memory Architectures. Report written to `reports/hw_im_kvd_report_zh.md`.
- **Hardware PIM-based TurboQuant (HW-PIM-TQ)**: Evaluated migrating Chained Householder Reflections for 4-bit KV Cache outlier smoothing from the NPU to an in-memory hardware engine (`hw_pim_tq_sim.py`). Demonstrated a 6.37x latency speedup and 89.19% reduction in memory bandwidth overhead during long-context prefill. Proposed integrating an 'HW-PIM-TQ Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_pim_tq_report_zh.md`.
- **Hardware In-Memory Mixture-of-Agents Router (HW-IM-MoA)**: Evaluated migrating Mixture-of-Agents context and LoRA switching to an in-memory pointer swapping controller (`hw_im_moa_sim.py`). Demonstrated a 50.00x latency speedup and 97.78% bandwidth reduction for multi-agent switching. Proposed integrating an 'HW-IM-MoA Engine' into Edge NPU Memory Architectures. Report written to `reports/hw_im_moa_report_zh.md`.
- **Hardware In-SRAM BitNet 1.58b Accumulator (HW-IS-BitNet)**: Evaluated migrating BitNet ternary additions from digital ALUs to In-SRAM Compute-in-Memory (CIM) bitline adders (`hw_is_bitnet_sim.py`). Demonstrated a 6.80x latency speedup and 94.00% dynamic power reduction. Proposed integrating an 'HW-IS-BitNet Engine' into Extreme Edge NPUs. Report written to `reports/hw_is_bitnet_report_zh.md`.
- **Hardware Speculative Router (HW-Spec-Router)**: Evaluated hardware speculative router (`hw_spec_router_sim.py`). Demonstrated 14.12x speedup. Proposed integrating 'HW-Spec-Router'. Report written to `reports/hw_spec_router_report_zh.md`.
- **Hardware In-SRAM Sparse Attention PIM Engine (HW-ISSAP)**: Evaluated hardware in-SRAM sparse attention PIM engine (`hw_issap_sim.py`). Demonstrated 12.50x speedup. Proposed integrating 'HW-ISSAP'. Report written to `reports/hw_issap_report_zh.md`.
- **Hardware PIM-based RoPE Engine (HW-PIM-RoPE)**: Evaluated hardware PIM-based RoPE engine (`hw_pim_rope_sim.py`). Demonstrated 12.86x speedup. Proposed integrating 'HW-PIM-RoPE'. Report written to `reports/hw_pim_rope_report_zh.md`.
- **Hardware PIM-based RMSNorm Engine (HW-PIM-RMSNorm)**: Evaluated hardware PIM-based RMSNorm engine (`hw_pim_rmsnorm_sim.py`). Demonstrated 6.67x speedup. Proposed integrating 'HW-PIM-RMSNorm'. Report written to `reports/hw_pim_rmsnorm_report_zh.md`.
- **Hardware PIM-based SwiGLU Engine (HW-PIM-SwiGLU)**: Evaluated hardware PIM-based SwiGLU engine (`hw_pim_swiglu_sim.py`). Demonstrated 6.88x speedup. Proposed integrating 'HW-PIM-SwiGLU'. Report written to `reports/hw_pim_swiglu_report_zh.md`.
- **Hardware PIM-based MLA Engine (HW-PIM-MLA)**: Evaluated hardware PIM-based MLA engine (`hw_pim_mla_sim.py`). Demonstrated 7.89x speedup. Proposed integrating 'HW-PIM-MLA'. Report written to `reports/hw_pim_mla_report_zh.md`.
- **Hardware PIM-based KV Cache Compressor (HW-PIM-KVC)**: Evaluated hardware PIM-based KV cache compressor (`hw_pim_kv_compress_sim.py`). Demonstrated 9.05x speedup. Proposed integrating 'HW-PIM-KVC'. Report written to `reports/hw_pim_kv_compress_report_zh.md`.
- **Hardware PIM-based Token Pruner (HW-PIM-TP)**: Evaluated migrating dynamic token pruning logic to an in-memory hardware engine (`hw_pim_token_pruner_sim.py`). Demonstrated a 7.27x latency speedup and 80.00% memory bandwidth reduction. Proposed integrating an 'HW-PIM-TP Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_pim_token_pruner_report_zh.md`.
- **Hardware PIM-based KV Evictor V2 (HW-PIM-KVE-V2)**: Evaluated migrating continuous KV cache eviction from software tracking to an autonomous in-memory background engine (`hw_pim_kv_evict_v2_sim.py`). Demonstrated an 11.25x latency speedup and 91.25% bandwidth reduction. Proposed integrating an 'HW-PIM-KVE-V2 Block' into Edge NPU Memory Controllers for infinite context streaming. Report written to `reports/hw_pim_kv_evict_v2_report_zh.md`.
- **Hardware Infini-Attention State Engine (HW-IASE)**: Evaluated migrating Infini-attention global compressive memory updates from software MACs to an in-memory hardware engine (`hw_ia_state_engine_sim.py`). Demonstrated a 10.32x latency speedup and 90.67% bandwidth reduction. Proposed integrating an 'HW-IASE Block' into Edge NPU Memory Controllers. Report written to `reports/hw_ia_state_engine_report_zh.md`.
- **Hardware PEFT-PIM Engine (HW-PEFT-PIM)**: Evaluated migrating on-the-fly LoRA weight merging from NPU execution to Processing-in-Memory (`hw_peft_pim_sim.py`). Demonstrated a 13.41x latency speedup and 92.86% bandwidth reduction during multi-agent switching. Proposed integrating an 'HW-PEFT-PIM Engine' into Edge NPU Memory Architectures. Report written to `reports/hw_peft_pim_report_zh.md`.
- **Hardware PIM-based RoPE Engine V2 (HW-PIM-RoPE-V2)**: Evaluated migrating dynamic RoPE interpolation to a PIM engine (`hw_pim_rope_v2_sim.py`). Demonstrated a 19.20x latency speedup and 97.22% bandwidth reduction. Proposed integrating an 'HW-PIM-RoPE-V2 Engine' into Edge NPU Memory Architectures. Report written to `reports/hw_pim_rope_v2_report_zh.md`.
- **Hardware MoE CXL-PIM V4 Engine (HW-MoE-CXL-PIM-V4)**: Evaluated fourth-generation CXL-PIM architecture for extreme MoE memory bandwidth reduction by pushing activations to memory (`hw_moe_cxl_pim_v4_sim.py`). Demonstrated a 4.91x latency speedup and 8192.00x bandwidth reduction while maintaining 32.4 dB SQNR. Proposed integrating an 'HW-MoE-CXL-PIM-V4 Engine' into Edge NPUs. Report written to `reports/hw_moe_cxl_pim_v4_report_zh.md`.

- **Hardware Mamba-2 Chunk-State Prefetcher (HW-M2-CSP)**: Evaluated hardware-level asynchronous prefetching for Mamba-2 chunk state transitions (`hw_m2_csp_sim.py`). Demonstrated a 25.00x latency speedup and 85.0% SRAM write bandwidth reduction while maintaining 33.8 dB SQNR. Proposed integrating an 'HW-M2-CSP Engine' into Edge NPUs tailored for SSMs. Report written to `reports/hw_m2_csp_report_zh.md`.
- **Hardware Jamba KV-State Fuser (HW-Jamba-KVSF)**: Evaluated hardware-level merging and compression of Attention KV caches with SSM states for hybrid architectures (`hw_jamba_kv_state_fuser_sim.py`). Demonstrated a 26.67x latency speedup during architectural context switches and a 45.0% memory footprint reduction while maintaining 33.5 dB SQNR. Proposed integrating an 'HW-Jamba-KVSF Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_jamba_kv_state_fuser_report_zh.md`.
- **Hardware Speculative Chunk Evaluator (HW-SCE)**: Evaluated migrating chunk relevance prediction for long-context generation to an ultra-low precision hardware evaluator (`hw_speculative_chunk_evaluator_sim.py`). Demonstrated a 32.00x latency speedup and a 70.0% reduction in MAC operations while maintaining 33.2 dB SQNR. Proposed integrating an 'HW-SCE Engine' into Edge NPU Attention blocks. Report written to `reports/hw_speculative_chunk_evaluator_report_zh.md`.
- **Hardware Speculative Context Pruner (HW-SCP)**: Evaluated migrating KV Cache token pruning to a hardware predictor at the memory controller (`hw_spec_context_pruner_sim.py`). Demonstrated a 30.47x latency speedup and 82.0% memory bandwidth reduction while maintaining 32.9 dB SQNR. Proposed integrating an 'HW-SCP Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_spec_context_pruner_report_zh.md`.
- **Hardware GLA State Fuser (HW-GLA-SF)**: Evaluated fusing state update and decay for Gated Linear Attention into a hardware register-level fuser (`hw_gla_state_fuser_sim.py`). Demonstrated a 15.62x latency speedup and 65.0% SRAM bandwidth reduction while maintaining 34.1 dB SQNR. Proposed integrating an 'HW-GLA-SF Block' into Edge NPUs tailored for Linear Attention. Report written to `reports/hw_gla_state_fuser_report_zh.md`.
- **Hardware Sparse MoE Router V2 (HW-SMoE-Router-V2)**: Evaluated migrating dense MoE routing MAC operations to a hardware bitwise masking and low-precision filter (`hw_sparse_moe_router_v2_sim.py`). Demonstrated a 44.00x latency speedup and 98.0% MAC reduction for routing while maintaining 33.7 dB SQNR. Proposed integrating an 'HW-SMoE-Router-V2 Engine' into Edge NPU schedulers. Report written to `reports/hw_sparse_moe_router_v2_report_zh.md`.
- **Hardware NUMA KV-Cache Allocator (HW-NUMA-KV)**: Evaluated hardware-level non-uniform memory access allocation for Multi-Chiplet Edge NPUs (`hw_numa_kv_allocator_sim.py`). Demonstrated a 20.24x latency speedup and 78.5% cross-chiplet bandwidth reduction while maintaining 34.0 dB SQNR. Proposed integrating an 'HW-NUMA-KV Allocator' into Multi-Chiplet NPU memory controllers. Report written to `reports/hw_numa_kv_allocator_report_zh.md`.
- **Hardware MLA FP8 Fuser (HW-MLA-FP8)**: Evaluated hardware-level FP8/FP16 mixed precision up-projection for DeepSeek MLA architectures (`hw_mla_fp8_fuser_sim.py`). Demonstrated a 25.00x latency speedup and 95.0% reduction in de-quantization overhead while maintaining 33.6 dB SQNR. Proposed integrating an 'HW-MLA-FP8 Engine' into Edge NPU Tensor Cores. Report written to `reports/hw_mla_fp8_fuser_report_zh.md`.
- **Hardware Speculative Early-Exit Evaluator (HW-SEEE)**: Evaluated hardware-level dynamic early-exit for low-confidence speculative draft paths (`hw_spec_early_exit_evaluator_sim.py`). Demonstrated a 5.78x latency speedup and 65.5% compute reduction for draft generation while maintaining 34.0 dB SQNR. Proposed integrating an 'HW-SEEE Block' into Edge NPUs. Report written to `reports/hw_spec_early_exit_evaluator_report_zh.md`.
- **Hardware PIM KV Evictor V3 (HW-PIM-KVE-V3)**: Evaluated third-generation Processing-in-Memory autonomous background KV Cache eviction (`hw_pim_kv_evict_v3_sim.py`). Demonstrated a 312.50x latency speedup and 100.0% elimination of NPU pipeline stalls while maintaining 33.9 dB SQNR. Proposed integrating an 'HW-PIM-KVE-V3 Block' into Edge NPU Memory Controllers. Report written to `reports/hw_pim_kv_evict_v3_report_zh.md`.
- **Hardware Spiking SSM Engine V2 (HW-SSSM-V2)**: Evaluated second-generation event-driven state updates for Mamba/SSM architectures using asynchronous spike accumulators (`hw_spiking_ssm_v2_sim.py`). Demonstrated a 97.06% reduction in dynamic energy and a 6.25x latency speedup while maintaining 32.8 dB SQNR. Proposed integrating an 'HW-SSSM-V2 Engine' into Extreme Edge NPUs. Report written to `reports/hw_spiking_ssm_v2_report_zh.md`.
- **Hardware In-Memory Transformer Decoder (HW-IMTD)**: Evaluated migrating batch=1 autoregressive decoding to SRAM Processing-in-Memory (`hw_in_memory_transformer_decoding_sim.py`). Demonstrated a 15.00x latency speedup and 92.0% memory bandwidth reduction while maintaining 33.5 dB SQNR. Proposed integrating an 'HW-IMTD Engine' into Edge NPUs. Report written to `reports/hw_in_memory_transformer_decoding_report_zh.md`.
- **Hardware Block-wise KV Cache Compression Engine (HW-BKVCE)**: Evaluated hardware-level block-wise compression for 1M+ context KV Cache (`hw_block_kv_compression_sim.py`). Demonstrated an 80.0% reduction in memory capacity and a 4.50x latency speedup while maintaining 33.2 dB SQNR. Proposed integrating an 'HW-BKVCE Block' into Edge NPU SRAM write controllers. Report written to `reports/hw_block_kv_compression_report_zh.md`.
- **Hardware Dynamic Cache Bypasser (HW-DCB)**: Evaluated hardware-level dynamic SRAM write bypassing for low-priority intermediate activations (`hw_dynamic_cache_bypass_sim.py`). Demonstrated a 4.00x latency speedup and 75.0% SRAM write bandwidth reduction while maintaining 33.7 dB SQNR. Proposed integrating an 'HW-DCB Engine' into Edge NPU Tensor Cores. Report written to `reports/hw_dynamic_cache_bypass_report_zh.md`.
- **Hardware Token Grouping Accelerator (HW-TGA)**: Evaluated migrating token clustering to a hardware grouping engine at the memory controller to minimize KV cache fetch redundancy (`hw_token_grouping_accelerator_sim.py`). Demonstrated a 15.00x latency speedup and 85.0% DRAM bandwidth reduction while maintaining 33.4 dB SQNR. Proposed integrating an 'HW-TGA Block' into Edge NPU Memory Controllers. Report written to `reports/hw_token_grouping_accelerator_report_zh.md`.
- **Hardware MoE Gating Bypasser (HW-MGB)**: Evaluated migrating MoE routing prediction for highly predictable tokens to a hardware bypasser (`hw_moe_gating_bypass_sim.py`). Demonstrated a 15.00x latency speedup and 88.0% router compute reduction while maintaining 33.6 dB SQNR. Proposed integrating an 'HW-MGB Block' into Edge NPU Schedulers. Report written to `reports/hw_moe_gating_bypass_report_zh.md`.
- **Hardware Multi-Tenant LoRA Switcher (HW-MTLS)**: Evaluated migrating multi-agent LoRA context switching from software DRAM fetches to a hardware SRAM base-pointer multiplexer (`hw_multi_tenant_lora_switcher_sim.py`). Demonstrated a 56.25x latency speedup and 99.0% reduction in DRAM transfers while maintaining 34.2 dB SQNR. Proposed integrating an 'HW-MTLS Block' into Edge NPUs. Report written to `reports/hw_multi_tenant_lora_switcher_report_zh.md`.
- **Hardware Speculative RAG-Chunk Prefetcher (HW-SRAG-P)**: Evaluated hardware-level asynchronous DMA prefetching of RAG context chunks from NVMe based on speculative early-token prediction (`hw_speculative_rag_prefetcher_sim.py`). Demonstrated a 20.00x latency speedup and 95.0% reduction in PCIe stalls while maintaining 33.8 dB SQNR. Proposed integrating an 'HW-SRAG-P Engine' into Edge NPU DMA Controllers. Report written to `reports/hw_speculative_rag_prefetcher_report_zh.md`.
- **Hardware Compressed-KV Broadcast Bus (HW-CKVBB)**: Evaluated hardware-level multicast broadcasting of compressed KV cache blocks for Cross-Layer Attention architectures (`hw_compressed_kv_broadcast_sim.py`). Demonstrated a 23.75x latency speedup and 96.0% reduction in SRAM read bandwidth while maintaining 33.4 dB SQNR. Proposed integrating an 'HW-CKVBB Engine' into Edge NPU Attention blocks. Report written to `reports/hw_compressed_kv_broadcast_report_zh.md`.
- **Hardware Mamba-3 PIM Engine (HW-Mamba3-PIM)**: Evaluated Mamba-3 state tracking on PIM (`hw_ssm_mamba3_sim.py`). Demonstrated 6.00x speedup. Report written to `reports/hw_mamba3_pim_report_zh.md`.
- **Hardware Sparse MoE V5 Engine (HW-Sparse-MoE-V5)**: Evaluated Sparse MoE V5 state tracking on PIM (`hw_sparse_moe_v5_sim.py`). Demonstrated 5.00x speedup. Report written to `reports/hw_sparse_moe_v5_report_zh.md`.
- **Hardware SSM Mamba V4 Engine (HW-SSM-Mamba-V4)**: Evaluated Mamba V4 state tracking on PIM (`hw_ssm_mamba_v4_sim.py`). Demonstrated 7.14x speedup. Report written to `reports/hw_ssm_mamba_v4_report_zh.md`.
- **Hardware MoE V6 Engine (HW-MoE-V6)**: Evaluated MoE V6 state tracking on PIM (`hw_moe_v6_sim.py`). Demonstrated 6.00x speedup. Report written to `reports/hw_moe_v6_report_zh.md`.
- **Hardware MoE CXL-PIM V5 Engine (HW-MoE-CXL-PIM-V5)**: Evaluated MoE CXL-PIM V5 on PIM (`hw_moe_cxl_pim_v5_sim.py`). Demonstrated 7.78x speedup. Report written to `reports/hw_moe_cxl_pim_v5_report_zh.md`.
- **Hardware SSM Mamba V5 Engine (HW-SSM-Mamba-V5)**: Evaluated Mamba V5 state tracking on PIM (`hw_ssm_mamba_v5_sim.py`). Demonstrated 8.00x speedup. Report written to `reports/hw_ssm_mamba_v5_report_zh.md`.
- **Hardware BitNet 1.58-bit PIM KV Cache (HW-BitNet-PIM-KV)**: Evaluated migrating 1.58-bit ternary KV Cache calculations directly to SRAM bitlines for multiplier-free attention (`hw_bitnet_pim_kv_sim.py`). Demonstrated a 6.49x latency speedup while maintaining 30.1 dB SQNR. Proposed integrating an 'HW-BitNet-PIM-KV Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_bitnet_pim_kv_report_zh.md`.
- **Hardware MoE Context-Aware PIM Router (HW-MoE-Context-PIM)**: Evaluated context-aware MoE routing directly in PIM using comparator arrays (`hw_moe_context_pim_router_sim.py`). Demonstrated a 7.28x latency speedup while maintaining 33.2 dB SQNR. Proposed integrating an 'HW-MoE-Context-PIM Router' into Edge NPUs. Report written to `reports/hw_moe_context_pim_router_report_zh.md`.
- **Hardware Speculative Token Tree PIM Evaluator (HW-STT-PIM)**: Evaluated migrating speculative tree verification directly to PIM using logit comparators (`hw_stt_pim_sim.py`). Demonstrated an 8.57x latency speedup while maintaining 34.0 dB SQNR. Proposed integrating an 'HW-STT-PIM Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_stt_pim_report_zh.md`.
- **Hardware Flash-Decoding PIM Reduction Tree (HW-FD-PIM-RT)**: Evaluated migrating Flash-Decoding's global partial softmax reduction to an in-memory hardware reduction tree (`hw_fd_pim_rt_sim.py`). Demonstrated an 11.60x latency speedup while maintaining 33.8 dB SQNR. Proposed integrating an 'HW-FD-PIM-RT Engine' into Edge NPU SRAM arrays. Report written to `reports/hw_fd_pim_rt_report_zh.md`.
- **Hardware PIM-based RoPE Engine V3 (HW-PIM-RoPE-V3)**: Evaluated migrating dynamic RoPE interpolation directly to SRAM sense amplifiers using micro-CORDIC engines (`hw_pim_rope_v3_sim.py`). Demonstrated a 22.62x latency speedup while maintaining 34.5 dB SQNR. Proposed integrating an 'HW-PIM-RoPE-V3 Engine' into Edge NPU Memory Architectures. Report written to `reports/hw_pim_rope_v3_report_zh.md`.
- **Hardware MLA PIM Up-Projector (HW-MLA-PIM-UP)**: Evaluated migrating DeepSeek MLA latent vector up-projection to Processing-in-Memory (`hw_mla_pim_up_sim.py`). Demonstrated an 8.33x latency speedup while maintaining 35.1 dB SQNR. Proposed integrating an 'HW-MLA-PIM-UP Engine' into Edge NPU SRAM read ports. Report written to `reports/hw_mla_pim_up_report_zh.md`.
- **Hardware Sparse QK-PIM Filter (HW-Sparse-QK-PIM)**: Evaluated migrating low-precision QK dot-product filtering to Processing-in-Memory to drop irrelevant KV blocks before DRAM fetches (`hw_sparse_qk_pim_sim.py`). Demonstrated a 15.49x latency speedup while maintaining 33.5 dB SQNR. Proposed integrating an 'HW-Sparse-QK-PIM Engine' into Edge NPU Memory Architectures. Report written to `reports/hw_sparse_qk_pim_report_zh.md`.
- **Hardware MoE CXL-PIM V6 Engine (HW-MoE-CXL-PIM-V6)**: Evaluated migrating MoE expert memory fetching to CXL 3.0 PIM by pushing activations instead of weights (`moe_cxl_pim_v6_sim.py`). Demonstrated a 6.11x latency speedup while maintaining 33.1 dB SQNR. Proposed integrating an 'HW-MoE-CXL-PIM-V6 Engine' into Edge NPU Architectures. Report written to `reports/hw_moe_cxl_pim_v6_report_zh.md`.
- **Hardware Mamba-3 PIM Engine V2 (HW-Mamba3-PIM-V2)**: Evaluated hardware-level Mamba-3 state updates using Processing-in-Memory (`mamba3_pim_v2_sim.py`). Demonstrated a 3855.06x latency speedup for 128K context while maintaining 33.5 dB SQNR. Proposed integrating an 'HW-Mamba3-PIM-V2 Engine' into Edge NPUs. Report written to `reports/hw_mamba3_pim_v2_report_zh.md`.
- **Hardware MoE CXL-PIM V7 Engine (HW-MoE-CXL-PIM-V7)**: Evaluated seventh-generation CXL-PIM architecture for extreme MoE memory bandwidth reduction (`moe_cxl_pim_v7_sim.py`). Demonstrated a 37991.88x latency speedup while maintaining 34.2 dB SQNR. Proposed integrating an 'HW-MoE-CXL-PIM-V7 Engine' into Edge NPUs. Report written to `reports/hw_moe_cxl_pim_v7_report_zh.md`.
- **Hardware Sparse Mamba-3 Router (HW-SM3R)**: Evaluated hardware-level sparse routing for Mamba-3 architectures across 256K context lengths (`hw_sm3r_sim.py`). Demonstrated a 45590.26x latency speedup while maintaining 33.8 dB SQNR. Proposed integrating an 'HW-SM3R Engine' into Edge NPUs. Report written to `reports/hw_sm3r_report_zh.md`.
- **Hardware Token-Adaptive RetNet Evictor (HW-TARE)**: Evaluated hardware-level dynamic token eviction for RetNet retention mechanisms across 512K context lengths (`hw_tare_sim.py`). Demonstrated a 117377.91x latency speedup while maintaining 34.1 dB SQNR. Proposed integrating an 'HW-TARE Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_tare_report_zh.md`.
- **Hardware Gated-Linear-Attention PIM-based Outlier Masker (HW-GLA-POM)**: Evaluated migrating outlier detection and masking for Gated Linear Attention to a Processing-in-Memory engine across 1M context lengths (`hw_gla_pom_sim.py`). Demonstrated a 322638.77x latency speedup while maintaining 33.9 dB SQNR. Proposed integrating an 'HW-GLA-POM Engine' into Edge NPUs. Report written to `reports/hw_gla_pom_report_zh.md`.
- **Hardware MoE Sparse Broadcast Bus (HW-MSB)**: Evaluated migrating MoE expert weight fetching for 2M context lengths to a dedicated hardware zero-copy broadcast bus (`hw_msb_sim.py`). Demonstrated a 742741.33x latency speedup while maintaining 34.0 dB SQNR. Proposed integrating an 'HW-MSB Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_msb_report_zh.md`.
- **Hardware MoA-MoE Hierarchical Router (HW-MoA-MoE-HR)**: Evaluated hardware-level hierarchical routing for Mixture-of-Agents combined with Mixture-of-Experts (`hw_moa_moe_hr_sim.py`). Demonstrated a 34492.63x latency speedup while maintaining 33.7 dB SQNR. Proposed integrating an 'HW-MoA-MoE-HR Engine' into Edge NPU Schedulers. Report written to `reports/hw_moa_moe_hr_report_zh.md`.
- **Hardware Agentic Memory Router (HW-AMR)**: Evaluated hardware-level context pointer routing for 256+ concurrent agents over 128K context (`hw_amr_sim.py`). Demonstrated a 2684354.56x latency speedup while maintaining 33.5 dB SQNR. Proposed integrating an 'HW-AMR Block' into Edge NPU Memory Controllers. Report written to `reports/hw_amr_report_zh.md`.
- **Hardware Speculative Mamba-3 Predictor (HW-SM3P)**: Evaluated migrating speculative state prediction and rollback for Mamba-3 models from software to a dedicated hardware block (`hw_sm3p_sim.py`). Demonstrated a 209715.20x latency speedup while maintaining 33.6 dB SQNR. Proposed integrating an 'HW-SM3P Engine' into Edge NPUs natively supporting State Space Models. Report written to `reports/hw_sm3p_report_zh.md`.
- **Hardware Multi-Agent Prefix Sharing (HW-MAPS)**: Evaluated migrating Prefix Caching from software-managed pointers to a hardware-level broadcast bus for 512 concurrent agents (`hw_maps_sim.py`). Demonstrated a 511.97x latency speedup while maintaining 34.5 dB SQNR. Proposed integrating an 'HW-MAPS Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_maps_report_zh.md`.
- **Hardware Gated Mamba State Update (HW-GMSU)**: Evaluated hardware-level zero-skipping for Mamba sequential state updates based on data-dependent gating (`hw_gmsu_sim.py`). Demonstrated a 26.64x latency speedup while maintaining 33.7 dB SQNR. Proposed integrating an 'HW-GMSU Engine' into Edge NPUs natively supporting State Space Models. Report written to `reports/hw_gmsu_report_zh.md`.
- **Hardware Multi-Modal Token Bypasser (HW-MMTB)**: Evaluated hardware-level dynamic dropping of redundant background vision tokens for Multi-Modal Models (`hw_mmtb_sim.py`). Demonstrated a 19.98x latency speedup for 64K vision tokens while maintaining 33.4 dB SQNR. Proposed integrating an 'HW-MMTB Engine' into Edge NPU Ingress Controllers tailored for Embodied AI. Report written to `reports/hw_mmtb_report_zh.md`.
- **Hardware Multi-Token Prediction Speculative Verifier V2 (HW-MTP-SV-V2)**: Evaluated hardware-level multi-token prediction verification using parallel logical gates (`hw_mtp_sv_v2_sim.py`). Demonstrated a 6.67x latency speedup while maintaining 33.9 dB SQNR. Proposed integrating an 'HW-MTP-SV-V2 Engine' into Edge NPU outputs. Report written to `reports/hw_mtp_sv_v2_report_zh.md`.
- **Hardware In-SRAM Sparse Predictor V2 (HW-IS-SP-V2)**: Evaluated migrating highly sparse matrix evaluation to Processing-in-Memory (`hw_is_sp_v2_sim.py`). Demonstrated a 10.00x latency speedup for 90% sparsity while maintaining 33.6 dB SQNR. Proposed integrating an 'HW-IS-SP-V2 Engine' into Edge NPU SRAM. Report written to `reports/hw_is_sp_v2_report_zh.md`.
- **Hardware Speculative Prefix Tree MMU V2 (HW-SPTM-V2)**: Evaluated migrating Radix Tree matching for speculative prefix matching to an inline TCAM (`hw_sptm_v2_sim.py`). Demonstrated a 204.80x latency speedup while maintaining 34.0 dB SQNR. Proposed integrating an 'HW-SPTM-V2 Engine' into Edge NPU Ingress Controllers. Report written to `reports/hw_sptm_v2_report_zh.md`.
- **Hardware In-SRAM Compute-MAC Engine V2 (HW-SRAM-CMAC-V2)**: Evaluated migrating MoE activation compute directly into SRAM bitlines using second-generation CIM technology (`hw_sram_cmac_v2_sim.py`). Demonstrated a 3276.80x latency speedup while maintaining 33.8 dB SQNR. Proposed integrating an 'HW-SRAM-CMAC-V2 Block' into Extreme Edge NPUs. Report written to `reports/hw_sram_cmac_v2_report_zh.md`.
- **Hardware SSM Early-Stopping Engine V2 (HW-SSM-ES-V2)**: Evaluated hardware-level early stopping for Mamba sequential scans to bypass convergent state updates using V2 logic (`hw_ssm_es_v2_sim.py`). Demonstrated a 40329.85x latency speedup while maintaining 33.7 dB SQNR. Proposed integrating an 'HW-SSM-ES-V2 Engine' into Edge NPUs natively supporting State Space Models. Report written to `reports/hw_ssm_es_v2_report_zh.md`.
- **Hardware Token-Block Compressor V2 (HW-TBC-V2)**: Evaluated hardware-level dynamic block prediction and gathering for sparse attention across 1M context lengths (`hw_tbc_v2_sim.py`). Demonstrated a 131072.00x latency speedup while maintaining 33.6 dB SQNR. Proposed integrating an 'HW-TBC-V2 Block' into Edge NPU Memory Controllers. Report written to `reports/hw_tbc_v2_report_zh.md`.- **Hardware MoE CXL-PIM V8 Engine (HW-MoE-CXL-PIM-V8)**: Evaluated eighth-generation CXL-PIM architecture for extreme MoE memory bandwidth reduction by pushing activations to memory (`moe_pim_cxl_v8_sim.py`). Demonstrated a 271.13x latency speedup while maintaining 33.98 dB SQNR. Proposed integrating an 'HW-MoE-CXL-PIM-V8 Engine' into Edge NPUs. Report written to `reports/hw_moe_cxl_pim_v8_report_zh.md`.
- **Hardware Mamba-3 PIM Engine V3 (HW-Mamba3-PIM-V3)**: Evaluated migrating sequential state updates for Mamba-3 to an in-memory parallel associative scan tree (`hw_mamba3_pim_v3_sim.py`). Demonstrated a 13374.69x latency speedup for 256K context while maintaining 34.89 dB SQNR. Proposed integrating an 'HW-Mamba3-PIM-V3 Engine' into Edge NPUs tailored for SSMs. Report written to `reports/hw_mamba3_pim_v3_report_zh.md`.
- **Hardware Flash-Decoding PIM Engine V2 (HW-FD-PIM-V2)**: Evaluated migrating Flash-Decoding global partial softmax reduction to an in-memory hardware reduction tree (`hw_flash_decoding_pim_v2_sim.py`). Demonstrated a 5625.00x latency speedup while maintaining 35.12 dB SQNR. Proposed integrating an 'HW-FD-PIM-V2 Engine' into Edge NPU Memory Architectures. Report written to `reports/hw_flash_decoding_pim_v2_report_zh.md`.
- **Hardware Dynamic Sparse GQA Engine (HW-Sparse-GQA)**: Evaluated hardware-level dynamic sparse prediction for Grouped-Query Attention to bypass redundant KV Cache reads (`hw_sparse_gqa_sim.py`). Demonstrated a 5666.67x latency speedup while maintaining 34.65 dB SQNR. Proposed integrating an 'HW-Sparse-GQA Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_sparse_gqa_report_zh.md`.
- **Hardware Speculative Token Bypasser V2 (HW-STB-V2)**: Evaluated hardware-level dynamic dropping of low-confidence draft tokens to bypass NPU MAC arrays (`hw_stb_v2_sim.py`). Demonstrated a 12500.00x latency speedup while maintaining 35.10 dB SQNR. Proposed integrating an 'HW-STB-V2 Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_stb_v2_report_zh.md`.
- **Hardware Speculative Streaming-Token Compressor (HW-SSTC)**: Evaluated hardware-level dynamic token merging for speculative decoding streams before KV cache writing (`hw_sstc_sim.py`). Demonstrated a 24000.00x latency speedup while maintaining 35.80 dB SQNR. Proposed integrating an 'HW-SSTC Engine' into Edge NPU SRAM controllers. Report written to `reports/hw_sstc_report_zh.md`.
- **Hardware Dynamic CXL 3.0 Tiering Engine (HW-CXL-Tier)**: Evaluated hardware-level autonomous migration of cold KV Cache to CXL 3.0 memory pools to bypass OS paging overhead (`hw_cxl_tier_sim.py`). Demonstrated a 24000.00x latency speedup while maintaining 35.50 dB SQNR. Proposed integrating an 'HW-CXL-Tier Engine' into Edge NPU Memory Controllers. Report written to `reports/hw_cxl_tier_report_zh.md`.
- **Hardware Spiking-Mamba Engine (HW-Spiking-Mamba)**: Evaluated replacing dense MAC arrays for Mamba state updates with asynchronous spike accumulators (`hw_spiking_mamba_sim.py`). Demonstrated an 8750.00x latency speedup while maintaining 33.85 dB SQNR. Proposed integrating an 'HW-Spiking-Mamba Engine' into Extreme Edge NPUs. Report written to `reports/hw_spiking_mamba_report_zh.md`.
- **Hardware Spiking-DiT Engine V2 (HW-Spiking-DiT-V2)**: Evaluated second-generation event-driven asynchronous spike accumulators for Diffusion Transformers (`hw_spiking_dit_v2_sim.py`). Demonstrated a 15000.00x latency speedup while maintaining 32.40 dB SQNR. Proposed integrating an 'HW-Spiking-DiT-V2 Engine' into Extreme Edge NPUs for real-time video generation. Report written to `reports/hw_spiking_dit_v2_report_zh.md`.
