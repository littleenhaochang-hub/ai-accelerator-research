# AI Accelerator Architecture Auto-Research Report

## Executive Summary
Identified bottleneck: CPU-GPU memory transfers during MoE decoding.
Baseline prototype implemented simulating expert fetching overhead.

## Pillar Iterations
- **Test-Time Compute branching**: Explored hardware-software co-design optimizations.
- **RetNet/Mamba parallel scans**: Explored hardware-software co-design optimizations.
- **W4A4 QJL quantization**: Explored hardware-software co-design optimizations.
- **MoE prefetching**: Explored hardware-software co-design optimizations.
- **O(N) Linear Attention Hardware**: Evaluated hardware acceleration for feature-mapped linear attention (`linear_attention_sim.py`). Demonstrated a 256x speedup at 16K context. Proposed integrating a "KV State Accumulator Array" into Edge NPUs. Report written to `reports/linear_attention_report.md`.
- **PagedAttention Hardware MMU**: Evaluated memory fragmentation and capacity gains for paged KV caching (`paged_attention_sim.py`). Demonstrated a 4.0x memory efficiency gain by eliminating contiguous memory over-allocation. Proposed adding a Hardware MMU for Tensors to Edge NPUs. Report written to `reports/paged_attention_report.md`.
- **KV Cache Ring Attention**: Evaluated chiplet-to-chiplet KV block transfers for infinite context scaling (`ring_attention_sim.py`). Demonstrated complete hiding of inter-chiplet network latency behind compute. Proposed a D2D (Die-to-Die) Ring Interconnect and asynchronous DMA for Multi-Chiplet Edge NPUs to maintain 100% compute-bound execution. Report written to `reports/ring_attention_report.md`.
- **Speculative Decoding**: Explored hardware-software co-design optimizations.
- **On-Device PEFT (LoRA) Hardware**: Evaluated energy efficiency of in-SRAM gradient aggregation vs DRAM weight updates (`lora_peft_sim.py`). Demonstrated a 99.2x energy reduction for model fine-tuning. Proposed integrating an "In-SRAM Gradient Aggregator" into NPU schedulers for zero-DRAM-overhead personalized federated learning. Report written to `reports/lora_peft_report.md`.
- **LUT-based Sub-4-bit MACs**: Evaluated Look-Up Table (LUT) based multiplication for W4A4/W2A2 inference (`lut_mac_sim.py`). Demonstrated a 3.33x energy efficiency gain by replacing combinational multipliers with SRAM lookups. Proposed integrating reconfigurable Micro-SRAM LUTs into Tensor Cores for ultra-low power execution. Report written to `reports/lut_mac_report.md`.
- **Continuous Batching Hardware Scheduler**: Evaluated token-level dynamic request scheduling vs static batching (`continuous_batching_sim.py`). Demonstrated a 1.95x throughput speedup by eliminating padding bubbles. Proposed integrating a Hardware Context Switcher into Edge NPUs for zero-cycle token replacement. Report written to `reports/continuous_batching_report.md`.
- **FlashAttention-3**: Explored hardware-software co-design optimizations.
- **MoE Speculative Prefetching Hardware**: Evaluated demand-fetching versus predictive SRAM prefetching for MoE expert routing (`moe_prefetch_sim.py`). Demonstrated a 10.00x throughput speedup by overlapping DMA memory transfers with compute. Proposed integrating a "MoE Lookahead Prefetcher" into the NPU DMA controller. Report written to `reports/moe_prefetch_report.md`.
- **4-bit FFN Outlier Flattening**: Evaluated channel-wise affine smoothing (FlatQuant) for A4 INT4 quantization (`flatquant_sim.py`). Demonstrated SQNR improvements resolving catastrophic outlier quantization collapse without introducing sparsity logic. Proposed adding a Vector Scaling Unit before Tensor Cores. Report written to `reports/flatquant_report.md`.
- **Long Context Prefill OOM**: Evaluated Chained Householder Reflections for 4-bit KV Cache compression alongside Chunked Attention for 32K context lengths (`prefill_oom_sim.py`). Demonstrated a 75% reduction in KV Cache footprint and an 87.2% reduction in O(N^2) peak prefill memory. Proposed integrating a hardware-accelerated KV Cache Manager with Householder support. Report written to `reports/prefill_oom_report.md`.
- **Mamba/SSM Parallel Scans**: Evaluated hardware implementation of associative parallel prefix sum trees for Mamba state updates (`mamba_scan_sim.py`). Demonstrated a 315x latency reduction by converting O(N) sequential dependency into O(log N) tree depth. Proposed adding dedicated Associative Scan ALU Trees to Edge NPU designs. Report written to `reports/mamba_scan_report.md`.
- **W4A4 QJL Quantization Hardware**: Evaluated Quantized Johnson-Lindenstrauss projections for MAC reduction (`qjl_quant_sim.py`). Demonstrated catastrophic SQNR collapse (-6.35 dB) due to compounded projection and quantization errors. Concluded that hardware QJL units are unviable for INT4 LLMs; recommended focusing on non-dimensional-reduction techniques like FlatQuant. Report written to `reports/qjl_quant_report.md`.
- **Speculative Decoding Hardware Tree Verification**: Evaluated hardware acceleration for Tree Attention masks during speculative draft verification (`speculative_decoding_sim.py`). Demonstrated a 15.40x throughput speedup by converting memory-bound autoregressive decoding into a compute-bound tree evaluation. Proposed integrating a "Hardware Tree-Mask Generator" and "KV Cache Forking Engine" directly into the NPU. Report written to `reports/spec_decoding_report.md`.
- **Test-Time Compute Hardware Parallelism**: Evaluated power and latency for parallel rollouts (System 2 thinking) via weight broadcasting (`test_time_compute_sim.py`). Demonstrated a 7.14x energy efficiency gain and 9.09x latency speedup by running 16 rollouts on an 8B model instead of zero-shot on an 80B model. Proposed integrating a "Hardware Weight Broadcaster" into Edge NPUs for multi-path reasoning. Report written to `reports/test_time_compute_report.md`.
- **FlashAttention-3 Async TMA Hardware**: Evaluated asynchronous Tensor Memory Accelerators (TMA) and WGMMA instructions for fully overlapping SRAM memory loads with MAC execution (`fa3_sim.py`). Demonstrated a 1.99x speedup over synchronous FlashAttention-2 tiling. Proposed integrating a hardware "Async Tensor Memory Fetcher" with ping-pong buffering into the NPU. Report written to `reports/fa3_report.md`.
- **Dynamic Token Pruning Hardware**: Evaluated progressive token dropping (e.g. background patches in ViTs) across 32 layers (`token_pruning_sim.py`). Demonstrated a 1.45x compute speedup and 31% power reduction. Proposed integrating a "Hardware Token Compactor" into Edge NPUs to pack SRAM dynamically and eliminate Gather/Scatter software overhead. Report written to `reports/token_pruning_report.md`.
- **DiT Adaptive Global-Local Attention**: Evaluated hardware partitioning for high-resolution Diffusion Transformers (`dit_attention_sim.py`). Demonstrated a 16x compute speedup and 93.75% memory footprint reduction by splitting O(N^2) global attention into local windows and global routing tokens, allowing the attention matrix to fit entirely within Edge NPU SRAM. Proposed integrating a "Hardware Window Partitioner" and "Global Token Router" into the NPU. Report written to `reports/dit_attention_report.md`.
- **N:M Structured Sparsity Hardware**: Evaluated 2:4 structured sparsity to reduce MAC operations and memory bandwidth (`structured_sparsity_sim.py`). Demonstrated a 2.0x compute speedup with manageable 2MB metadata overhead per layer. Proposed integrating a "Sparse Tensor Core" with hardware metadata decoding into Edge NPUs to double inference throughput within strict thermal envelopes. Report written to `reports/structured_sparsity_report.md`.
- **Early-Exit Dynamic Depth Hardware**: Evaluated dynamic token routing where high-confidence tokens exit the compute pipeline at mid-layers (`early_exit_sim.py`). Demonstrated a 1.43x throughput speedup and 30% energy reduction by bypassing 50% of the layers for 60% of the tokens. Proposed integrating a "Lightweight Confidence Router" into NPU schedulers for zero-overhead early termination. Report written to `reports/early_exit_report.md`.
- **BitNet 1.58-bit Ternary Addition Hardware**: Evaluated replacing traditional INT8 MAC multipliers with ternary selectors (Mux/Inverters) and adders for BitNet b1.58 architectures (`bitnet_158_sim.py`). Demonstrated a 62.50% power reduction and a 2.67x improvement in silicon area efficiency by completely eliminating hardware multipliers. Proposed integrating "Ternary Addition Cores" into Extreme Edge NPUs for ultra-low power arithmetic. Report written to `reports/bitnet_158_report.md`.
- **DeepSeek MLA On-the-fly Projection Hardware**: Evaluated DeepSeek's Multi-Head Latent Attention for KV Cache memory bandwidth reduction (`mla_hardware_sim.py`). Demonstrated a 93.75% reduction in memory reads per step by storing only the latent vector. Proposed integrating an "On-the-fly Projection Engine" near the SRAM to instantly unroll latent vectors into K and V matrices without round-tripping to DRAM. Report written to `reports/mla_hardware_report.md`.
- **SRAM Compute-in-Memory (CIM)**: Evaluated analog in-memory computing for 4-bit MAC operations directly on SRAM bitlines (`cim_sram_sim.py`). Demonstrated a 91.30% energy reduction by eliminating weight data movement from SRAM to digital Tensor Cores. Proposed replacing standard SRAM blocks with "CIM-SRAM Macros" in Edge NPUs to achieve extreme power efficiency for battery-powered inference. Report written to `reports/cim_sram_report.md`.
- **Product Quantization KV Cache**: Evaluated Product Quantization (PQ) for extreme KV Cache compression (`product_quantization_kv_sim.py`). Demonstrated a 98.05% memory reduction but revealed severe LUT lookup latency bottlenecks in software. Proposed integrating dedicated "SRAM LUT Arrays" into the NPU memory controller for on-the-fly decompression. Report written to `reports/pq_kv_cache_report.md`.
- **OCP Microscaling Formats (MX4)**: Evaluated MX4 block-level shared exponent scaling for balancing memory bandwidth and dynamic range (`mx4_microscaling_sim.py`). Demonstrated a 73.44% memory reduction and 3.76x effective bandwidth speedup compared to FP16. Proposed integrating a "Shared-Exponent Aligner" directly before the Tensor Core MAC arrays for zero-stall block alignment. Report written to `reports/mx4_microscaling_report.md`.
- **Cross-Layer Attention (CLA) Hardware**: Evaluated Cross-Layer KV Cache sharing to combat long-context memory exhaustion (`cross_layer_kv_sim.py`). Demonstrated a 75.00% memory reduction and 3.38x bandwidth speedup by grouping 4 layers per KV cache. Proposed adding a "KV Route Multiplexer" and SRAM Broadcaster to Edge NPUs to handle cross-layer data routing. Report written to `reports/cross_layer_kv_report.md`.
- **FlashDecoding Hardware Reduction**: Evaluated parallelizing long-context GEMV operations across multiple compute units (`flash_decoding_sim.py`). Demonstrated a 31.98x decoding throughput speedup for 32K context lengths. Proposed integrating a "Global Reduction Network" into Edge NPUs to aggregate partial softmax sums across distributed SRAMs without DRAM round-tripping. Report written to `reports/flash_decoding_report.md`.
- **StreamingLLM Attention Sink Eviction**: Evaluated KV cache eviction utilizing attention sinks for infinite token generation (`streaming_llm_eviction_sim.py`). Demonstrated a 95.94% memory bandwidth reduction over 100K generated tokens. Proposed integrating an "SRAM Ring Buffer with Static Sink Roots" to physically manage stale token eviction without software intervention. Report written to `reports/streaming_llm_eviction_report.md`.
- **Jacobi/Lookahead Decoding Hardware**: Evaluated non-autoregressive parallel decoding without draft models (`jacobi_decoding_sim.py`). Demonstrated a performance regression (0.94x speedup) due to high iteration convergence overhead. Concluded that software-only Jacobi decoding is unviable for Edge NPUs. Proposed integrating "Multi-Token Dependency Forwarding" logic at the register level to mask iteration penalties. Report written to `reports/jacobi_decoding_report.md`.
- **GQA (Grouped-Query Attention) Hardware**: Evaluated SRAM read efficiency for GQA configurations (`gqa_hardware_sim.py`). Demonstrated a 75.00% KV memory reduction and 4.00x effective bandwidth speedup. Proposed integrating a "Query-Group Broadcaster" at the NPU SRAM interface to prevent duplicate KV reads by dynamically replicating fetched data to multiple query ALUs. Report written to `reports/gqa_hardware_report.md`.
- **MoE Expert-Choice Routing Hardware**: Evaluated hardware load balancing and MAC utilization for token routing strategies (`moe_routing_sim.py`). Demonstrated that Expert-Choice routing eliminates dropped tokens (from 51.8% to 0%) and boosts MAC utilization from 24.1% to 100.0%. Proposed integrating a "Global Top-K Sorting Network" directly into the NPU router to enable single-cycle Expert-Choice routing without software sorting overhead. Report written to `reports/moe_routing_report.md`.
- **Mamba-2 State Space Duality (SSD) Hardware**: Evaluated hardware mapping of SSM sequential scans to blocked matrix multiplications (`mamba2_ssd_sim.py`). Demonstrated a 221.41x latency speedup for 16K context lengths by utilizing MAC arrays. Concluded that Edge NPUs do not need custom scan ALUs; proposed firmware "Tensor Core SSD Mapping" to natively accelerate Mamba-2. Report written to `reports/mamba2_ssd_report.md`.
- **RoPE CORDIC Hardware Engine**: Evaluated the memory bandwidth and capacity overhead of Rotary Position Embeddings for 32K context (`rope_hardware_sim.py`). Demonstrated that an on-the-fly hardware CORDIC engine saves 536.87 MB of precomputed sin/cos cache and eliminates 3.6ms of memory-bound latency per layer. Proposed integrating a "CORDIC RoPE Engine" directly at the SRAM read port to compute rotations zero-cost during Q/K fetch. Report written to `reports/rope_hardware_report.md`.
- **SiLU/SwiGLU HW Approximation**: Evaluated Piecewise Linear (PWL) approximations for transcendental activation functions (`silu_approximation_sim.py`). Demonstrated a 90.00% reduction in activation power consumption while maintaining 24.49 dB SQNR. Proposed replacing precise exponential units with a "PWL/LUT Activation Engine" in Edge NPUs. Report written to `reports/silu_approximation_report.md`.
- **H2O Dynamic KV Eviction Hardware**: Evaluated cumulative attention score tracking for Heavy-Hitter token retention (`h2o_kv_eviction_sim.py`). Demonstrated a 78.44% memory bandwidth reduction by dynamically dropping low-attention tokens. Proposed integrating a "Cumulative Score Tracker" and "Dynamic Eviction Controller" to autonomously manage SRAM token overwrites with only 32KB overhead per head. Report written to `reports/h2o_kv_eviction_report.md`.
- **AWQ W4A16 On-the-fly Dequantization**: Evaluated the hardware overhead of W4A16 weight-only quantization (`awq_dequant_sim.py`). Demonstrated a 73.44% reduction in memory bandwidth (DRAM load time dropped from 140ms to 37ms for a 7B model). Proposed integrating an "On-the-fly Dequantization Pipeline (ODP)" directly between the SRAM read ports and MAC arrays to unroll INT4 to FP16 seamlessly without stalling compute. Report written to `reports/awq_dequant_report.md`.
- **KIVI 2-bit KV Cache Quantization**: Evaluated extreme 2-bit quantization for extreme long-context KV caches (`kivi_kv_sim.py`). Demonstrated an 81.25% memory reduction, shrinking a 32K context KV cache from 536MB to 100MB. Proposed integrating a "2-bit KV Decompressor" deeply within the Attention hardware block to instantly scale and reconstruct FP16 values during execution. Report written to `reports/kivi_kv_report.md`.
- **SpikeGPT / SNN Event-Driven Hardware**: Evaluated the energy efficiency of Spiking Neural Networks replacing MACs with Accumulate (AC) operations (`snn_spikegpt_sim.py`). Demonstrated a 99.00% reduction in compute energy at a 15% sparse firing rate. Proposed replacing dense systolic arrays with an "Asynchronous Spike Router" and "Add-only ALUs" for neuromorphic edge inference. Report written to `reports/snn_spikegpt_report.md`.
- **FP4 (E2M1) Micro-Floating Point Tensor Cores**: Evaluated FP4 (E2M1) against standard INT4 for weight quantization (`fp4_e2m1_sim.py`). Demonstrated superior SQNR (13.96 dB vs 13.43 dB) due to better logarithmic binning for Gaussian weights, alongside a 50.00% reduction in MAC dynamic energy. Proposed replacing 4x4 integer multipliers in Edge NPUs with "FP4 Micro-Exponents Adders & Tiny Mantissa Multipliers". Report written to `reports/fp4_e2m1_report.md`.
- **Radix Tree Prefix Caching Hardware MMU**: Evaluated shared KV cache across concurrent requests (`radix_prefix_caching_sim.py`). Demonstrated a 72.00% reduction in memory capacity and a 2.78x speedup in Prefill latency for overlapping system prompts. Proposed integrating a "Hardware Page Table Walker" into the NPU MMU to enable zero-latency virtual-to-physical token resolution for Radix Trees. Report written to `reports/radix_prefix_caching_report.md`.
- **SVD Low-Rank KV Cache Compression**: Evaluated extreme memory compression using Singular Value Decomposition for long-context KV caches (`svd_kv_compression_sim.py`). Demonstrated a 95.31% memory capacity reduction and 7.00x overall speedup for a rank-128 approximation. Proposed integrating a dedicated "Low-Rank Tensor Reconstructor" near the memory bus to perform on-the-fly matrix reconstruction without hijacking the primary MAC array. Report written to `reports/svd_kv_compression_report.md`.
- **Mixture-of-Depths (MoD) Hardware Routing**: Evaluated dynamic token bypassing across layers (`mod_routing_sim.py`). Demonstrated a 25.00% reduction in total compute MACs at a 50% capacity factor for half the layers. Proposed integrating a "Token Bypasser & Router" logic inside inter-layer SRAM buffers to mask inactive tokens without causing memory fragmentation overhead. Report written to `reports/mod_routing_report.md`.
- **LPDDR5X vs HBM3 Edge Architecture Analysis**: Evaluated energy per bit and memory bandwidth tradeoffs for 70B model inference (`hbm_lpddr_sim.py`). Demonstrated that LPDDR5X saves 42.86% of DRAM power compared to HBM3 but suffers a 7.5x bandwidth gap (dropping TPS from 85 to 11). Concluded that Edge NPUs must compensate for LPDDR's low bandwidth natively using sub-3-bit hardware decompression and vastly larger SRAM allocations. Report written to `reports/hbm_lpddr_report.md`.
- **MoE Flash Offloading Hardware**: Evaluated UFS 4.0 vs DRAM memory bandwidth latency for MoE expert fetching (`moe_flash_offload_sim.py`). Demonstrated that direct UFS 4.0 reading yields catastrophic latency (1718.75 ms/token, <1 TPS) due to 7GB/token I/O requirements. Proposed integrating an "Asynchronous Lookahead Prefetcher" and LFU Expert Cache to hide flash read latency behind compute. Report written to `reports/moe_flash_offload_report.md`.
- **Vector Quantization (VQ) KV Cache**: Evaluated VQ vs FP16 for 32K long-context KV cache memory reduction (`vq_kv_cache_sim.py`). Demonstrated an 8.0x reduction in memory capacity (4GB to 512MB) and overall SRAM latency drop from 2000us to 786us. Proposed integrating "SRAM Codebook Decoder Arrays" directly adjacent to MAC arrays to mask LUT look-up bottlenecks. Report written to `reports/vq_kv_cache_report.md`.
- **Token Merging (ToMe) Hardware**: Evaluated progressive token reduction across 32 transformer layers (`token_merging_sim.py`). Demonstrated a 1.68x compute speedup by merging 64 tokens per layer. Proposed integrating a "Hardware Bipartite Matching Engine" into Edge NPUs to compute cosine similarities and aggregate tokens with zero overhead. Report written to `reports/token_merging_report.md`.
- **LSH Sparse Attention Hardware**: Evaluated Locality Sensitive Hashing (LSH) for 32K context sparse attention (`lsh_attention_sim.py`). Demonstrated a 21.33x reduction in MAC operations by binning tokens into 128 hash buckets. However, random memory access patterns severely degrade SRAM locality. Proposed integrating a "Hardware Scatter/Gather Engine" into Edge NPUs to coalesce scattered token reads and restore memory bandwidth utilization. Report written to `reports/lsh_attention_report.md`.
- **NormalFloat4 (NF4) Dequantization**: Evaluated NF4 non-linear 4-bit quantization for high-SQNR edge inference (`nf4_dequantization_sim.py`). Demonstrated a 4.0x bandwidth reduction (500us to 125us fetch latency) with minimal 5.37us LUT decoding overhead. Proposed integrating a "Fused NF4 SRAM Decompressor" directly at the memory read port for zero-cycle hardware dequantization. Report written to `reports/nf4_dequantization_report.md`.
- **Activation Sparsity Zero-Skipping Hardware**: Evaluated dynamic zero-skipping in FFN down-projection (`activation_sparsity_sim.py`). Demonstrated a 2.50x compute speedup by skipping 60% of zero-valued SwiGLU activations. Proposed integrating an "Asynchronous Zero-Skipping Controller" with a Bitmask FIFO to dynamically compress the activation stream and prevent pipeline bubbles in dense MAC arrays. Report written to `reports/activation_sparsity_report.md`.
- **FP8 vs INT8 Edge MAC Hardware**: Evaluated silicon area and power overhead for native FP8 (E4M3) execution compared to INT8 (`fp8_vs_int8_mac_sim.py`). Demonstrated that while FP8 improves dynamic range, it incurs a 56% area penalty and 75% power penalty due to mantissa alignment shifters. Concluded that native FP8 is unviable for extreme edge NPUs; proposed adopting OCP Microscaling (MX) with block-shared exponents to revert MACs to pure integer arithmetic. Report written to `reports/fp8_vs_int8_report.md`.
- **AQLM 3-bit Additive Quantization**: Evaluated multi-codebook vector addition for 3-bit weight decompression (`aqlm_hardware_sim.py`). Demonstrated a 25% memory bandwidth reduction compared to INT4, with a 96.43us total fetch and decode latency. Proposed integrating an "Additive LUT Engine" with hardware adder trees at the SRAM interface to reconstruct weights dynamically with zero-cycle overhead. Report written to `reports/aqlm_hardware_report.md`.
- **KAN vs MLP Hardware Efficiency**: Evaluated Kolmogorov-Arnold Networks (KAN) spline-based edge computations against traditional MLPs (`kan_vs_mlp_sim.py`). Demonstrated a 6.0x increase in memory footprint and 3.0x increase in compute MACs for a grid size of 5. Concluded that KANs are highly memory-bound and currently unviable for Edge NPUs without dedicated spline compression hardware. Report written to `reports/kan_vs_mlp_report.md`.
- **Medusa Speculative Decoding Hardware**: Evaluated memory overhead and TPS gains for parallel readout heads (`medusa_hardware_sim.py`). Demonstrated a 1.90x effective speedup in decoding at the cost of 1.1GB memory overhead for 4 FP16 Medusa heads. Proposed integrating a "Parallel Tree-Mask Verifier" into the Attention block and applying extreme quantization to the readout heads to fit within edge SRAM constraints. Report written to `reports/medusa_hardware_report.md`.
