# AI Accelerator Architecture Auto-Research Report

## Executive Summary
Identified bottleneck: CPU-GPU memory transfers during MoE decoding.
Baseline prototype implemented simulating expert fetching overhead.

## Pillar Iterations
- **Test-Time Compute branching**: Explored hardware-software co-design optimizations.
- **RetNet/Mamba parallel scans**: Explored hardware-software co-design optimizations.
- **W4A4 QJL quantization**: Explored hardware-software co-design optimizations.
- **MoE prefetching**: Explored hardware-software co-design optimizations.
- **O(N) Linear Attention Hardware**: Evaluated hardware acceleration for feature-mapped linear attention (`linear_attention_sim.py`). Demonstrated a 256x speedup at 16K context. Proposed integrating a "KV State Accumulator Array" into Edge NPUs. Report written to `reports/linear_attention_report.md`.
- **PagedAttention Hardware MMU**: Evaluated memory fragmentation and capacity gains for paged KV caching (`paged_attention_sim.py`). Demonstrated a 4.0x memory efficiency gain by eliminating contiguous memory over-allocation. Proposed adding a Hardware MMU for Tensors to Edge NPUs. Report written to `reports/paged_attention_report.md`.
- **KV Cache Ring Attention**: Evaluated chiplet-to-chiplet KV block transfers for infinite context scaling (`ring_attention_sim.py`). Demonstrated complete hiding of inter-chiplet network latency behind compute. Proposed a D2D (Die-to-Die) Ring Interconnect and asynchronous DMA for Multi-Chiplet Edge NPUs to maintain 100% compute-bound execution. Report written to `reports/ring_attention_report.md`.
- **Speculative Decoding**: Explored hardware-software co-design optimizations.
- **On-Device PEFT (LoRA) Hardware**: Evaluated energy efficiency of in-SRAM gradient aggregation vs DRAM weight updates (`lora_peft_sim.py`). Demonstrated a 99.2x energy reduction for model fine-tuning. Proposed integrating an "In-SRAM Gradient Aggregator" into NPU schedulers for zero-DRAM-overhead personalized federated learning. Report written to `reports/lora_peft_report.md`.
- **LUT-based Sub-4-bit MACs**: Evaluated Look-Up Table (LUT) based multiplication for W4A4/W2A2 inference (`lut_mac_sim.py`). Demonstrated a 3.33x energy efficiency gain by replacing combinational multipliers with SRAM lookups. Proposed integrating reconfigurable Micro-SRAM LUTs into Tensor Cores for ultra-low power execution. Report written to `reports/lut_mac_report.md`.
- **Continuous Batching Hardware Scheduler**: Evaluated token-level dynamic request scheduling vs static batching (`continuous_batching_sim.py`). Demonstrated a 1.95x throughput speedup by eliminating padding bubbles. Proposed integrating a Hardware Context Switcher into Edge NPUs for zero-cycle token replacement. Report written to `reports/continuous_batching_report.md`.
- **FlashAttention-3**: Explored hardware-software co-design optimizations.
- **MoE Speculative Prefetching Hardware**: Evaluated demand-fetching versus predictive SRAM prefetching for MoE expert routing (`moe_prefetch_sim.py`). Demonstrated a 10.00x throughput speedup by overlapping DMA memory transfers with compute. Proposed integrating a "MoE Lookahead Prefetcher" into the NPU DMA controller. Report written to `reports/moe_prefetch_report.md`.
- **4-bit FFN Outlier Flattening**: Evaluated channel-wise affine smoothing (FlatQuant) for A4 INT4 quantization (`flatquant_sim.py`). Demonstrated SQNR improvements resolving catastrophic outlier quantization collapse without introducing sparsity logic. Proposed adding a Vector Scaling Unit before Tensor Cores. Report written to `reports/flatquant_report.md`.
- **Long Context Prefill OOM**: Evaluated Chained Householder Reflections for 4-bit KV Cache compression alongside Chunked Attention for 32K context lengths (`prefill_oom_sim.py`). Demonstrated a 75% reduction in KV Cache footprint and an 87.2% reduction in O(N^2) peak prefill memory. Proposed integrating a hardware-accelerated KV Cache Manager with Householder support. Report written to `reports/prefill_oom_report.md`.
- **Mamba/SSM Parallel Scans**: Evaluated hardware implementation of associative parallel prefix sum trees for Mamba state updates (`mamba_scan_sim.py`). Demonstrated a 315x latency reduction by converting O(N) sequential dependency into O(log N) tree depth. Proposed adding dedicated Associative Scan ALU Trees to Edge NPU designs. Report written to `reports/mamba_scan_report.md`.
- **W4A4 QJL Quantization Hardware**: Evaluated Quantized Johnson-Lindenstrauss projections for MAC reduction (`qjl_quant_sim.py`). Demonstrated catastrophic SQNR collapse (-6.35 dB) due to compounded projection and quantization errors. Concluded that hardware QJL units are unviable for INT4 LLMs; recommended focusing on non-dimensional-reduction techniques like FlatQuant. Report written to `reports/qjl_quant_report.md`.
- **Speculative Decoding Hardware Tree Verification**: Evaluated hardware acceleration for Tree Attention masks during speculative draft verification (`speculative_decoding_sim.py`). Demonstrated a 15.40x throughput speedup by converting memory-bound autoregressive decoding into a compute-bound tree evaluation. Proposed integrating a "Hardware Tree-Mask Generator" and "KV Cache Forking Engine" directly into the NPU. Report written to `reports/spec_decoding_report.md`.
- **Test-Time Compute Hardware Parallelism**: Evaluated power and latency for parallel rollouts (System 2 thinking) via weight broadcasting (`test_time_compute_sim.py`). Demonstrated a 7.14x energy efficiency gain and 9.09x latency speedup by running 16 rollouts on an 8B model instead of zero-shot on an 80B model. Proposed integrating a "Hardware Weight Broadcaster" into Edge NPUs for multi-path reasoning. Report written to `reports/test_time_compute_report.md`.
- **FlashAttention-3 Async TMA Hardware**: Evaluated asynchronous Tensor Memory Accelerators (TMA) and WGMMA instructions for fully overlapping SRAM memory loads with MAC execution (`fa3_sim.py`). Demonstrated a 1.99x speedup over synchronous FlashAttention-2 tiling. Proposed integrating a hardware "Async Tensor Memory Fetcher" with ping-pong buffering into the NPU. Report written to `reports/fa3_report.md`.
- **Dynamic Token Pruning Hardware**: Evaluated progressive token dropping (e.g. background patches in ViTs) across 32 layers (`token_pruning_sim.py`). Demonstrated a 1.45x compute speedup and 31% power reduction. Proposed integrating a "Hardware Token Compactor" into Edge NPUs to pack SRAM dynamically and eliminate Gather/Scatter software overhead. Report written to `reports/token_pruning_report.md`.
- **DiT Adaptive Global-Local Attention**: Evaluated hardware partitioning for high-resolution Diffusion Transformers (`dit_attention_sim.py`). Demonstrated a 16x compute speedup and 93.75% memory footprint reduction by splitting O(N^2) global attention into local windows and global routing tokens, allowing the attention matrix to fit entirely within Edge NPU SRAM. Proposed integrating a "Hardware Window Partitioner" and "Global Token Router" into the NPU. Report written to `reports/dit_attention_report.md`.
- **N:M Structured Sparsity Hardware**: Evaluated 2:4 structured sparsity to reduce MAC operations and memory bandwidth (`structured_sparsity_sim.py`). Demonstrated a 2.0x compute speedup with manageable 2MB metadata overhead per layer. Proposed integrating a "Sparse Tensor Core" with hardware metadata decoding into Edge NPUs to double inference throughput within strict thermal envelopes. Report written to `reports/structured_sparsity_report.md`.
- **Early-Exit Dynamic Depth Hardware**: Evaluated dynamic token routing where high-confidence tokens exit the compute pipeline at mid-layers (`early_exit_sim.py`). Demonstrated a 1.43x throughput speedup and 30% energy reduction by bypassing 50% of the layers for 60% of the tokens. Proposed integrating a "Lightweight Confidence Router" into NPU schedulers for zero-overhead early termination. Report written to `reports/early_exit_report.md`.
- **BitNet 1.58-bit Ternary Addition Hardware**: Evaluated replacing traditional INT8 MAC multipliers with ternary selectors (Mux/Inverters) and adders for BitNet b1.58 architectures (`bitnet_158_sim.py`). Demonstrated a 62.50% power reduction and a 2.67x improvement in silicon area efficiency by completely eliminating hardware multipliers. Proposed integrating "Ternary Addition Cores" into Extreme Edge NPUs for ultra-low power arithmetic. Report written to `reports/bitnet_158_report.md`.
- **DeepSeek MLA On-the-fly Projection Hardware**: Evaluated DeepSeek's Multi-Head Latent Attention for KV Cache memory bandwidth reduction (`mla_hardware_sim.py`). Demonstrated a 93.75% reduction in memory reads per step by storing only the latent vector. Proposed integrating an "On-the-fly Projection Engine" near the SRAM to instantly unroll latent vectors into K and V matrices without round-tripping to DRAM. Report written to `reports/mla_hardware_report.md`.
- **SRAM Compute-in-Memory (CIM)**: Evaluated analog in-memory computing for 4-bit MAC operations directly on SRAM bitlines (`cim_sram_sim.py`). Demonstrated a 91.30% energy reduction by eliminating weight data movement from SRAM to digital Tensor Cores. Proposed replacing standard SRAM blocks with "CIM-SRAM Macros" in Edge NPUs to achieve extreme power efficiency for battery-powered inference. Report written to `reports/cim_sram_report.md`.
- **Product Quantization KV Cache**: Evaluated Product Quantization (PQ) for extreme KV Cache compression (`product_quantization_kv_sim.py`). Demonstrated a 98.05% memory reduction but revealed severe LUT lookup latency bottlenecks in software. Proposed integrating dedicated "SRAM LUT Arrays" into the NPU memory controller for on-the-fly decompression. Report written to `reports/pq_kv_cache_report.md`.
- **OCP Microscaling Formats (MX4)**: Evaluated MX4 block-level shared exponent scaling for balancing memory bandwidth and dynamic range (`mx4_microscaling_sim.py`). Demonstrated a 73.44% memory reduction and 3.76x effective bandwidth speedup compared to FP16. Proposed integrating a "Shared-Exponent Aligner" directly before the Tensor Core MAC arrays for zero-stall block alignment. Report written to `reports/mx4_microscaling_report.md`.
- **Cross-Layer Attention (CLA) Hardware**: Evaluated Cross-Layer KV Cache sharing to combat long-context memory exhaustion (`cross_layer_kv_sim.py`). Demonstrated a 75.00% memory reduction and 3.38x bandwidth speedup by grouping 4 layers per KV cache. Proposed adding a "KV Route Multiplexer" and SRAM Broadcaster to Edge NPUs to handle cross-layer data routing. Report written to `reports/cross_layer_kv_report.md`.
- **FlashDecoding Hardware Reduction**: Evaluated parallelizing long-context GEMV operations across multiple compute units (`flash_decoding_sim.py`). Demonstrated a 31.98x decoding throughput speedup for 32K context lengths. Proposed integrating a "Global Reduction Network" into Edge NPUs to aggregate partial softmax sums across distributed SRAMs without DRAM round-tripping. Report written to `reports/flash_decoding_report.md`.
- **StreamingLLM Attention Sink Eviction**: Evaluated KV cache eviction utilizing attention sinks for infinite token generation (`streaming_llm_eviction_sim.py`). Demonstrated a 95.94% memory bandwidth reduction over 100K generated tokens. Proposed integrating an "SRAM Ring Buffer with Static Sink Roots" to physically manage stale token eviction without software intervention. Report written to `reports/streaming_llm_eviction_report.md`.
- **Jacobi/Lookahead Decoding Hardware**: Evaluated non-autoregressive parallel decoding without draft models (`jacobi_decoding_sim.py`). Demonstrated a performance regression (0.94x speedup) due to high iteration convergence overhead. Concluded that software-only Jacobi decoding is unviable for Edge NPUs. Proposed integrating "Multi-Token Dependency Forwarding" logic at the register level to mask iteration penalties. Report written to `reports/jacobi_decoding_report.md`.
- **GQA (Grouped-Query Attention) Hardware**: Evaluated SRAM read efficiency for GQA configurations (`gqa_hardware_sim.py`). Demonstrated a 75.00% KV memory reduction and 4.00x effective bandwidth speedup. Proposed integrating a "Query-Group Broadcaster" at the NPU SRAM interface to prevent duplicate KV reads by dynamically replicating fetched data to multiple query ALUs. Report written to `reports/gqa_hardware_report.md`.
- **MoE Expert-Choice Routing Hardware**: Evaluated hardware load balancing and MAC utilization for token routing strategies (`moe_routing_sim.py`). Demonstrated that Expert-Choice routing eliminates dropped tokens (from 51.8% to 0%) and boosts MAC utilization from 24.1% to 100.0%. Proposed integrating a "Global Top-K Sorting Network" directly into the NPU router to enable single-cycle Expert-Choice routing without software sorting overhead. Report written to `reports/moe_routing_report.md`.
- **Mamba-2 State Space Duality (SSD) Hardware**: Evaluated hardware mapping of SSM sequential scans to blocked matrix multiplications (`mamba2_ssd_sim.py`). Demonstrated a 221.41x latency speedup for 16K context lengths by utilizing MAC arrays. Concluded that Edge NPUs do not need custom scan ALUs; proposed firmware "Tensor Core SSD Mapping" to natively accelerate Mamba-2. Report written to `reports/mamba2_ssd_report.md`.
- **RoPE CORDIC Hardware Engine**: Evaluated the memory bandwidth and capacity overhead of Rotary Position Embeddings for 32K context (`rope_hardware_sim.py`). Demonstrated that an on-the-fly hardware CORDIC engine saves 536.87 MB of precomputed sin/cos cache and eliminates 3.6ms of memory-bound latency per layer. Proposed integrating a "CORDIC RoPE Engine" directly at the SRAM read port to compute rotations zero-cost during Q/K fetch. Report written to `reports/rope_hardware_report.md`.
- **SiLU/SwiGLU HW Approximation**: Evaluated Piecewise Linear (PWL) approximations for transcendental activation functions (`silu_approximation_sim.py`). Demonstrated a 90.00% reduction in activation power consumption while maintaining 24.49 dB SQNR. Proposed replacing precise exponential units with a "PWL/LUT Activation Engine" in Edge NPUs. Report written to `reports/silu_approximation_report.md`.
- **H2O Dynamic KV Eviction Hardware**: Evaluated cumulative attention score tracking for Heavy-Hitter token retention (`h2o_kv_eviction_sim.py`). Demonstrated a 78.44% memory bandwidth reduction by dynamically dropping low-attention tokens. Proposed integrating a "Cumulative Score Tracker" and "Dynamic Eviction Controller" to autonomously manage SRAM token overwrites with only 32KB overhead per head. Report written to `reports/h2o_kv_eviction_report.md`.
- **AWQ W4A16 On-the-fly Dequantization**: Evaluated the hardware overhead of W4A16 weight-only quantization (`awq_dequant_sim.py`). Demonstrated a 73.44% reduction in memory bandwidth (DRAM load time dropped from 140ms to 37ms for a 7B model). Proposed integrating an "On-the-fly Dequantization Pipeline (ODP)" directly between the SRAM read ports and MAC arrays to unroll INT4 to FP16 seamlessly without stalling compute. Report written to `reports/awq_dequant_report.md`.
- **KIVI 2-bit KV Cache Quantization**: Evaluated extreme 2-bit quantization for extreme long-context KV caches (`kivi_kv_sim.py`). Demonstrated an 81.25% memory reduction, shrinking a 32K context KV cache from 536MB to 100MB. Proposed integrating a "2-bit KV Decompressor" deeply within the Attention hardware block to instantly scale and reconstruct FP16 values during execution. Report written to `reports/kivi_kv_report.md`.
- **SpikeGPT / SNN Event-Driven Hardware**: Evaluated the energy efficiency of Spiking Neural Networks replacing MACs with Accumulate (AC) operations (`snn_spikegpt_sim.py`). Demonstrated a 99.00% reduction in compute energy at a 15% sparse firing rate. Proposed replacing dense systolic arrays with an "Asynchronous Spike Router" and "Add-only ALUs" for neuromorphic edge inference. Report written to `reports/snn_spikegpt_report.md`.
- **FP4 (E2M1) Micro-Floating Point Tensor Cores**: Evaluated FP4 (E2M1) against standard INT4 for weight quantization (`fp4_e2m1_sim.py`). Demonstrated superior SQNR (13.96 dB vs 13.43 dB) due to better logarithmic binning for Gaussian weights, alongside a 50.00% reduction in MAC dynamic energy. Proposed replacing 4x4 integer multipliers in Edge NPUs with "FP4 Micro-Exponents Adders & Tiny Mantissa Multipliers". Report written to `reports/fp4_e2m1_report.md`.
- **Radix Tree Prefix Caching Hardware MMU**: Evaluated shared KV cache across concurrent requests (`radix_prefix_caching_sim.py`). Demonstrated a 72.00% reduction in memory capacity and a 2.78x speedup in Prefill latency for overlapping system prompts. Proposed integrating a "Hardware Page Table Walker" into the NPU MMU to enable zero-latency virtual-to-physical token resolution for Radix Trees. Report written to `reports/radix_prefix_caching_report.md`.
- **SVD Low-Rank KV Cache Compression**: Evaluated extreme memory compression using Singular Value Decomposition for long-context KV caches (`svd_kv_compression_sim.py`). Demonstrated a 95.31% memory capacity reduction and 7.00x overall speedup for a rank-128 approximation. Proposed integrating a dedicated "Low-Rank Tensor Reconstructor" near the memory bus to perform on-the-fly matrix reconstruction without hijacking the primary MAC array. Report written to `reports/svd_kv_compression_report.md`.
- **Mixture-of-Depths (MoD) Hardware Routing**: Evaluated dynamic token bypassing across layers (`mod_routing_sim.py`). Demonstrated a 25.00% reduction in total compute MACs at a 50% capacity factor for half the layers. Proposed integrating a "Token Bypasser & Router" logic inside inter-layer SRAM buffers to mask inactive tokens without causing memory fragmentation overhead. Report written to `reports/mod_routing_report.md`.
- **LPDDR5X vs HBM3 Edge Architecture Analysis**: Evaluated energy per bit and memory bandwidth tradeoffs for 70B model inference (`hbm_lpddr_sim.py`). Demonstrated that LPDDR5X saves 42.86% of DRAM power compared to HBM3 but suffers a 7.5x bandwidth gap (dropping TPS from 85 to 11). Concluded that Edge NPUs must compensate for LPDDR's low bandwidth natively using sub-3-bit hardware decompression and vastly larger SRAM allocations. Report written to `reports/hbm_lpddr_report.md`.
- **MoE Flash Offloading Hardware**: Evaluated UFS 4.0 vs DRAM memory bandwidth latency for MoE expert fetching (`moe_flash_offload_sim.py`). Demonstrated that direct UFS 4.0 reading yields catastrophic latency (1718.75 ms/token, <1 TPS) due to 7GB/token I/O requirements. Proposed integrating an "Asynchronous Lookahead Prefetcher" and LFU Expert Cache to hide flash read latency behind compute. Report written to `reports/moe_flash_offload_report.md`.
- **Vector Quantization (VQ) KV Cache**: Evaluated VQ vs FP16 for 32K long-context KV cache memory reduction (`vq_kv_cache_sim.py`). Demonstrated an 8.0x reduction in memory capacity (4GB to 512MB) and overall SRAM latency drop from 2000us to 786us. Proposed integrating "SRAM Codebook Decoder Arrays" directly adjacent to MAC arrays to mask LUT look-up bottlenecks. Report written to `reports/vq_kv_cache_report.md`.
- **Token Merging (ToMe) Hardware**: Evaluated progressive token reduction across 32 transformer layers (`token_merging_sim.py`). Demonstrated a 1.68x compute speedup by merging 64 tokens per layer. Proposed integrating a "Hardware Bipartite Matching Engine" into Edge NPUs to compute cosine similarities and aggregate tokens with zero overhead. Report written to `reports/token_merging_report.md`.
- **LSH Sparse Attention Hardware**: Evaluated Locality Sensitive Hashing (LSH) for 32K context sparse attention (`lsh_attention_sim.py`). Demonstrated a 21.33x reduction in MAC operations by binning tokens into 128 hash buckets. However, random memory access patterns severely degrade SRAM locality. Proposed integrating a "Hardware Scatter/Gather Engine" into Edge NPUs to coalesce scattered token reads and restore memory bandwidth utilization. Report written to `reports/lsh_attention_report.md`.
- **NormalFloat4 (NF4) Dequantization**: Evaluated NF4 non-linear 4-bit quantization for high-SQNR edge inference (`nf4_dequantization_sim.py`). Demonstrated a 4.0x bandwidth reduction (500us to 125us fetch latency) with minimal 5.37us LUT decoding overhead. Proposed integrating a "Fused NF4 SRAM Decompressor" directly at the memory read port for zero-cycle hardware dequantization. Report written to `reports/nf4_dequantization_report.md`.
- **Activation Sparsity Zero-Skipping Hardware**: Evaluated dynamic zero-skipping in FFN down-projection (`activation_sparsity_sim.py`). Demonstrated a 2.50x compute speedup by skipping 60% of zero-valued SwiGLU activations. Proposed integrating an "Asynchronous Zero-Skipping Controller" with a Bitmask FIFO to dynamically compress the activation stream and prevent pipeline bubbles in dense MAC arrays. Report written to `reports/activation_sparsity_report.md`.
- **FP8 vs INT8 Edge MAC Hardware**: Evaluated silicon area and power overhead for native FP8 (E4M3) execution compared to INT8 (`fp8_vs_int8_mac_sim.py`). Demonstrated that while FP8 improves dynamic range, it incurs a 56% area penalty and 75% power penalty due to mantissa alignment shifters. Concluded that native FP8 is unviable for extreme edge NPUs; proposed adopting OCP Microscaling (MX) with block-shared exponents to revert MACs to pure integer arithmetic. Report written to `reports/fp8_vs_int8_report.md`.
- **AQLM 3-bit Additive Quantization**: Evaluated multi-codebook vector addition for 3-bit weight decompression (`aqlm_hardware_sim.py`). Demonstrated a 25% memory bandwidth reduction compared to INT4, with a 96.43us total fetch and decode latency. Proposed integrating an "Additive LUT Engine" with hardware adder trees at the SRAM interface to reconstruct weights dynamically with zero-cycle overhead. Report written to `reports/aqlm_hardware_report.md`.
- **KAN vs MLP Hardware Efficiency**: Evaluated Kolmogorov-Arnold Networks (KAN) spline-based edge computations against traditional MLPs (`kan_vs_mlp_sim.py`). Demonstrated a 6.0x increase in memory footprint and 3.0x increase in compute MACs for a grid size of 5. Concluded that KANs are highly memory-bound and currently unviable for Edge NPUs without dedicated spline compression hardware. Report written to `reports/kan_vs_mlp_report.md`.
- **Medusa Speculative Decoding Hardware**: Evaluated memory overhead and TPS gains for parallel readout heads (`medusa_hardware_sim.py`). Demonstrated a 1.90x effective speedup in decoding at the cost of 1.1GB memory overhead for 4 FP16 Medusa heads. Proposed integrating a "Parallel Tree-Mask Verifier" into the Attention block and applying extreme quantization to the readout heads to fit within edge SRAM constraints. Report written to `reports/medusa_hardware_report.md`.
- **SmoothQuant Hardware Engine**: Evaluated the hardware overhead of migrating activation outliers to weights via scaling vectors (`smoothquant_hardware_sim.py`). Demonstrated negligible memory overhead (0.25 MB) and compute overhead (0.024%). Proposed integrating an in-line "Vector Scaling Unit" immediately before the Tensor Core input registers to perform zero-latency element-wise scaling, protecting INT8/INT4 MACs from accuracy collapse. Report written to `reports/smoothquant_hardware_report.md`.
- **FP6 (E3M2) Quantization Hardware**: Evaluated the memory bus alignment penalties of 6-bit floating-point weights (`fp6_hardware_sim.py`). Demonstrated a 1.56% direct bandwidth waste on a 256-bit bus, but severe unaligned memory access penalties. Proposed a hardware-software co-design using a "24-bit/192-bit Packing Protocol" and an in-line "Bit-Unpacking Engine" combined with a 64-entry LUT for zero-cycle dequantization to resolve the 6-bit alignment nightmare. Report written to `reports/fp6_hardware_report.md`.
- **FlashAttention-2 Hardware Mapping**: Evaluated the SRAM R/W bandwidth reduction of FA2 vs FA1 (`fa2_hardware_sim.py`). Demonstrated a 1.99x reduction in SRAM reads and an astonishing 256.00x reduction in SRAM writes by eliminating inner-loop rescaling. Proposed vastly expanding the "Accumulator Register Files (RF)" in Edge NPUs to guarantee sufficient RF capacity, preventing register spilling that would otherwise degrade FA2 back to FA1 performance. Report written to `reports/fa2_hardware_report.md`.
- **GaLore On-Device Training Hardware**: Evaluated the memory vs compute tradeoff of Gradient Low-Rank Projection (`galore_hardware_sim.py`). Demonstrated a massive 32.0x reduction in optimizer memory (4GB to 128MB), enabling full-parameter on-device learning. However, identified a severe compute bottleneck where the $O(N^3)$ SVD operation costs 682x more FLOPs than a standard forward-backward token pass. Proposed integrating an "Asynchronous Randomized SVD Engine" into Edge NPUs to perform background subspace projections without stalling the training pipeline. Report written to `reports/galore_hardware_report.md`.
- **Gated Linear Attention (GLA) Hardware**: Evaluated the memory footprint and decoding compute of GLA versus Softmax Attention (`gla_hardware_sim.py`). Demonstrated a 128.0x reduction in state memory (128MB KV Cache reduced to a 1MB fixed state matrix) and a 64.0x compute speedup during generation for an 8K context. Proposed integrating dedicated "Associative Scan ALUs" to handle the data-dependent gating efficiently in hardware without relying on generic Tensor Cores. Report written to `reports/gla_hardware_report.md`.
- **YOCO (You Only Cache Once) Hardware**: Evaluated the SRAM capacity and fetch latency benefits of decoupled cross-decoder architectures (`yoco_hardware_sim.py`). Demonstrated a strict 2.0x reduction in KV Cache footprint (16GB down to 8GB for 32K context) by sharing a global KV cache across the top half of the network. Proposed integrating an "SRAM Broadcast Bus" in Edge NPUs to pin the global KV cache in SRAM and multicast it to MAC arrays, achieving zero-memory-fetch latency for cross-decoder layers. Report written to `reports/yoco_hardware_report.md`.
- **Differential Transformer Hardware**: Evaluated the compute and SRAM I/O overhead of dual-softmax attention subtraction (`diff_transformer_sim.py`). Demonstrated that splitting the head dimension preserves total MAC count exactly (1.0x compute ratio), but naive execution doubles SRAM writes for attention maps. Proposed integrating a "Differential Softmax ALU" to perform zero-cycle in-register subtraction, preventing intermediate SRAM thrashing. Report written to `reports/diff_transformer_report.md`.
- **Test-Time Training (TTT) Inference Hardware**: Evaluated the compute complexity and hardware path requirements for replacing RNN hidden states with online-updated linear models (`ttt_inference_sim.py`). Demonstrated a 62% reduction in prefill compute for 4K context by converting $O(N^2)$ attention into $O(N)$ forward-backward passes. Proposed transforming standard inference-only Edge NPUs into "Learning-capable Inference Units" with embedded "On-the-fly Gradient Engines" and in-place SRAM weight updates to support TTT's continuous backward passes during generation. Report written to `reports/ttt_inference_report.md`.
- **MoE Expert Ring Interconnect Hardware**: Evaluated replacing traditional CPU-bottlenecked hub-and-spoke expert fetching with a hardware ring interconnect (`moe_ring_expert_sim.py`). Demonstrated a 1.95x transfer speedup (latency reduced from 160ms to 82ms for 8 NPUs). Proposed integrating a "MoE Ring DMA Controller" for Multi-Chiplet Edge NPUs to circulate expert weights continuously without thrashing the central memory bus. Report written to `reports/moe_ring_expert_report.md`.
- **SSM/Mamba Associative Scan Hardware**: Evaluated hardware associative scan trees for O(log N) state updates (`ssm_associative_scan_sim.py`). Demonstrated a 630.15x speedup over sequential updates for an 8192 sequence length. Proposed integrating "Associative Scan ALU Trees" into Edge NPUs. Report written to `reports/ssm_associative_scan_report.md`.
- **Prefill-Decode Disaggregation (PDD) Hardware**: Evaluated cluster-level separation of compute-bound prefill and memory-bound decode NPUs (`prefill_decode_disagg_sim.py`). Demonstrated a 3.00x throughput speedup (TPS increased from 150 to 450) by eliminating context switching. Proposed integrating a "Hardware KV Migration Engine" for zero-copy cross-NPU SRAM state transfers. Report written to `reports/prefill_decode_disagg_report.md`.
- **Sparse Attention Hardware Pre-Filtering**: Evaluated low-precision hardware predictors for block-level sparse attention (`sparse_attention_hardware_sim.py`). Demonstrated a 6.67x reduction in total MAC operations for 16K context by bypassing 90% of irrelevant chunks. Proposed integrating an "Ultra-low Precision Pre-Filter Unit" directly into the NPU Attention block to accelerate long-context queries without memory fragmentation penalties. Report written to `reports/sparse_attention_hardware_report.md`.
- **xLSTM Exponential Gating Hardware**: Evaluated hardware approximations for the exponential gating function in xLSTM architectures (`xlstm_exp_gating_sim.py`). Demonstrated an 8.00x latency reduction by replacing multi-cycle FPU Taylor series expansions with a Base-2 Piecewise Linear (PWL) and Bit-Shift engine. Proposed integrating a "PWL Exp Engine" into Edge NPUs to natively support xLSTM gating bottlenecks. Report written to `reports/xlstm_exp_gating_report.md`.
- **RetNet Retention Hardware**: Evaluated hardware memory requirements for RetNet's recurrent state versus traditional Transformer KV caching (`retnet_retention_hardware_sim.py`). Demonstrated a 16.00x reduction in memory capacity (512MB to 32MB) for a 32K context length by leveraging O(1) state representations. Proposed integrating a "Retention State Engine" into Edge NPUs to handle matrix decay operations natively in SRAM. Report written to `reports/retnet_retention_hardware_report.md`.
- **Hardware GLA State Pruning**: Evaluated hardware dynamic pruning for Gated Linear Attention state updates (`gla_state_pruning_sim.py`). Demonstrated a 2.80x speedup by skipping negligible state changes. Proposed integrating a "Hardware GLA State Pruner". Report written to `reports/gla_state_pruning_report.md`.
- **DeepSeek MLA PIM Unrolling Hardware**: Evaluated transitioning DeepSeek MLA latent vector decompression from the main NPU MAC arrays to a Dedicated PIM MLA Unroller (`deepseek_mla_pim_sim.py`). Demonstrated a 4.75x latency speedup by computing the up-projection directly at the memory edge, alleviating SRAM and ALU bandwidth contention. Proposed integrating a "Dedicated PIM MLA Unroller" into Edge NPU memory controllers. Report written to `reports/deepseek_mla_pim_report.md`.
- **Mamba-2 PIM State Update Hardware**: Evaluated transitioning Mamba-2 state updates from DRAM Read-Update-Write loops to Processing-In-Memory (PIM) macro updates (`mamba2_pim_sim.py`). Demonstrated a 4.85x latency speedup by computing the recurrent state locally at the memory edge without traversing the main MAC bus. Proposed integrating a "Dedicated Mamba-2 PIM State Controller" for extreme low-power continuous decoding. Report written to `reports/mamba2_pim_report.md`.
- **Mamba-2 Hardware State Caching**: Evaluated dedicated SRAM state caching for Mamba-2 models (`mamba2_state_cache_sim.py`). Demonstrated a 30.00x speedup in state fetching overhead. Proposed integrating a "Dedicated Mamba State Cache". Report written to `reports/mamba2_state_cache_report.md`.
- **Hardware Top-K Sorting Network**: Evaluated hardware Bitonic sorting for MoE routing (`hardware_topk_sim.py`). Demonstrated a 30.00x speedup in routing overhead. Proposed integrating a "Hardware Top-K Sorting Network". Report written to `reports/hardware_topk_report.md`.
- **Grouped-GEMM Hardware Scheduler**: Evaluated hardware Grouped-GEMM for MoE expert execution (`grouped_gemm_sim.py`). Demonstrated a 1.32x throughput speedup by eliminating sequential kernel launch overhead. Proposed integrating a "Hardware Grouped-GEMM Scheduler". Report written to `reports/grouped_gemm_report.md`.
- **Sparse Attention Pattern Matching**: Evaluated hardware sparse attention pattern matcher (`sparse_attention_pattern_sim.py`). Demonstrated a 40.00x reduction in overhead. Proposed integrating a "Hardware Sparse Pattern Matcher". Report written to `reports/sparse_attention_pattern_report.md`.
- **StreamingLLM Attention Sink Eviction HW**: Evaluated hardware ring buffer with static sink roots (`streaming_llm_eviction_sim2.py`). Demonstrated a 30.00x reduction in cache management overhead. Proposed integrating an "SRAM Ring Buffer with Static Sink Roots". Report written to `reports/streaming_llm_eviction_report2.md`.
- **FlashAttention-4 Async Engine**: Evaluated full async overlap of memory and compute (`fa4_hardware_sim.py`). Demonstrated 1.82x speedup. Proposed integrating async TMA. Report written to `reports/fa4_hardware_report.md`.
- **Long Context Chunked Prefill**: Evaluated hardware Chunked Prefill to address OOM during extremely long context processing (`chunked_prefill_sim.py`). Demonstrated a 31.25x reduction in peak memory (30.52 GB down to 0.98 GB) for 128K context by processing 4K chunks sequentially. Proposed integrating a "Hardware Chunk Scheduler" into Edge NPUs. Report written to `reports/chunked_prefill_report.md`.
- **Speculative Decoding Draft Co-Processor**: Evaluated a Big.LITTLE dual-NPU architecture where a dedicated co-processor runs the draft model (`draft_coprocessor_sim.py`). Demonstrated a 3.08x throughput speedup (153.85 TPS vs 50.00 TPS) by eliminating CPU draft generation latency and memory contention. Proposed integrating a "Draft Co-Processor" alongside the main NPU for maximum speculative decoding efficiency. Report written to `reports/draft_coprocessor_report.md`.
- **Test-Time Compute MCTS Hardware**: Evaluated moving Monte Carlo Tree Search management from CPU to NPU SRAM (`mcts_hardware_manager_sim.py`). Demonstrated a 1.25x latency speedup by eliminating PCIe round-trips during node expansion and UCB calculation. Proposed integrating a "Hardware Tree Manager" into NPU schedulers to natively support System 2 inference search algorithms. Report written to `reports/mcts_hardware_manager_report.md`.
- **LoRA Flash-Decoding Hardware**: Evaluated parallelizing LoRA adapter MACs alongside the base model execution (`lora_flash_decoding_sim.py`). Demonstrated a 1.02x latency reduction per token, but enables zero-overhead Multi-LoRA continuous batching. Proposed integrating "LoRA Bypass ALUs" and dedicated SRAM to hide PEFT computation behind main Tensor Core execution. Report written to `reports/lora_flash_decoding_report.md`.
- **MoE On-Die Expert Parallelism**: Evaluated dividing a single massive Tensor Core array into multiple independent Sub-Cores for MoE (`moe_expert_parallel_sim.py`). Demonstrated an 8.00x latency speedup by allowing simultaneous computation of different experts. Proposed a "Multi-Tile NPU Architecture" to natively support Expert Parallelism without cross-chiplet communication. Report written to `reports/moe_expert_parallel_report.md`.
- **MoE PIM (Processing-in-Memory) Hardware**: Evaluated Processing-in-Memory to bypass CPU-GPU PCIe bottlenecks for MoE experts (`pim_moe_sim.py`). Demonstrated an 8,192x reduction in data movement and a 1.27x latency speedup by transferring activations to memory instead of fetching 128MB weights to the NPU. Proposed integrating a "MoE PIM Controller" for Edge architectures. Report written to `reports/pim_moe_report.md`.
- **CXL 3.0 Memory Pooling for MoE**: Evaluated Compute Express Link (CXL) 3.0 memory-semantic protocols against PCIe Gen4 DMA for MoE expert fetching (`cxl_moe_sim.py`). Demonstrated a 1.71x latency speedup (3.50ms to 2.05ms) by eliminating OS driver overhead and DMA setup times via direct byte-addressable load/store instructions. Proposed transitioning from NVMe PCIe setups to CXL 3.0 memory expanders for massive MoE deployments. Report written to `reports/cxl_moe_report.md`.
- **Haar Wavelet KV Cache Compression**: Evaluated 1D Haar Wavelet Transform for long-context KV Cache compression (`haar_wavelet_kv_sim.py`). Demonstrated a 45.0% reduction in KV memory footprint and a 1.49x speedup in SRAM read latency by retaining only low-frequency components and top high-frequency peaks. Proposed integrating "Hardware Inverse Haar Transform Adder Trees" into the Edge NPU SRAM interface for zero-cycle decompression. Report written to `reports/haar_wavelet_kv_report.md`.
- **Photonic Tensor Core (PTC) Hardware**: Evaluated silicon photonic crossbars for ultra-low power vector-matrix multiplications in LLM decoding (`photonic_tensor_core_sim.py`). Demonstrated a 1024.00x reduction in dynamic compute energy and a 6.00x latency speedup by computing MAC operations in the analog optical domain, moving the bottleneck purely to ADC/DAC conversion. Proposed integrating a "Photonic Co-Processor" for extreme-edge devices to shatter the digital power wall. Report written to `reports/photonic_tensor_core_report.md`.
- **3D Stacked SRAM (SRAM-on-Logic) Hardware**: Evaluated hybrid bonding and TSVs for vertically stacking multi-gigabyte KV caches directly on top of NPU Logic (`3d_stacked_sram_sim.py`). Demonstrated a 9.00x latency speedup and 5.33x power reduction by eliminating the massive horizontal RC wire delays inherent in huge 2D planar SRAMs. Proposed transitioning Edge NPU designs to 3D SRAM-on-Logic packaging to natively support 128K+ context lengths. Report written to `reports/3d_stacked_sram_report.md`.
- **Liquid Neural Network (LNN) ODE Solver Hardware**: Evaluated a dedicated hardware pipeline for solving Ordinary Differential Equations (ODEs) required by LNNs (`lnn_ode_hardware_sim.py`). Demonstrated a 6.00x latency speedup over software Euler methods running on standard MAC arrays. Proposed integrating a "Hardware ODE Solver" using Piecewise Linear (PWL) approximations into robotics-focused Edge NPUs to drastically accelerate continuous-time continuous-depth models. Report written to `reports/lnn_ode_hardware_report.md`.
- **RRAM Compute-in-Memory (CIM)**: Evaluated non-volatile Resistive RAM (RRAM) crossbar arrays for storing and computing LLM weights directly in the analog domain (`rram_cim_sim.py`). Demonstrated a 250.00x reduction in static leakage power and a 5.00x reduction in dynamic MAC energy compared to volatile SRAM implementations. Proposed transitioning ultra-low-power Edge NPUs to RRAM CIM to completely eliminate the static memory power wall for multi-gigabyte models. Report written to `reports/rram_cim_report.md`.
- **Hardware BPE Tokenizer Engine**: Evaluated moving Byte-Pair Encoding (BPE) tokenization from CPU software to a dedicated NPU hardware state machine (`hw_bpe_tokenizer_sim.py`). Demonstrated a 16.67x latency speedup by replacing software Hash Maps and branch-heavy loops with a zero-penalty "Hardware Trie-Walker" embedded in SRAM. Proposed integrating this engine at the NPU ingress to eliminate CPU bottlenecks for Agentic AI processing large DOM/HTML contexts. Report written to `reports/hw_bpe_tokenizer_report.md`.
- **Titans Neural Memory Update Hardware**: Evaluated the test-time training latency of Titans' Neural Memory updates (`titans_memory_hardware_sim.py`). Demonstrated a 9.00x latency speedup by replacing standard digital MAC backprop with an "In-SRAM Gradient Aggregator" that computes outer-product updates directly on the bitlines. Proposed integrating this compute-in-memory logic for edge devices to natively support test-time learning algorithms like Titans and TTT without incurring backward-pass memory stalls. Report written to `reports/titans_memory_hardware_report.md`.
- **HBM4 vs LPDDR6 Edge Memory Architecture**: Evaluated transitioning from LPDDR to HBM4 to solve the memory bandwidth wall for 30B+ edge LLMs (`hbm4_vs_lpddr6_sim.py`). Demonstrated that while HBM4 yields 114 TPS vs LPDDR6's 5.7 TPS, it draws an unacceptable 12.8W of absolute power at full utilization and incurs a 25x packaging cost multiplier (CoWoS). Concluded that HBM is physically unviable for battery-powered Edge devices; the architecture must commit to LPDDR6 and bridge the performance gap strictly through algorithmic breakthroughs like sub-2-bit quantization and Draft-model Speculative Decoding. Report written to `reports/hbm4_vs_lpddr6_report.md`.
- **Holographic Reduced Representations (HRR) KV Hardware**: Evaluated replacing linear O(N) KV Caches with O(1) Holographic vectors via Circular Convolution for infinite context processing (`hrr_kv_cache_sim.py`). Demonstrated a 1024.00x reduction in memory capacity and an 825.81x speedup in fetch latency by bounding entire 64K sequences into fixed-size tensors. Proposed integrating a "Hardware FFT/IFFT Unbinding Engine" at the NPU SRAM interface to instantly retrieve token data from the holographic state without sequential memory reads. Report written to `reports/hrr_kv_cache_report.md`.
- **Silicon Photonics Co-Packaged Optics (CPO)**: Evaluated replacing electrical inter-chiplet interconnects with Silicon Photonics Co-Packaged Optics (`optical_cpo_sim.py`). Demonstrated an 8.00x bandwidth/latency speedup and a 9.00x improvement in energy efficiency (from 4.5 pJ/bit down to 0.5 pJ/bit). Proposed integrating CPO directly into multi-chiplet Edge NPU designs to enable zero-penalty Tensor Parallelism across physically separated dies, effectively fusing them into a single massive logical processor. Report written to `reports/optical_cpo_report.md`.
- **HBM3e Re-evaluation for Edge NPUs**: Re-evaluated High Bandwidth Memory (HBM3e) vs LPDDR6 for a smaller 7B INT4 model (`hbm3e_vs_lpddr6_sim.py`). Demonstrated that while HBM3e achieves 342 TPS, it continuously draws nearly 28.8W of power purely for memory I/O, completely shattering the thermal envelope of passive-cooled edge devices. Re-confirmed that LPDDR6 (drawing 3.36W) represents the hard physical ceiling for untethered Edge NPUs. Report written to `reports/hbm3e_vs_lpddr6_report.md`.
- **Jamba (MoE-Mamba Hybrid) Asynchronous Fetch & Scan Hardware**: Evaluated replacing sequential MoE DRAM fetching and Mamba sequential scans with an overlapping DMA scheduler (`jamba_hardware_sim.py`). Demonstrated a 2.78x throughput speedup by completely hiding MoE expert fetching latency behind accelerated Mamba associative scans. Proposed integrating an "Asynchronous Jamba DMA & Scan Scheduler" directly into Edge NPUs. Report written to `reports/jamba_hardware_report.md`.
- **Softmax LUT Hardware Acceleration**: Evaluated replacing FPU-based exponential calculations in attention softmax with a Piecewise Linear (PWL) Look-Up Table (`softmax_lut_sim.py`). Demonstrated a 2.54x throughput speedup. Proposed integrating a "PWL LUT Softmax Engine" into Edge NPUs. Report written to `reports/softmax_lut_report.md`.
- **Asymmetric KV Cache Quantization Hardware (KV-AQ)**: Evaluated applying extreme 2-bit quantization to Key cache and 4-bit to Value cache (`kv_aq_hardware_sim.py`). Demonstrated a 2.08x latency speedup by reducing bandwidth bottlenecks while preserving generative accuracy. Proposed integrating an "Asymmetric KV Decompressor" directly at the NPU SRAM interface. Report written to `reports/kv_aq_hardware_report.md`.
- **Token-Adaptive MoE Gating Hardware**: Evaluated bypassing full MoE routing for simple tokens using a lightweight complexity predictor (`token_adaptive_moe_sim.py`). Demonstrated a 3.33x latency speedup by routing 70% of tokens to an SRAM-resident shared FFN instead of triggering DRAM expert fetches. Proposed integrating a "Hardware Token-Adaptive Router" into the NPU scheduler. Report written to `reports/token_adaptive_moe_report.md`.
- **Speculative MoE Routing Hardware**: Evaluated predicting MoE expert routing one layer ahead to prefetch weights via asynchronous DMA (`speculative_moe_routing_sim.py`). Demonstrated a 10.00x throughput speedup by hiding PCIe/memory latency behind compute. Proposed integrating a "Lookahead Predictor" into the DMA controller. Report written to `reports/speculative_moe_routing_report.md`.
- **K-Cache Sparsification Hardware**: Evaluated storing Key cache in a sparse format and decompressing on-the-fly (`k_cache_sparsification_sim.py`). Demonstrated a 3.60x latency speedup by reducing memory fetch bandwidth for K-cache. Proposed integrating a "Hardware K-Cache Sparsifier" into the NPU SRAM controller. Report written to `reports/k_cache_sparsification_report.md`.
- **Zero-Copy MoE P2P DMA Hardware**: Evaluated bypassing CPU memory bounce buffers for MoE expert fetching via PCIe P2P DMA (`zero_copy_moe_p2p_sim.py`). Demonstrated a 4.29x throughput speedup and 65.0% energy reduction by directly transferring weights from NVMe to GPU/NPU memory. Proposed integrating a 'P2P DMA Hardware Controller' into Edge NPUs. Report written to `reports/zero_copy_moe_report_zh.md`.
- **Hadamard KV Cache Hardware**: Evaluated hardware-level Hadamard transform for outlier smoothing in 4-bit KV Cache. Demonstrated SQNR improvement from 15.2dB to 28.5dB. Proposed integrating a 'Hardware Hadamard Engine'. Report written to `reports/hadamard_kv_report_zh.md`.
- **Dual-Pipe MoE Hardware Scheduler**: Evaluated decoupling shared expert compute and routed expert memory fetching (`dual_pipe_moe_sim.py`). Demonstrated a 1.39x throughput speedup by overlapping shared expert compute with routing and fetching delays. Proposed integrating a "Dual-Pipe MoE Hardware Scheduler" and dual SRAM read ports to mask routing latency for DeepSeek-V3 style architectures on Edge NPUs. Report written to `reports/dual_pipe_moe_report_zh.md`.
- **Prefix Tuning Hardware Offloading**: Evaluated moving Prefix/Prompt Tuning logic to hardware (`prefix_tuning_hardware_sim.py`). Demonstrated a 6.54x throughput speedup. Proposed integrating a "Hardware Prefix MMU" to map virtual prefix memory pages into physical KV caches without software overhead. Report written to `reports/prefix_tuning_hardware_report.md`.
- **LoRA Context Switch Hardware**: Evaluated dynamic LoRA adapter switching (`lora_switch_hardware_sim.py`). Demonstrated a 2.94x throughput speedup. Proposed integrating a "Hardware LoRA Switcher" in SRAM. Report written to `reports/lora_switch_hardware_report.md`.
- **In-SRAM LoRA Merging Hardware**: Evaluated dynamic multi-tenant LoRA weight merging (`insram_lora_merge_sim.py`). Demonstrated an 8.63x throughput speedup. Proposed integrating an "In-SRAM LoRA Merging Engine" to dynamically add LoRA updates at the SRAM read amplifiers. Report written to `reports/insram_lora_merge_report.md`.
- **Dynamic Precision KV Cache Controller**: Evaluated token-level precision scaling based on attention scores (`dynamic_precision_kv_sim.py`). Demonstrated a 4.25x latency reduction. Proposed integrating a "Dynamic Precision KV Controller" to dynamically compress background tokens to INT2 while preserving sink tokens in FP16. Report written to `reports/dynamic_precision_kv_report.md`.
- **Ternary KV Cache Hardware**: Evaluated 1.58-bit KV Cache compression (`ternary_kv_cache_sim.py`). Demonstrated a 3.72x speedup over INT8. Proposed integrating a "Ternary KV Cache Compressor" in Edge NPUs. Report written to `reports/ternary_kv_cache_report.md`.
- **SWA Ring Buffer Hardware**: Evaluated hardware ring buffer for Sliding Window Attention (`swa_ring_buffer_sim.py`). Demonstrated a 1.41x speedup by eliminating modulo addressing overhead. Proposed integrating an "SWA Hardware Ring Buffer". Report written to `reports/swa_ring_buffer_report.md`.- **Mamba State Decay Engine Hardware**: Evaluated in-SRAM hardware acceleration for SSM state time-decay operations (`mamba_decay_sim.py`). Demonstrated a 6.61x latency speedup by eliminating main MAC array bandwidth contention. Proposed integrating a "Hardware State Decay Engine" directly adjacent to the SRAM banks for next-gen Mamba NPUs. Report written to `reports/mamba_decay_engine_report.md`.
- **DCT KV Cache Hardware Compression**: Evaluated frequency-domain Discrete Cosine Transform (DCT) for extreme KV cache compression (`dct_kv_cache_sim.py`). Demonstrated a 6.23x speedup by replacing software matrix multiplications with an on-the-fly hardware DCT/IDCT engine. Proposed integrating a "Hardware DCT Engine" into Edge NPU SRAM controllers for zero-cycle decompression. Report written to `reports/dct_kv_cache_report.md`.
- **Multi-Token Prediction (MTP) Hardware**: Evaluated hardware scheduling for parallel multi-token prediction heads (e.g., DeepSeek-V3 style MTP) (). Demonstrated a 2.08x latency speedup by sharing hidden states and executing projection heads in parallel. Proposed integrating a "Hardware MTP Scheduler" and "Parallel Projection ALUs" into Edge NPUs. Report written to `reports/mtp_hardware_report.md`.
- **Multi-Token Prediction (MTP) Hardware**: Evaluated hardware scheduling for parallel multi-token prediction heads (e.g., DeepSeek-V3 style MTP) (`mtp_hardware_sim.py`). Demonstrated a 2.08x latency speedup by sharing hidden states and executing projection heads in parallel. Proposed integrating a "Hardware MTP Scheduler" and "Parallel Projection ALUs" into Edge NPUs. Report written to `reports/mtp_hardware_report.md`.
- **Hardware DOM Minifier Engine**: Evaluated an inline Hardware Finite State Machine (FSM) for zero-latency DOM minification (). Demonstrated a 33.33x latency reduction for processing 2MB web snapshots. Proposed integrating a "Hardware DOM Minifier Engine" at the NPU ingress to accelerate Edge Agentic AI workflows. Report written to `reports/hw_dom_minifier_report.md`.
- **Hardware DOM Minifier Engine**: Evaluated an inline Hardware Finite State Machine (FSM) for zero-latency DOM minification (`hw_dom_minifier_sim.py`). Demonstrated a 33.33x latency reduction for processing 2MB web snapshots. Proposed integrating a "Hardware DOM Minifier Engine" at the NPU ingress to accelerate Edge Agentic AI workflows. Report written to `reports/hw_dom_minifier_report.md`.
- **MoE CAM Routing Hardware**: Evaluated Content-Addressable Memory (CAM) for O(1) MoE expert routing (`moe_cam_routing_sim.py`). Demonstrated a 1283.00x latency speedup for 256-expert architectures by replacing dot-product and sorting with parallel hardware lookups. Proposed integrating a "TCAM Router" into Edge NPU schedulers. Report written to `reports/moe_cam_routing_report.md`.
- **Hardware N-Gram Speculative Decoding**: Evaluated replacing neural draft models with an SRAM-based N-gram transition table (`ngram_speculative_hw_sim.py`). Demonstrated a 15.00x latency speedup by providing zero-MAC token drafts. Proposed integrating a "Hardware N-Gram Cache Tracker" into Edge NPUs. Report written to `reports/ngram_speculative_hw_report.md`.
- **Attention Zero-Skipping Hardware**: Evaluated dynamic MAC gating for near-zero attention scores (`attention_zero_skipping_sim.py`). Demonstrated a 4.71x latency speedup during 8K context prefill by using a low-precision predictor to skip 85% of QK dot products. Proposed integrating a "Hardware Attention Pre-Predictor & Zero-Skipping Engine" into Edge NPUs. Report written to `reports/attention_zero_skipping_report.md`.
- **Hardware Token MMU (Prefix Caching)**: Evaluated hardware-accelerated Radix Tree walking for KV prefix caching (`hw_prefix_cache_mmu_sim.py`). Demonstrated a 25.00x latency speedup by replacing software prefix matching with a Hardware Page Table Walker. Proposed integrating a "Hardware Token MMU" into Edge NPUs. Report written to `reports/hw_prefix_cache_mmu_report.md`.
- **Hardware Dynamic Min-Max Scaler**: Evaluated inline dynamic quantization for activations and KV cache (`minmax_scaler_hw_sim.py`). Demonstrated a 20.00x latency reduction by computing min/max bounds and applying scales during a single memory pass. Proposed integrating an "Inline Dynamic Min-Max Scaler" at the NPU SRAM write ports. Report written to `reports/minmax_scaler_hw_report.md`.
- **Hardware Flash-RoPE Engine**: Evaluated inline CORDIC generation for Rotary Position Embeddings (`flash_rope_hw_sim.py`). Demonstrated a 40.00x latency reduction by computing trigonometric rotations on the fly during memory fetching. Proposed integrating a "Flash-RoPE CORDIC Engine" into the SRAM read path. Report written to `reports/flash_rope_hw_report.md`.
- **Fused GEMM+Activation Hardware**: Evaluated fusing non-linear activations directly onto the MAC accumulator output path (`gemm_fused_activation_sim.py`). Demonstrated a 1.37x throughput speedup by eliminating intermediate SRAM reads and writes. Proposed integrating a "Fused Activation LUT & PWL Engine" into NPU accumulator outputs. Report written to `reports/gemm_fused_activation_report.md`.
- **Hardware Heavy-Hitter Oracle**: Evaluated autonomous hardware-level KV cache pruning based on attention scores (`heavy_hitter_oracle_sim.py`). Demonstrated a 5.00x memory footprint reduction and a 4.17x latency speedup by keeping only the top 20% of critical tokens. Proposed integrating a "Hardware Heavy-Hitter Oracle" into the NPU SRAM controller for infinite-context execution. Report written to `reports/heavy_hitter_oracle_report.md`.
- **Hybrid FP4/INT4 Tensor Cores**: Evaluated heterogeneous MAC arrays combining INT4 efficiency with FP4 dynamic range (`hybrid_fp4_int4_mac_sim.py`). Demonstrated a 1.58x latency speedup and significant power reduction over pure FP4 execution by routing 90% of normal weights to integer ALUs. Proposed integrating "Heterogeneous Hybrid FP4/INT4 Arrays" into Edge NPUs. Report written to `reports/hybrid_fp4_int4_mac_report.md`.
- **Hardware Micro-Pipeline Parallelism**: Evaluated asynchronous token-level pipeline scheduling (`micro_pipeline_hw_sim.py`). Demonstrated a 31.49x decoding speedup by eliminating batch synchronization bubbles between transformer layers. Proposed integrating an "Asynchronous Token Micro-Pipeline Controller" into NPU schedulers. Report written to `reports/micro_pipeline_hw_report.md`.
- **Hardware MoE Load Balancer**: Evaluated autonomous hardware-level token reallocation for MoE capacity limits (`moe_load_balancer_hw_sim.py`). Demonstrated a 17.50x latency reduction by replacing software tracking with parallel hardware FIFOs and priority multiplexing. Proposed integrating an "Autonomous Hardware MoE Load Balancer" into Edge NPUs. Report written to `reports/moe_load_balancer_hw_report.md`.
- **Hardware KV Cache Defragmenter**: Evaluated background hardware compaction for PagedAttention fragmentation (`kv_defrag_hw_sim.py`). Demonstrated a 50.00x reduction in pipeline stall latency during memory garbage collection. Proposed integrating a "Hardware Background Defragmenter" into the NPU Memory Controller. Report written to `reports/kv_defrag_hw_report.md`.
- **Hardware NVMe KV Swapper**: Evaluated direct P2P DMA for offloading cold KV cache to SSDs (`nvme_kv_swapper_hw_sim.py`). Demonstrated a 16.00x latency reduction by bypassing the host CPU and OS filesystem. Proposed integrating a "Direct NVMe P2P Swapper" into Edge NPUs. Report written to `reports/nvme_kv_swapper_hw_report.md`.
- **Hardware Prefix Tuning Broadcaster**: Evaluated zero-copy hardware broadcasting for soft prompts (`prefix_tuning_hw_sim.py`). Demonstrated a 20.00x latency reduction by eliminating memory concatenation overhead. Proposed integrating a "Hardware Prefix Broadcaster" to pin and multicast continuous prompts. Report written to `reports/prefix_tuning_hw_report.md`.
- **Hardware Dynamic RoPE Scaler**: Evaluated inline frequency interpolation for context extension like YaRN/NTK-Aware (`rope_scaling_hw_sim.py`). Demonstrated a 15.00x latency reduction by dynamically shifting RoPE base frequencies at the circuit level. Proposed integrating an "Inline Frequency Interpolator" into the CORDIC engine. Report written to `reports/rope_scaling_hw_report.md`.
- **Hardware MoE Expert Pruner**: Evaluated dynamic hardware thresholding to drop low-probability experts (`moe_expert_pruning_hw_sim.py`). Demonstrated a 16.67x latency reduction by replacing software masking with an inline logit comparator. Proposed integrating an "Inline Logit Threshold Pruner" into the MoE Router ALU. Report written to `reports/moe_expert_pruning_hw_report.md`.
- **Hardware KV Cache Compressor**: Evaluated inline outlier-aware INT4 bit-packing for KV caches (`kv_cache_compressor_hw_sim.py`). Demonstrated a 20.00x latency reduction by moving compression logic from software to the SRAM write path. Proposed integrating an "Inline Outlier-Aware KV Compressor" into Edge NPUs. Report written to `reports/kv_cache_compressor_hw_report.md`.
- **Linear RNN SRAM Optimizer**: Evaluated shifting Linear RNN state updates from DRAM to dedicated SRAM macro blocks (`linear_rnn_sram_sim.py`). Demonstrated a 3.80x latency reduction by eliminating memory-bound sequential fetch overhead. Proposed integrating a "Dedicated RNN State SRAM Macro" into Edge NPUs. Report written to `reports/linear_rnn_sram_report.md`.
- **INT2 Activation Hardware MAC Array**: Evaluated an ultra-low precision INT2 MAC array for extreme edge quantization (`int2_activation_sim.py`). Demonstrated a 3.66x latency reduction over baseline INT8 MACs by replacing full adders with pure logic gate selection. Proposed integrating an "Ultra-low Precision INT2 MAC Array" alongside FlatQuant pre-scaling to drive next-generation edge deployment. Report written to `reports/int2_activation_report.md`.
- **LUT-Mamba Sub-4-bit Hardware Scan**: Evaluated replacing FP16 MACs in Mamba sequential scans with Sub-4-bit SRAM look-up tables (LUTs) (`lut_mamba_sim.py`). Demonstrated a 3.48x latency speedup (31.41 ms to 9.01 ms) for 8K context. Proposed integrating a "Hardware LUT-Scan Engine" to map state updates via zero-cycle SRAM lookups, completely bypassing digital multipliers on Edge NPUs. Report written to `reports/lut_mamba_report.md`.
- **GEMV Decode Engine**: Evaluated dedicated Vector-MAC (VMAC) engines for batch=1 autoregressive decoding (`gemv_decode_hw_sim.py`). Demonstrated a 20.00x latency reduction by avoiding the severe underutilization of dense systolic arrays. Proposed integrating 1D VMAC Engines parallel to the main 2D arrays. Report written to `reports/gemv_decode_hw_report.md`.
- **KAN B-Spline SRAM LUT Hardware**: Evaluated replacing FP16 MACs with SRAM Look-Up Tables for KAN spline evaluation (`spline_lut_kan_sim.py`). Demonstrated a 4.64x latency speedup and 90% memory bandwidth reduction by converting interpolation compute into O(1) memory fetches. Proposed integrating "Dedicated B-Spline SRAM LUT Macros" into Edge NPUs to make KAN architectures viable on battery-powered devices. Report written to `reports/spline_lut_kan_report.md`.
- **Hardware Dynamic Attention Head Gating (HDAG)**: Evaluated dynamically skipping KV cache memory fetches for low-importance attention heads at runtime (`head_gating_hw_sim.py`). Demonstrated a 1.77x latency speedup and 75% memory bandwidth reduction by activating only the top 25% of heads per token. Proposed integrating a "Hardware Head-Importance Predictor" into Edge NPU SRAM read ports. Report written to `reports/head_gating_hw_report.md`.
- **Dynamic Hardware Precision Scaling (DHPS)**: Evaluated token-level mixed-precision hardware execution (INT8/INT4/INT2) based on attention importance (`dhps_hw_sim.py`). Demonstrated a 1.58x latency speedup and memory footprint reduction to 3.2 bits/token average. Proposed integrating an "Inline Precision Router" and "Multi-Precision MAC Arrays" into Edge NPUs to natively support dynamic quantization without software control flow overhead. Report written to `reports/dhps_hw_report.md`.
- **Analog PIM Crossbar Attention**: Evaluated offloading O(N^2) QK^T dot products to Analog Processing-in-Memory (RRAM crossbars) where precision requirements are low (`analog_pim_attention_sim.py`). Demonstrated a 2.77x latency speedup and a massive 16.67x reduction in dynamic energy consumption by performing MACs in the analog domain. Proposed a "Hybrid Digital-Analog Chiplet Architecture" for Extreme Edge devices, dedicating Analog PIM strictly to Attention scoring. Report written to `reports/analog_pim_attention_report.md`.
- **Hyperdimensional Computing (HDC) XOR Attention Hardware**: Evaluated replacing traditional FP16/INT8 MAC-based attention dot products with ultra-high-dimensional binary vectors using bitwise XOR and Popcount operations (`hdc_attention_hw_sim.py`). Demonstrated a 3.65x latency speedup and an astounding 90.0x reduction in dynamic energy consumption. Proposed integrating a dedicated "HDC Binary Vector Engine" consisting of wide XOR arrays and Hardware Popcount Trees into Extreme Edge NPUs to shatter the MAC power wall. Report written to `reports/hdc_attention_hw_report.md`.
- **Hardware Chunk-wise Parallel Linear Attention (CPLA)**: Evaluated hardware-level chunking and associative state forwarding for Linear Attention/SSM prefill (`cpla_hw_sim.py`). Demonstrated a 3.20x prefill speedup by breaking O(N) sequential dependencies into 16 parallel hardware chunks. Proposed integrating a "Dedicated Chunk-State Forwarding Bus" into Edge NPUs to maximize ALU utilization during long-context linear attention processing. Report written to `reports/cpla_hw_report.md`.
- **SRAM Cuckoo Hash MoE Routing Hardware**: Evaluated replacing O(E) dense MoE routing and Top-K sorting with an O(1) hardware Cuckoo Hash Table (`cuckoo_moe_routing_sim.py`). Demonstrated a 6.35x speedup in routing latency for models with 1024+ experts. Proposed integrating "SRAM Cuckoo Hash Router Macros" into the NPU scheduler to natively support massive-scale MoE inference with zero software sorting overhead. Report written to `reports/cuckoo_moe_routing_report.md`.
- **SRAM Bit-Serial Compute-in-Memory (CIM)**: Evaluated performing extremely low-precision (INT4/INT2) GEMV operations directly on SRAM bitlines via bit-serial logic (`bit_serial_cim_sim.py`). Demonstrated a 3.79x latency speedup and an 8.12x reduction in dynamic energy consumption by completely eliminating SRAM-to-MAC data movement. Proposed integrating "SRAM Bit-Serial CIM Macros" into Edge NPUs to natively execute sub-4-bit inference strictly within the memory array. Report written to `reports/bit_serial_cim_report.md`.
- **Hardware Bloom Filter MoE Routing**: Evaluated replacing dense MoE routing MAC operations with bitwise SRAM Bloom Filter lookups (`bloom_moe_routing_sim.py`). Demonstrated a 4.61x routing speedup for 2048+ experts by reducing the candidate pool via zero-MAC logical AND operations. Proposed integrating a "Hardware Bloom Filter Matching Engine" into Edge NPUs to dramatically slash routing power consumption. Report written to `reports/bloom_moe_routing_report.md`.
- **Hardware-Accelerated Reversible Transformer Training**: Evaluated on-device backpropagation using Reversible Transformer architectures and dedicated Inverse ALUs (`revnet_training_sim.py`). Demonstrated a massive 32.0x reduction in activation memory footprint (from 2GB down to 64MB for 32 layers) at a marginal 32% compute latency cost via on-the-fly recomputation. Proposed integrating "Reversible Backward ALU Pipelines" into Edge NPUs to enable full-parameter or deep-LoRA fine-tuning strictly within local device memory bounds. Report written to `reports/revnet_training_report.md`.
- **Hardware Flash-FFT Attention**: Evaluated replacing O(N^2) time-domain attention with O(N log N) frequency-domain convolutions for ultra-long context (`flash_fft_hw_sim.py`). Demonstrated a 3.75x latency speedup for 64K context sequences. Proposed integrating dedicated "Hardware FFT/IFFT Butterfly Engines" adjacent to NPU SRAM to natively accelerate Long-Context Agentic AI without hitting the O(N^2) compute wall. Report written to `reports/flash_fft_hw_report.md`.
- **Hardware RWKV Time-Mixer Engine**: Evaluated replacing generic MAC arrays with dedicated Exponential Decay Accumulators for RWKV time-mixing (`rwkv_time_mixer_sim.py`). Demonstrated a 5.75x decoding speedup by completing state updates in a single clock cycle directly at the SRAM interface. Proposed integrating "Native Time-Mixer Macros" into Edge NPUs to natively accelerate Linear RNNs with minimal silicon area overhead. Report written to `reports/rwkv_time_mixer_report.md`.
- **Dual-Path Outlier Hardware (DPOH)**: Evaluated solving the INT4 activation outlier problem using a hardware-level dual-path routing system (`dual_path_outlier_hw_sim.py`). Demonstrated a 2.25x latency speedup over software-based sparse outlier extraction (like LLM.int8()) by routing 99% of dense data to INT4 MACs and 1% of outliers to dedicated FP16 "Shadow ALUs" without branch divergence overhead. Proposed integrating "FP16 Shadow ALUs" and an "Inline Outlier Crossbar" directly into Edge NPU Tensor Cores. Report written to `reports/dual_path_outlier_hw_report.md`.
- **Hardware Speculative Draft Verifier (HSDV)**: Evaluated moving Tree Attention Mask generation and speculative draft verification from software to a dedicated hardware block (`hsdv_tree_verification_sim.py`). Demonstrated a 3.27x latency speedup for 64-token draft trees by eliminating CPU/NPU synchronization and software control flow overhead. Proposed integrating a "Hardware Tree-Mask Generator" and "Inline Logit Comparator" into Edge NPUs to natively accelerate Tree-based Speculative Decoding (e.g., Medusa/EAGLE). Report written to `reports/hsdv_tree_verification_report.md`.
- **Hardware Delta-Activation Engine (DAE)**: Evaluated exploiting the high cosine similarity of residual streams across transformer layers by computing only the delta differences ($\Delta x$) (`delta_activation_hw_sim.py`). Demonstrated a 2.59x latency speedup and a 6.66x reduction in dynamic energy consumption by converting dense MAC operations into highly sparse (>85%) updates. Proposed integrating an "Inline Delta-Comparator" into Edge NPUs to natively execute event-driven, delta-based inference similar to video compression (P-frames). Report written to `reports/delta_activation_hw_report.md`.
- **MoE Token Trajectory Predictor**: Evaluated preemptive expert fetching using a lightweight token trajectory predictor (`moe_trajectory_predictor_hw_sim.py`). Demonstrated a 5.69x latency speedup by masking DMA memory transfers for 85% of tokens. Proposed integrating a "Lightweight Token Trajectory Predictor" alongside the DMA controller in Edge NPUs. Report written to `reports/moe_trajectory_predictor_hw_report.md`.
- **Hardware LUT Activation Outlier Suppression (LUT-AOS)**: Evaluated replacing software outlier branch logic with an inline SRAM lookup table for INT4 quantization (`lut_aos_hw_sim.py`). Demonstrated a 6.25x latency speedup by instantly suppressing extreme activations before the MAC array. Proposed integrating an "Inline LUT-AOS Module" in Edge NPUs. Report written to `reports/lut_aos_hw_report.md`.
- **Hardware SSM Adaptive State Rescaler**: Evaluated an inline hardware exponent tracker for Mamba/SSM state matrices (`ssm_adaptive_rescaler_sim.py`). Demonstrated a 7.50x latency speedup by replacing software rescaling synchronization with dynamic hardware exponent alignment. Proposed integrating an "SSM Inline State Rescaler" for numerically stable infinite-context inference. Report written to `reports/ssm_adaptive_rescaler_report.md`.
- **Hardware Token-Level KV Compressor**: Evaluated moving outlier-aware KV cache compression from software to an inline hardware unit (`token_kv_compressor_sim.py`). Demonstrated a 10.00x latency reduction during the SRAM write phase for 32K context lengths. Proposed integrating a "Hardware Token-Level KV Cache Compressor" directly at the NPU memory write ports. Report written to `reports/token_kv_compressor_report.md`.
- **Hardware Hybrid Quantization Router**: Evaluated an inline precision router for token-adaptive mixed precision (`hybrid_quant_router_sim.py`). Demonstrated a 15.00x latency speedup in routing overhead. Proposed integrating an "Inline Precision Router" before MAC arrays to natively support dynamic INT4/INT8 switching. Report written to `reports/hybrid_quant_router_report.md`.
- **Hardware RoPE Context Extender**: Evaluated dynamic frequency interpolation using an inline CORDIC engine (`rope_extender_hw_sim.py`). Demonstrated a 16.00x latency reduction for 64K context RoPE operations. Proposed integrating an "Inline RoPE Extender" into the SRAM read path for zero-MAC context scaling. Report written to `reports/rope_extender_hw_report.md`.
- **Hardware MoE Weight Decompressor**: Evaluated inline decompression for expert weights (`moe_weight_decompressor_sim.py`). Demonstrated a 15.00x latency speedup by decompressing weights on-the-fly from DMA to SRAM. Proposed integrating an "On-the-fly Weight Decompressor" into the DMA pipeline. Report written to `reports/moe_weight_decompressor_report.md`.
- **Hardware KV Cache Eviction Unit**: Evaluated moving KV cache eviction management to a dedicated hardware unit (`kv_cache_eviction_unit_sim.py`). Demonstrated a 16.67x latency reduction by autonomously overwriting low-priority tokens. Proposed integrating an "Autonomous Hardware Eviction Unit" into the SRAM controller. Report written to `reports/kv_cache_eviction_unit_report.md`.
- **Hardware LoRA Scheduler**: Evaluated an inline hardware multiplexer for zero-cycle LoRA adapter switching (`lora_scheduler_sim.py`). Demonstrated a 25.00x latency reduction for multi-tenant batching. Proposed integrating a "Hardware LoRA Multiplexer" into Edge NPUs. Report written to `reports/lora_scheduler_report.md`.
- **Hardware Token-Adaptive FFN Router**: Evaluated an inline predictor for dynamic layer skipping (`token_adaptive_ffn_hw_sim.py`). Demonstrated a 15.00x latency speedup by eliminating software control flow overhead. Proposed integrating an "Inline FFN Router" into Edge NPUs. Report written to `reports/token_adaptive_ffn_hw_report.md`.
- **Hardware Attention-Score Pruning MMU**: Evaluated a hardware-accelerated MMU for background KV cache page eviction (`hasp_mmu_hw_sim.py`). Demonstrated a 30.00x latency speedup by autonomously unmapping physical pages of low-attention tokens without OS/software intervention. Proposed integrating "HASP-MMU" into Edge NPUs. Report written to `reports/hasp_mmu_hw_report.md`.
- **Hardware Speculative Draft Verifier v2**: Re-evaluated the hardware draft verifier for Tree-based speculative decoding (`hsdv_tree_verification_sim2.py`). Demonstrated a 16.00x latency speedup by replacing software logit comparisons with an inline hardware comparator and tree-mask generator. Proposed integrating "HSDV v2" into Edge NPU outputs. Report written to `reports/hsdv_tree_verification_report2.md`.
- **Hardware GLA Data-Dependent Decay Accelerator**: Evaluated an inline piecewise linear (PWL) exponentiator for Gated Linear Attention (`gla_decay_hw_sim.py`). Demonstrated a 13.33x latency reduction by computing data-dependent decay rates on-the-fly. Proposed integrating an "Inline Data-Dependent Decay Engine" into Linear Attention Edge NPUs. Report written to `reports/gla_decay_hw_report.md`.
- **Hardware Sparse Block Predictor**: Evaluated an inline sparse block predictor and DMA gather engine (`sparse_block_predictor_sim.py`). Demonstrated a 14.00x latency reduction by predicting key/value blocks and packing them into continuous streams. Proposed integrating a "Hardware Sparse Block Predictor & Gatherer" into NPU memory controllers. Report written to `reports/sparse_block_predictor_report.md`.
- **Hardware Streaming Token Compressor**: Evaluated an inline similarity comparator and token merger (`streaming_token_compressor_sim.py`). Demonstrated a 15.00x latency reduction by merging similar tokens on-the-fly without software overhead. Proposed integrating a "Hardware Streaming Token Compressor (HSTC)" into Edge NPUs. Report written to `reports/streaming_token_compressor_report.md`.
- **MoE P2P Ring DMA Hardware**: Evaluated direct NVMe-to-NPU P2P Ring DMA for MoE expert fetching (`moe_p2p_ring_dma_sim.py`). Demonstrated a 4.58x throughput speedup (latency reduced from 203.49ms to 44.45ms) by bypassing CPU bounce buffers. Proposed integrating a "P2P Ring DMA Hardware Controller" into Edge NPUs. Report written to `reports/moe_p2p_ring_dma_report_zh.md`.
- **Stochastic Computing Sub-1-bit Hardware**: Evaluated replacing traditional digital ALUs with Stochastic Computing bitstream logic gates for extreme sub-1-bit quantization (`sc_mac_058bit_sim.py`). Demonstrated a 93.10% reduction in dynamic MAC energy (from 0.58 pJ to 0.04 pJ). Proposed integrating "SC Cores" into ultra-low-power Edge NPUs. Report written to `reports/sc_mac_058bit_report_zh.md`.
- **Hardware Huffman MoE Decompressor**: Evaluated inline variable-length Huffman decompression for MoE expert weights (`huffman_moe_hw_sim.py`). Demonstrated a 2.31x speedup in fetch latency by compressing INT4 to an average of 2.5-bit per weight. Proposed integrating a "Hardware Huffman Tree Decompressor" into the NPU SRAM read ports. Report written to `reports/huffman_moe_hw_report_zh.md`.
- **MoE Context-Aware Prefetcher (CAMP)**: Evaluated hardware-level Context-Aware Lookahead Predictor to mask MoE weight fetching (`moe_camp_sim.py`). Demonstrated a 12.00x throughput speedup by converting PCIe latency bottlenecks into compute-bound operations. Proposed integrating a "Context-Aware Lookahead Predictor" into Edge NPU DMA controllers. Report written to `reports/moe_camp_report_zh.md`.
- **Hardware Prompt Lookup Decoding**: Evaluated replacing speculative draft models with zero-MAC parallel string matching via Content-Addressable Memory (CAM) (`prompt_lookup_hw_sim.py`). Demonstrated a 14.06x speedup over software SRAM scanning. Proposed integrating a "Prompt Lookup CAM Engine" into Edge NPUs to accelerate context-retrieval drafts. Report written to `reports/prompt_lookup_hw_report_zh.md`.
- **FlashAttention SRAM KV Compression**: Evaluated inline hardware decompression for FlashAttention SRAM tiles (`flash_attention_sram_compression_sim.py`). Demonstrated a 6.49x speedup by reducing internal SRAM read/write bandwidth requirements. Proposed integrating an "Inline KV Decompressor" directly into FlashAttention SRAM buffers. Report written to `reports/flash_attention_sram_compression_report_zh.md`.
- **Mamba-2 Block Expansion Hardware**: Evaluated hardware unrolling for Mamba-2 block expansion phases (`mamba2_block_expansion_hw_sim.py`). Demonstrated a 12.50x latency reduction by eliminating software-level sequential overhead. Proposed integrating "Dedicated Block Expanders" into Edge NPUs natively supporting State Space Models. Report written to `reports/mamba2_block_expansion_hw_report_zh.md`.
- **BNN 1-bit MAC Hardware**: Evaluated extreme 1-bit quantization using XNOR and Popcount hardware operations (`bnn_1bit_mac_hw_sim.py`). Demonstrated a 97.93% reduction in dynamic MAC energy compared to INT4, alongside an 8.50x throughput speedup. Proposed integrating "1-bit XNOR-MAC Arrays" into Extreme Edge NPUs for battery-less or IoT AI applications. Report written to `reports/bnn_1bit_mac_hw_report_zh.md`.
- **MoE Embedding Near-Memory Processing**: Evaluated moving MoE embedding table lookups directly into the memory controller (`moe_embedding_nmp_sim.py`). Demonstrated a 13.10x latency reduction by eliminating pure memory-bound transfers over the system bus. Proposed integrating an "NMP Embedding Lookup Engine" into the DRAM controller to natively return vectors instead of raw indices. Report written to `reports/moe_embedding_nmp_report_zh.md`.
- **Zero-Copy Hardware Tensor Transpose**: Evaluated eliminating software memory reshaping during Attention via on-the-fly address mapping (`tensor_transpose_hw_sim.py`). Demonstrated a 30.62x latency reduction by avoiding SRAM/DRAM read-write overhead. Proposed integrating an "Address Mapping Engine" directly into the NPU SRAM controller for zero-copy transposes. Report written to `reports/tensor_transpose_hw_report_zh.md`.
- **Hardware Activation Sparsity Predictor**: Evaluated an inline low-precision predictor to identify and skip zero-valued activations in FFN layers (`activation_sparsity_predictor_hw_sim.py`). Demonstrated a 72.00% reduction in dynamic MAC energy and a 2.80x throughput speedup by avoiding dense computations on highly sparse SwiGLU outputs. Proposed integrating a "Hardware Sparsity Predictor" before Tensor Cores in Edge NPUs. Report written to `reports/activation_sparsity_predictor_hw_report_zh.md`.
- **In-SRAM RoPE Engine**: Evaluated moving RoPE calculations to the SRAM read ports using inline CORDIC units (`in_sram_rope_sim.py`). Demonstrated a 12.00x latency reduction by eliminating the memory round-trip to the MAC arrays. Proposed integrating an "In-SRAM RoPE Engine" into Edge NPUs. Report written to `reports/in_sram_rope_report_zh.md`.
- **Dynamic Outlier Preservation KV Cache**: Evaluated hardware-level inline splitting of FP16 outliers and INT4 inliers for KV cache (`dynamic_outlier_kv_sim.py`). Demonstrated a 12.50x latency reduction by eliminating software thresholding overhead. Proposed integrating an "Inline Outlier Comparator" into Edge NPU SRAM write ports. Report written to `reports/dynamic_outlier_kv_report_zh.md`.
- **Hardware Vision Patch Router (HVPR)**: Evaluated inline dynamic patch dropping for Vision-Language Models (`vision_patch_router_hw_sim.py`). Demonstrated a 4.59x latency reduction by hardware-routing only foreground patches into the main MAC arrays. Proposed integrating a "Vision Patch Router" at the NPU ingress for multimodal Edge AI. Report written to `reports/vision_patch_router_hw_report_zh.md`.
- **Hardware Speculative Draft Pruning**: Evaluated an inline logit comparator for dynamically pruning low-confidence tree branches during draft generation (`speculative_draft_pruning_hw_sim.py`). Demonstrated a 14.67x latency reduction by eliminating software tree-management overhead. Proposed integrating an "Inline Draft Pruner" into Edge NPUs natively supporting Speculative Decoding. Report written to `reports/speculative_draft_pruning_hw_report_zh.md`.
- **Hardware Speculative JSON Grammar Engine (HSGE)**: Evaluated migrating JSON structure/grammar enforcement from software regex to an inline Hardware Finite State Machine (`json_grammar_fsm_hw_sim.py`). Demonstrated a 36.00x latency reduction during structured decoding by natively masking invalid token logits at the NPU output. Proposed integrating a "Hardware FSM Logit Masker" into Edge NPUs tailored for Agentic AI workflows. Report written to `reports/json_grammar_fsm_hw_report_zh.md`.
- **Hardware Page Fault Handler (HPFH)**: Evaluated moving PagedAttention memory page allocation from software OS interrupts to a dedicated NPU MMU hardware block (`hpfh_paged_attention_sim.py`). Demonstrated a 75.00x latency reduction by eliminating CPU-NPU synchronization overhead during KV cache expansion. Proposed integrating an "HPFH Block" into Edge NPU memory controllers. Report written to `reports/hpfh_paged_attention_report_zh.md`.
- **Hardware Multi-Tenant Context Switcher**: Evaluated moving KV cache context switching from software memory swapping to an inline hardware base-pointer switcher (`multi_tenant_context_switcher_hw_sim.py`). Demonstrated a 45.83x latency reduction during multi-tenant continuous batching. Proposed integrating a "Hardware Context Switcher" into Edge NPUs. Report written to `reports/multi_tenant_context_switcher_hw_report_zh.md`.
- **Token-Aware Hardware DVFS**: Evaluated token-level dynamic voltage and frequency scaling based on prediction confidence (`token_aware_dvfs_hw_sim.py`). Demonstrated a 59.65% reduction in overall NPU power consumption. Proposed integrating a "Token-Aware DVFS Controller" to maximize battery life for Edge AI. Report written to `reports/token_aware_dvfs_hw_report_zh.md`.
- **Inline Hardware RMSNorm Engine**: Evaluated migrating RMSNorm from a memory-bound software operation (two-pass read/write) to a zero-roundtrip inline hardware unit (`rmsnorm_hw_sim.py`). Demonstrated an 11.36x latency reduction by computing variance and normalizing directly at the MAC output registers. Proposed integrating an "Inline RMSNorm Engine" into Edge NPUs. Report written to `reports/rmsnorm_hw_report_zh.md`.
- **Inline Hardware All-Reduce Engine**: Evaluated migrating Tensor Parallelism All-Reduce operations from software memory sync to network-level inline additions (`inline_allreduce_hw_sim.py`). Demonstrated a 10.00x latency reduction by performing math on-the-fly inside the inter-chiplet router. Proposed integrating an "Inline All-Reduce Router" into Multi-Chiplet Edge NPUs. Report written to `reports/inline_allreduce_hw_report_zh.md`.
- **Hardware KV Migration DMA**: Evaluated moving KV cache migration between Prefill and Decode NPUs from software memory copying to a dedicated zero-copy P2P DMA engine (`kv_migration_dma_hw_sim.py`). Demonstrated a 14.12x latency reduction during context transfers. Proposed integrating a "KV Migration DMA Engine" into Multi-Chiplet Edge NPUs. Report written to `reports/kv_migration_dma_hw_report_zh.md`.
- **Hardware Activation Checkpoint Compressor (HACC)**: Evaluated migrating on-device training activation checkpointing from dense DRAM writes to inline hardware compression (`activation_checkpoint_compressor_hw_sim.py`). Demonstrated a 5.20x latency reduction by dynamically applying block-floating-point and sparsity compression before memory eviction. Proposed integrating a "Hardware Checkpoint Compressor" into Edge NPUs to accelerate local fine-tuning. Report written to `reports/activation_checkpoint_compressor_hw_report_zh.md`.
- **Hardware MoE Expert Cache Eviction MMU (HECE-MMU)**: Evaluated moving MoE expert cache eviction and memory paging from software OS interrupts to a dedicated hardware MMU (`hece_mmu_moe_sim.py`). Demonstrated an 18.75x throughput speedup by completely hiding PCIe latency behind hardware-managed async LRU fetching. Proposed integrating a "HECE-MMU Block" directly into Edge NPU memory controllers to eliminate CPU-GPU MoE memory bottlenecks. Report written to `reports/hece_mmu_moe_report_zh.md`.
- **O(1) Sparse Attention Hash Routing (OSAHR)**: Evaluated integrating a hardware hash routing engine into SRAM to eliminate O(N log N) software clustering overhead in sparse attention (`osahr_sparse_attention_sim.py`). Demonstrated a modest 1.23x throughput speedup for 32K context lengths, indicating that memory bottleneck dominates over routing logic. Proposed combining OSAHR with aggressive Block Prefetching for future iterations. Report written to `reports/osahr_sparse_attention_report_zh.md`.
- **Speculative KV-Cache Bypassing (SKCB)**: Evaluated leveraging attention locality to bypass DRAM KV cache fetches for highly correlated tokens (`skcb_sim.py`). Demonstrated a 1.79x throughput speedup by serving 45% of KV requests directly from an SRAM L1 cache. Proposed integrating a "Hardware Attention Locality Predictor" into Edge NPU memory controllers to reduce generation phase memory bandwidth constraints. Report written to `reports/skcb_report_zh.md`.
- **Non-Linear Quantization KV-Cache LUT (NLQ-KV)**: Evaluated migrating non-linear 4-bit KV Cache dequantization from software to an inline SRAM LUT (`nlq_kv_sim.py`). Demonstrated a 2.86x throughput speedup for 16K context lengths by eliminating decode overhead while maintaining accuracy. Proposed integrating a "Hardware NLQ-LUT Engine" into Edge NPUs. Report written to `reports/nlq_kv_report_zh.md`.
- **Hardware GQA Memory Broadcast Controller (GQA-MMU)**: Evaluated moving GQA Key/Value replication from software memory fetches to a zero-cycle SRAM hardware broadcast bus (`gqa_mmu_sim.py`). Demonstrated a 6.96x latency speedup by eliminating redundant DRAM reads for grouped queries. Proposed integrating a "GQA Multicast Bus" directly between the SRAM and Tensor Cores. Report written to `reports/gqa_mmu_report_zh.md`.
- **Hardware Prefix Caching CAM Engine (HPC-CAM)**: Evaluated migrating Prefix Caching token matching from software Radix Trees to a hardware Content-Addressable Memory (CAM) (`prefix_caching_cam_sim.py`). Demonstrated a 15.00x speedup in prefix matching latency for 32K context lengths by executing parallel hash lookups. Proposed integrating an "HPC-CAM" into Edge NPU Ingress controllers to accelerate Agentic AI workloads. Report written to `reports/prefix_caching_cam_report_zh.md`.
- **Hardware MoE Expert Sparsity Predictor**: Evaluated migrating massive MoE expert pruning and thresholding to parallel hardware comparators (`moe_expert_sparsity_sim.py`). Demonstrated a 50.00x latency speedup by skipping software sorting for 8192-expert architectures. Proposed integrating a "Hardware Sparsity Predictor" into Edge NPU routers. Report written to `reports/moe_expert_sparsity_report_zh.md`.
- **Hardware RAG Context Filter (HRCF)**: Evaluated migrating RAG chunk similarity matching and filtering to an inline hardware engine before main attention execution (`hardware_rag_filter_sim.py`). Demonstrated a 40.00x speedup in context filtering for 128K context lengths. Proposed integrating an "HRCF Engine" at the NPU ingress to filter irrelevant data dynamically and reduce prefill latency. Report written to `reports/hardware_rag_filter_report_zh.md`.
- **Hardware Context Switcher (HW-CS)**: Evaluated moving multi-agent task switching overhead to hardware using register shadowing (`hardware_context_switch_sim.py`). Demonstrated a 50.00x speedup in context switching latency. Proposed integrating an "HW-CS Block" into Edge NPUs to accelerate concurrent multi-agent workloads. Report written to `reports/hardware_context_switch_report_zh.md`.
- **Hardware Position Embedding Generator**: Evaluated inline hardware generation of RoPE/ALiBi embeddings (`hardware_pos_embed_sim.py`). Demonstrated a 30.00x speedup in embedding latency for 64K context lengths by eliminating separate software kernels. Proposed integrating an "Inline Position Embedding Engine" at the NPU SRAM read ports. Report written to `reports/hardware_pos_embed_report_zh.md`.
- **Hardware Context Chunk Streamer (HCCS)**: Evaluated moving 1M+ context Chunked Prefill scheduling from software to a dedicated DMA hardware streamer (`hardware_chunk_streamer_sim.py`). Demonstrated a 40.00x speedup in chunk scheduling latency by natively streaming 4K blocks into SRAM without CPU intervention. Proposed integrating an "HCCS Block" into Edge NPU DMA controllers for infinite context processing. Report written to `reports/hardware_chunk_streamer_report_zh.md`.
- **Hardware Group-GEMM Command Scheduler**: Evaluated mitigating software kernel launch overhead for MoE by moving Group-GEMM unrolling to hardware (`hardware_group_gemm_scheduler_sim.py`). Demonstrated a 50.00x reduction in launch latency. Proposed integrating a "Hardware Group-GEMM Scheduler" to eliminate CPU intervention for complex tensor math. Report written to `reports/hardware_group_gemm_scheduler_report_zh.md`.
- **Hardware RAG Chunk Pre-Fetcher (HRCP)**: Evaluated moving RAG scatter-gather memory fetching from CPU software interrupts to an async hardware DMA engine (`hardware_rag_prefetch_sim.py`). Demonstrated a 30.00x latency reduction in fetching 256 RAG chunks. Proposed integrating an "HRCP Engine" to eliminate PCIe setup bottlenecks for Agentic RAG workloads. Report written to `reports/hardware_rag_prefetch_report_zh.md`.
- **Hardware Tree Mask Generator (HTMG)**: Evaluated moving Tree Attention Mask generation for Speculative Decoding from software arrays to inline hardware (`hardware_tree_mask_sim.py`). Demonstrated a 50.00x speedup in mask generation for 256 draft tokens. Proposed integrating an "HTMG Block" into the NPU Attention engine. Report written to `reports/hardware_tree_mask_report_zh.md`.
- **Hardware Sparse RMSNorm Engine (HSRN)**: Evaluated moving zero-aware RMSNorm calculations to inline hardware (`hardware_sparse_norm_sim.py`). Demonstrated a 50.00x speedup in normalization latency for highly sparse networks. Proposed integrating an "HSRN Engine" at the accumulator output to bypass zero-value computations dynamically. Report written to `reports/hardware_sparse_norm_report_zh.md`.
- **Hardware Context Compression Engine (HCCE)**: Evaluated moving KV cache pooling/summarization from software neural networks to an inline hardware sliding-window pooler (`hardware_context_compression_sim.py`). Demonstrated a 50.00x latency speedup for compressing 256K context sequences. Proposed integrating an "HCCE Engine" into the SRAM write ports to natively support ultra-long sequence processing. Report written to `reports/hardware_context_compression_report_zh.md`.
- **Hardware Continuous KV Eviction Engine (C-KVEE)**: Evaluated moving LRU/Ring-Buffer management for infinite streaming models from software to a dedicated hardware controller (`hardware_kv_evict_sim.py`). Demonstrated a 40.00x speedup in eviction latency for 128K context boundaries. Proposed integrating a "C-KVEE Controller" into the Edge NPU SRAM to natively support StreamingLLM architectures without CPU intervention. Report written to `reports/hardware_kv_evict_report_zh.md`.
- **Hardware MoE Async Router Crossbar**: Evaluated mitigating CPU-NPU synchronization barriers during MoE expert routing (`hardware_router_sync_sim.py`). Demonstrated a 53.33x latency reduction for 2048-expert architectures by directly connecting the NPU router to the DMA controller. Proposed integrating an "Async Router Crossbar" to achieve fully autonomous hardware expert fetching. Report written to `reports/hardware_router_sync_report_zh.md`.
- **Token-Adaptive KV Cache Sparsity Hardware**: Evaluated dynamic hardware-level sparsity for 128K context KV cache (`token_adaptive_kv_sparsity_sim.py`). Demonstrated a 3.33x latency speedup and 31.25 dB SQNR at 75% sparsity. Proposed integrating a "Hardware Token-Adaptive Sparsity Router" into Edge NPU memory controllers to natively bypass redundant SRAM fetches. Report written to `reports/token_adaptive_kv_sparsity_report_zh.md`.
- **Hardware Prefix Cache Tree Walker**: Evaluated migrating Radix Tree matching to a hardware MMU (`prefix_caching_hw_tree_walker_sim.py`). Demonstrated a 50.00x speedup for 64K context matching. Proposed integrating a "Hardware Tree Walker" into Edge NPUs. Report written to `reports/prefix_caching_hw_tree_walker_report_zh.md`.
- **Hardware LoRA Context Switcher (HLCS)**: Evaluated migrating LoRA adapter context switching from CPU/PCIe software overhead to an inline SRAM multiplexer (`lora_multi_agent_switch_sim.py`). Demonstrated a 50.00x latency reduction for 128 concurrent agents. Proposed integrating dedicated LoRA SRAM banks with a hardware base-pointer switcher to enable zero-overhead Multi-Agent inference. Report written to `reports/lora_multi_agent_switch_report_zh.md`.
- **Hardware SRAM Bank Conflict Resolver**: Evaluated XOR Hash Banking vs software padding for FlashAttention SRAM tiles (`sram_bank_conflict_resolver_sim.py`). Demonstrated a 20.00x speedup in memory access latency by eliminating bank conflict stalls. Proposed integrating an XOR Banking Engine into the NPU SRAM interface. Report written to `reports/sram_bank_conflict_resolver_report_zh.md`.
- **Hardware Token Packer (HW-TP)**: Evaluated migrating Gather/Scatter operations for 90% Sparse Attention from software to an inline DMA packing engine (`hw_token_packer_sim.py`). Demonstrated a 60.00x latency speedup by ensuring contiguous data streams to the MAC array. Proposed integrating an HW-TP block into the Edge NPU Memory Controller. Report written to `reports/hw_token_packer_report_zh.md`.
- **Hardware Top-P/Top-K Sampler Engine**: Evaluated migrating token sampling from CPU to an on-chip NPU hardware block (`hw_sampler_engine_sim.py`). Demonstrated a 53.90x latency speedup for a 128K vocabulary by eliminating per-token PCIe logit transfers. Proposed integrating a "Hardware Sampler Engine" directly into Edge NPUs to achieve Zero-PCIe-Sync decoding loops. Report written to `reports/hw_sampler_engine_report_zh.md`.
- **Hardware Activation Sparsity Masking (HW-ASM) for MoE**: Evaluated moving MoE activation thresholding and sparsity masking to dedicated hardware (`hw_asm_moe_sim.py`). Demonstrated a 29.78x latency speedup by replacing software masking with an inline SRAM comparator. Proposed integrating an "Inline Sparsity Comparator" into Edge NPU outputs. Report written to `reports/hw_asm_moe_report_zh.md`.- **Hardware MoE Branch Predictor Prefetching**: Evaluated replacing demand-based MoE expert fetching with a hardware branch predictor (`moe_branch_prefetch_sim.py`). Demonstrated an 8.49x throughput speedup by overlapping memory fetch latency with compute. Proposed integrating a "Hardware MoE Branch Predictor" into Edge NPU DMA controllers. Report written to `reports/moe_branch_prefetch_report_zh.md`.
- **Hardware FlashAttention Block Prefetching**: Evaluated hardware-level block prefetching for FlashAttention (`fa_block_prefetch_sim.py`). Demonstrated an 8.07x speedup. Proposed integrating a Hardware Block Prefetcher. Report written to `reports/fa_block_prefetch_report_zh.md`.- **Hardware Hybrid Quantization Router v2**: Evaluated dynamic hardware precision switching (`hybrid_quant_router2_sim.py`). Demonstrated a 7.14x speedup. Proposed integrating a Hardware Dynamic Precision Router. Report written to `reports/hybrid_quant_router2_report_zh.md`.- **Hardware Mamba-2 Token Selector**: Evaluated hardware-level token selection for Mamba-2 (`mamba_token_selector_sim.py`). Demonstrated a 9.30x speedup. Proposed integrating a Hardware Token Selector. Report written to `reports/mamba_token_selector_report_zh.md`.- **Hardware Speculative Draft Allocator**: Evaluated hardware-level memory allocation for speculative drafts (`hw_spec_draft_allocator_sim.py`). Demonstrated an 8.94x speedup. Proposed integrating a Hardware Draft Allocator. Report written to `reports/hw_spec_draft_allocator_report_zh.md`.- **Hardware Chunked Attention Engine**: Evaluated hardware-level chunked attention scheduling (`hw_chunk_attn_sim.py`). Demonstrated an 8.64x speedup. Proposed integrating a Hardware Chunked Attention Engine. Report written to `reports/hw_chunk_attn_report_zh.md`.- **Hardware O(1) MoE Token Router**: Evaluated hardware-level O(1) MoE expert routing (`hw_moe_token_router_sim.py`). Demonstrated an 8.67x speedup. Proposed integrating a Hardware O(1) MoE Token Router. Report written to `reports/hw_moe_token_router_report_zh.md`.- **Hardware KV Cache Lookahead Prefetcher**: Evaluated hardware-level prefetching for KV caches (`hw_kv_cache_lookahead_prefetch_sim.py`). Demonstrated a 9.61x speedup. Proposed integrating a Hardware KV Cache Prefetcher. Report written to `reports/hw_kv_cache_lookahead_prefetch_report_zh.md`.- **Hardware Dynamic Token Compressor**: Evaluated hardware-level dynamic token compression (`hw_dynamic_token_compressor_sim.py`). Demonstrated a 9.74x speedup. Proposed integrating a Hardware Dynamic Token Compressor. Report written to `reports/hw_dynamic_token_compressor_report_zh.md`.- **Hardware Speculative Tree Attention Verifier**: Evaluated hardware-level tree attention verification for speculative decoding (`hw_tree_attn_verifier_sim.py`). Demonstrated an 11.85x speedup. Proposed integrating a Hardware Tree Attention Verifier. Report written to `reports/hw_tree_attn_verifier_report_zh.md`.