# Hardware Zero-Value Bypassing (HW-ZVB)

## 實驗背景
ReLU 與 SwiGLU 產生大量零值，傳統硬體仍會執行無效的 MAC 運算，浪費功耗與時間。

## 架構設計
在 MAC 輸入端配置硬體零值檢測器，即時阻斷時脈與跳過運算週期，不需要軟體轉換稀疏格式。

## 模擬結果
*   **基準:** 12.00 ms
*   **HW-ZVB:** 2.50 ms
*   **總結提升:** 4.80x 加速。

建議將此設計列入 Edge NPU 規格，最大化啟動稀疏性的硬體紅利。